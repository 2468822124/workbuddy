# 当前代码状态（DS第一份阅读文件）

> **维护**：DeepSeek（**唯一可编辑**；GPT/GLM 只读不得编辑，见 `规范类/阶段闭环门禁规则.md`·权限隔离）　**用途**：DS执行代码任务的第一份阅读文件
> **更新方式**：覆盖式更新，只记录最近一次代码任务情况

---

## 当前状态
**任务类型**：阶段6 修复批次（旧模块切换清理）
**任务名称**：阶段6 — 旧模块切换清理
**状态**：🟡 修复批次2 完成，待 GPT 复审（2026-08-20；复审1 的 F1/F5/T1 全部修复，证据链见闭环记录 §修复环节·续）
**更新时间**：2026-08-20

## 关联文档（必读）
**闭环记录（唯一权威）**：`用户打回重构/任务数据流通重构/阶段6-旧模块切换清理-全流程闭环记录.md`（§规格 + §实现日志 + §审查 + §修复 + §复审占位）
**项目计划**：`用户打回重构/任务数据流通重构/项目计划.md`（6 阶段）
**立项（34 决策）**：`用户打回重构/任务数据流通重构/v0.2-用户反馈4-任务数据流通重做.md`
**技术边界**：`规范类/技术栈规范.md`（§4 数据层铁律 / §9 冻结区）
**当前审查状态**：`当前审查状态.md`（GPT 维护，DS 只读）

## 当前任务概要
阶段6 = 旧模块切换清理（规格 §5 五大设计）。**修复批次 2（2026-08-20）：复审1 的 F1/F5/T1 全部修复，356/356 测试 + build + 双 tsc + 真实 Electron CDP + 损坏 DB 实机 + 分层扫描全部通过，门禁 🟡待复审**。修复内容（详见闭环记录 §修复环节·续「修复批次 2」）：

- **F1 🔴（复审1 未关闭）** validatePayload 增加 16 白名单键完整性校验——`{}` 空对象/缺键载荷在 transaction 前拒绝（IMPORT_FAILED），原库计数不变实测（dataRoundtrip ⑩⑪）。
- **F5 🟡（复审1 部分关闭）** connection.ts 只读打开后增加 `SELECT 1 FROM sqlite_master` 探针，探针失败转 `unavailable` → 全 55 通道 `DB_UNAVAILABLE`；实机损坏 DB 三通道（health/settings.set/isFirstLaunch）全 `DB_UNAVAILABLE`，页面零异常。
- **T1 🟡（复审1 新增阻断）** 阶段6 SQL 全部下沉 repositories：新增 dataTransferRepo（16 表清单+exportTables+replaceAll 事务）/legacyArchiveRepo（探测+快照+DROP 事务）/unitOfWork（统一事务入口）/migrationRepo（schema 版本）；data/legacyArchive/flowCloneEngine 服务层改纯编排，ipc DB_HEALTH 走 migrationRepo；分层扫描 services/ipc 直接 SQL 零命中。

此前修复批次 1（审查 F1-F5）内容（F2/F3/F4 已被复审1 关闭）：

- **F1 🔴** data.ts `tables:[]` 等非对象载荷拒绝（数组/null/原型异常三类），transaction 前校验，原库计数不变实测。
- **F2 🟡** 三 flow 页白屏：composable 返回普通对象 + 嵌套 ref 未解包 → 视图顶层解构；真实 Electron CDP 四页×三档 12/12 零异常。
- **F3 🟡** 非法日期（2026-02-31 等）复用 isValidDate 真实日历校验回落默认；monthly 旧 URL YYYY-MM-DD 截前 7 位转月；router 测试不再固化非法值。
- **F4 🔵** 导出日志只记 counts/错误码，路径脱敏（日志断言测试）。
- **F5 🟡** DB 三态安全模式（normal/readonly/unavailable）：readonly 跳过迁移/seed/写入，35 写通道 READ_ONLY，不可读全通道 DB_UNAVAILABLE；损坏 DB 实机启动验证（NOTADB→SAFE MODE→迁移跳过→写通道 READ_ONLY）。

此前首次实现概要（17 步任务拆解全部完成、30 个验收点自测通过）：

- **§5.1 路由与导航收口**：router 注册 /flow/week、/flow/month、/flow/day、/flow/review 四页为正式入口；旧 /planning/:level、/plan、/review 只保留纯重定向函数（planningRedirect/reviewRedirect，不含 DB/旧 API 调用；daily 的 date→date、weekly 的 date→week、monthly 的 date→month、review 仅 date→week）；PlanningView.vue 不再注册（懒加载消失）；AppSidebar 四入口全部指向 /flow/*。
- **§5.2 冻结区清理**：删除 21 个冻结区源文件（planRepo/reviewRepo/taskRepo/templateRepo/todoRepo + plans/reviews/templates/todos 四 ipc + parentRef/planParser/todoSource + PlanningView/MarkdownEditor/usePlanning/planTid + planning/ 五组件）+ 7 个旧 spec；llm.ts 删除约 180 行旧规划/复盘路径仅留热点搜索降级；shared 清理旧通道常量与 LLM 护栏常量。
- **§5.3 项目任务与周期提醒兼容**：新增 `projectTaskRepo.ts`（确定性 reminderKey 派生 id 保证幂等）+ `projectTasks.ipc.ts`（create/update/delete/toggle/findByDate 五 handler，Result<T>）+ preload 桥 `api.projectTasks.*`；flowActions/flowDerived/reminders 全部从 todoRepo 迁移（reminder 改按 reminderKey 精确查，废弃内容匹配）；项目详情页迁移 api.projectTasks 含失败反馈。
- **§5.4 旧表归档**：`legacyArchive.ts` 先备份、后事务删除（tasks→plans→templates→reviews 子先父 DROP），成功/取消/写失败/回滚四路径；设置页「归档并清理旧规划数据」入口；绝不启动/迁移自动执行，绝不触碰 projects/todos/flow_*。
- **§5.5 导入导出 16 表白名单**：data.ts 重写（8 保留表 + 8 flow_*），固定 IMPORT_ORDER + DELETE_ORDER（反向子先父）；旧备份含旧表键 → LEGACY_BACKUP_UNSUPPORTED 引导走归档 JSON；载荷预校验在 transaction 前，失败整体回滚；移除导出路径明文日志。
- **§3.2 六轮场景全量回归**：phase6Regression.spec 12 用例（真实 DB + 真实 IPC handler）覆盖改名/删除/完成态/rail/手动完成+克隆/项目组合/提醒+复盘趋势。

## 自测结果
| 状态 | 结果 |
|---|---|
| vitest | ✅ **356/356（26 files）**：修复批次2 +3（dataRoundtrip ⑩⑪ 键完整性 + dbMode 探针用例），批次1 353 全绿 |
| build | ✅ 绿（2.77s，产物不含 PlanningView-*.js） |
| tsc.node | ✅ 仅 news.ts:22 存量基线（1 条，与阶段5/批次1 一致），零新增 |
| tsc.web | ✅ 11 条全为存量/同类诊断（update 签名 2 + .vue 解析 9），零新增 |
| **真实 Electron/CDP 四页三档** | ✅ **12/12 PASS**（flow-day/week/month/review × 1440×900/768×1024/390×844，Runtime.exceptionThrown 零捕获、console.error 零计数；截图存 %TEMP%\wb-cdp-shots\） |
| **损坏 DB 实机降级（批次2 新语义）** | ✅ garbage 文件：NOTADB → 只读打开成功 → **探针 NOTADB → unavailable** → 55 通道全 `DB_UNAVAILABLE` → 跳过迁移/seed；渲染层 health/settings.set/isFirstLaunch 三通道全 `{ok:false,error:{code:'DB_UNAVAILABLE'}}`；页面零未处理异常 |
| **分层扫描（T1）** | ✅ `rg "getDb\(|\.prepare\(|\.transaction\(|db\.exec\(" src/main/services src/main/ipc` **零命中**（SQL/事务/健康查询全部下沉 repositories） |
| 归档四路径 | ✅ 成功/取消/写失败保留数据/DROP 回滚（legacyArchive 5 + archiveIpc 6；T1 重构后 service API 未变全过） |
| 导入导出原子性 | ✅ 16 表白名单 roundtrip + FK 链完整 + 非空库导入 + 旧备份拒绝 + FK 违例整体回滚零残留 + 非对象行/数组/null/异常原型/空对象/缺键前置校验（dataRoundtrip ①-⑪） |
| 六轮场景回归 | ✅ 12/12（phase6Regression） |
| 静态 grep 门禁 | ✅ 冻结区 0 引用；renderer 0 DB/SQL；0 console.log；0 `<i data-lucide>`；IPC 全 Result<T>；0 新依赖/迁移 |
| 重定向单测 | ✅ routerRedirect 18 |

## 规格偏差（全部如实记录于实现日志 §规格偏差表）
1. **归档 DROP 顺序**：spec 未指定 → 实际 tasks→plans→templates→reviews（FK 子先父，plans 被 tasks.planId 引用，父先删必撞 FK）；事务内失败回滚，无风险。
2. **导入清库顺序**：DELETE_ORDER = IMPORT_ORDER 反向（全部 FK 默认 NO ACTION；flow_week_instances 自引用由单条 DELETE 整体处理）；dataRoundtrip ①b 固化，无风险。
3. **重定向参数语义**：旧 focus/模板/Markdown 参数不传递（阶段3 立项 #21 周为最小复盘单位），低风险。
4. **renameInstance 拒绝 fixed**（阶段5 既有守卫，fixed 改名走 saveFixedDef→syncTitlesFromDef 仅当前周）。
5. **data.ts 日志**：移除导出完整路径泄露，只记 counts/错误码（规格 §5.4）。
- 无其它偏差；package.json、migrations、冻结区外代码零改动（不擅自扩大范围）。

## 已知问题
- **修复批次2 已实测**：真实 Electron CDP 四页三档 12/12 零异常；损坏 DB 实机全通道 `DB_UNAVAILABLE`（unavailable 三通道 + 55 通道守卫日志）；分层扫描 services/ipc 直接 SQL 零命中（证据见闭环记录 §修复环节·续）。
- **unavailable 模式例行提醒**：启动期 `Morning routine error {}`（写入被保护性拒绝，错误已捕获不崩溃）——属保护期预期行为，非缺陷。
- **仍留 GPT 复审/用户 E2E 的交互**：四 flow 页点击导航、旧 URL 别名重定向点击、项目任务 CRUD、设置页归档按钮、导入导出对话框交互未做真实点击流（逻辑层已被单测 + handler 直调 + 页面加载 CDP 覆盖）。
- **readonly 模式读通道**：handler 正常注册，库可查询（探针通过才进 readonly），读写按 READ_ONLY/正常分层；unavailable 全通道 DB_UNAVAILABLE 已实机。
- **归档不可逆**（DROP 后仅剩备份 JSON）：UI 有确认步骤，无二次密码确认（禁阻断式模态弹窗约束）。
- LLM 仅验证热点搜索失败降级（规格 §11.3 明确不要求真实 LLM 复盘能力）。
- tsc.web/node 存量诊断不修，如实记录。

## 下一步
待 GPT 用 05 prompt 复审阶段6 修复批次2（基准：规格 §9 三十验收点 + §10 禁止边界 + §11.3 审查重点，必跑四道门禁 + 分层扫描）。复审最低证据已备齐：
- `npm.cmd test` 356/356；`npm.cmd run build` 绿；双 tsc 与阶段5基线对比零新增；
- 真实 Electron/CDP 四页三档 12/12（复跑命令：`env -u ELECTRON_RUN_AS_NODE WORKBUDDY_USER_DATA=... ./node_modules/electron/dist/electron.exe . --remote-debugging-port=9222`，脚本 `%TEMP%\wb-cdp-stage6-check.mjs`，须先 `npm run build`；两实例同跑时注意 CDP WS 偶发挂起，重启用 `netstat -ano | grep :9222` 按 PID 杀）；
- 非法导入载荷（数组/null/异常原型/空对象 {} /缺键/旧表/FK）前后计数：dataRoundtrip ⑥⑦⑧⑩⑪ + 既有用例；
- 损坏 DB 启动与 IPC 错误通道：dbMode 4 用例（valid→normal、locked→readonly、garbage→unavailable、目录→unavailable）+ ipcGuard 三模式 + 实机全通道 `DB_UNAVAILABLE` 封套与 55 通道守卫日志；
- 分层扫描：`rg "getDb\(|\.prepare\(|\.transaction\(|db\.exec\(" src/main/services src/main/ipc` 零命中；SQL 属地 = `src/main/db/repositories/`（dataTransferRepo/legacyArchiveRepo/unitOfWork/migrationRepo）。
复审建议重点：F1 空/缺键载荷无任何「返回 ok」路径且计数不变、F5 探针失败即 unavailable 全通道 DB_UNAVAILABLE、T1 越层零命中且服务层行为未变（356 测试佐证）。启动：`cd E:\workspace\workbuddy && npm run dev`。
