# 变更日志模板（CR 实现日志）

> **维护**：DeepSeek　**用途**：维护期 CR 全流程闭环记录的**「变更日志」环节**书写格式（CR 的实现环节，类比构建期全流程文档的「实现日志」环节）。
> **落点**：`变更开发/vX-{功能名}/CR-{NNN}-{简述}-全流程闭环记录.md` 的「变更日志」环节；同一 CR 多轮修复在本环节追加「修复{M}」小节（不再使用 `项目日志/` 文件夹）。
> **配套规则**：`规范类/变更闭环规则.md`。

---

## 日志元信息
- **CR 编号**：CR-{NNN}（{简述}）
- **轮次**：{首次修复 / 修复复审 M}
- **日期**：{YYYY-MM-DD}
- **实现者**：DeepSeek（{08 prompt / 04 prompt}）

## 实现概要
- {一句话：按 CR §4 修复计划做了什么}

## 新增 / 修改文件
| 文件 | 类型 | 对应 CR §4 哪条 | 说明 |
|---|---|---|---|
| {path} | 新增/修改 | §4 #{n} | {职责} |

## 关键决策与偏差
| 偏差 | CR 原计划 | 实际做法 | 原因 |
|---|---|---|---|
| {如无填「无，严格按 §4」} | | | |

## 自测记录（含复跑 CR 失败场景）
| 验收点 | 命令/操作 | 结果 |
|---|---|---|
| {CR §5 验收点} | {命令} | {输出摘要} |
| **复跑 §1 复现场景** | {原失败操作} | {✅ 不再复现 / ❌ 仍复现} |
| `npm run build` 绿 | `npm run build` | {✅/❌} |

> **复跑门（H3）**：必须实跑 CR §1 的原始失败场景确认不再复现；不能只查代码（教训：动态 `require` 因 headless 未实跑潜伏 6 阶段）。

## 已知问题 / 技术债
- {如无填「无」}

## 下一步
- {可交 GLM 审查（09 prompt）/ 需先补 / 等依赖}
