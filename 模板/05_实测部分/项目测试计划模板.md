# {{版本}}用户实测计划（{{计划标识}}）

> **文档状态**：`📝草案 / 🟡待补前置 / ✅可执行 / 🟢测试完成`
> **计划类型**：`R 轮次计划 / G1 版本级整合计划`
> **计划唯一标识**：`{{计划标识}}`
> **维护方**：GPT（项目经理）
> **规则**：R 计划每次实际测试都新建一份；同一问题来源轮次只能有一份全流程闭环文档。G1 是独立的版本级整合计划，不属于任何 R 轮次，也不创建 G1 问题文档。现有历史 v0.3 文件保留原样，新规则只向前生效。

---

## 一、计划元信息

| 项目 | 内容 |
|---|---|
| 实测版本 | `{{版本}}` |
| 功能名称 | `{{功能名}}` |
| 计划类型 | `{{R 轮次 / G1 版本级整合}}` |
| 问题来源轮次 | `{{R{{N}}；G1 写“不属于 R”}}` |
| 本次计划序号 | `{{n；G1 写“不适用”}}` |
| 计划唯一标识 | `{{计划标识}}` |
| 计划创建日期 | `{{YYYY-MM-DD}}` |
| 继承自计划 | `{{上一份 R 计划绝对路径；首份写“无”；G1 写“各活跃 R 计划/闭环索引”}}` |
| 关联问题来源闭环 | `{{同轮唯一全流程闭环绝对路径；首份可写“待 13 创建”；G1 写“不适用，回写本计划合并区”}}` |
| 版本/构建标识 | `{{}}` |
| C0/QA 候选状态 | `{{C0 / QA{{N}}；G1 写整合来源状态}}` |
| 实测快照 ID | `{{S0 / S{{N}}}}` |
| G1 整合来源 | `{{G1 不新增 R；列出纳入的功能、活跃 R 和统一被测 S}}` |
| 来源 Git 提交号 | `{{完整提交号 / 未提交；C0 必填，QA 可选}}` |
| 来源工作树指纹 | `{{完整指纹；未提交 QA 必填}}` |
| 构建目录 | `{{独立构建目录}}` |
| 构建物哈希 | `{{算法和哈希}}` |
| 快照冻结时间 | `{{YYYY-MM-DD HH:mm:ss}}` |
| 代码根 | `{{相对或绝对路径；仅作验证，不是治理文档落点}}` |
| 测试计划状态 | `{{}}` |

## 二、计划门与快照门

R 计划中，当前目标版本的首份计划使用 `C0/S0` 门，Fix/Review 后的后续计划使用新的 `QA/S` 快照门。后续计划不要求重新提交 Git，也不要求工作树重新变干净。G1 使用独立的版本级整合门，必须绑定覆盖所有活跃功能的统一 `S` 快照。任何计划只有在对应门禁的条件全部只读核对通过后才能标记 `✅可执行`：

| 门禁项 | 预期 | 实际证据 | 结论 |
|---|---|---|---|
| 首份计划的开发闭环 | `C0` 计划：全部为 `✅开发闭环`；后续计划：继承已批准基线 | `{{闭环记录路径和状态}}` | `✅/❌/不适用` |
| C0 基线 | 首份计划有提交号、Tag、版本字段；后续计划引用原 C0 | `{{git show/tag 或继承计划}}` | `✅/❌/不适用` |
| 候选与快照 | 计划绑定 `C0/S0` 或 `QA{{N}}/S{{N}}` | `{{来源状态和快照记录}}` | `✅/❌` |
| 来源指纹 | C0/QA 的提交号或完整工作树指纹可复核 | `{{}}` | `✅/❌` |
| 构建快照 | 独立构建目录、构建物哈希、冻结时间已记录 | `{{}}` | `✅/❌` |
| 工作树 | 仅 C0 首次计划要求干净；后续 QA/S 不要求干净但必须记录指纹 | `{{git status 摘要}}` | `✅/❌/不适用` |
| 程序入口 | 可执行入口和启动方式明确 | `{{入口/启动方式}}` | `✅/❌` |
| 测试目录 | 专用目录明确且不等于真实用户目录 | `{{路径类别核对}}` | `✅/❌` |
| 测试数据 | 数据目录、前缀和隔离边界明确 | `{{}}` | `✅/❌` |
| 重置方式 | 无；或只指向测试副本且有恢复步骤 | `{{}}` | `✅/❌` |

适用门禁任何一项无法确认，必须保持 `🟡待补前置`，停止创建“可执行”计划并在此列出需补充信息。不得以代码猜测或历史截图代替。未提交代码只有在已形成并冻结可复核 `QA/S` 时才可以进入后续实测。

### 需补充信息

`{{无 / 文件、字段、冲突、责任人和需要确认的值}}`

### G1 版本级整合计划门（仅计划类型为 G1 时填写）

| 门禁项 | 预期 | 实际证据 | 结论 |
|---|---|---|---|
| 活跃 R 轮次 | 所有活跃 `R` 均已关闭，且各轮双方正式证据齐全 | `{{R 轮次及其唯一闭环文档状态}}` | `✅/❌` |
| 整合范围 | 计划列出全部纳入的功能、R 轮次、核心链路和已修复问题 | `{{}}` | `✅/❌` |
| G1 快照 | 统一 `S` 快照覆盖整合范围，来源、指纹、构建物哈希和独立目录可复核 | `{{}}` | `✅/❌` |
| G1 编号 | 测试点只使用 `G1-T<NN>`，不使用 `R` 编号，不创建新的 R 轮次 | `{{}}` | `✅/❌` |
| 双方证据落点 | GPT 报告、GPT 证据和用户证据路径已明确且互不混用 | `{{}}` | `✅/❌` |
| C1 边界 | 计划明确 G1 通过只是形成 `C1` 的必要条件，不直接宣称 C1 或用户验收通过 | `{{}}` | `✅/❌` |

G1 任一门禁无法确认时，保持 `🟡待补前置`，不得启动业务实测。G1 失败后按证据回写现有 R 或新建下一 R；不得把 G1 改名为 R 或创建第二份 G1 闭环文档。

## 三、测试环境与数据隔离

- **操作系统**：`{{Windows 版本}}`
- **窗口尺寸**：`{{预期窗口尺寸或默认窗口}}`
- **网络状态要求**：`{{正常/断网/按场景切换}}`
- **被测程序**：`{{exe、可执行目录或明确的启动入口}}`
- **启动方式**：`{{必须可由用户可见操作执行的命令/脚本/点击步骤}}`
- **测试专用目录**：`{{}}`
- **测试数据目录**：`{{必须位于测试专用目录}}`
- **真实用户数据目录**：`{{只记录类别或经脱敏的核对结果，不记录敏感完整路径}}`
- **重置命令**：`{{无；或明确指向测试数据副本的命令}}`
- **测试数据唯一前缀**：`{{版本-GPT-日期-或版本-USER-日期-}}`
- **安装/运行前置**：`{{}}`
- **敏感信息处理**：`{{不得使用/记录真实 API Key、Cookie、隐私数据}}`
- **被测快照共享规则**：GPT 与用户必须使用同一个 `S` 构建物；用户无需另填快照信息，GPT 从本计划核对。

## 四、双方测试范围

> R 计划填写本轮用户范围、GPT 范围和共同核心链路；G1 计划填写版本级整合范围。G1 仍要求 GPT 与用户共同执行指定核心链路，双方证据互不替代。

### 4.1 用户本次测试内容

用户必须亲自完成核心链路和主观体验，不得只做“好不好用”的泛泛评价：

| 范围 | 测试点 ID | 用户要完成的操作 | 主观关注 | 必须回传 |
|---|---|---|---|---|
| 用户核心链路 | `{{计划标识}}-T{{NN}}` | `{{}}` | `{{理解、自然性、信任、确认/取消}}` | `{{实际、证据}}` |

### 4.2 GPT 本次测试内容

GPT 负责客观运行事实、深度回归、数据持久化、错误降级、隔离和安全：

| 范围 | 测试点 ID | GPT 要完成的操作 | 证据要求 |
|---|---|---|---|
| 客观状态/回归 | `{{计划标识}}-T{{NN}}` | `{{}}` | `{{可见窗口截图/只读核对}}` |

### 4.3 双方共同执行的核心链路

同一核心链路必须由 GPT 和用户各自执行；双方证据不能互相替代：

| 测试点 ID | 共同目标 | GPT 范围 | 用户范围 | 双方成功标准 |
|---|---|---|---|---|
| `{{计划标识}}-T{{NN}}` | `{{}}` | `{{客观结果/持久化/错误}}` | `{{核心操作/体验}}` | `{{}}` |

## 五、稳定测试点清单

> 每个测试点 ID 在该计划及其报告、用户回传、证据中保持不变。R 计划使用 `R<N>-<n>-T<NN>`；G1 计划使用 `G1-T<NN>`。不要复用旧计划的 ID 记录不同操作。

| 测试点 ID | 场景/名称 | 执行方 | 前置 | 操作步骤 | 预期反馈 | 成功标准 | 失败标准 | 阻断标准 | 证据命名 |
|---|---|---|---|---|---|---|---|---|---|
| `{{计划标识}}-T01` | 启动与恢复 | 双方 | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{计划标识}}-T01-启动.png` |
| `{{计划标识}}-T02` | 共同核心链路 | 双方 | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{计划标识}}-T02-核心链路.png` |
| `{{计划标识}}-T03` | 取消/二次确认 | 双方 | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{计划标识}}-T03-取消.png` |
| `{{计划标识}}-T04` | 错误降级 | GPT/用户 | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{计划标识}}-T04-错误.png` |
| `{{计划标识}}-T05` | 刷新/离开后重进与一致性 | GPT/用户 | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{计划标识}}-T05-持久化.png` |
| `{{计划标识}}-T06` | 安全/隔离边界 | GPT | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{计划标识}}-T06-隔离.png` |

### 测试点补充

`{{如需增加，R 计划继续使用 R{{N}}-{{n}}-T07…；G1 计划继续使用 G1-T07…；删除不适用项时说明原因}}`

## 六、取消、错误降级、恢复和数据一致性要求

### 取消/确认

- **取消路径**：`{{动作、应保持的数据状态、证据}}`
- **确认路径**：`{{动作、应产生的数据变化、证据}}`
- **二次确认文案**：`{{}}`

### 错误降级

- **失败场景**：`{{空输入/非法字段/无效 Key/不可达地址/非法文件/缺必填字段等}}`
- **可读反馈**：`{{}}`
- **不中断标准**：不崩溃、不白屏、无无限重试，非相关功能仍可用。
- **数据保护**：错误不能清空、重复写入或串用既有数据。

### 刷新/离开后重进

- **需要验证的数据变化**：`{{}}`
- **刷新步骤**：`{{}}`
- **离开/重进步骤**：`{{}}`
- **一致性标准**：`{{}}`

### 安全与隔离

- **不可触碰的真实数据**：`{{}}`
- **测试专用数据边界**：`{{}}`
- **密钥/隐私脱敏规则**：`{{}}`
- **越权、串档、敏感泄露判定**：`{{}}`

## 七、明确不测试项

| 项目 | 不测试原因 | 依赖/未来入口 |
|---|---|---|
| `{{}}` | `{{未在本计划范围/需要用户环境/没有安全测试副本/依赖未满足}}` | `{{}}` |

不得将不测试项写入 GPT 或用户已通过项。

## 八、证据命名与归档

### GPT

```text
R 计划：GPT实测记录\{{版本}}实测内容\第{{N}}轮\{{版本}}-{{计划标识}}-GPT实测.md
R 计划：GPT实测记录\{{版本}}实测内容\第{{N}}轮\证据\{{计划标识}}-<场景>.png
G1 计划：GPT实测记录\{{版本}}实测内容\G1\{{版本}}-G1-GPT实测.md
G1 计划：GPT实测记录\{{版本}}实测内容\G1\证据\G1-T<NN>-<场景>.png
```

### 用户

```text
R 计划：用户实测阶段\{{版本}}-{{功能名}}\第{{N}}轮\{{计划标识}}相关证据\<测试点ID>-<场景>.<扩展名>
G1 计划：用户实测阶段\{{版本}}-版本级整合\G1\用户证据\G1-T<NN>-<场景>.<扩展名>
```

证据必须能定位到测试点，不能包含真实密钥、Cookie、私人数据或完整敏感路径。用户不建立独立实测报告，通过 `06` 回传。

## 九、完成标准与分支

- GPT 与用户的共同核心链路均有双方各自的执行记录；
- 每个计划测试点有明确执行方、操作、预期、成功/失败/阻断标准和证据要求；
- 计划可执行前置全部通过；
- R 计划经 `13` 合并后，若仅有回归则留在 `R{{N}}`，修复/复审后新建 `R{{N}}-{{n+1}}`；若出现全新问题，当前轮次按无回归事实收束并新建 `R{{N+1}}-1`。
- 所有活跃轮次关闭后才创建 G1 计划；G1 只使用 `G1-T<NN>`，不创建新的 R 轮次。
- G1 无问题时由 `13` 回写本计划的“合并与裁决”区；若发现回归或全新问题，按证据回写现有 R 或新建下一 R。
- `C1` 只有在所有活跃轮次关闭、双方证据齐全、`G1` 通过且用户完成最终验收后才可形成；单次 R 计划或 G1 通过不等于 `C1`。

## 十、G1 合并与裁决（仅计划类型为 G1 时填写）

> R 计划填写“非 G1 计划，不适用”。G1 计划由 `13` 在双方证据齐全后回写本区，不创建独立问题闭环文档。

### 10.1 双方证据索引

| 内容 | 路径 | 是否齐全 | 备注 |
|---|---|---|---|
| GPT 正式报告 | `{{GPT实测记录\<版本>实测内容\G1\<版本>-G1-GPT实测.md}}` | `✅/❌` | `{{}}` |
| GPT 证据 | `{{GPT实测记录\<版本>实测内容\G1\证据\}}` | `✅/❌` | `{{}}` |
| 用户 06 回传 | `{{回传原文位置}}` | `✅/❌` | `{{}}` |
| 用户证据 | `{{用户实测阶段\<版本>-版本级整合\G1\用户证据\}}` | `✅/❌` | `{{}}` |
| 纳入的活跃 R 闭环 | `{{R 文档路径和关闭状态}}` | `✅/❌` | `{{}}` |

### 10.2 G1 测试点合并

| 测试点 ID | GPT 结果/证据 | 用户结果/证据 | 共同链路是否一致 | 未测试/待确认 | 结论 |
|---|---|---|---|---|---|
| `G1-T{{NN}}` | `{{}}` | `{{}}` | `{{一致/有差异}}` | `{{}}` | `{{通过/缺陷/待补证据}}` |

### 10.3 G1 问题分流

| 问题 ID | 测试点 ID | 分类 | 严重度 | 事实与证据 | 回写现有 R / 新建下一 R | 状态 |
|---|---|---|---|---|---|---|
| `{{}}` | `G1-T{{NN}}` | `{{现有 R 回归/全新/待核实/非缺陷候选}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` |

### 10.4 G1 正式裁决

- **G1 结论**：`{{📝待补证据 / ⏸️停止（需补充信息） / 🟡需修复 / ✅G1通过}}`
- **G1 通过后的边界**：`G1 通过不直接形成 C1；仍需用户最终验收及其他 C1 前置。`
- **下一步**：`{{补证据 / 回写现有 R / 新建下一 R / 用户最终验收 / C1 核对}}`

## 十一、创建确认

- **计划创建人**：`{{GPT}}`
- **计划类型**：`{{R 轮次 / G1 版本级整合}}`
- **计划状态**：`{{🟡待补前置 / ✅可执行}}`
- **用户已知范围**：`{{}}`
- **GPT 已知范围**：`{{}}`
- **待补信息**：`{{无 / 列表}}`
