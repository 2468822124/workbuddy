# {{版本}}-第{{N}}轮-{{问题概述}}-全流程闭环记录

> **文档状态**：`🟡待实测 / 📝待补证据/待核实 / ⏸️待环境/产品决策 / 🟡待修复 / 🟢等待下一轮测试 / ✅当前轮次闭环`
>
> **问题来源轮次**：`R{{N}}`。R 是实测处置来源轮次，可以承接回归、独立新增问题、覆盖缺口和环境/产品处置事项。
> **唯一性**：同一 R 只有这一份闭环文档。所有测试计划、GPT 报告、用户 06、补充核验、Fix、Review、QA/S 和跨 R 映射均在本文索引或追加；不得按每个测试或问题另建第二份 R 文档。
> **创建方**：13 的 `R 初始化` 或 `R 测试合并` 模式。测试计划只由 14 创建；G1 只回写 G1 计划，不创建 R 文档。
> **历史兼容**：现有 v0.3 历史文件原样保留；本模板只用于新规则新建或明确要求追加的文档。

## 阅读与追加规则

- 本文允许包含多个状态、Fix、Review、QA/S 和分流记录；阅读当前状态时必须从文档底部向上定位最新的“正式结论/状态更新”，不能用顶部旧状态覆盖底部新状态。
- 历史事实只追加更正说明，不删除或改写原记录；冻结事实、历史证据和数据库不修改。
- `当前审查状态.md` 只保存当前快照；本文是对应 R 的唯一事实和历史落点。每次追加必须写 `updatedAt`、`updatedBy`、证据路径和 `nextAction`。
- 台账缺失先记为待补台账；只有影响版本身份、入口安全、数据隔离、测试范围或结果判断时，才升级为业务阻塞。

### 当前状态卡

| 字段 | 内容 |
|---|---|
| `planStatus` | `{{not-created / created / needs-prerequisite / ready / superseded}}` |
| `staticGate` | `{{not-applicable / pending / passed / failed}}` |
| `testPointStatus` | `{{按测试点逐项填写；未执行为 not-tested}}` |
| `closureStatus` | `{{not-started / in-progress / blocked / closed / not-applicable}}` |
| 当前正式结论位置 | `{{本文底部标题、时间和作者}}` |
| 当前指针 | `{{当前审查状态.md 或其他唯一指针路径}}` |
| 下一动作/回退 | `{{13 / 16 / 07 / 05 / 15 / 14 / G1；写清原因}}` |

---

## 一、轮次元信息

| 项目 | 内容 |
|---|---|
| 版本 | `{{版本}}` |
| 功能 | `{{功能名}}` |
| 问题来源轮次 | `R{{N}}` |
| 创建模式 | `R 初始化 / R 测试合并` |
| 继承自 R/来源事实 | `{{无 / 原 R、计划、报告和证据路径}}` |
| 首份关联测试计划 | `{{R{{N}}-1 路径 / 待 14 创建 / 覆盖补充计划路径}}` |
| 本次合并输入计划 | `{{R{{N}}-{{n}} 路径 / 不适用（初始化或新 R 骨架）}}` |
| 当前问题来源 | `{{无 / 回归 / 独立新增 / 覆盖缺口 / 环境或产品处置 / 混合}}` |
| 当前状态 | `{{六种固定 R 状态之一}}` |
| 修复计划 | `{{不适用，首次实测尚未产生问题 / 见第十一节统一 Fix}}` |
| 版本/构建标识 | `{{}}` |
| C0/QA 候选状态 | `{{C0 / QA{{N}} / 覆盖补充复用来源}}` |
| 当前实测快照 | `{{S0 / S{{N}} / 有效 S 复用}}` |
| 来源提交号 | `{{完整提交号 / 未提交}}` |
| 来源工作树指纹 | `{{完整指纹；未提交候选必填}}` |
| 构建目录/构建物哈希 | `{{独立构建目录 / 算法和哈希}}` |
| 测试数据隔离 | `{{测试目录、前缀和真实数据禁止触碰边界}}` |
| 创建/更新时间 | `{{}}` |

### 状态口径

- `🟡待实测`：13 已建立 R 初始化骨架，首次计划和双方实测尚未产生。
- `📝待补证据/待核实`：关键事实、证据、复核或用户确认缺失/冲突；不得进入 Fix。
- `⏸️待环境/产品决策`：环境事项或替代/删除选择等待用户/PM 决定；不得进入 Fix。
- `🟡待修复`：本 R 每一项已有终态处置，且代码 Fix/替代/删除已组成可执行统一 Fix；不表示 Fix 已完成。
- `🟢等待下一轮测试`：05 已对整批 Review 通过且 15 已冻结 QA/S，等待 14 创建下一计划。
- `✅当前轮次闭环`：本 R 所有事项已完成并重新验证，无未关闭回归、覆盖、环境、核实或用户决策缺口；仍须 G1。

---

## 二、证据门禁

| 门禁项 | 路径/事实 | 结论 |
|---|---|---|
| 开发阶段/C0/S0（初始化或首测） | `{{}}` | `✅/❌/不适用` |
| 测试计划及标识 | `{{R{{N}}-{{n}}；绝对路径}}` | `✅匹配 / ❌缺失或冲突 / 初始化待 14` |
| GPT 正式报告 | `{{}}` | `✅正式且覆盖范围 / ❌缺失 / 初始化尚未产生` |
| GPT GUI 证据 | `{{}}` | `✅可定位 / ❌缺失或敏感` |
| 用户 06 原文 | `{{}}` | `✅属于同一标识 / ❌缺失 / 初始化尚未产生` |
| 用户证据 | `{{}}` | `✅可定位 / ❌缺失或敏感` |
| 双方共同核心链路 | `{{测试点 ID}}` | `✅双方各有事实 / ❌缺一方 / 初始化尚未产生` |
| S 来源、构建和隔离 | `{{C0/QA、指纹、构建物哈希、测试目录}}` | `✅明确 / ❌待补` |
| 用户补充问题核验 | `{{独立 GPT 证据路径}}` | `✅已分栏 / ⚠️待补 / 不适用` |

### 缺口清单和补证步骤

| 缺口/阻塞 | 影响范围 | 最小补证步骤 | 责任人 | 截止/复评点 | 状态 |
|---|---|---|---|---|---|
| `{{无 /}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` |

> 用户只列出发现问题的用例时，未列用户用例写为 `本次暂未发现问题（用户未回传详细记录）`，不补写页面、数据或通过证据。问题级 Fix 门通过也不代表 R 整体关闭。

---

## 三、测试计划索引

| 计划 | 测试标识 | 类型 | 绑定 S | 继承/来源 | GPT 报告 | 用户 06/证据 | 状态 |
|---|---|---|---|---|---|---|---|
| `{{路径}}` | `R{{N}}-{{n}}` | `首次 / 修复后 / 覆盖补充` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` |

初始化时填写 `尚未创建；由 14 在合法节点创建`，不得伪造计划标识或实测结果。

---

## 四、双方实测合并

> 本区只追加已有计划的事实，不回写计划正文。计划、GPT 报告和用户 06 原文中要求保留的步骤（包括步骤 1-6）及全部实际判断必须完整保留。

### 4.1 GPT 客观实测

| 测试点/子用例 ID | 实际操作与预期 | 稳定页面/数据实际 | 证据 | 结论/严重度 |
|---|---|---|---|---|
| `R{{N}}-{{n}}-T{{NN}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` |

### 4.2 用户 06 原始回传

- **提交状态**：`发现问题 / 全部完成且无问题 / 无法完成-环境阻塞`
- **用户执行范围**：`{{实际列出的测试点、脚本和步骤}}`
- **客观事实**：`{{原样保留页面、跳转、数据和阻塞}}`
- **主观体验**：`{{理解成本、自然性、文案、信任、确认/取消感受}}`
- **用户证据**：`{{}}`
- **未列出的用户用例**：`本次暂未发现问题（用户未回传详细记录）` 或 `不适用（用户完整回传）`
- **未完成/环境阻塞**：`{{}}`

### 4.3 用户补充问题与 GPT 补充核验

> `用户补充问题 N` 只是来源标签。没有对应计划操作时写“计划覆盖缺口”，不得创建测试点、子用例或用 GPT 核验替代用户验收。

| 来源标签 | 关联测试点/子用例 | 用户是否执行 | 用户原始事实/体验 | GPT 补充核验与独立证据 | 分类与证据等级 | 当前分流 |
|---|---|---|---|---|---|---|
| `用户补充问题 {{N}}` | `{{计划 ID / 无（计划覆盖缺口）}}` | `{{是/否}}` | `{{原样引用；客观与主观分栏}}` | `{{同一 S/副本 GUI 或只读结果；无则写缺口}}` | `{{回归/全新/覆盖缺口/待核实/非缺陷候选；已证实/强推断/待核实}}` | `{{当前 R Fix / 下一 R / 覆盖补充 / 待补 / 待用户确认}}` |

连续点击必须分别登记动作顺序、每次即时反馈、稳定页面状态和按用户前缀的持久化行数；不得用混合前缀总数替代。

### 4.4 双方共同核心链路对照

| 测试点 ID | GPT 客观结果/证据 | 用户实际/证据 | 一致性 | 待处理 |
|---|---|---|---|---|
| `R{{N}}-{{n}}-T{{NN}}` | `{{}}` | `{{}}` | `{{一致/冲突/一方缺失}}` | `{{}}` |

---

## 五、问题集合

| 稳定问题 ID | 测试点/覆盖定位 | 来源（含标签） | 分类 | 严重度 | 复现步骤/预期 | 客观实际 | 用户主观体验 | 影响 | 证据 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|
| `U{{N}}` | `R{{N}}-{{n}}-T{{NN}}` 或 `无（计划覆盖缺口）` | `{{GPT/用户/用户补充问题 N}}` | `{{回归/独立新增/覆盖缺口/环境/待核实/非缺陷候选}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` |

### 分类与证据等级

- 回归留在发现它的原 R；独立新增产品问题、独立覆盖缺口或独立处置事项由 13 建立下一 R，原 R 不删除来源事实。
- 同一问题的补证、冲突和用户确认始终回写原 R；不能因补证另造副本问题。
- 证据等级只使用 `已证实`、`强推断`、`待核实`。代码阅读不能单独构成 GUI 问题复现。

### 5.1 诊断

| 事项/问题 ID | 已证实事实 | 根因诊断 | 证据等级 | 待核实项 |
|---|---|---|---|---|
| `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` |

---

## 六、事项处置总表

> 测试合并后必填。初始化时填“尚未产生事项”。每个事项必须有处置结果；待核实、环境阻塞和待用户确认在完成前会阻断统一 Fix。

| 事项 ID | 类型 | 来源/测试点 | 客观事实 | 用户主观体验 | 证据等级 | 处置类型 | 责任人 | 截止/复评点 | 允许范围/排除范围 | 验收条件 | 阻塞 | 当前状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `{{}}` | `{{代码/覆盖/环境/产品}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{代码 Fix/覆盖补充计划/环境处置决策/替代/删除/补证/待用户确认}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{是/否}}` | `{{}}` |

### 处置类型口径

- `代码 Fix`、批准的 `替代`、批准的 `删除`：纳入同一个统一 `FixN`；
- `覆盖补充计划`：进入下一测试计划，不进入代码 Fix；由 14 创建覆盖补充计划；
- `环境处置决策`：由 16 记录继续排障/替代/删除，决定前保持阻塞；
- `补证/待核实`、`待用户确认`：保持阻塞，不进入 Fix。

### 统一 Fix 可执行检查

| 条件 | 结论 |
|---|---|
| 本 R 每一项都有明确终态处置，无漏项 | `✅/❌` |
| 代码 Fix、替代、删除的范围、责任人、原始场景和验收条件齐全 | `✅/❌` |
| 覆盖缺口已转为下一计划纳入项，未进入代码 Fix | `✅/❌` |
| 无待补证据/待核实、环境/产品决策或待用户确认 | `✅/❌` |
| 所有获准代码事项可组成一次统一 `Fix{{N}}` | `✅/❌` |

只有全部为 `✅` 才能把 R 状态设为 `🟡待修复` 并生成可执行 `FixN`。否则状态为 `📝待补证据/待核实` 或 `⏸️待环境/产品决策`，不得把部分 Fix 写成可执行。

---

## 七、覆盖补充项

| 覆盖项 ID | 来源标签/原计划 | 缺失入口或场景 | 下一计划脚本 | 预期和分支 | 证据要求 | 是否建立覆盖补充 R | 14 状态 |
|---|---|---|---|---|---|---|---|
| `COV{{N}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{是/否}}` | `{{待 14 / 已计划 / 已完成}}` |

覆盖补充不是产品缺陷，不伪造 QA/S；可复用有效 S，但必须在 14 计划中说明来源、隔离和复用理由。

---

## 八、环境/产品决策区

| 事项 ID | 最小环境核对 | 进程参数/环境变量/实际数据目录/GUI | 候选替代入口验证 | 排障责任人 | 独立期限 | 用户/PM 复评点 | 三个选项 | 用户明确选择 | 当前决定 | 是否阻断 Fix |
|---|---|---|---|---|---|---|---|---|---|---|
| `{{}}` | `{{同一 S/副本正式入口重复启动}}` | `{{}}` | `{{至少一个候选或不适用原因}}` | `{{}}` | `{{}}` | `{{}}` | `继续排障 / 替代 / 删除` | `{{原文}}` | `{{}}` | `{{是/否}}` |

无逐项期限和复评点的环境事项持续阻塞。GPT 记录证据和影响，不把建议写成用户决定；16 不修改代码。

---

## 九、替代计划

| 替代事项 | 用户批准原文/16 路径 | 被替代对象 | 新方案 | 迁移/保留边界 | 允许改动范围 | 验收条件 | 责任人 | 状态 |
|---|---|---|---|---|---|---|---|---|
| `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `GLM+DS` | `{{待 Fix / 已执行 / 待 Review}}` |

替代只能在用户明确批准后进入统一 Fix；07 不自行决定替代。

## 十、删除计划

| 删除事项 | 用户批准原文/16 路径 | 删除对象 | 软删除/数据保留/恢复策略 | 不可逆风险 | 允许改动范围 | 验收条件 | 责任人 | 状态 |
|---|---|---|---|---|---|---|---|---|
| `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `GLM+DS` | `{{待 Fix / 已执行 / 待 Review}}` |

删除只能在用户明确批准后进入统一 Fix；不得用删除结果掩盖数据损坏或失败证据。

---

## 十一、统一 Fix 计划

> 由 13 在所有事项完成处置且统一 Fix 门通过后填写；由 07 执行。没有可执行 Fix 时填写“尚未生成，阻塞条件见第六节”。

| 批次 | 事项 ID | 类型 | 修复/替代/删除要求 | 允许范围 | 排除范围 | 原始失败场景 | 验收条件 | 责任人 | 状态 |
|---|---|---|---|---|---|---|---|---|---|
| `Fix{{N}}` | `{{全部获准事项}}` | `{{代码/替代/删除}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `GLM+DS` | `{{可执行/待执行}}` |

不得把待核实、环境阻塞、用户待确认或覆盖缺口带入 Fix。代码变更、依赖或构建输入变化时，07 只记录 QA 候选，05 复审通过后由 15 冻结 QA/S。

---

## 十二、Fix 执行追加区

### Fix{{N}}

- **执行日期/执行者**：`{{}}`
- **处理事项**：`{{}}`
- **未处理/不适用事项**：`{{}}`
- **改动文件/模块及目的**：`{{}}`
- **替代/删除执行事实**：`{{}}`
- **原始失败复现**：`{{}}`
- **自动化测试/构建命令与结果**：`{{}}`
- **原始用户场景复跑、页面稳定态、持久化和证据**：`{{}}`
- **候选 QA、版本字段、来源指纹、构建物哈希、待冻结 S**：`{{}}`
- **未验证项/剩余风险**：`{{}}`
- **下一步**：`Review{{N}}`
- **声明**：未创建测试计划，未创建第二份 R 文档，未修改用户回传和 GPT 正式报告。

---

## 十三、Review 复审追加区

### Review{{N}}

- **对应批次**：`Fix{{N}}`
- **复审模式**：用户实测统一批次
- **复审日期/复审者**：`{{}}`
- **复审构建/来源指纹**：`{{}}`
- **复审依据**：`{{事项处置、Fix、原始计划、GPT/用户证据和技术栈规范}}`

| 事项 ID | 原始失败场景与预期 | 复审操作/实际 | 替代/删除结果 | 证据 | 结论 | 越界/新问题 |
|---|---|---|---|---|---|---|
| `{{}}` | `{{}}` | `{{}}` | `{{不适用 /}}` | `{{}}` | `✅已解决/❌未解决/⏸️待环境补证（05复审未完成，未进入用户实测）` | `{{无/}}` |

- **覆盖补充登记核对**：`{{}}`
- **技术栈和隔离核对**：`{{无红线 /}}`
- **复审结论**：`✅通过（修复复审） / 🟡仍需修复 / 🔴驳回`
- **下一步**：`15A 最终候选同步 / 回到 07 / 补证`

05 通过后不直接冻结 QA/S；必须先由 15A 完成源代码同步并追加以下记录，再由 15 追加第十五节。15A 失败时只能保留 `blocked`/`failed`，不得写 QA/S 已冻结或 `🟢等待下一轮测试`。

---

## 十四、最终候选源代码同步记录（15A）

| 字段 | 内容 |
|---|---|
| 批次/候选策略 | `{{B{{N}}；direct（单 R）/merge（多 R）}}` |
| 最终候选目录 | `{{E:\workbuddy 复审\<B>\R<N>-Fix<M>-only 或 merge}}` |
| 固定命令 | `{{python E:\workspace\tools\r_fix_review.py sync-final-candidate ...}}` |
| Review 状态文件 | `{{E:\workbuddy 复审\<B>\...\control\review-status.json}}` |
| 机器结果 | `{{E:\workbuddy 复审\<B>\control\source-sync-result.json}}` |
| sourceSyncStatus | `{{verified / blocked / failed}}` |
| 源包 | `{{E:\workbuddy-snapshots\<B>-<candidate>-source-sync\；完整 source-tree、清单、approved-source-diff、只读}}` |
| 同步前/后工作树 | `{{workspaceFingerprint、treeFingerprint、HEAD、Git index hash}}` |
| 批准差异 | `{{approvedDiffFingerprint、逐文件 applied/preserved、增删文件、未批准变化}}` |
| 冲突/回滚 | `{{无 / 冲突文件 / 回滚状态 / 残留文件}}` |
| 当前开发指针 | `{{E:\workbuddy 复审\current-development-pointer.json；与结果一致}}` |
| B operationalClosureStatus | `{{verified / blocked / failed；未 verified 不得关闭}}` |
| 15A 结论/下一步 | `{{同步已验证，交 15 / 留在 15A 补证或解决冲突}}` |

15A 只引用 `source-sync-result.json` 的机器事实，不在文档中重复维护另一套哈希；不负责代码审查、用户实测、QA/S 编号或测试计划。

---

## 十五、QA/S 冻结记录

| 关联 Fix/Review | QA 编号 | S 编号 | 版本字段 | 来源提交/工作树指纹 | 构建目录 | 构建物哈希 | 快照目录/清单 | 冻结时间 | 数据隔离/入口 | 复用旧 S 理由 | 15 结论 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `Fix{{N}}/Review{{N}}` | `{{QA{{N}} / 复用}}` | `{{S{{N}} / 复用}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{不适用 /}}` | `{{已冻结 / 待补}}` |

QA 编号在当前版本全局递增。15 只在第十四节 `sourceSyncStatus=verified`、当前开发指针匹配且源包/批准差异可复核后核对/冻结候选，不修改代码、版本字段或测试计划。冻结成功后 R 才能进入 `🟢等待下一轮测试`，再由 14 创建计划。

---

## 十六、问题集合冻结与跨 R 映射

### 16.1 问题集合冻结

| 冻结时间 | R | 本轮事项总数 | 代码 Fix/替代/删除事项 | 覆盖项 | 待核实/环境/用户决策 | 证据完整性 | 冻结者 | 结论 |
|---|---|---|---|---|---|---|---|---|
| `{{}}` | `R{{N}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `GPT` | `{{未冻结/已冻结}}` |

### 16.2 跨 R 映射

| 原 R/来源问题或事项 | 原始测试点/证据 | 分类理由 | 承接 R | 新 R 唯一闭环路径 | 后续唯一归属 | 原 R 保留内容 |
|---|---|---|---|---|---|---|
| `{{}}` | `{{}}` | `{{回归留原 R / 独立新增或覆盖缺口开下一 R}}` | `{{R{{N+1}} / 不适用}}` | `{{}}` | `{{Fix/Review/计划归属}}` | `{{事实、体验、证据和分流理由}}` |

原 R 只保留来源和映射，不把新 R 的未来测试写成已执行；同一问题补证仍回写原 R。

---

## 十七、轮次分流和时间线

### 分流结论

- 仅回归：留在当前 R，`Fix → Review → 15A → 15 → 14` 创建同 R 下一计划。
- 仅独立新增：原 R 保留无回归事实和来源；13 建立下一 R 唯一闭环，不创建下一计划。
- 回归 + 独立新增：原 R 继续处理回归，下一 R 承接独立新增；两 R 可独立推进，代码修改串行。
- 覆盖缺口：进入覆盖补充 R/14 计划，不自动成为产品问题或代码 Fix。
- 待核实、环境阻塞、待用户确认：保持相应阻塞状态，未完成前不得 Fix 或关闭 R。

### 时间线

| 日期 | 事件 | 执行者 | 文档/证据 | 结论 |
|---|---|---|---|---|
| `{{}}` | R 初始化/测试合并 | GPT | `{{}}` | `{{}}` |
| `{{}}` | GPT 报告/用户 06 | GPT/用户 | `{{}}` | `{{}}` |
| `{{}}` | 事项处置/16 决策 | GPT/用户 | `{{}}` | `{{}}` |
| `{{}}` | Fix{{N}} | {GLM / DS / GLM+DS} | `{{}}` | `{{}}` |
| `{{}}` | Review{{N}} | GPT | `{{}}` | `{{}}` |
| `{{}}` | 15A 最终候选源代码同步 | GPT/机器工具 | `{{source-sync-result.json、源包、current-development-pointer.json}}` | `{{verified / blocked / failed}}` |
| `{{}}` | 15 冻结 QA/S | GPT | `{{}}` | `{{}}` |
| `{{}}` | 14 下一计划/G1 | GPT | `{{}}` | `{{}}` |

---

## 十八、正式结论

- **当前 R 状态**：`🟡待实测 / 📝待补证据/待核实 / ⏸️待环境/产品决策 / 🟡待修复 / 🟢等待下一轮测试 / ✅当前轮次闭环`
- **双方证据是否齐全**：`{{}}`
- **当前统一 Fix 是否可执行**：`{{是/否；条件}}`
- **用户验收**：`本文件不替代用户验收；需用户亲自完成计划核心链路`
- **G1/C1/维护期**：`尚不能由单个 R 结论宣称`
- **下一步**：`{{补证 / 16 / 07 Fix / 05 Review / 15A / 15 冻结 QA/S / 14 / G1}}`

## 十九、批次级合并复审（仅 B<N>-merge 文档）

> 本节只用于跨多个 R 的批次级唯一全流程文档。普通 R 文档填写“不适用；本 R 不属于批次合并”。不得用本节替代 R 的 Fix/Review 或测试计划。

### 19.1 批次元信息

| 项目 | 内容 |
|---|---|
| 批次标识 | `B{{N}}` |
| 共同基线 | `C0/S0；提交、版本字段、构建目录和哈希` |
| 纳入 R | `{{R1、R2...及唯一闭环绝对路径}}` |
| 初始复审次数 | `{{R 数 + 1}}` |
| R-only 候选 | `{{每个 R 的来源、允许差异、构建目录、哈希、入口和 userData}}` |
| 合并候选 | `{{B{{N}}-merge 来源、版本字段、构建目录、哈希和完整清单}}` |
| 15A 同步结果 | `{{E:\workbuddy 复审\<B>\control\source-sync-result.json；sourceSyncStatus=verified}}` |
| 最终源包 | `{{E:\workbuddy-snapshots\<B>-merge-source-sync\；完整、只读、清单哈希一致}}` |
| 当前开发指针 | `{{E:\workbuddy 复审\current-development-pointer.json；与同步后工作树一致}}` |
| 批次唯一文档 | `{{绝对路径}}` |
| operationalClosureStatus | `{{in-progress / blocked / failed / verified；未有 verified 15A 凭证时保持未关闭}}` |
| 当前状态 | `{{候选准备 / R-only 复审 / 合并复审 / 待 15A / 待 15 / 已冻结 / 阻断}}` |

### 19.2 R-only 复审追踪

| R | Fix/Review | 候选来源和哈希 | 独立入口/userData | GUI/只读/构建证据 | 结论 | 下一步 |
|---|---|---|---|---|---|---|
| `R{{N}}` | `{{Fix{{N}} / Review{{N}}}}` | `{{}}` | `{{}}` | `{{}}` | `{{✅通过 / 🟡仍需修复 / 🔴驳回}}` | `{{}}` |

### 19.3 合并复审追加区

#### B{{N}}-Merge-Review{{N}}

- **对应合并候选**：`{{}}`
- **复审日期/复审者**：`{{}}`
- **复审依据**：`{{R-only 通过记录、合并清单、原始失败场景、技术栈和隔离规范}}`

| R/事项 | 原始失败场景与预期 | 合并后操作和实际 | 替代/删除/保留边界 | 证据 | 结论 | 新问题/越界 |
|---|---|---|---|---|---|---|
| `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{}}` | `{{✅已解决 / ❌未解决 / ⏸️待环境补证（05复审未完成，未进入用户实测） / ℹ️非本次范围}}` | `{{}}` |

- **批次跨功能回归**：`{{}}`
- **数据隔离/来源指纹/技术栈**：`{{}}`
- **复审结论**：`{{✅通过（修复复审） / 🟡仍需修复 / 🔴驳回}}`
- **下一步**：`{{追加 B{{N}}-merge 修复记录 / 下一合并复审 / 15A / 补证}}`

### 19.4 合并阶段修复追加区

#### B{{N}}-merge 修复记录{{N}}

- **触发的合并复审**：`{{}}`
- **失败事实和严重度**：`{{}}`
- **最小修复范围**：`{{}}`
- **排除范围**：`{{不得回流 R1/R2，不创建新的 R FixN}}`
- **执行文件/测试/构建事实**：`{{}}`
- **重建候选、来源指纹和哈希**：`{{}}`
- **下一步复审**：`B{{N}}-Merge-Review{{N+1}}`

### 19.5 批次冻结交接

- **15 前置**：`所有 R-only 与最终合并复审均通过；无阻断、决策缺口或越界`
- **最终冻结对象**：`{{仅最终合并包；不冻结中间候选}}`
- **QA/S**：`{{QA{{N}}/S{{N}}；由 15 核对全局编号}}`
- **冻结后部署**：`{{正式入口、部署前后哈希、userData 不迁移/不清空}}`
- **14 后续计划**：`{{各 R 计划标识，均绑定同一个 S}}`
