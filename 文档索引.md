# 文档索引（WorkBuddy 单一入口）

> **维护方**：GPT　**作用**：任何人/AI 第一次进场或找文件，从这里开始。
> **上位文件**：`规范类/文档治理总纲.md`（权威章程）。
> **建立日期**：2026-08-07。

---

## 新人快速入门（优先级最高）

1. **[项目状态看板](./项目状态看板.md)**：当前在做什么、下一步是什么（统一入口）
2. **[快速开始](./规范类/00-快速开始.md)**：10 分钟入门（L1 核心层）
3. **[日常开发流程](./规范类/01-日常开发流程.md)**：1 小时掌握（L2 流程层）
4. **[完整工作流](./工作流.md)**：详细参考手册（L3 完整层，按需查阅）

## 项目一句话 · 当前状态 · 真相源

- **WorkBuddy**：AI 原生、本地优先的 Windows 桌面个人工作台（Electron + Vue3 + TS + better-sqlite3 + OpenAI 兼容 LLM）。协作：**GPT&Claude=PM/审查 · GLM+DS=代码执行 · 用户=决策与实测**。
- **当前阶段**：任务数据流通重构 · 阶段 6 — 旧模块切换清理（✅开发闭环）；阶段4、阶段5已✅开发闭环，用户实测仍按功能分别记录——详见 `当前审查状态.md`。
- **版本目标**：`0.3.0` 先完成确定性结构化任务流；AI 增强延后。
- **真相源**：`用户打回重构/任务数据流通重构（v0.3）/`（当前主线项目计划与阶段全流程记录）。

> ⚠️ **双根提醒**：文档根 = `E:\workspace\`（所有 .md 在此）；代码根 = `E:\workspace\workbuddy\`。所有相对文档路径相对**文档根**。详见 `文档治理总纲.md §2`。

## 入口阶段判定

> **当前入口阶段：B1 当前批次业务实测。**
> B1 尚未形成完整业务实测结论，但这只是当前结果状态，不是入口阻塞原因。
> 在 B1 当前批次结束前，R1-3 与 R2-1 的唯一实际操作入口是 `E:\workbuddy-test\启动实测.bat`，角色选择器是 `E:\workbuddy-test\选择B1实测角色.bat`。
> `E:\workbuddy-test\长期入口\` 仅为 B1 完成后的后续长期实测入口，当前为“已预建、未启用”，不得用于本次 B1 实测。
> B1 结束后，旧入口保留为历史资产；“切换入口”仅表示后续运行改用长期入口，不表示覆盖、迁移、重命名或删除旧入口。

当前 R1-3、R2-1 计划状态本轮保持 `🟡待补前置`，后续由独立复核重新判断是否可执行；不得把该状态或“B1 业务实测未完成”解释为本次 B1 当前批次入口不可用。

## 当前规则在哪里 · 如何判断能否启动

新对话或换角色后，按以下顺序读取，不凭文件名、旧章节顶部状态或聊天记忆判断当前门禁：

1. 先读本文件的“入口阶段判定”和本节；
2. 再读 `当前审查状态.md`，确认当前生效规则、适用入口、计划状态、`E0/T01` 分层和下一步；
3. 再读 `规范类/阶段闭环门禁规则.md` 的 `01–16` 唯一开启条件矩阵和统一四层状态模型；
4. 最后读适用全流程闭环文档**底部最新正式结论**，再读计划、报告和证据索引；
5. 只有当前 Prompt 的开启条件全部满足，才允许调用对应 Prompt。

全流程闭环文档允许保留多份历史状态；旧顶部状态不自动覆盖底部最新正式结论。冻结文档、历史计划、历史报告、冻结 QA/S、历史证据和数据库不能修改；最新规则通过现行治理文档和 `当前审查状态.md` 指针向前生效。台账缺失先登记为待补台账，只有影响版本身份、入口安全、数据隔离或测试范围定位时才升级为业务阻塞。

判断一个阶段能否启动时，必须同时写出四个字段：`planStatus`、`staticGate/E0`、`testPointStatus`、`closureStatus`。`E0=passed` 只开放 `T01` 取证；`T01=not-tested` 不是启动阻塞。完整矩阵唯一见 `规范类/阶段闭环门禁规则.md`，Prompt 只补充本阶段操作字段。

## 2026-08-21 新用户实测规则入口

新规则只向前生效，现有 v0.3 历史计划、GPT 记录和全流程文件原样保留，不迁移、不重写。

> **代称先查**：`规范类/项目代称.md` 是 `C0`、`S`、`QA`、`R`、`Fix`、`Review`、`G1`、`C1`、`CR` 及角色/通用缩写的唯一词典。

| 需要 | 权威入口 |
|---|---|
| 判断当前适用规则、入口和能否启动 | `当前审查状态.md` + `规范类/阶段闭环门禁规则.md`；按本文件“当前规则在哪里”固定顺序读取 |
| 初始化 R 唯一闭环骨架 | `用户prompt/13-生成用户实测全流程闭环.md` 的 `R 初始化`；C0/S0 后只写骨架，不要求测试计划、GPT 报告或用户 06 |
| 创建一次 R 测试计划 | `用户prompt/14-生成用户实测计划.md` + `模板/05_实测部分/项目测试计划模板.md`；支持首次、修复后和覆盖补充计划，13 不创建 |
| 创建 G1 版本级整合计划 | `模板/05_实测部分/项目测试计划模板.md`；落点 `用户实测阶段/vX-版本级整合/G1/vX-G1版本级整合测试计划.md` |
| GPT 自主 GUI 实测 | `用户prompt/12-GPT自主实测.md`；输入测试计划绝对路径 + `R<N>-<n>` 或 `G1` |
| 用户三态回传 | `用户prompt/06-用户反馈测试问题.md`；状态为发现问题/全部完成且无问题/无法完成-环境阻塞；发现问题可只列问题用例并附 `用户补充问题 N` |
| 合并双方实测 | `用户prompt/13-生成用户实测全流程闭环.md`；R 续写原 `R<N>` 或新建下一 `R` 唯一闭环文档，G1 回写 G1 计划合并区；不创建测试计划 |
| GLM+DS 修复 | `用户prompt/07-修复用户反馈.md`；输入轮次闭环绝对路径 + `FixN`；记录真实执行者 |
| GPT 用户反馈复审 | `用户prompt/05-gpt复审.md`；输入轮次闭环绝对路径 + `ReviewN` |
| 最终候选源代码同步 | `用户prompt/15A-最终候选同步开发基线.md`；仅在 05 整批 Review 通过后执行，机器事实为 `source-sync-result.json` |
| 冻结 QA/S | `用户prompt/15-冻结QA-S.md`；仅在 15A `sourceSyncStatus=verified` 后执行 |
| 环境/产品处置 | `用户prompt/16-环境与产品处置决策.md`；记录同一 S/副本证据和用户明确选择 |
| 多 R 批次合并复审 | `用户prompt/05-gpt复审.md` 的 B<N> 模式；批次 SSOT `用户实测阶段/vX-功能名/合并复审/B<N>-merge的全流程闭环.md` |
| GPT 报告模板 | `模板/GPT实测记录模板.md`；R 报告落 `GPT实测记录/vX实测内容/第N轮/`，G1 报告落 `.../G1/` |
| 用户实测轮次模板 | `模板/05_实测部分/用户实测问题全流程闭环模板.md` |

版本/候选节点：`C0` 是开发进入实测的一次性基线提交，`S0` 是 C0 构建快照，`QA/S` 是实测阶段候选与不可变快照，`C1` 是当前版本所有活跃功能/问题轮次完成、G1 通过且用户完成最终验收后的正式版本节点。编号含义和历史同名兼容规则详见 `规范类/项目代称.md`。GPT 报告与用户证据分开归档；双方共同核心链路必须分别执行，GPT 通过不等于用户验收通过。

---

## 文档地图（归属 + 用途）

### 文档根散落文件
| 文件 | 维护方 | 用途 |
|---|---|---|
| `当前代码状态.md` | **GLM+DS** | 实时代码看板（阶段状态/自测/已知项）；同一时刻单写者并标 `updatedBy` — 审查前必读 |
| `当前审查状态.md` | **GPT&Claude** | 审查看板（当前审查任务/门禁/用户证据边界） |
| `文档索引.md` | GPT | 本文件，入口地图 + 新旧路径映射表 |
| `工作流.md` | GPT | 当前全阶段工作流、角色边界和启动/回退顺序（以规范类唯一矩阵为准） |
| `改革计划.md` | GPT | 文档结构改革方案（执行手册，已定稿） |
| `外缘审查及优化建议.md` | GPT | 外部审查建议原文及现行角色覆盖说明；历史建议与证据只读保留 |
| `开发阶段/v0.1-今日总览与项目管理（历史）/用户测试流程（开发阶段项目）.md` | GPT | v0.1 用户实测步骤 A–H（历史保留） |
| `开发阶段/v0.2-计划与复盘体系（历史）/用户测试流程（计划与复盘体系）.md` | GPT | v0.2 用户实测步骤（历史保留） |
| `安装说明.md` | GPT | SmartScreen/安装/数据位置/卸载 |
| `交接记录/GPT接任GLM项目经理交接.md` | GPT | 项目历史/接管记录；追加式保存角色迁移、事实证据、决策和执行日志 |

### 文件夹（主要分类）
| 文件夹 | 维护方 | 内容 / 用途 |
|---|---|---|
| `模板/` | GPT + GLM+DS | 模板：01_计划部分/ 02_日志部分/ 03_审查部分/ 04_反馈部分/ 05_实测部分/；根目录保留唯一 GPT 实测报告模板 |
| `规范类/` | GPT + GLM+DS | GPT维护治理、门禁、代称和变更规则；GLM+DS维护命名与实现侧规范；包含 L1/L2/L3 三级文档 |
| `开发阶段/` | GPT + GLM+DS | 新阶段规格/审查由 GPT 维护，实现日志由 GLM+DS 维护；历史 GLM 闭环原文只读 |
| `用户实测阶段/` | GPT + 用户 + GLM+DS | GPT记录治理与诊断，用户提供实测，GLM+DS执行代码修复 |
| `变更开发/` | GPT + GLM+DS | 维护期 CR 全流程闭环记录 |
| `用户prompt/` | GPT | 闭环 prompt：00（总索引）·01-05（构建期/复审）·06（用户三态回传）·07（R 统一 Fix）·08-09（维护期 CR）·10（功能立项）·11（Git 归档）·12（GPT 实测）·13（R 初始化/实测合并/G1 合并）·14（实测计划）·15A（最终候选源代码同步）·15（QA/S 冻结）·16（环境/产品决策） |
| `变更记录/` | GPT | CHANGELOG.md 发版历史 —— **活工具，保留活跃** |
| `用户打回重构/` | GPT + GLM+DS | 当前任务数据流通重构主线；阶段计划与各阶段全流程闭环记录 |
| `项目管理改革前文档记录/` | **归档** | 开发阶段项目计划/ 计划与复盘体系实现项目计划/ 审查日志/ 项目日志/ 用户最终测试反馈/ 初级文档/ —— **历史记录归档** |

---

## 「我要找 X，该看哪」任务路由

| 我想做什么 | 看哪里 |
|---|---|
| 了解项目总规划、阶段划分、验收/禁止边界 | `开发阶段/v0.1-今日总览与项目管理（历史）/` → 历史记录归档于 `项目管理改革前文档记录（历史）/开发阶段项目计划（历史）/` |
| 了解某阶段的实现基准 | `开发阶段/` → 对应版本的全流程闭环记录文档；或 `项目管理改革前文档记录/` 的历史规格 |
| 了解"现在到哪了" | `当前代码状态.md`（实现侧）+ `当前审查状态.md`（审查侧） |
| 查 GLM+DS 某阶段/CR 做了什么 | `开发阶段/` → 全流程文档「实现日志」环节；或 `项目管理改革前文档记录/项目日志/` 的历史日志 |
| 查 GPT 某阶段/CR 审了什么、结论 | `开发阶段/` 或 `变更开发/` → 全流程文档「审查/复审」环节 |
| 查历史 GLM 某阶段/CR 审了什么、结论 | `开发阶段/` 的既有历史环节；或 `项目管理改革前文档记录/审查日志/` 的历史审查 |
| 启动一个新阶段 | `用户prompt/01` |
| GLM 首次执行代码前熟悉仓库 | `用户prompt/02A-GLM代码熟悉.md`（一次性只读熟悉；不替代 02/04/07/08 门禁） |
| 查 prompt 怎么用 / 跑功能子阶段闭环 | `用户prompt/00-开发单元闭环Prompt使用说明.md`（已适配新结构） |
| 了解/启动「计划与复盘」功能 | `开发阶段/v0.2-计划与复盘体系（历史）/`（历史功能记录） |
| 了解/启动「任务数据流通重构」阶段 | `用户打回重构/任务数据流通重构（v0.3）/项目计划.md` → 对应阶段全流程闭环记录 |
| 维护期改一个 bug / 做一个小功能 | `规范类/变更闭环规则.md` → `变更开发/` → 用户 prompt 08 |
| 阶段 / CR 已闭环，准备在 git 中归档 | `用户prompt/11-在git中归档.md` + `规范类/git提交规范.md` |
| 初始化/创建/执行用户实测 | `13 R 初始化` → `14` 首次/修复后/覆盖补充计划 → `12` + 用户 06；测试完成交 `13` 合并 |
| GPT 完成自主 GUI 实测 | `用户prompt/12-GPT自主实测.md` → R 归档到 `第N轮/`，G1 归档到 `G1/` |
| 用户提交无问题、问题或环境阻塞 | `用户prompt/06-用户反馈测试问题.md` |
| 合并 GPT 与用户实测 | `用户prompt/13-生成用户实测全流程闭环.md` → R 续写原 R 并按独立事项建立下一 R；G1 只回写 G1 计划 |
| 执行 R 修复闭环 | `13 事项处置` → `16`（如有）→ `07` 统一 Fix → `05` 整批 Review → `15A` 最终候选同步 → `15` QA/S → `14` 后续计划 |
| 用户报了个问题 | `用户prompt/06` → 13 保留原始事实并合并到对应 `R<N>` 唯一轮次文档；补充问题由 13 另做同副本客观核验 |
| 查版本演进 / 某版本改了啥 | `变更记录/CHANGELOG.md` |
| 查命名/路径/双根规则 | `规范类/文档治理总纲.md` + `规范类/命名规则.md` |
| 查项目代称、版本节点和编号作用域 | `规范类/项目代称.md` |
| 查 git 提交 / 归档规范 | `规范类/git提交规范.md` |
| 新建某类文档 | `模板/` 对应部分编写模板（服务新增环节） |
| 查闭环门禁规则 | `规范类/阶段闭环门禁规则.md` |
| 查改革方案本身 | `改革计划.md`（本改革执行手册） |

---

## 两套闭环快速索引

**构建期（阶段 0–6）**：规格 → 实现日志 → 审查 → Fix → Review → ✅ 开发单元闭环
- **历史记录**：`项目管理改革前文档记录/` 中的分散文档
- **新结构**：`开发阶段/` 中的**全流程闭环记录**文档（整合所有环节）

**维护期（全部功能和阶段 6 用户 E2E 完成后）**：CR → 实现日志 → 审查 → 修复 → 复审 → ✅ 关闭 + CHANGELOG + 版本 bump
- **历史记录**：`项目管理改革前文档记录/` 中的分散文档
- **新结构**：`变更开发/` 中的**全流程闭环记录**文档；单个功能用户实测闭环不自动进入维护期

**功能实测闭环**：开发闭环 → `C0/S0` → 13 建立 R 骨架 → 14 创建计划 → GPT 12 + 用户 06 → 13 合并并登记事项处置 → 16 决策（如有）→ 07 统一 Fix → 05 整批 Review → 15A 最终候选同步 → 15 冻结 QA/S → 14 后续计划；独立新增/覆盖/处置事项由 13 建立下一 R。所有活跃轮次关闭后执行 `G1`，用户完成最终验收后才具备形成当前目标版本 `C1` 的条件。
- **历史记录**：`项目管理改革前文档记录/用户最终测试反馈/` + `项目日志/` 中的分散修复记录
- **新结构**：`用户实测阶段/vX-功能名/第N轮/` 中的**轮次唯一全流程闭环记录**；GPT 报告单独在 `GPT实测记录/`

> **关键改革**：构建期继续采用开发单元全流程单文档；用户实测采用“同轮唯一全流程文档 + 多份测试计划 + 独立 GPT 报告”的结构。历史记录已归档但保持可读，详见 `用户prompt/00-开发单元闭环Prompt使用说明.md` 与 `规范类/阶段闭环门禁规则.md`。

> 完整规则见 `规范类/变更闭环规则.md`；制度章程见 `规范类/文档治理总纲.md`。

### B1 当前批次入口

| 项目 | 权威路径/规则 |
|---|---|
| 批次范围 | `B1 = R1 + R2`，共同基线 `C0/S0` |
| R1-only | `C0 + R1 Fix1`；R1 使用 `Review1` |
| R2-only | `C0 + R2 Fix1`；R2 已有前置停止 `Review1`，后续使用 `Review2` |
| B1-merge SSOT | `用户实测阶段/v0.3-任务数据流通重构/合并复审/B1-merge的全流程闭环.md` |
| B1 一次性入口前置补充 | `用户实测阶段/v0.3-任务数据流通重构/合并复审/B1-实测入口一次性解阻前置补充.md`；当前预检证据 `E:\workbuddy-test\R1-3&R2-1\B1-20260829\entry\evidence\B1-entry-once-preflight.json`；只记录本批次入口/隔离前置，不含业务实测结果；当前 GUI 前缀、关闭重进和角色切换证据待补 |
| 初始复审次数 | `3 = 2 个 R + 1 个合并复审` |
| 冻结顺序 | 三次复审及必要合并追加复审通过后调用 `15A` 同步最终 merge 源树；核对 `sourceSyncStatus=verified` 后由 `15` 仅冻结最终合并包为 `QA1/S1` |
| 正式实测入口 | B1 当前批次使用 `E:\workbuddy-test\启动实测.bat`；冻结部署完成后继续由该入口启动 S1；长期入口不参与本次 B1 |

### B/R 最终候选源代码同步

| 项目 | 权威路径/规则 |
|---|---|
| 15A Prompt | `用户prompt/15A-最终候选同步开发基线.md` |
| 固定命令 | `python E:\workspace\tools\r_fix_review.py sync-final-candidate --batch-id B<N> --candidate-strategy direct|merge --candidate-dir <最终候选目录> --review-status <Review 状态文件> --code-root E:\workspace\workbuddy --review-root "E:\workbuddy 复审" --snapshot-root E:\workbuddy-snapshots` |
| 单 R 策略 | `candidateStrategy=direct`；R-only 最终候选可作为 B 候选 |
| 多 R 策略 | `candidateStrategy=merge`；只允许最终 `merge` 候选同步，R-only 中间候选不得同步为 B 最终对象 |
| 同步结果 | `E:\workbuddy 复审\<B>\control\source-sync-result.json`，唯一机器事实；成功必须为 `sourceSyncStatus=verified` |
| 开发指针 | `E:\workbuddy 复审\current-development-pointer.json`；记录候选、源包、HEAD、工作树指纹和清单哈希 |
| 源包根 | `E:\workbuddy-snapshots\<B>-<candidate>-source-sync\`；完整源树和批准差异，完成后只读 |
| 15A 后置 | `sourceSyncStatus=verified` 且开发指针匹配后才可 `15`；失败留在 15A，不得写 QA/S 冻结或后续计划 |

---

## 维护方速记

- **GPT&Claude 写**：规格 / 审查与复审 / 治理 / 模板 / 用户文档 / 索引 / CHANGELOG / CR 起草 / GPT&Claude 实测报告 / 用户实测轮次合并。**不写产品代码**。
- **GLM+DS 写**：代码 / `当前代码状态.md` / `规范类/命名规则.md` / 实现日志环节；每次写明实际执行者，状态文件同一时刻单写者。

---

## 新旧路径映射表

> **说明**：本表记录文档结构改革（2026-08-12）中的路径变更，帮助用户和AI定位迁移后的文件。

### 规范类文档
| 旧路径 | 新路径 | 说明 |
|---|---|---|
| `项目规范/文档治理总纲.md` | `规范类/文档治理总纲.md` | 重命名文件夹 |
| `项目规范/变更闭环规则.md` | `规范类/变更闭环规则.md` | 重命名文件夹 |
| `项目规范/命名规则.md` | `规范类/命名规则.md` | 重命名文件夹 |
| `项目规范/_README.md` | `规范类/_README.md` | 重命名文件夹 |
| **新增**：`规范类/阶段闭环门禁规则.md` | 三层门禁落地文档 |
| **新增**：`规范类/项目代称.md` | 项目治理代称、版本节点和历史同名兼容词典 |

### 开发阶段文档
| 旧路径 | 新路径 | 说明 |
|---|---|---|
| `开发阶段项目计划/规格-阶段N-*.md` | 已归档 | 历史规格文档 |
| `项目日志/阶段N.md` | 已归档 | 历史项目日志 |
| `审查日志/阶段N.md` | 已归档 | 历史审查日志 |
| `开发阶段/v0.1-今日总览与项目管理（历史）/` | 历史保留 | v0.1 全流程闭环记录 |
| `开发阶段/v0.2-计划与复盘体系（历史）/` | 历史保留 | v0.2 全流程闭环记录 |

### 任务数据流通重构文档
| 文档 | 用途 |
|---|---|
| `用户打回重构/任务数据流通重构（v0.3）/项目计划.md` | 六阶段范围、依赖、验收、禁止边界与错误降级总规划 |
| `用户打回重构/任务数据流通重构（v0.3）/阶段1-数据模型与基建-全流程闭环记录.md` | 阶段1 权威闭环记录 |
| `用户打回重构/任务数据流通重构（v0.3）/阶段2-周统筹页-全流程闭环记录.md` | 阶段2 权威闭环记录 |
| `用户打回重构/任务数据流通重构（v0.3）/阶段3-日规划与rail-全流程闭环记录.md` | 阶段3 权威闭环记录 |
| `用户打回重构/任务数据流通重构（v0.3）/阶段4-今日总览四渠道-全流程闭环记录.md` | 阶段4 权威闭环记录 |
| `用户打回重构/任务数据流通重构（v0.3）/阶段5-复盘与趋势-全流程闭环记录.md` | 阶段5 权威闭环记录（✅开发闭环） |
| `用户打回重构/任务数据流通重构（v0.3）/阶段6-旧模块切换清理-全流程闭环记录.md` | 阶段6 权威闭环记录（✅开发闭环） |

### 用户实测阶段文档
| 旧路径 | 新路径 | 说明 |
|---|---|---|
| `用户测试流程（开发阶段项目）.md` | `用户实测阶段/v0.1-开发阶段实测/` | 测试流程文档 |
| `用户测试流程（计划与复盘体系）.md` | `用户实测阶段/v0.2-计划与复盘实测/` | 测试流程文档 |
| `用户最终测试反馈/用户反馈N.md` | `用户实测阶段/v0.1-开发阶段实测/` | 用户反馈文档 |
| **新规则（向前生效）** | `用户实测阶段/vX-功能名/第N轮/` | `R<N>` 轮次目录；每次计划、证据和同轮唯一闭环 |
| **新规则（向前生效）** | `GPT实测记录/vX实测内容/第N轮/` | `R<N>-<n>` GPT 报告和证据；不写入代码根 |
| **新规则（向前生效）** | `用户实测阶段/vX-版本级整合/G1/` + `GPT实测记录/vX实测内容/G1/` | 独立 G1 计划、双方证据和 GPT 报告；不归入任何 R 轮次 |

### 变更开发文档
| 旧路径 | 新路径 | 说明 |
|---|---|---|
| `项目日志/用户反馈N-修复M.md` | `变更开发/v0.1-用户反馈修复/` | 历史修复记录整合 |
| `v0.2测试修复计划/` | `变更开发/v0.2-参照完整性/` | 历史修复计划整合 |

### 模板文档
| 旧路径 | 新路径 | 说明 |
|---|---|---|
| `模板/项目计划模板.md` | `模板/01_计划部分/项目计划部分编写模板.md` | 改名 + 子分类 |
| `模板/项目日志模板.md` | `模板/02_日志部分/项目日志部分编写模板.md` | 改名 + 子分类 |
| `模板/审查日志模板.md` | `模板/03_审查部分/审查部分编写模板.md` | 改名 + 子分类 |
| `模板/用户反馈模板.md` | `模板/04_反馈部分/用户实测反馈部分编写模板.md` | 改名 + 子分类 |
| `模板/变更请求模板.md` | `模板/04_反馈部分/变更请求部分编写模板.md` | 改名 + 子分类 |
| `模板/变更日志模板.md` | `模板/02_日志部分/变更日志部分编写模板.md` | 改名 + 子分类 |
| **新规则** | `模板/GPT实测记录模板.md` | GPT 自主 GUI 报告唯一模板 |
| **新规则** | `模板/05_实测部分/项目测试计划模板.md` | 首次/修复后/覆盖补充 `R<N>-<n>` 计划或 `G1` 版本级整合计划 |
| **新规则** | `模板/05_实测部分/用户实测问题全流程闭环模板.md` | `R<N>` 唯一用户实测全流程闭环 |

| 多 R 批次合并复审闭环模板 | 模板\05_实测部分\多R批次合并复审闭环模板.md | B<N> 跨 R 候选、动态门禁、merge 集成复审、15A 源代码同步和 QA/S 交接 |

### 归档文档
| 原路径 | 归档位置 | 说明 |
|---|---|---|
| `开发阶段项目计划/` | `项目管理改革前文档记录/开发阶段项目计划/` | 历史项目计划 |
| `计划与复盘体系实现项目计划/` | `项目管理改革前文档记录/计划与复盘体系实现项目计划/` | 历史项目计划 |
| `项目日志/` | `项目管理改革前文档记录/项目日志/` | 历史项目日志 |
| `审查日志/` | `项目管理改革前文档记录/审查日志/` | 历史审查日志 |
| `用户最终测试反馈/` | `项目管理改革前文档记录/用户最终测试反馈/` | 历史用户反馈 |
| `初级文档/` | `项目管理改革前文档记录/初级文档/` | 历史冻结文档 |

### 活工具（保持活跃区）
| 文件/文件夹 | 状态 | 说明 |
|---|---|---|
| `用户prompt/` | **保留活跃** | 闭环 prompt，已适配新结构 |
| `变更记录/CHANGELOG.md` | **保留活跃** | 版本发版历史 |
| `当前代码状态.md` | **保留活跃** | GLM+DS 实时代码看板 |
| `当前审查状态.md` | **保留活跃** | GPT 实时审查看板 |
- **跨域问题**：GPT 发现 GLM+DS 代码域问题 → 写进对应全流程闭环记录或 CR 作“修复指令” → GLM+DS 用适用 Prompt 执行（不直接改对方文档）；历史 `审查日志/` 仅供回查。
## R Fix 批次自动编排入口

| 任务 | 唯一入口 | 机器产物 |
|---|---|---|
| Fix 开始前保存开发区 | E:\workspace\tools\r_fix_review.py begin-fix | E:\workbuddy-snapshots\<B>-<R>-<Fix>-begin\ |
| GLM+DS Fix 完成后生成单 R 候选并恢复开发区 | E:\workspace\tools\r_fix_review.py complete-fix | E:\workbuddy 复审\<B>\<R>-<Fix>-only\ |
| 冻结批次预期 R/Fix | E:\workspace\tools\r_fix_review.py finalize-batch | control/batch-state.json |
| 动态核对所有 R-only | E:\workspace\tools\r_fix_review.py scan-batch 或 watch-batch | control/batch-scan.json |
| 所有 R-only 通过后交 GLM+DS 整合 | prepare-merge → complete-merge | control/merge-ready.json、merge\ |
| 创建/验证批次唯一闭环文档 | 13 B<N> 模式 + create-merge-document | 用户实测阶段\vX-功能名\合并复审\B<N>-merge的全流程闭环.md |
| 合并候选 GPT 复审 | 05 B<N> 模式 + set-review-status | merge\control\review-status.json |
| 05 通过后同步最终候选源代码 | 用户prompt\15A-最终候选同步开发基线.md + E:\workspace\tools\r_fix_review.py sync-final-candidate | E:\workbuddy 复审\<B>\control\source-sync-result.json、E:\workbuddy 复审\current-development-pointer.json、E:\workbuddy-snapshots\<B>-<candidate>-source-sync\ |

批次执行顺序固定为：begin-fix → complete-fix → finalize-batch → scan-batch → R-only Review → prepare-merge → GLM+DS complete-merge → 13 创建/验证批次闭环 → B<N>-Merge-Review → 15A sync-final-candidate → 核对 sourceSyncStatus=verified → 15 → 14。工具动态扫描实际复审目录，不能以批次文档声明的 R 数量替代；merge 后不恢复代码开发区。新增模板见 模板\05_实测部分\多R批次合并复审闭环模板.md。既有 B1 目录和历史文档仅作历史资产，不由工具覆盖。

## 长期实测入口与运行归档

| 对象 | 权威路径/规则 |
|---|---|
| B1 当前批次实际入口 | `E:\workbuddy-test\启动实测.bat`；当前批次期间继续作为实际启动入口 |
| B1 当前批次角色选择器 | `E:\workbuddy-test\选择B1实测角色.bat`；只写 B1 选择状态，不启动程序 |
| B1 当前批次预检 | `E:\workbuddy-test\B1实测入口预检.ps1`；只证明 E0 静态入口子项，不替代 T01 动态取证 |
| B1 当前批次数据根 | `E:\workbuddy-test\R1-3&R2-1\B1-20260829\`；按 `R1-3/R2-1 × GPT/USER × YYYYMMDD` 分目录 |
| B1 完成后的长期入口手册 | `E:\workbuddy-test\长期实测入口与归档手册.md`；当前阶段仅作后续规则，不启用长期脚本 |
| B1 完成后的长期入口 | `E:\workbuddy-test\长期入口\启动实测.bat`；B1 完成后长期阶段的唯一启动入口，当前未启用 |
| 长期入口机器闸门 | `E:\workbuddy-test\current\phase-switch.json`；当前必须为 `state=disabled, stage=B1, b1Status=in-progress`，与 `当前审查状态.md` 同步后才可切换 |
| B1 完成后的长期角色选择器 | `E:\workbuddy-test\长期入口\选择实测角色.bat`；仅后续长期阶段使用，当前未启用 |
| B1 完成后的当前运行清单 | `E:\workbuddy-test\current\active-run.json`、`E:\workbuddy-test\current\selected-run.json`；本次 B1 不创建新的长期清单 |
| B1 完成后的主运行归档根 | `E:\workbuddy-test\archive\v版本\B批次\计划\角色\runId`；仅后续长期阶段使用 |
| B1 完成后的可写工作数据根 | `E:\workbuddy-test\work\v版本\B批次\计划\角色\runId\userData`；仅后续长期阶段使用 |
| B1 历史入口来源副本 | `E:\workbuddy-test\archive\v0.3\B1-20260829\entry\b1-source`；只记录来源，不迁移 B1 运行数据 |
| S 主档根 | `E:\workbuddy-snapshots\<S-ID>`；S 记录、全量清单和程序包不可变 |
| 备份根 | `D:\workbuddy-test-archive`；逐文件校验成功后才标记 `backup-complete` |
| B1 完成后的通用入口预检 | `E:\workbuddy-test\长期入口\实测入口预检.ps1`；当前未启用 |
| B1 完成后的运行封存 | `E:\workbuddy-test\长期入口\封存实测运行.ps1`；当前未启用 |
| B1 完成后的归档备份 | `E:\workbuddy-test\长期入口\备份实测归档.ps1`；当前未启用 |

长期设施规则仅适用于 B1 完成后的长期阶段：普通运行只引用冻结 S；同一计划/角色连续步骤复用同一数据库；重试、新计划或干净前置新建 `runId` 和数据库；中断运行也封存；workspace 实测文档是事实真相源，运行归档保存路径和哈希索引。当前 B1 不使用长期 `active-run`，完整门禁见 `规范类/阶段闭环门禁规则.md` §十。
