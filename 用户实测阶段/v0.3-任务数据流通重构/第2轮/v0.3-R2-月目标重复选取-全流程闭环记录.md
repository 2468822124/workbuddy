# v0.3-第2轮-月目标重复选取-全流程闭环记录

> **文档状态**：`✅当前轮次闭环（closureStatus=closed）`
> **问题来源轮次**：`R2`
> **同轮规则**：`R2` 只有这一份全流程闭环文档；`R2-1` 已由 `14` 合法创建，其 GPT 报告、用户回传、Fix、Review 和 13 合并事实均在本文档索引或续写；测试完成后的 `13` 不创建新计划。
> **创建方**：GPT 通过 `用户prompt/13-生成用户实测全流程闭环.md`
> **当前边界**：本文档由 `13` 在 R1-2 测试合并时创建，现承接 R2-1 的双方测试事实及后续补充核验；R2-1 计划原始台账保持不可变，13 合并层单独记录实际处置结果。本文不承接 R1 的 U-2、U-3、F-ISO-01 或 U-1；U-5 已独立分流至 R3。
> **重要说明**：R2 当前所有事项均已获得明确处置：U-4 已完成 Fix1/Review2，COV-R2-01 已留痕，O-01/O-02 已收束为非缺陷观察，U-5 由 R3 独立承接。R2 当前轮次闭环不等于版本级 `G1`、用户最终验收、`C1` 或维护期完成。

---

## 一、轮次元信息

| 项目 | 内容 |
|---|---|
| 版本 | `v0.3` |
| 功能 | `任务数据流通重构：月目标选取与每周固定` |
| 问题来源轮次 | `R2` |
| 首份关联测试计划 | `尚未创建；未来预期标识为 R2-1，须在 Fix/Review/QA/S 后由 14 创建` |
| 本次合并输入计划 | `不适用；本文件记录的是 R1-2 合并后新建 R2 的分流结果` |
| 当前问题来源 | `全新独立问题 U-4（继承 R1-2-T02-C02 的来源事实）` |
| 当前状态 | `📝待补证据/待核实（R2 Fix1 已执行；修复后 GUI/只读证据缺失）` |
| 版本/构建标识 | `0.3.0-c0` |
| C0/QA 候选状态 | `C0`；R2 Fix1 候选已生成但尚未冻结 |
| 当前实测快照 | `S0`；仅作为 R1-2 来源证据的继承核验，R2 Fix1 候选待正式绑定/冻结 |
| 来源提交号/Tag | `34f155a1886c52dae278fbc71d037975646b2563` / `v0.3.0-c0` |
| 来源工作树指纹 | `C0 提交已归档；R2 计划与闭环文档是治理文档，不作为代码 QA 指纹` |
| 构建目录/构建物哈希 | `E:\workbuddy-snapshots\v0.3.0-c0-r1-2-s0\out`；`WorkBuddy.exe SHA-256 B9F75D623F13069DCA8CE042737924905B62EE2BDD6418527C26365B5019B127`；S0 清单 `F7A30AB853F2F1478B4EC25C12FA64FCF45784033115D2E32F6948F00D1BD2AF` |
| 测试程序/入口 | `E:\workbuddy-test\app-v0.3\WorkBuddy.exe`；后续 R2 计划须由 14 明确正式入口 `E:\workbuddy-test\启动实测.bat` |
| 测试数据隔离 | `E:\workbuddy-test\userData\`；后续 R2 计划须使用新的 `[v0.3-R2-1-USER-YYYYMMDD]` 或 `[v0.3-R2-1-GPT-YYYYMMDD]` 前缀，不覆盖 R1 数据 |
| 创建/更新时间 | `2026-08-26` |

## 二、证据门禁

| 门禁项 | 路径/事实 | 结论 |
|---|---|---|
| R2-1 测试计划 | `尚未创建；目标路径不存在（已由用户删除）` | `📝待 U-4 Fix/Review/QA/S 后由 14 创建；不能按已存在计划处理` |
| R1-2 来源计划 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\v0.3-R1-2-实测计划.md`；来源测试点 `R1-2-T02-C02` | `✅可定位` |
| R1 唯一来源闭环 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\v0.3-R1-项目投影模板与周统筹创建-全流程闭环记录.md` | `✅已记录 U-4 分流` |
| R2-1 GPT 正式报告 | 待 `14` 创建 R2-1 计划后按计划确定路径；当前不存在 | `📝待 R2-1 计划创建和执行；不能用 R1 GPT 报告冒充` |
| R2-1 GPT 证据 | 待 `14` 创建 R2-1 计划后按计划确定证据落点；当前无 R2-1 GUI 证据 | `📝待计划创建/执行` |
| R2-1 用户 06 回传 | 待 R2-1 计划执行后由用户通过 `用户prompt\06-用户反馈测试问题.md` 回传；当前无 R2-1 回传 | `📝待计划创建/执行` |
| R2 用户证据 | `尚未创建；由 14 在 R2 首份计划创建后确定证据目录` | `📝待计划创建后确认归属` |
| U-4 来源用户证据 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\R1-2相关证据\R1-2-用户补充问题.png` | `✅继承来源证据；不是 R2-1 结果` |
| U-4 来源 GPT 核验 | `E:\workspace\GPT实测记录\v0.3实测内容\第1轮\证据\R1-2-补充核验-同副本与用户补充问题.md` 第 75-94 行 | `✅同一 S0/副本只读事实；不是 R2-1 GUI 结果` |
| C0/S0、构建和隔离 | 来源计划、GPT 报告、补充核验绑定同一 `C0/S0`；S0 哈希如上 | `✅来源可复核；R2 计划创建时必须重新绑定 QA/S` |
| 双方共同核心链路 | R2 首份计划的两个候选场景尚未分配测试点 ID，双方尚未执行 | `❌尚无 R2 计划和双方实测证据` |

### 缺口清单

1. R2 首份计划（未来预期标识 `R2-1`）尚未创建/执行，因此没有该计划的 GPT 正式报告、用户正式 06 回传、双方共同链路结果、用户主观体验或 R2 用户证据。
2. 首份计划候选场景的执行日期、D0、W0、M0、用户/GPT 前缀待由 `14` 创建计划时填写；不得直接把 R1-2 的日期和前缀当作 R2 结果。
3. “每周固定”是 R1-2 的计划覆盖缺口，未来由 `14` 补脚本；入口、产品语义和所有分支需在实际 GUI 中记录，未执行项不得写成通过。
4. U-4 的来源事实已满足问题级 Fix 门，但尚未证明修复后行为；后续 R2 首份计划复测前不能写“U-4 已解决”。
5. R1 的 U-2、U-3、F-ISO-01、U-1 不属于本 R2 文档的修复范围；U-3 的持久化冲突继续由 R1 补证，F-ISO-01 继续等待正式入口裁决。

## 三、测试计划索引

| 计划 | 测试标识 | 绑定快照 | 继承自 | GPT 报告 | 用户回传/证据 | 状态 |
|---|---|---|---|---|---|---|
| `R2 首份计划文件已由用户删除，当前不存在` | `R2-1（未来预期标识，未创建）` | `待 U-4 Fix/Review/QA/S 后由 14 重新核对并绑定新的 QA/S` | R1-2 的 `R1-2-T02-C02` 来源事实 | `尚未创建；不得用 R1 GPT 报告冒充 R2 首份计划报告` | `尚未创建；计划存在后才确定用户证据目录` | `📝未创建/不纳入当前实测` |

## 四、双方实测合并

### 4.1 GPT 客观实测摘要（R2 首份计划尚未创建/尚未执行）

| 测试点 ID | 操作/预期 | 实际反馈 | 证据 | 结论/严重度 |
|---|---|---|---|---|
| `候选场景 A（未分配测试点 ID）` | 创建 R2 M1，首次选取到 W0，再重复选取、刷新和离开重进；预期同一 M1/W0 只有一条 active `flow_week_focus` | `R2 首份计划尚未创建/执行；没有 GUI、只读或持久化结果` | `无 R2 GPT 证据；仅有 R1-2 来源核验证据，见第 5.1 节` | `📝待 14 创建计划后执行；不写通过/失败` |
| `候选场景 B（未分配测试点 ID）` | 创建/编辑/停用每周固定任务，核对当前周/下一周物化、惯常日、固定与临时对照、确认/取消和重进 | `R2 首份计划尚未创建/执行；没有 GUI 或只读结果` | `无 R2 GPT 证据` | `📝待 14 创建计划后执行；不写通过/失败` |

### 4.2 用户 06 回传摘要

- **提交状态**：`尚无 R2 首份计划用户 06 回传`。
- **用户执行范围**：`尚未执行候选场景 A/B；当前未形成正式测试点`。
- **客观实际**：`无 R2 实际结果；R1-2 U-4 来源事实单独见第 5.1 节`。
- **主观体验**：`无 R2 首份计划主观体验；不得用 R1-2 用户体验代替后续验收`。
- **用户证据**：`尚未创建；由 14 在计划创建后确定`。
- **用户未完成/阻塞**：`R2 首份计划尚未开始；候选场景 B 的“每周固定”由新计划补齐后再执行`。

### 4.3 用户补充问题与 GPT 补充核验

> `用户补充问题 1` 只是 R1 用户来源标签；在本 R2 文档中稳定问题 ID 为 `U-4`。来源核验证据可支撑 R2 问题级 Fix 门，但不替代 R2 首份计划的用户主观验收或 GUI 复测。

| 来源标签 | 关联测试点/子用例 | 用户是否实际执行 | 用户原始事实/体验 | GPT 补充核验与证据 | 分类/证据等级 | 分流/状态 |
|---|---|---|---|---|---|---|
| `用户补充问题 1` | `R1-2-T02-C02`；后续 R2 首份计划复测候选场景 A | 用户执行了 R1-2 C02 的重复选取；尚未执行后续 R2 首份计划场景 A | 用户原文含义：“月指导目标可以在‘本周核心推进目标’中反复选取”；截图中同一用户 M1 出现两次，计数为 `3`。主观上认为目标数量和关联异常 | 同一 `C0/S0`、同一 `E:\workbuddy-test\userData\` 副本按用户前缀只读核对：`flow_month_goals.id=2` 在 W0 有 `flow_week_focus.id=5`、`id=6` 两条 active 关联；第 6 条为重复选取新增。证据：`E:\workspace\GPT实测记录\v0.3实测内容\第1轮\证据\R1-2-补充核验-同副本与用户补充问题.md` | `全新独立问题；客观事实已证实；根因强推断` | `R2 U-4；问题级 Fix 门已通过；等待 14 合法创建首份计划后复测` |
| `用户补充问题 2` | `无（计划覆盖缺口）；后续 R2 首份计划候选场景 B` | 用户未执行；R1-2 没有专属脚本 | 用户明确“测试内无任何‘每周固定’的内容”，未执行不存在的操作；主观上指出测试范围缺失 | 来源 GPT 核验确认 R1-2 计划标题提到“固定/临时”，但 U02 脚本没有动作、预期、分支和证据要求；未进行 R2 GUI 核验 | `计划覆盖缺口；待补计划` | `不创建问题 ID；待 14 创建首份计划并补脚本后实测` |

### 4.4 双方共同核心链路对照

| 测试点 ID | GPT 客观结果/证据 | 用户实际/证据 | 是否一致 | 待处理 |
|---|---|---|---|---|
| `候选场景 A（未分配测试点 ID）` | 尚未执行；没有 R2 GPT 报告或证据 | 尚未执行；没有 R2 用户 06 或证据 | `不适用/双方均缺` | 14 创建首份计划并分配正式 ID 后，按脚本记录每次动作、页面稳定态和按前缀的持久化行数 |
| `候选场景 B（未分配测试点 ID）` | 尚未执行；没有 R2 GPT 报告或证据 | 尚未执行；没有 R2 用户 06 或证据 | `不适用/双方均缺` | 14 创建首份计划并补齐固定任务语义后，完成当前周/下一周、确认/取消、完成/跳过/停用/重进 |

## 五、来源事实、问题清单与分类

### 5.1 U-4 来源事实（继承，不是 R2 首份计划实测结果）

- **用户原始事实**：在 R1-2 `R1-2-T02-C02` 的重复选取场景中，用户看到同一 `[v0.3-R1-2-USER-20260825]-M1 月目标` 在“本周核心推进目标”中出现两次，列表计数为 `3`；用户表示月指导目标可以反复选取。
- **用户主观体验**：重复目标使数量和关联关系不可信，担心重复操作形成脏数据。
- **用户证据**：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\R1-2相关证据\R1-2-用户补充问题.png`。
- **GPT 独立只读事实**：在同一 S0 和同一 `E:\workbuddy-test\userData\workbuddy.db` 副本中，按用户前缀查询到用户 `flow_month_goals.id=2` 在 `weekStart=2026-08-24` 下存在两条 active `flow_week_focus`：`id=5`（首次选取产生）和 `id=6`（重复选取产生）；另有 `id=3` 软删，不计入 active。
- **GPT 补充证据**：`E:\workspace\GPT实测记录\v0.3实测内容\第1轮\证据\R1-2-补充核验-同副本与用户补充问题.md` 第 75-94 行；该文件是独立补充核验，未覆盖 GPT 正式报告。
- **边界**：以上确认了来源问题的后端事实，但尚未在新的 R2 前缀和 R2 首份计划 GUI 流程中验证修复前/修复后的用户体验；不能写成后续计划已通过。

### 5.2 问题清单

| 问题 ID | 测试点 | 来源 | 分类 | 严重度 | 复现条件 | 预期 | 实际 | 影响 | 证据 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|
| `U-4` | 来源 `R1-2-T02-C02`；后续 R2 首份计划候选场景 A | 用户 + GPT 补充核验；来源标签 `用户补充问题 1` | `全新独立问题` | `🟡需修复` | 同一用户、同一 W0、同一 M1 首次选取后再次选取 | 同一 `(用户前缀, weekStart, monthGoalId)` 只有一条 active `flow_week_focus`；重复动作幂等 | 来源副本已确认两条 active 行 `id=5`、`id=6`；后续 R2 首份计划尚未复测 | 目标数量、月/周关联和重进结果可能失真 | R1 用户截图；R1 同副本只读核验证据 | `后续首份计划待执行；问题级 Fix 门已通过` |
| `COV-R2-01` | 后续 R2 首份计划候选场景 B；来源 R1-2 计划覆盖缺口 | 用户补充问题 2 | `计划覆盖缺口` | `ℹ️` | R1-2 稳定测试点写“固定/临时”，但没有专属脚本；待未来由 14 创建计划时补脚本 | 先有唯一入口、字段、分支和成功标准，再由用户执行 | R1-2 未执行，R2 首份计划尚未创建/执行 | 不能判断固定任务产品行为是否符合预期 | R1-2 计划；未来由 14 创建的 R2 首份计划 | `不作为产品问题；待计划创建后实测` |

R1 的 U-2、U-3、F-ISO-01、U-1 不列为 R2 问题：U-2/U-3 留在 R1，F-ISO-01 等待正式入口裁决，U-1 为待用户确认的非缺陷候选。

### 分类依据

- U-4 首次由 R1-2 用户补充发现，且不属于 R1 已有 UF1/F2 修复范围；同一副本只读结果确认后，按独立全新问题开启 R2。
- `用户补充问题 2` 没有用户执行事实和专属计划 ID，只是 R1-2 计划覆盖缺口，不能自造缺陷 ID，也不能把页面“每周固定 0”写成失败。
- R2 首份计划尚未执行，不能根据来源证据直接写后续计划通过或修复后失败；来源证据只支持当前问题级分流和 Fix 范围。

## 六、诊断与修复计划

### 6.1 诊断

| 问题 ID | 已证实事实 | 根因 | 证据等级 | 待核实项 |
|---|---|---|---|---|
| `U-4` | 同一 M1、同一 W0 存在两条 active `flow_week_focus`；第二条创建时间晚于第一条，来源核验记录为重复选取新增 | 强推断为重复选取路径缺少同一月目标/周的 active 关联唯一性或幂等守卫；不得仅靠代码阅读写成已证实根因 | 后端现象已证实；根因强推断 | 后续 R2 首份计划需确认单次选取、重复点击、刷新/重进、列表计数和关联唯一；确认手动 W2 不受影响 |
| `COV-R2-01` | R1-2 标题和脚本覆盖不一致，用户未执行固定任务 | 测试计划缺少脚本，不是产品根因 | 计划事实已证实 | 未来首份计划候选场景 B 需先补齐实际入口、语义和用户验收要求 |

### 6.2 修复计划

| 问题 ID | 修复批次 | 修复要求 | 允许改动范围 | 验收条件 | 原始复跑场景 | 责任人 | 状态 |
|---|---|---|---|---|---|---|---|
| `U-4` | `R2 Fix1` | 同一月目标在同一周重复选取不得产生第二条 active 周核心目标关联；重复提交、刷新和重进保持幂等 | 仅月目标→周核心目标选取/保存路径、同一周同一月目标 active 关联约束或等价幂等实现；不得改数据库清理来源证据、删除历史软删行或带入 R1 其他问题 | 在新的 QA/S、同一 R2 前缀和同一 GUI 脚本下：首次选取恰好一条 active；重复点击/重复提交不新增；刷新/离开重进仍一条；页面计数与列表一致；手动 W2 不受影响；只读核对与 GUI 一致 | 后续 R2 首份计划的候选场景 A；来源场景 `R1-2-T02-C02` | DeepSeek | `待 Fix；后续首份计划尚未创建/执行` |

> `COV-R2-01` 不进入 `R2 Fix1`。若未来固定任务实测只发现入口/预期不明确，先由计划维护补齐，不写产品 Fix；若在明确规格和双方证据下出现独立缺陷，再按当时分流规则建立新的问题来源轮次。

## 七、Fix 修复批次追加区

### Fix1（2026-08-27，DeepSeek 按 07 执行）

> 本节由 DeepSeek 按 `07-修复用户反馈.md` 追加。上方全部历史事实、来源证据、13/16 处置和统一 Fix1 计划均保留；本节只记录执行事实，不代表 05 已复审、QA/S 已冻结或 R2 已闭环。

#### Fix1-1 批次元信息

| 项目 | 内容 |
|---|---|
| 批次编号 | `R2 Fix1`（唯一，未拆 Fix1a/Fix1b） |
| 执行日期 | 2026-08-27 |
| 执行者 | DeepSeek（07） |
| 处理事项 | `U-4`（代码 Fix，幂等守卫） |
| 未处理/不适用 | `COV-R2-01`（覆盖补充计划，只进 14，不进代码 Fix）；R1 `U-2`/`U-3`/`F-ISO-01`/`U-1`（不属于 R2 范围，未带入）；R2 首份计划（尚未创建，未伪造） |

#### Fix1-2 逐项执行事实（U-4）

- **原始失败场景**：来源 `R1-2-T02-C02`——同一用户、同一 W0、同一 M1 首次选取后再次选取，同一 `(weekStart, monthGoalId)` 出现两条 active `flow_week_focus`（来源副本 `id=5`、`id=6`）。
- **复现结果（自动化）**：`tests/flowRepos.spec.ts` 新增 5 例按来源场景构造（同周同月目标 `createFocus` ×2/×3、跨周同目标、软删后重选、手写同标题两条）→ 修复前 4/5 失败（重复选取确实新增第二条），修复后 5/5 全绿；该 RED→GREEN 结果证明测试能捕获原始缺陷。`⚠️待复测`：GUI 层复现受本自动化上下文（非交互桌面）限制——从 `E:\workbuddy-test\启动实测.bat` 同参启动测试包进程可存活（4 个 PID 存活）但无可见主窗口，无法重放窗口内选取点击（与 R1 Fix1 第 22.2 节同类限制，未据此写成产品缺陷）。
- **根因（代码级确诊）**：`flow.ipc.ts` 的 `FLOW_WEEK_FOCUS_SAVE` 无 `id` 分支无条件调用 `flowGoalRepo.createFocus`；月目标→周核心目标选取/保存路径没有“同周同月目标 active 唯一”守卫，每次选取都 INSERT 新行。
- **执行方案**：在仓储层 `flowGoalRepo.createFocus` 加幂等守卫——`monthGoalId` 非空时先查 `weekStart + monthGoalId + isDeleted=0` 既有行，存在则直接返回既有行（不新增）。守卫位于仓储层单一入口，天然覆盖 IPC 保存、重复点击、重复提交、刷新/离开重进（重进列表由 DB 重查，仍只有一条）；手写目标（`monthGoalId=null`）不触发守卫、仍允许多条；软删后重新选取可新建 active 行（守卫只拦 active，不阻塞删除后的重新选取）。
- **改动文件**：`workbuddy/src/main/db/repositories/flowGoalRepo.ts`（产品逻辑，+7 行）；`workbuddy/tests/flowRepos.spec.ts`（回归测试，+50 行，5 例）。未改动：`flow.ipc.ts` 入参校验、渲染层组件、`flowTypes.ts`、依赖清单（package.json/lock 零改动）、版本字段、`当前审查状态.md`、测试计划、GPT 报告、用户 06、数据库。

#### Fix1-3 自动化测试、构建与静态检查（真实命令与结果）

| 检查 | 命令 | 结果 |
|---|---|---|
| 单测全量 | `cd E:\workspace\workbuddy && npx vitest run` | ✅ 28 files，384/384（R1 Fix1 基线 379 + 新增 5） |
| 新增回归 | `tests/flowRepos.spec.ts` U-4 5 例 | ✅ 全绿；RED 验证：修复前 4/5 失败（能捕获原始缺陷） |
| 构建 | `npm run build` | ✅ 12.95s |
| 打包 | `npm run pack:win` | ✅ win-unpacked 产出（候选 QA 包） |
| tsc.node | `npx tsc --noEmit -p tsconfig.node.json` | ⚠️ 仅存量 `news.ts:22` 1 条，零新增 |
| tsc.web | `npx tsc --noEmit -p tsconfig.web.json` | ⚠️ 存量/同类 11 条，零新增 |
| 越界 | `git diff --check` | ✅ 仅存量 LF/CRLF 提示 |

- 本批产品改动文件（1）：`flowGoalRepo.ts`；测试文件（1）：`flowRepos.spec.ts`。工作树其余已修改文件均属 R1 Fix1 批次（`flowWeekRepo.ts`、`flowReviewDerived.ts`、`ReviewTaskList.vue`、`flowTypes.ts`、`reviewComponents.spec.ts`、`r1Fix1.spec.ts` 等），本批未触碰。

#### Fix1-4 候选 QA 信息（未冻结）

| 项目 | 内容 |
|---|---|
| 候选 QA 编号 | `R2-Fix1-Candidate`（待 15 在 05 通过后正式冻结命名） |
| 来源提交号 | `a6aa730`（当前 HEAD；本批改动尚未提交，且工作树含 R1 Fix1 未提交改动，完整指纹以 `git status` 为准） |
| 构建目录 | `E:\workbuddy-snapshots\v0.3.0-c0-r2fix1-candidate\out` |
| 构建物 | 100 个文件，335400188 bytes；WorkBuddy.exe SHA-256 `BDA93E77F2071FA57B0648E1071AE97E96B38E03C4DFC98408733E3A4EB292B2` |
| 清单 | `E:\workbuddy-snapshots\v0.3.0-c0-r2fix1-candidate\manifest.sha256`（哈希 `3F014BE1478D1A50B541E49B6C18BA23A0EC828F03C1DCFE53FD8C3A0AF45375`） |
| 待冻结 S | 由 15 在 05 整批 Review1 通过后冻结；07 不自行宣称 QA/S 冻结，不修改版本字段，不创建 C0/C1 Tag |
| 部署去向 | 未部署；`E:\workbuddy-test\app-v0.3\` 保持 S0 构建（哈希 `B9F7…`，与 R1 记录一致）；GUI 复审部署决策由 05 或用户处理（如需备份，参照既有 `app-v0.3-before-r1-2` 目录模式） |

#### Fix1-5 原始用户场景复跑

| 场景 | 自动化复跑 | 状态 |
|---|---|---|
| U-4：同一周同一月目标首次选取恰好一条 active | 仓储层 ✅ | `⚠️待用户实测`（GUI 层） |
| U-4：重复点击/重复提交不新增（×3） | 仓储层 ✅ | `⚠️待用户实测`（GUI 层） |
| U-4：刷新/离开重进仍一条（DB 重查幂等） | 仓储层 ✅（重复 create 后 list 仍 1 条） | `⚠️待用户实测`（GUI 层） |
| U-4：页面计数与列表一致 | 仓储层 ✅（list 长度=1） | `⚠️待用户实测`（GUI 层） |
| U-4：手动 W2 不受影响（手写同标题多条允许、跨周同月目标各一条、软删后重选可新建） | 仓储层 ✅（3 例） | `⚠️待用户实测`（GUI 层） |
| 持久化核对 | 来源 DB `E:\workbuddy-test\userData\workbuddy.db` SHA-256 仍为 `9E26D1149856BD8F2A6F0A1B18005293A5AE3F9A938459C7210086359AEDD8AF`（未修改；历史 `id=5/id=6` 重复行按排除范围保留，未清理） | ✅ 只读核对一致 |

#### Fix1-6 未验证项、剩余风险与下一步

- **未验证**：U-4 的 GUI 证据（页面稳定态、重复点击、重进保持、计数与列表一致）均标 `⚠️待用户实测`；自动化测试不能替代用户实测，须由 05 按复审规则补取或由用户在 R2 首份计划中实测。
- **剩余风险**：来源副本与存量数据中的历史重复行保留不清理（排除范围要求），修复只防新增不治理存量；候选构建未部署，部署前需备份 S0 测试包；R2 首份计划尚未创建，U-4 修复后行为须由该计划复测确认。
- **下一步**：`Review1`（GPT 05 整批复审：U-4 原始失败场景、越界、幂等边界、隔离、回归）；05 通过后 `15` 冻结 QA/S；`14` 创建 R2 首份计划（预期标识 R2-1）并为候选场景分配正式测试点 ID。
- **执行边界声明**：本批次未创建测试计划、R 文档或问题 ID；未修改用户回传、GPT 正式报告、数据库、测试计划或治理 Prompt/模板；未把修复执行写成 Review 通过、QA/S 冻结、用户验收通过或 R2 闭环。

## 八、Review 复审批次追加区

### Review 预留状态（截至 2026-08-26）

`此前仅保留 Review1 占位，不构成已完成复审；本次实际 Review1 只在本文档末尾追加。R1-2 来源证据不能替代修复后的 GUI 复审。`

## 九、轮次分流

### 当前分流：R1 全新问题 → R2

- `U-4` 已从 R1-2 分流到本 R2，原因是同一 M1/W0 下两条 active `flow_week_focus` 已由同一 S0/副本只读核验确认，且不属于 R1 既有回归修复范围。
- R1 的 U-2 和 U-3 UI 子范围继续留在 R1；U-3 的重复持久化子范围仍待核实，不转入 R2。
- R1 的 F-ISO-01 不与 U-4 合并；R2 当前尚无已冻结的首份计划正式入口，F-ISO-01 是否为独立产品问题仍由入口规则裁决。
- R1 的 U-1 不进入自动修复；`COV-R2-01` 只以计划覆盖缺口形式进入未来 R2 首份计划候选，不变成 R2 产品问题。

### 未来 R2 首份计划执行后的分支

1. U-4 在首份修复后计划仍出现：回到 R2 后续统一 Fix，不得把覆盖项带入代码批次。
2. U-4 不再出现且首份计划双方证据齐全：由 GPT 评估 `✅当前轮次闭环` 条件，但仍须等待所有活跃 R 和 G1。
3. 首份计划发现独立于 U-4 的新问题：另开下一问题来源 R，不混入 U-4 Fix。
4. 首份计划证据不足、环境阻塞或规格未确认：保持 `📝待补证据/待核实` 或 `⏸️待环境/产品决策`，不写通过。

## 十、G1 版本级整合回归

- **触发条件**：R1 和 R2 所有活跃问题轮次关闭；当前不满足。
- **G1 计划/报告/证据**：尚未创建；G1 不创建 R 文档。
- **当前 G1 结论**：`不适用；R1/R2 仍有活跃问题和待执行项`。
- **边界**：R2 首份计划通过（未来如发生）也不直接形成 `C1`、用户最终验收或维护期。

## 十一、时间线

| 日期 | 事件 | 执行者 | 文档/证据 | 结论 |
|---|---|---|---|---|
| `2026-08-25` | R1-2 用户 06 回传 U-4 来源事实 | 用户 | R1-2 用户证据 `R1-2-用户补充问题.png` | 首次发现重复选取候选 |
| `2026-08-25` | 同一 S0/副本只读核验 U-4 | GPT | R1-2 补充核验独立文件 | 两条 active 关联确认，分流 R2 |
| `2026-08-26` | R1-2 13 全量合并 | GPT | R1 唯一闭环末尾 13 合并区 | U-4 建立 R2 唯一问题来源闭环；R2 首份计划未创建，R1 保持阻塞 |
| `2026-08-26` | `13` 创建 R2 唯一闭环；首份计划未创建（用户已删除目标文件） | GPT | 本文档；被删除的未来计划路径已核对不存在 | U-4 统一 Fix 门已通过；首份计划仅保留未来候选范围 |
| `待执行` | 未来 R2 首份计划用户/GPT 实测 | 用户/GPT | 14 未来计划指定报告和证据目录 | 待补 |
| `待执行` | R2 Fix/Review | DeepSeek/GPT | 本文档 Fix/Review 追加区 | 待补 |

## 十二、正式结论

- **当前轮次结论**：`🟡待修复`。R2 当前事项均已有终态处置，U-4 可进入统一 `Fix1`；这不表示首份计划已测试或 U-4 已解决。
- **首份计划执行状态**：`📝待创建/待补证据`；尚无 R2 首份计划 GPT 正式报告、用户 06 回传、双方 GUI 证据或用户验收。
- **当前可进入 Fix 的范围**：仅 U-4 的“同一月目标、同一周重复选取不得生成第二条 active 周核心目标关联”；不得把 `COV-R2-01`、R1 U-2/U-3/F-ISO-01/U-1 或未测试项带入。
- **未来首份计划候选范围**：候选场景 A 执行 U-4 修复后复测；候选场景 B 先补齐“每周固定”测试覆盖并按实际页面执行，不能预写结果；当前尚无正式测试点。
- **禁止宣称**：`✅已解决`、`✅当前轮次闭环`、`✅功能用户实测闭环`、用户验收通过、`✅G1通过`、`C1` 或维护期。
- **下一步**：本次不创建或补回 R2-1 计划；先由 `07` 执行 R2 统一 `Fix1`，完成后由 `05` 对整批 Review，再由 `15` 冻结 QA/S。满足该节点后，才由 `14` 创建 R2 首份计划并为候选场景分配正式测试点 ID，再执行双方实测和分别留存证据。

---

## 十三、新流程事项处置与统一 Fix（2026-08-26）

> 本节是 R2 当前有效的统一处置记录。R2 由 R1-2 合并产生，原始来源事实继续保留在 R1 唯一闭环文档；本节不创建测试计划、不把后续计划候选写成测试结果。

### 13.1 事项处置总表

| 事项 ID | 类型 | 来源/测试点 | 客观事实 | 用户主观体验 | 证据等级 | 处置类型 | 责任人 | 截止/复评点 | 允许范围/排除范围 | 验收条件 | 阻塞 | 当前状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `U-4` | 代码问题 / 全新独立问题 | `R1-2-T02-C02`；来源标签 `用户补充问题 1` | 同一 M1/W0 存在两条 active `flow_week_focus`：`id=5`、`id=6`；第二条由重复选取产生 | 目标数量、月/周关联和重进结果不可信，担心形成脏数据 | 后端现象已证实；根因强推断 | 代码 Fix，纳入 R2 统一 `Fix1` | DeepSeek | `Fix1` 完成后由 05 复审；再由 15 核对 QA/S | 仅月目标→周核心目标选取/保存路径及同周同月目标 active 幂等；排除 R1 其他问题、数据库清理和覆盖项 | 同一 R2 前缀/同一 GUI 场景下首次选取一条；重复点击/提交、刷新、离开重进不新增；页面计数、关联和只读结果一致；手动 W2 不受影响 | `否（覆盖项仍阻断 R2 关闭）` | `已处置；R2 Fix1 可执行，尚未执行` |
| `COV-R2-01` | 计划覆盖缺口 | `用户补充问题 2`；R1-2 U02“固定/临时”范围 | 计划标题提到“固定/临时”，但没有入口、动作、预期、分支和证据要求；用户未执行不存在的脚本 | 用户认为“每周固定”没有被实际覆盖 | 计划事实已证实；不是产品缺陷 | 覆盖补充计划，进入 R2 首份计划；不进入代码 Fix | GPT/14；用户按后续计划实测 | 只补测试计划和证据要求；排除把页面 `0` 判成产品失败或带入 Fix1 | 14 明确入口、字段、创建/编辑/停用、当前周/下一周、固定/临时、确认/取消、刷新/重进和成功/失败标准 | `否（不阻断 Fix1；阻断 R2 关闭）` | `已登记；待 14 合法创建计划` |

### 13.2 统一 Fix 门核对

| 条件 | 结论 |
|---|---|
| R2 每一项已有明确终态处置 | `✅`；U-4 进入代码 Fix，`COV-R2-01` 转覆盖补充计划 |
| 代码 Fix 范围、排除范围、责任人、原始场景和验收条件完整 | `✅`；仅 U-4 |
| 覆盖缺口已明确转为后续计划纳入项 | `✅`；不进入代码 Fix1 |
| 无待核实、环境阻塞、用户待确认或未决产品决策 | `✅`；R1 的 F-ISO-01/U-1 不属于 R2 事项 |
| 所有获准代码事项可组成一次统一 `R2 Fix1` | `✅`；只有 U-4，不能拆成子批次 |

R2 因此可以进入 `🟡待修复`，但该状态只表示统一 Fix 可执行，不表示 U-4 已解决、首份测试计划已创建或用户已验收。

### 13.3 覆盖补充项

| 覆盖项 | 后续计划类型 | 必须补齐的脚本/预期/分支 | 证据要求 | S 处理 | 当前状态 |
|---|---|---|---|---|---|
| `COV-R2-01` “每周固定” | R2 首份计划的覆盖补充范围（未来预期标识 `R2-1`，当前不存在） | 明确唯一入口、字段、创建/编辑/停用、当前周和下一周物化、惯常日、固定/临时对照、确认/取消、刷新/离开重进、成功/失败/阻断标准 | GPT GUI 证据和用户 06/用户证据分别落在未来计划指定目录；不能以 R1 来源证据冒充后续计划结果 | 可由 14 核对后复用有效来源 S；不伪造新的 QA/S | `待 14；不创建测试点 ID，直到计划合法创建` |

### 13.4 环境/产品决策区

R2 当前没有独立环境阻塞或需要产品选择的事项。R1 的 `F-ISO-01`、`U-1` 仍由 R1 事项处置总表和 `16` 处理，不得为了让 R2 Fix 可执行而转移、替代或删除它们。若后续 R2 实测产生环境事项，必须按同一 S/副本、正式入口、进程参数、环境变量、实际数据目录、GUI 结果和候选替代入口完成最小核对，并登记责任人、独立期限、复评点及继续排障/替代/删除三项选择。

### 13.5 替代计划与删除计划

| 区块 | 当前结论 |
|---|---|
| 替代计划 | `无已批准替代；无 16 用户选择；不进入 R2 Fix1` |
| 删除计划 | `无已批准删除；不进入 R2 Fix1` |
| 决策边界 | `07` 不自行决定替代或删除；未来若有批准事项，必须和 U-4 一起纳入同一个统一 Fix 批次 |

### 13.6 R2 统一 Fix1 计划

| 批次 | 事项 | 修复要求 | 允许范围 | 排除范围 | 原始失败场景 | 验收条件 | 状态 |
|---|---|---|---|---|---|---|---|
| `R2 Fix1` | `U-4` | 同一月目标在同一周重复选取不得产生第二条 active 周核心目标关联；重复提交、刷新和重进保持幂等 | 月目标→周核心目标选取/保存路径及同周同月目标 active 唯一性或等价幂等实现 | `COV-R2-01`、R1 U-2/U-3/F-ISO-01/U-1、历史数据清理和无关模块 | 来源 `R1-2-T02-C02` 重复选取场景；后续首份计划创建后复跑 | 新 S/同 R2 前缀下首次一条、重复不新增、刷新/重进一条、计数与列表一致、手动 W2 不受影响、只读与 GUI 一致 | `已执行；Review1 前置停止，待补证后 Review2` |

`07` 只能读取本表执行 `R2 Fix1`，不得先修覆盖项或把 R1 问题带入；执行事实必须追加到本文档。代码、依赖或构建输入变化时只记录候选 QA/S，由 05 复审通过后交 15 冻结。

### 13.7 Fix、Review 与 QA/S 空白记录

| 节点 | 当前状态 | 约束 |
|---|---|---|
| `07 R2 Fix1` | `已执行（2026-08-27）；Review1 前置停止` | 只处理 U-4；不创建测试计划或第二份 R2 文档 |
| `05 Review1` | `本次已记录为前置停止，未启动业务复审` | Fix1 候选须经正式入口和当前 GUI/持久化证据补齐后再复审；后续复审使用下一 Review 编号，不覆盖本记录 |
| `15 QA/S` | `尚未冻结` | 仅在 05 整批通过后核对当前版本全局 QA 编号、来源指纹、构建物哈希、快照目录和数据隔离 |
| `14 R2-1` | `尚未创建` | 仅在 `07 → 05 → 15` 完成后创建；本次不补回用户已删除的计划 |

### 13.8 问题集合冻结与跨 R 映射

| 冻结区块 | 当前记录 |
|---|---|
| 问题集合冻结 | `❌未冻结；须等 R2 Fix1、Review1 和后续首份计划复测结果` |
| 当前事项集合 | `U-4` 代码 Fix + `COV-R2-01` 覆盖补充；无替代、删除或环境决策事项 |
| R2 关闭条件 | U-4 经修复后计划复测通过、覆盖补充完成、双方核心链路证据齐全，且无新问题/待核实/用户决策缺口 |

| 来源 R/事项 | 原始测试点/证据 | 分类理由 | 当前承接 R | 后续唯一归属 | 原 R 保留内容 |
|---|---|---|---|---|---|
| R1 `U-4` | `R1-2-T02-C02`、用户补充问题 1、R1 用户截图和独立只读核验 | 独立新增产品问题，不属于 R1 既有回归范围 | R2 `U-4` | R2 `Fix1 → Review1 → 15 → R2 首份计划` | R1 保留用户原文、主观体验、全部证据、补充核验、分类和分流理由 |
| R1 `COV-R2-01` | 用户补充问题 2、R1-2 U02 计划覆盖描述 | 计划覆盖缺口，不是产品问题 | R2 覆盖项 | R2 首份计划由 14 补脚本；不进 Fix1 | R1 保留未执行声明、计划缺口事实和后续计划要求 |

### 13.9 R2 当前正式结论

> 本节是 2026-08-26 的 Fix 门快照；2026-08-27 的 Fix1 执行事实和 Review1 最新结论以第七节及第十四节为准。

- **当前状态**：`🟡待修复`；R2 所有当前事项已有终态处置，且统一 `R2 Fix1` 可执行。该状态不表示修复完成或测试通过。
- **双方证据**：U-4 的来源用户证据、同一 S0/副本 GPT 独立核验证据和原始计划定位已齐；R2 首份计划尚未创建，因此不存在该计划的双方实测证据或用户验收。
- **当前修复范围**：仅 U-4；`COV-R2-01` 只进入 14 覆盖补充计划，不能进入代码 Fix。
- **计划边界**：本次 `13` 没有创建测试计划；`R2-1` 文件当前不存在，只有在 `07 → 05 → 15` 后由 `14` 合法创建。
- **下一步**：调用 `07` 执行 R2 统一 `Fix1`；随后 `05` 复审整个批次，`15` 冻结 QA/S，再由 `14` 创建 R2 首份计划并执行双方复测。
- **禁止宣称**：`✅已解决`、`✅当前轮次闭环`、用户验收通过、`✅G1通过`、`C1` 或维护期。

---

## 十四、Review 复审批次追加区

### Review1（2026-08-27，GPT；前置停止）

- **对应统一批次**：`R2 Fix1`
- **复审模式**：用户实测 R 的统一 Fix 批次复审
- **复审者/日期**：GPT / `2026-08-27`
- **权威落点**：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-月目标重复选取-全流程闭环记录.md`
- **前置结果**：R2 唯一闭环文档、统一 `R2 Fix1`、事项处置总表、覆盖补充登记和 R2 内替代/删除决策均可定位；但当前无法启动合规的修复后 GUI 复审，因此本批次在前置阶段停止。
- **复审结论**：`🔴驳回（前置停止，未启动业务 GUI 复审）`。这不是对 `flowGoalRepo.ts` 代码 Fix 已失败的判定，也不等于用户验收或 R2 关闭。

#### Review1 前置核对

| 核对项 | 当前事实 | 结论 |
|---|---|---|
| R2 唯一性 | R2 第 2 轮目录只有本文件一份全流程闭环文档 | `✅` |
| Review 编号 | 原有 `Review1` 只是“尚未创建”占位，已保留为历史占位说明；本节是唯一正式 `Review1` 记录 | `✅` |
| 统一 Fix | 事项处置总表只允许 `R2 Fix1` 处理 `U-4`；未拆成子批次 | `✅` |
| 覆盖补充项 | `COV-R2-01` 已登记进入后续 `14` 计划，不进入代码 Fix | `✅`；本次不复审其产品结果 |
| 替代/删除 | R2 无已批准替代、删除或待用户决定事项；`16` 无需重新裁决 | `✅` |
| 原始计划 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\v0.3-R1-2-实测计划.md` 可定位，作为 U-4 来源失败场景的原始计划；R2 首份计划按既定顺序留待 `Review → 15 → 14`，不冒充本次复审证据 | `✅来源可用` |
| 来源 GPT/用户事实 | R1-2 GPT 正式报告、R1 用户补充截图/回传和独立补充核验均可定位；它们只能证明来源问题，不能证明 Fix1 修复后 GUI 结果 | `✅来源可用；❌当前修复后 GUI 证据缺失` |
| 候选构建 | `E:\workbuddy-snapshots\v0.3.0-c0-r2fix1-candidate\out` 存在，`WorkBuddy.exe` SHA-256 为 `BDA93E77F2071FA57B0648E1071AE97E96B38E03C4DFC98408733E3A4EB292B2`；Fix1 记录尚未冻结 QA/S | `⚠️候选未冻结` |
| 正式入口绑定 | `E:\workbuddy-test\启动实测.bat` 仍绑定 `E:\workbuddy-test\app-v0.3\WorkBuddy.exe`，其 SHA-256 为 S0 的 `B9F75D623F13069DCA8CE042737924905B62EE2BDD6418527C26365B5019B127`；R2 候选未部署到正式入口 | `❌不能启动本批次合规 GUI 复审` |
| R2 当前 GUI/持久化证据 | 没有使用 R2 候选和 R2 前缀取得页面稳定态、重复点击、刷新/重进、计数/关联或只读持久化证据 | `❌缺失；按门禁停止` |

#### Review1 逐项结果

| Fix/事项 ID | 原始失败场景与原始预期 | 复审操作与当前实际 | 替代/删除结果 | GPT GUI/只读/构建证据 | 结论 | 越界/新问题 |
|---|---|---|---|---|---|---|
| `R2 Fix1 / U-4` | 来源 `R1-2-T02-C02`：同一用户、同一 W0、同一 M1 首次选取后再次选取；预期同一 `(weekStart, monthGoalId)` 只有一条 active `flow_week_focus`，重复提交、刷新和离开重进保持幂等，手动 W2 不受影响 | 未执行修复后 GUI 复跑。只读看到候选包存在，但正式入口仍指向 S0；没有当前页面稳定态、用户操作序列或同一 R2 前缀的持久化结果 | 不适用；R2 无批准替代/删除 | Fix1 执行事实和代码/自动化结果见本文 §七；这些是辅助材料，不替代当前 GUI。当前没有 R2 修复后 GUI/只读证据路径 | `⚠️待用户实测`（本次复审未启动，不判定代码已解决或已失败） | 当前未发现可归因于 Fix1 的新产品问题；候选未绑定正式入口本身是复审前置缺口 |
| `COV-R2-01` | “每周固定”是 R1-2 的计划覆盖缺口；必须由后续计划补齐入口、字段、固定/临时分支、确认/取消、刷新/重进和成功/失败标准 | 不属于 R2 Fix1 代码复审，未执行产品判断 | 不适用 | 本文 §13.3 已登记；无后续计划结果 | `ℹ️非本次复审范围` | 不得把覆盖缺口带入 `Fix1` 或写成产品通过 |

#### 技术栈、数据隔离和候选指纹（仅前置辅助核对）

- **代码范围**：当前 Fix1 差异为 `workbuddy/src/main/db/repositories/flowGoalRepo.ts` 7 行和 `workbuddy/tests/flowRepos.spec.ts` 50 行；未见 `package.json`/lock、版本字段、IPC 校验或冻结区改动。工作树仍包含 R1 Fix1 的其他未提交改动，`HEAD` 为 `a6aa7307bb6426486059dfc01b1d17297ff3312e`，不能仅以 HEAD 作为本批候选的完整来源指纹。
- **Fix1 执行辅助事实**：本文 §七记录了 `384/384` 测试、build、Windows unpacked 打包和 `git diff --check` 的结果；本次不重新把自动化结果写成 GUI 通过。
- **候选与 S0**：R2 候选为 100 个文件、335400188 bytes；S0 正式入口目录为 100 个文件、335398344 bytes。两者 `WorkBuddy.exe` 哈希不同，当前不能复用 S0 作为 R2 Fix1 被测构建。
- **测试数据**：现有 `E:\workbuddy-test\userData\workbuddy.db` SHA-256 为 `9E26D1149856BD8F2A6F0A1B18005293A5AE3F9A938459C7210086359AEDD8AF`；本次未写入、清空或清理数据库，也没有生成 R2 前缀业务数据。来源中的历史重复行仍按 Fix1 排除范围保留。
- **入口/隔离**：后续复核必须让同一 R2 候选经 `E:\workbuddy-test\启动实测.bat` 启动，核对进程参数、`WORKBUDDY_USER_DATA`、实际数据目录和可见窗口；不得用当前直接启动的其他候选进程或 R1 数据代替。

#### 需补充信息

1. 将 `E:\workbuddy-snapshots\v0.3.0-c0-r2fix1-candidate\out` 按批准的测试环境部署/绑定到正式入口，保留可恢复的 S0 副本，并重新核对候选 `WorkBuddy.exe` 哈希；不得把当前 S0 包或 R1 候选进程当作 R2 Fix1 被测物。
2. 在同一 R2 候选、测试专用数据目录和新 R2 前缀下，按来源 `R1-2-T02-C02` 重新执行：首次月目标选取、重复点击/重复提交（含连续 3 次）、刷新、离开重进、页面计数/列表/月份关联核对。
3. 按 Fix1 边界补做手动 W2 同标题多条、跨周同月目标各一条、软删除后重新选取可新建；每一步保留页面稳定态、可读反馈、错误/崩溃/白屏观察和只读持久化结果。
4. 由 GPT/用户分别取得当前复审 GUI 与用户验收证据；本次没有创建证据文件，`R2-1` 首份计划仍按既定顺序留待 Review 通过和 `15` 冻结 QA/S 后由 `14` 创建，不能提前伪造计划、报告或 06。
5. 补证完成后不得覆盖本 `Review1`；应以 `Review2` 重新复审同一完整 `R2 Fix1`。若 GUI 复审实际失败，回到 `07` 生成下一统一 Fix，不能拆批次绕过 `U-4`。

#### Review1 最新门禁结论

- **复审结论**：`🔴驳回（前置停止）`；原因是 R2 Fix1 候选未绑定正式入口，当前没有修复后 GUI/只读持久化证据，核心范围无法可靠验证。
- **R2 当前状态**：`📝待补证据/待核实`；不进入 `✅通过（修复复审）`、`🟢等待下一轮测试` 或 `15`。
- **QA/S**：未冻结；`15` 不得启动。
- **下一步**：`补证`；补齐后使用 `Review2`，通过后才交 `15`，再由 `14` 创建 R2 首份计划并纳入 `COV-R2-01` 覆盖脚本。
- **禁止宣称**：`✅已解决`、`✅当前轮次闭环`、用户验收通过、`G1`、`C1` 或维护期。

---

## 十五、B1 批次关联指针（2026-08-27）

本 R 已纳入批次 `B1`，但本节不覆盖既有 `Review1（前置停止）`。批次级唯一事实、R-only 候选、合并失败/修复、`B1-Merge-ReviewN` 和最终 QA/S 冻结统一记录于：

`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\合并复审\B1-merge的全流程闭环.md`

| 项目 | 本 R 关联 |
|---|---|
| 批次 | `B1 = R1 + R2` |
| 本 R 候选 | `B1-R2 = C0 + R2 Fix1`，只能包含 U-4，必须从 C0 独立构建 |
| 本 R 复审 | 既有 `Review1` 为前置停止；B1-R2-only 后续使用 `Review2`，不能覆盖旧记录 |
| 共同基线 | C0 提交 `34f155a1886c52dae278fbc71d037975646b2563`，Tag `v0.3.0-c0`，S0 SHA-256 `B9F75D623F13069DCA8CE042737924905B62EE2BDD6418527C26365B5019B127` |
| 跨 R 边界 | R1 的 U-2、U-3 UI、F-ISO-01、U-1 不进入 R2；COV-R2-01 仍是后续覆盖补充项，不进入 R2 Fix1 |
| 后续关系 | R1-only 与 R2-only 均通过且 B1-merge 最终通过后，统一交 15；15 只冻结最终合并包，随后由 14 创建 R2-1 |

本关联指针未创建 `Review2`、未创建 `R2-1`、未创建新 R 或测试计划，未修改用户回传/GPT 报告/当前代码状态，也未宣称 R2、B1 或 QA/S 已完成。

---

## 十六、R2 Fix1 Review2（2026-08-28，GPT；B1-R2-only）

> 本节是在既有 `Review1（前置停止）` 之后的正式修复复审，不能覆盖第十四节历史记录。复审对象是从共同 `C0` 独立构建、仅包含 R2 `Fix1` 的 `B1-R2-only` 候选；不使用混合工作树、R2 历史混合候选或 R1-only 候选。

### 16.1 前置、范围和候选

| 项目 | 复审事实 |
|---|---|
| 复审模式 | 用户实测 R 的统一 `R2 Fix1` 修复复审；B1 内 R-only 复审 |
| Review 编号 | 既有 `Review1` 为前置停止；本次使用 `Review2`，不覆盖历史记录 |
| 统一 Fix | `R2 Fix1` 仅处理 `U-4`；`COV-R2-01` 是后续覆盖补充，不进入代码 Fix |
| 替代/删除 | R2 无已批准替代、删除或待用户决定事项；GPT 未重新作产品决定 |
| 共同基线 | `C0=34f155a1886c52dae278fbc71d037975646b2563`，Tag `v0.3.0-c0`，S0 `WorkBuddy.exe` SHA-256 `B9F75D623F13069DCA8CE042737924905B62EE2BDD6418527C26365B5019B127` |
| 被测候选 | `E:\workbuddy-snapshots\v0.3.0-c0-b1-r2-only\out`，版本字段 `0.3.0-c0`，100 个文件，335398629 bytes |
| 候选指纹 | 来源树 `2BB71469569CEECF37CD4112A6F21CAB3E392603F39772F0ADF72AFFEEBEA9DA`；允许差异清单哈希 `147D98B190361374BB602C70CD2EBA4E5A7F6BE7BDB8A4AE35CD1C3F94394E40` |
| 构建物哈希 | `WorkBuddy.exe` SHA-256 `735B4E33334BB2840A990B1AC020A031FA096A7FCF9BB6239B53A44A59EBA270`；候选清单哈希 `F561A938E8C5A043DA6D76120CA226CF6EB5C0DF1D1C62DBAA621C3FBDF2E773` |
| 复审目录 | 程序 `E:\workbuddy 复审\B1\R2-only\app`；独立数据 `E:\workbuddy 复审\B1\R2-only\userData`；入口 `E:\workbuddy 复审\B1\R2-only\启动复审.bat`；证据 `E:\workbuddy 复审\B1\R2-only\evidence` |

复审包装显式设置 `WORKBUDDY_APP`、`WORKBUDDY_USER_DATA` 和调试端口 `9332`，仅从包装启动候选。包装使用 `%~dp0` 绑定自身目录，未直接双击 exe，也未使用 R1 或 `E:\workbuddy-test\userData`。

### 16.2 `U-4` / `R1-2-T02-C02`：同周同月目标幂等

- **原始失败场景**：同一用户、同一周 W35、同一月目标 M1 首次选取后再次选取，来源副本曾出现两条 active `flow_week_focus`。原始预期是同一 `(weekStart, monthGoalId)` 只有一条 active 关联，重复提交、刷新和离开重进不新增。
- **首次选取**：通过“月指导”创建 `B1-R2-U4-Month-M1`，在 W35“本周核心推进目标”从月目标选取一次；页面计数为 `1`，显示该目标。
- **重复提交/重复点击**：在同一 W35 对同一月目标重复打开选取表单并提交（含连续多次重复提交，超过 Fix 要求的 3 次）；每次页面反馈“已添加”，但计数始终为 `1`，没有第二条关联显示。证据：`R2-Review2-U4-repeat-selection-form.png`、`R2-Review2-U4-repeat-selection-selected.png`、`R2-Review2-U4-repeat-after.png`、`R2-Review2-U4-after-repeated-three-submits.png`。
- **刷新/离开重进**：刷新周统筹页面后 W35 仍为 1 条；离开到月指导，再回周统筹并重新查看 W35 后仍为 1 条。证据：`R2-Review2-U4-after-refresh-week.png`、`R2-Review2-U4-reenter-month.png`、`R2-Review2-U4-reenter-week.png`。
- **只读持久化**：`R2-Review2-U4-after-repeat-readonly.json` 与 `R2-Review2-U4-final-W35-readonly.json` 均为 `integrity=ok`；W35 的 `flow_week_focus` 只有一条 active 月目标关联，页面计数与持久化一致。

### 16.3 合法边界：跨周、手写同标题、软删除后重选

- **跨周**：进入 W36（`2026-08-31 ~ 09-06`）再次选取同一 M1，页面显示 1 条；只读 `R2-Review2-U4-cross-week-readonly.json` 显示 W35/W36 各一条 active 关联，未误把月目标限制为全局只能选一次。证据：`R2-Review2-U4-cross-week-W36.png`。
- **手写同标题**：在 W36 通过“手写一个目标”创建同标题 `B1-R2-U4-Month-M1`，页面显示两条同标题核心目标；只读结果显示其中一条 `monthGoalId=1`、另一条 `monthGoalId=NULL`，证明 Fix 只约束月目标关联，不禁止合法手写重复标题。证据：`R2-Review2-U4-manual-same-title-W36.png`、`R2-Review2-U4-after-manual-readonly.json`。
- **软删除后重选**：在 W36 删除原月目标关联，先经过“确认删除”对话框，再确认；页面剩余手写目标。`R2-Review2-U4-after-soft-delete-readonly.json` 显示原关联行仍保留但 `isDeleted=1`，没有物理清除；随后再次从月目标选取 M1，页面恢复为两条，且只读 `R2-Review2-U4-after-reselect-readonly.json` 显示新的 active 行与历史软删除行并存。证据：`R2-Review2-U4-soft-delete-confirmation.png`、`R2-Review2-U4-after-soft-delete.png`、`R2-Review2-U4-after-soft-delete-reselect.png`。
- **最终重进**：刷新并从月指导离开/回到 W36 后仍显示两条合法目标；证据：`R2-Review2-U4-after-reselect-refresh.png`、`R2-Review2-U4-after-leave-reenter-W36.png`。最终只读 `R2-Review2-U4-final-readonly.json` 为完整性 `ok`，四行总计中两条 active 月关联分别位于 W35/W36，一条 W36 手写，一条 W36 软删除历史。
- **结论**：`✅已解决`。同周同月重复选取、重复提交、刷新/重进和页面计数通过；跨周、手写同标题和软删除后重选均保持合法；未发现数据损坏、重复 active 关联、崩溃、白屏或敏感泄露。

### 16.4 技术栈、隔离、覆盖登记和门禁结论

- 候选允许差异仅为 R2 的 `flowGoalRepo.ts` 和对应 `flowRepos.spec.ts`，R1 的四个产品文件及 R1 测试未进入 R2-only；依赖清单、版本字段、IPC 校验和打包方式未改。
- `npx vitest run`：R2-only 来源 27 个测试文件、369/369 通过；`npm run build`、`npm run pack:win` 和 `git diff --check` 通过；tsc 仅记录既有诊断，未发现本 Fix 新增技术栈红线。
- GUI 全程使用 `E:\workbuddy 复审\B1\R2-only\userData` 和 `B1-R2-` 数据前缀；只读 helper 以 `readonly=true` 打开数据库。没有读取、修改、清空或迁移真实用户数据，R1 数据未进入本副本。
- `COV-R2-01` 已在第十三节登记为后续 `14` 覆盖补充，未混入本次代码 Fix；本次不把它写成产品通过，也不创建 `R2-1` 测试计划。
- **复审结论**：`✅通过（修复复审）`。R2-only `Review2` 的完整 `R2 Fix1` 有当前 GUI、只读、构建和隔离证据，无未关闭阻断、需修复、决策缺口或越界问题。
- **下一步**：R2 不单独调用 15；与 R1-only `Review1` 一并进入 B1-merge 集成复审。B1-merge 最终通过后，统一交 15 冻结最终合并包；本节不代表用户验收、QA/S、G1、C1 或维护期。

---

## 十七、R2-1 测试合并（2026-09-03，GPT；13）

> 本节是对已有 R2 唯一闭环文档的续写。历史章节、R2 Fix1、Review1 前置停止、Review2 通过和 B1 批次关联均保留；本节只合并 R2-1 计划、GPT 正式报告和当前调用中收到的用户 06 原文。计划、GPT 正式报告、用户 06 原文、产品代码、数据库、`当前代码状态.md` 和既有状态看板均未修改。
>
> 本节底部的最新状态是 R2 当前裁决；文档顶部早期的“R2-1 尚未创建”是历史快照，不覆盖本节事实。

### 17.1 模式、唯一性和门禁快照

| 字段 | 本次合并事实 |
|---|---|
| 处理模式 | `R 测试合并` |
| 问题来源轮次 | `R2`；仍使用本文档作为同轮唯一闭环 |
| 测试标识 | `R2-1`；与计划和 GPT 报告一致 |
| 测试计划 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-1-实测计划.md`，存在且唯一 |
| GPT 正式报告 | `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\v0.3-R2-1-GPT实测.md`，存在；未发现草稿标识 |
| 本次唯一 R 闭环 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-月目标重复选取-全流程闭环记录.md`；第 2 轮仅此一份 R2 闭环 |
| `planStatus` | `ready`；只表示计划和 E0 已就绪，不表示业务测试通过 |
| `staticGate/E0` | `passed`；按计划和 GPT 报告核对 |
| 计划原始 `testPointStatus` | `not-tested`；保持计划原值，13 不改写为通过或 E0 失败 |
| 计划原始 `closureStatus` | `not-started`；保持计划原值，13 不改写测试计划台账 |
| 13 合并层 `closureStatus` | `in-progress`；事实已合并，但双方证据、O-01/O-02 和用户补充决策仍未收束 |
| `updatedAt` / `updatedBy` | `2026-09-03T12:03:05+08:00` / `GPT` |
| 当前 R 状态 | `📝待补证据/待核实` |

#### 17.1.1 版本、快照、参数和入口

| 项目 | 事实 |
|---|---|
| 版本/候选 | `0.3.0-qa.1` / `QA1`；来源为 B1-merge 最终包 |
| 共同基线 | `C0=34f155a1886c52dae278fbc71d037975646b2563`，Tag `v0.3.0-c0` |
| 被测快照 | `S1`；QA/S 冻结时间 `2026-08-28 23:49:27 +08:00` |
| S1 构建目录 | `E:\workbuddy-snapshots\v0.3.0-qa.1-b1-merge-s1\out` |
| S1 清单/哈希 | 100 个文件、335401715 bytes；清单 SHA-256 `0044A2AE3A0075C3733748006308FD3E5F05B19388C5CDEAF02C584DA94FE581`；`WorkBuddy.exe` SHA-256 `EEC94A500952CC83A15A9BE1907B0059D8A6262BE63503FA9E54F94716011874` |
| 正式入口 | `E:\workbuddy-test\选择B1实测角色.bat` → `E:\workbuddy-test\启动实测.bat` → `E:\workbuddy-test\app-v0.3\WorkBuddy.exe` |
| GPT 实测日期/前缀 | 报告日期 `2026-08-31`；`v0.3-R2-1-GPT-20260831`；数据目录 `E:\workbuddy-test\R1-3&R2-1\B1-20260829\R2-1\GPT-20260831\userData` |
| 用户回传日期/参数 | 用户原文声明 `D0=2026-09-02`；逻辑 `W0=2026-09-07`、`W+1=2026-09-14`、`W+2=2026-09-21`；用户实际前缀、USER 数据目录和入口证据未在 06 原文提供 |
| 参数边界 | GPT 报告使用 W35=`2026-08-24`、W36=`2026-08-31`、W37=`2026-09-07`；不能因同为 S1 就把 GPT 副本的 W35/W36/W37 行当作用户副本的 W0/W+1/W+2 行 |
| 真实数据边界 | GPT 报告声明未读取、复制、修改、清空或删除真实用户数据；用户 06 声明无环境阻塞，但没有提供可独立定位的入口/目录/哈希证据 |

### 17.2 计划、报告、用户回传和证据完整性

| 核对项 | 绝对路径/来源 | 结果 |
|---|---|---|
| 测试计划 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-1-实测计划.md` | `✅存在；标识 R2-1 匹配；只读引用` |
| GPT 正式报告 | `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\v0.3-R2-1-GPT实测.md` | `✅正式报告；报告内 S1/QA1、入口、前缀和 GUI/只读结果可定位` |
| GPT GUI/只读证据 | `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\` | `⚠️部分完整：T01-T03、T06-C04/C05 有证据；T04-C03/C04、T05 专用分支和 T06-C01 的专用 GUI 留存存在缺口，按报告原结论保留` |
| GPT 只读核验 | `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-补充核验-T06生命周期.json` | `✅文件存在；S1、GPT 前缀、query_only=1、integrity=ok、外键为空、读前后哈希不变` |
| 用户 06 原文 | 当前调用输入中的 `# R2-1 用户实测回传`；无独立文件路径 | `✅原文已在 17.4 逐字留痕；不能伪造文件路径` |
| 用户证据目录 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\R2-1相关证据\` | `⚠️仅发现 T02-C02 两份 PNG；没有其余用户逐点证据、USER 入口、前缀、数据库目录或哈希文件` |
| 用户 T02-C02 证据 1 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\R2-1相关证据\R2-1-T02-C02（1）.png`；SHA-256 `9290D6F4C12FEA7DACCE91388FF177F33953AA152FE9885EC3474358D6D3128C` | `✅保留；用于用户日期语义纠正，不改写为产品缺陷证据` |
| 用户 T02-C02 证据 2 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\R2-1相关证据\R2-1-T02-C02（2）.png`；SHA-256 `06BE19CE282E0BFA3087D98F9D43DAE8D4107E60A0D1AEABFA6EADC92140479F` | `✅保留；用于用户日期语义纠正，不替代其余用户测试证据` |
| 双方同一 S/副本/入口 | GPT 报告提供 S1 和 GPT 副本；06 未提供 USER 侧实际 S1/入口/目录证据 | `❌双方证据未齐；不得写用户侧通过或 R2 关闭` |
| 敏感信息 | 当前可定位路径、回传文字和证据文件名未显示密钥/Cookie/真实任务内容 | `✅未发现需要停止采纳的敏感证据；不据此扩大结论` |

### 17.3 GPT 客观事实合并

| 测试点 | GPT 结果 | GPT 证据/事实 | 13 合并层状态 |
|---|---|---|---|
| `R2-1-T01` | `passed` | S1 正式入口、窗口、同角色重进、角色切换隔离可见；报告把同角色重进和角色切换写作 T01-C03/C04，但计划正式 ID 只有 C01/C02，13 不新造测试点 | `in-progress`；GPT 已通过，用户仅有无问题声明，无逐点证据 |
| `R2-1-T02` | `passed` | W35/M1 首次选取后连续 3 次重复提交，页面计数保持 1；只读 active 关联 1 条；U-4 修复后未重现 | `in-progress`；GPT 已通过，用户仅有无问题声明；T02-C02 另有日期语义纠正 |
| `R2-1-T03` | `passed` | 刷新、月指导离开再回周统筹后计数和关联保持；只读 `integrity=ok` | `in-progress`；用户没有逐点页面/数据证据 |
| `R2-1-T04` | `in-progress` | C01/C02 有 GUI；C03/C04 只读和跨点 GUI 可见，但缺计划要求的独立取消/确认/重选截图 | `📝待补证据/待核实` |
| `R2-1-T05` | `in-progress` | 固定定义、实例、日行和临时对照有跨点 GUI/只读事实，但缺 T05 专用 C01-C04 GUI 留存 | `📝待补证据/待核实` |
| `R2-1-T06` | `failed`（GPT 报告层） | C04/C05 停用边界通过；C01 缺独立截图；C02/C03 产生 O-01 标题不同步、O-02 惯常日仍为周一两项客观观察 | `📝待补证据/待核实`；不把报告观察直接升级为 Fix |

#### 17.3.1 GPT 观察事项

| 观察编号 | 关联测试点 | GPT 客观事实 | 当前判断 |
|---|---|---|---|
| `O-01` | `R2-1-T06-C02/C03` | F1 定义保存为 `...-F1-已编辑` 后，W35 未删除存量实例仍显示旧标题，W36 新物化实例显示新标题；证据为 T06-C02/C03 GUI 和只读 JSON 的 `flow_week_instances.id=1/3` | 报告观察；用户 06 未逐点确认，暂列 `待核实/规格确认`，不生成新的 `Fix` |
| `O-02` | `R2-1-T06-C02/C03` | 保存惯常日后，W36 页面和只读 `weekdayMask=1` 仍显示“固定·周一”，与计划要求的 WD3/周三预期不一致；证据为 T06-C02/C03 GUI 和只读 JSON | 报告观察；星期位语义仍需核对，暂列 `待核实/规格确认`，不生成新的 `Fix` |

### 17.4 用户 06 原文（逐字保留）

> 下列内容来自本次调用输入；没有把它改写成用户文件，也没有删减其中的步骤、结论或证据说明。

````text
# R2-1 用户实测回传

- R 计划测试路径：
  `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-1-实测计划.md`
- 测试标识：`R2-1`
- 提交状态：全部完成；原计划测试未发现问题
- 计划状态：`planStatus=ready`（✅ E0 通过，可进入 T01）
- 静态入口状态：`staticGate/E0=passed`
- 计划当前台账：`testPointStatus=not-tested`、`closureStatus=not-started`（06 不修改）
- 实际执行范围：U01-U06，覆盖 R2-1-T01 至 R2-1-T06 的全部 20 个子用例
- 环境阻塞范围：无
- 实际测试参数：D0=2026-09-02；逻辑 W0=2026-09-07、W+1=2026-09-14、W+2=2026-09-21
- 回传时间：2026-09-03；用户未精确至秒

## 逐点状态

- `R2-1-T01`：已执行，本次暂未发现问题
- `R2-1-T02`：已执行，本次暂未发现问题
- `R2-1-T03`：已执行，本次暂未发现问题
- `R2-1-T04`：已执行，本次暂未发现问题
- `R2-1-T05`：已执行，本次暂未发现问题
- `R2-1-T06`：已执行原计划测试，本次暂未发现问题

覆盖子用例：

`R2-1-T01-C01/C02`、`R2-1-T02-C01/C02/C03`、`R2-1-T03-C01/C02`、`R2-1-T04-C01/C02/C03/C04`、`R2-1-T05-C01/C02/C03/C04`、`R2-1-T06-C01/C02/C03/C04/C05`

## T02-C02 纠正说明

2026-08-31 起始周属于 8 月周，因此 9 月 M1 不应出现在该周的月目标选取列表中。切换到 W+1 后 M1 可以正常选取。本事项不成立为产品问题。

保留原始证据：

- `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\R2-1相关证据\R2-1-T02-C02（1）.png`
- `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\R2-1相关证据\R2-1-T02-C02（2）.png`

## 用户补充问题 1

- 关联测试点/子用例：`R2-1-T06-C05`
- 用户是否实际执行：是
- 用户原计划测试结果：未发现问题
- 用户主观调整要求：F1 停用后不再克隆到 W+1；F1-W0 存量保留；页面计数与列表稳定
- 原计划口径：F1 停用后不再克隆到 W+2；F1-W0/F1-W+1 存量保留
- 问题性质：主观功能调整，不认定为本轮产品缺陷
- 用户证据：无独立问题证据；无问题不上传证据
- 稳定问题 ID：不由 06 创建，由 13 分流

## 证据说明

除 T02-C02 两份原始问题证据外，本次无问题用例未上传用户证据。GPT 参考报告路径：

`E:\workspace\GPT实测记录\v0.3实测内容\第2轮\v0.3-R2-1-GPT实测.md`

请 13 保留用户原始结论和 T02-C02 两份证据，将未列为问题的测试点记录为“本次暂未发现问题”，并单独分流“用户补充问题 1”。06 不创建问题文档、不修改测试计划或产品状态。
用户证据目录及文件路径：E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\R2-1相关证据
````

### 17.5 用户逐测试点结果与 13 合并状态

> 用户 06 对每组列出的子用例均给出“本次暂未发现问题”结论；按计划规则，因没有逐点详细记录，13 保留为该原文结论，不补写用户“通过”事实。

| 计划测试点/子用例 | 用户 06 原始结论 | 用户证据 | 13 合并层 `testPointStatus` | 处理说明 |
|---|---|---|---|---|
| `R2-1-T01`：C01/C02 | `本次暂未发现问题（用户未回传详细记录）` | 未提供 T01 用户证据 | `in-progress` | GPT T01 有客观证据；用户侧不写通过 |
| `R2-1-T02`：C01/C02/C03 | `本次暂未发现问题（用户未回传详细记录）`；另保留 T02-C02 日期语义纠正 | 仅有 T02-C02 两份 PNG；无逐次重复提交证据 | `in-progress` | U-4 的 GPT 回归通过；T02-C02 按用户原文不成立产品问题 |
| `R2-1-T03`：C01/C02 | `本次暂未发现问题（用户未回传详细记录）` | 未提供刷新前后/离开重进证据 | `in-progress` | GPT T03 通过；双方核心链路证据仍不齐 |
| `R2-1-T04`：C01/C02/C03/C04 | `本次暂未发现问题（用户未回传详细记录）` | 未提供用户证据 | `📝待补证据/待核实` | GPT C03/C04 也存在专用 GUI 留存缺口，不写完整通过 |
| `R2-1-T05`：C01/C02/C03/C04 | `本次暂未发现问题（用户未回传详细记录）` | 未提供用户证据 | `📝待补证据/待核实` | 覆盖计划已存在并执行声明已记录，但双方证据不齐 |
| `R2-1-T06`：C01/C02/C03/C04/C05 | `本次暂未发现问题（用户未回传详细记录）`；C05 另有主观调整要求 | 未提供用户证据；补充问题明确无独立问题证据 | `📝待补证据/待核实` | GPT O-01/O-02 与用户无问题摘要冲突；U-5 需要产品决策，不能直接 Fix |

20 个用户子用例的 ID 集合保持为：`T01-C01/C02`（2）、`T02-C01/C02/C03`（3）、`T03-C01/C02`（2）、`T04-C01/C02/C03/C04`（4）、`T05-C01/C02/C03/C04`（4）、`T06-C01/C02/C03/C04/C05`（5）。合计 20；没有创建额外测试点。

### 17.6 双方共同核心链路对照

| 测试点 | GPT 客观事实 | 用户事实/体验 | 一致性 | 待处理 |
|---|---|---|---|---|
| `R2-1-T01` | S1 入口、页面、同角色重进和角色隔离有 GUI/入口证据 | 用户声明执行且未发现问题，无详细记录 | 一方有证据，不能写双方一致通过 | 补用户入口/前缀/稳定态记录；不改计划台账 |
| `R2-1-T02` | U-4 首次选取、3 次重复提交、计数和 active 行均符合 Fix1 预期 | 用户声明未发现问题；T02-C02 另确认日期语义误判，不是产品问题 | 核心结论方向一致；用户详细证据缺失 | 保留两份 T02-C02 证据；不把纠正写成缺陷 |
| `R2-1-T03` | 刷新和离开重进后页面/只读结果稳定 | 用户声明未发现问题，无详细记录 | 方向一致，证据不对称 | 补用户页面稳定态或保持未形成双方通过 |
| `R2-1-T04` | 跨周/手写边界有部分 GUI/只读，删除取消/重选专用 GUI 不完整 | 用户声明未发现问题，无证据 | 未完成双边核对 | 继续补证；不新建 Fix |
| `R2-1-T05` | 固定定义/实例/日行/临时对照有跨点事实，专用分支证据不完整 | 用户声明未发现问题，无证据 | 未完成双边核对 | 记录为覆盖结果待补证，不判产品通过或失败 |
| `R2-1-T06` | 停用边界通过，但 GPT 观察到 O-01/O-02；C01 专用 GUI 缺失 | 用户声明未发现问题；提出 F1 停用后不克隆 W+1 的主观调整 | 存在事实/口径冲突和产品选择事项 | O-01/O-02 待核实；用户补充进入 U-5/16，不进 R2 Fix |

### 17.7 用户补充问题、问题分类与证据等级

#### 17.7.1 用户补充问题五栏表

| 来源标签 | 关联测试点/子用例 | 用户是否执行 | 用户原始事实/主观体验 | GPT 补充核验与独立证据 | 分类与证据等级 | 当前分流 |
|---|---|---|---|---|---|---|
| `用户补充问题 1` | `R2-1-T06-C05` | 是 | 原计划停用 F1 后不克隆到 W+2，用户本次未发现问题；用户提出：停用后不再克隆到 W+1，保留 F1-W0 存量，页面计数与列表稳定；用户明确不认定为本轮产品缺陷 | 当前没有针对用户 USER 副本、W+1 行和用户前缀的独立 GPT GUI/只读核验；GPT 报告只证明 GPT 副本按原计划 W+2 边界的客观结果 | R2 内稳定事项 `U-5`；`非缺陷候选/待用户确认`；证据等级 `待核实` | 进入 `16` 记录产品选择/用户决定；本次不进 R2 Fix。若确认是独立调整事项，再由 13 建立 R3 唯一闭环；本次不创建 R3 |

#### 17.7.2 用户 T02-C02 纠正

- 用户原始判断完整保留：2026-08-31 起始周属于 8 月周，9 月 M1 不应出现在该周的选取列表；切换到 W+1 后可以正常选取；该事项不成立为产品问题。
- 两份用户 PNG 已在 17.2 登记并保留；它们不能被解释为用户对 T01/T03/T04/T05/T06 的通过证据，也不能替代 GPT 的 R2-1 报告。
- 该纠正与 GPT 报告在 W35 创建 2026-08 月 M1 的 T02 场景不构成同一月份/同一周的直接冲突；用户和 GPT 的实际日期参数仍须在后续补证时分别保留。

#### 17.7.3 GPT 观察的分流

| 观察 | 当前分类 | 证据等级 | 是否建立稳定产品问题 | 分流 |
|---|---|---|---|---|
| `O-01` | `待核实/规格确认`；可能是当前周存量实例与定义编辑的产品语义偏离，也可能是冻结规格理解差异 | `待核实`；GPT GUI/只读证据充分，但用户未逐点确认 | 暂不升级为 `U-6`，保留原观察编号 | 先补同一 S1、同一 GPT/USER 语义对照或由产品决策确认；确认独立产品问题后再按下一 R 规则处理 |
| `O-02` | `待核实/规格确认`；weekdayMask 与 WD3/周三的映射尚未完成规格核对 | `待核实` | 暂不升级为 `U-7`，保留原观察编号 | 先核对星期位定义和用户体验；在确认前不进入 07 |

### 17.8 问题集合与根因诊断

| 事项 ID | 测试点/覆盖定位 | 来源与分类 | 已确认事实 | 用户主观体验 | 证据等级 | 当前状态 |
|---|---|---|---|---|---|---|
| `U-4` | `R2-1-T02` 至 `T04`；来源 `R1-2-T02-C02` | 已有 R2 独立新增问题；本轮为修复后回归 | GPT 在 S1/GPT 前缀下首次选取、重复提交、刷新/重进和合法边界均未重现重复 active；用户声明未发现问题 | 用户未提供逐点体验记录 | 来源问题根因已由 R2 Fix1 代码级确诊；本轮 GPT 复测证据充分，用户侧证据不完整 | 既有 `R2 Fix1/Review2` 不新开 Fix；R2-1 双方证据待补后再收束 |
| `COV-R2-01` | `R2-1-T05/T06` | 覆盖补充，不是自动产品缺陷 | R2-1 计划已把固定创建、物化、编辑、停用和临时对照纳入；GPT 有部分结果，专用证据不全 | 用户声明未发现问题；无逐点记录 | 计划覆盖事实已证实；结果证据部分待补 | 保留在 R2；不创建新缺陷 ID，不进入 R2 Fix |
| `O-01` | `R2-1-T06-C02/C03` | GPT 客观观察，待核实 | W0 存量标题与未来实例标题不一致 | 用户未报告该问题，但未提供细节 | `待核实` | 补证/产品规格确认；不进 Fix |
| `O-02` | `R2-1-T06-C02/C03` | GPT 客观观察，待核实 | 保存 WD3 后只读和界面仍显示 weekdayMask=1/周一 | 用户未报告该问题，但未提供细节 | `待核实` | 补证/星期位规格确认；不进 Fix |
| `U-5` | `R2-1-T06-C05` | 用户补充问题 1；非缺陷候选/产品选择事项 | 用户要求停用后从 W+1 起不再克隆、保留 W0 存量并保持计数/列表稳定；用户明确不认定为本轮缺陷 | 用户提出的是行为调整要求，不是本轮失败报告 | `待核实`；没有独立问题证据或 16 决策路径 | 交 16；确认前不自动修复，不创建 R3 |

#### 17.8.1 根因诊断

| 事项 | 已证实事实 | 根因诊断 | 证据等级 | 待核实项 |
|---|---|---|---|---|
| `U-4` | R2 Fix1 已使同周同月目标重复提交保持一条 active 关联；Review2 已在 B1-R2-only 复审通过 | 既有 R2 Fix1 的仓储层幂等守卫有效；本次不重新诊断或扩大范围 | `已证实`（修复结果）；原始根因已在 R2 历史章节确诊 | 用户侧同一 S1/USER 证据仍需补齐 |
| `O-01` | GPT 观察到 W0 旧实例标题与新定义标题不同 | 可能是当前周存量实例按快照保留的既定语义，也可能不符合计划“当前周未删实例标题同步”预期；不能只读代码裁决 | `待核实` | 产品规格是否要求改名回写当前周存量；用户是否感知为问题 |
| `O-02` | GPT 观察到保存 WD3 后 weekdayMask=1/周一 | 可能是 WD1/WD3 与 weekdayMask 的映射或计划参数登记不一致，也可能是实现偏离 | `待核实` | 星期位定义、WD3 实际日期、用户预期和是否属于独立产品问题 |
| `U-5` | 用户提出新的停用后未来克隆边界 | 这是行为/产品选择，不是已有 R2 Fix1 的根因 | `待核实` | 16 是否将其批准为独立需求/事项；若批准，后续 R 归属和验收计划 |

### 17.9 事项处置总表

| 事项 ID | 类型 | 来源/测试点 | 客观事实 | 用户主观体验 | 证据等级 | 处置类型 | 责任人 | 截止/复评点 | 允许范围/排除范围 | 验收条件 | 阻塞 | 当前状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `U-4` | 代码回归 | `R2-1-T02/T03/T04`；R2 Fix1 | GPT S1/GPT 副本未重现重复 active，跨周/手写/软删除边界符合报告结论 | 用户声明未发现问题，无详细记录 | 本轮 GPT 证据充分；用户证据不完整 | `代码 Fix`（沿用既有 `R2 Fix1`；本次不新建） | DeepSeek 已完成；GPT 负责合并 | 补齐用户侧证据后确认 R2-1 双方核心链路 | 仅既有 U-4 幂等边界；排除 COV、O-01/O-02、U-5 | 用户与 GPT 在同一 S1/各自隔离副本下的共同链路可定位且无回归 | 是，因用户证据缺口 | `Review2 已通过；R2-1 用户侧待补证` |
| `COV-R2-01` | 覆盖 | `R2-1-T05/T06` | 覆盖补充计划已经创建并执行；GPT 专用证据仍不全 | 用户声明未发现问题，无详细记录 | 计划覆盖已证实；结果证据部分待补 | `覆盖补充计划`（已由 R2-1 承接） | GPT/14；用户按计划补证 | 补齐 T05/T06 用户事实和 GPT 缺失分支证据 | 只补固定任务范围；不将未证实偏离带入 R2 Fix | 固定创建/编辑/停用/当前周/未来周/固定临时边界双方可复核 | 是，因双边证据不齐 | `R2-1 结果待补证；不新开覆盖 R` |
| `O-01` | 产品观察 | `R2-1-T06-C02/C03` | W0 存量标题与未来实例标题不一致 | 用户未报告；无逐点体验 | `待核实` | `补证/待核实` | GPT + 用户/产品负责人 | 规格确认和同 S1 补证后复评 | 先不改代码；排除未经确认的存量同步策略 | 明确当前周改名语义并取得用户确认/可见证据 | 是 | `不进入 Fix；保留 O-01 原观察编号` |
| `O-02` | 产品观察 | `R2-1-T06-C02/C03` | 保存 WD3 后仍显示 weekdayMask=1/周一 | 用户未报告；无逐点体验 | `待核实` | `补证/待核实` | GPT + 产品负责人 | 核对 weekdayMask/WD3 规格后复评 | 先不改代码；排除仅凭代码推断星期映射 | 星期位映射和预期一致，或形成可定位独立问题 | 是 | `不进入 Fix；保留 O-02 原观察编号` |
| `U-5` | 产品选择/非缺陷候选 | `用户补充问题 1`，`R2-1-T06-C05` | 用户要求停用后 W+1 起不再克隆，W0 存量保留，计数/列表稳定 | 用户明确这是主观功能调整，不认定本轮缺陷 | `待核实`；无独立问题证据 | `待用户确认` | 用户/PM；由 16 记录 | 16 决策后复评；确认独立事项再由 13 分流 | 不带入 R2 Fix；不把用户要求写成已实现或已验收 | 16 明确继续排障/替代/删除或需求承接，并由后续计划定义验收 | 是 | `待 16；本次不建 R3、不调用 07` |

### 17.10 统一 Fix 门检查

| 门禁条件 | 本次结论 |
|---|---|
| 本 R 每一项都有明确终态处置、无漏项 | `❌`；O-01、O-02 尚待核实，U-5 尚待 16，用户侧核心证据未齐 |
| 所有代码 Fix/替代/删除范围、责任人、原始场景、验收条件齐全 | `❌`；没有获准的新代码 Fix；U-5 不是已批准替代/删除 |
| 覆盖缺口已转为测试计划且未进入代码 Fix | `✅`；COV-R2-01 已由 R2-1 计划承接，未带入 R2 Fix1 |
| 无待补证据、待核实、环境/产品决策或待用户确认 | `❌` |
| 所有获准代码事项可以组成一次统一 Fix | `不适用/❌`；既有 R2 Fix1 已完成，当前没有可执行的 `R2 Fix2` |

**统一 Fix 结论**：本次不生成新的 `R2 FixN`，不调用 `07`，不把 O-01/O-02 或 U-5 拆成绕过阻塞的小 Fix。既有 `R2 Fix1 → Review2` 事实保持不变；若 O-01/O-02 经补证确认是独立产品问题，或 U-5 经 16 明确批准为独立调整事项，再按规则由 13 建立下一 R 的唯一闭环和后续计划分流。

### 17.11 跨 R 映射和计划边界

| 来源 | 当前归属 | 本次动作 |
|---|---|---|
| R1 `U-4` / `R1-2-T02-C02` | R2 `U-4`；既有唯一问题来源和 Fix/Review 链 | 本次只记录 R2-1 修复后回归；不回写 R1，不创建新的 R2 Fix |
| R1 `COV-R2-01` | R2 覆盖补充；由 R2-1 承接 | 不创建覆盖补充 R，不把覆盖结果提前写成通过 |
| GPT `O-01/O-02` | 当前仍留在 R2-1 合并待核实事项 | 不新建稳定问题 ID/下一 R；补证或规格确认后再分流 |
| 用户 `用户补充问题 1` | R2 内稳定事项 `U-5`；非缺陷候选/待用户确认 | 交 16；若明确成为独立调整事项，未来由 13 建立 R3；本次不创建 R3 |

本次没有创建测试计划、第二份 R2 闭环、R3 闭环、Fix、Review、QA/S、G1 或 C1 文档；没有修改已有计划正文、GPT 正式报告、用户 06 原文、产品代码、数据库或 `当前代码状态.md`。

### 17.12 时间线和下一动作

| 日期 | 事件 | 文档/证据 | 结论 |
|---|---|---|---|
| `2026-08-31` | GPT 执行 R2-1 T01-T06 | `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\v0.3-R2-1-GPT实测.md` 及第 2 轮证据目录 | GPT 报告结论 `🟡需修复`；交 13 分流 |
| `2026-09-02` | 用户声明执行 U01-U06，未发现原计划问题 | 当前调用提供的用户 06 原文；T02-C02 两份 PNG | 用户结果原文保留；无问题用例不补写用户通过 |
| `2026-09-03` | GPT 执行 R2-1 测试合并 | 本文 §17；用户 06 原文和证据路径已登记 | `closureStatus=in-progress`；R 状态 `📝待补证据/待核实` |
| 下一节点 | 补齐用户核心链路事实/证据；核对 O-01/O-02；由 16 记录 U-5 产品决定 | 不修改计划和 GPT 报告；补证落点须另行定位 | 只有阻塞解除后，才判断是否进入 07、下一 R、05/15/14；当前不进入 Fix/15 |

### 17.13 R2-1 测试合并正式结论

- **`planStatus`**：`ready`；测试计划存在且标识匹配。
- **`staticGate/E0`**：`passed`；S1/QA1 和 GPT 侧正式入口门可核对。
- **计划 `testPointStatus`**：`not-tested`；这是计划原始台账，保持不变。13 的 GPT/用户合并状态见 17.3 和 17.5，不回写计划。
- **计划 `closureStatus`**：`not-started`；这是计划原始台账，保持不变。13 合并层为 `in-progress`。
- **各测试点合并状态**：T01-T03 为“GPT 结果有证据、用户声明无问题但无逐点证据”；T04/T05 为双方证据待补；T06 为 GPT O-01/O-02 待核实且用户 U-5 待产品决策。
- **证据完整性**：GPT 侧部分完整，用户侧仅 T02-C02 两份纠正证据；双方完整证据 `❌未齐`，不能宣称用户通过或 R2 关闭。
- **问题分类**：既有 U-4 回归的 GPT 修复后结果通过；COV-R2-01 是覆盖补充；O-01/O-02 是待核实观察；U-5 是用户主观功能调整/待用户确认；本次没有独立新增问题进入下一 R。
- **统一 Fix 范围**：无新的可执行 Fix；既有 R2 Fix1/Review2 不被覆盖，不生成 R2 Fix2。
- **当前 R 状态**：`📝待补证据/待核实`。
- **用户验收、G1、C1**：均未形成；本节不替代用户最终验收、G1 或 C1。
- **`nextAction`**：补齐用户侧共同核心链路事实/证据；对 O-01/O-02 做规格/GUI 补证；由 16 记录 U-5 决策。完成前不调用 `07`、`05`、`15`，不创建下一测试计划或 R3。

### 17.14 用户无问题回传口径纠正与当前状态（2026-09-03，GPT；13）

> 用户明确补充回传规则：GPT 和用户都必须实际执行计划约定的 GUI 测试；用户只对发现问题的测试详细说明，未发现问题的测试统一表示“未发现问题，测试通过”。GPT 的 GUI/只读证据用于补充客观事实，但不能替代用户实际执行，也不能补写用户没有回传的主观体验。本节是当前最新裁决，覆盖 17.5、17.6、17.9、17.10、17.12、17.13 中“用户无问题不能记为通过”或“因用户未逐点证据而阻断”的表述；17.4 的用户原文保持不变。

#### 17.14.1 测试点状态

| 测试点 | 用户 `testPointStatus` | GPT `testPointStatus` | 13 当前合并 `testPointStatus` |
|---|---|---|---|
| `R2-1-T01` | `passed`；用户统一回传未发现问题 | `passed`；GUI 证据可定位 | `passed` |
| `R2-1-T02` | `passed`；T02-C02 另有日期语义纠正，不成立为产品问题 | `passed`；GUI/只读证据可定位 | `passed` |
| `R2-1-T03` | `passed`；用户统一回传未发现问题 | `passed`；刷新/离开重进事实可定位 | `passed` |
| `R2-1-T04` | `passed`；用户统一回传未发现问题 | `in-progress`；计划要求的独立取消/确认/重选证据仍有缺口 | `📝待补证据/待核实`；阻塞来自 GPT 证据，不来自用户未上传截图 |
| `R2-1-T05` | `passed`；用户统一回传未发现问题 | `in-progress`；T05 专用分支 GUI 留存仍有缺口 | `📝待补证据/待核实`；阻塞来自 GPT 证据，不来自用户未上传截图 |
| `R2-1-T06` | `passed`；用户统一回传未发现问题，另提出 U-5 | `failed`（报告层；O-01/O-02 是待核实观察，不等于已确认缺陷） | `📝待补证据/待核实`；阻塞来自 O-01/O-02 和 U-5 决策 |

#### 17.14.2 证据完整性和门禁重判

| 项目 | 当前结论 |
|---|---|
| 用户实际执行 | `✅`；06 明确执行 U01-U06，覆盖 20 个子用例；未发现问题统一视为用户测试通过 |
| 用户无问题项的逐点证据 | `不要求`；用户没有上传问题证据不构成缺口，13 不补写页面/数据细节 |
| 用户问题证据 | `✅可定位`；T02-C02 两份 PNG 按用户原文保留；U-5 是功能调整要求，不是用户缺陷证据 |
| GPT 客观证据 | `⚠️部分完整`；T04/T05 专用 GUI 留存仍需补齐，O-01/O-02 需要规格/GUI 核对 |
| 用户主观体验 | `✅`；仅记录用户明确回传的“未发现问题”和 U-5 原始要求，不以 GPT 事实替代 |
| `planStatus` / `staticGate/E0` | `ready` / `passed` |
| 计划原始 `testPointStatus` / `closureStatus` | `not-tested` / `not-started`；保持不改 |
| 13 合并层 `closureStatus` / `updatedAt` / `updatedBy` | `in-progress` / `2026-09-03T12:23:46+08:00` / `GPT` |
| 当前 R 状态 | `📝待补证据/待核实`；不是因用户无问题项缺少逐点证据，而是 GPT 证据、O-01/O-02、U-5 尚未收束 |

#### 17.14.3 当前行动边界

- 用户无需为 T01-T06 的“未发现问题”重新补逐点截图或详细页面记录；若后续发现问题，只需按问题测试点补充原始事实、复现步骤和证据。
- GPT/13 继续补齐 GPT 侧 T04/T05 计划要求的独立 GUI 证据，并核对 O-01 标题同步语义和 O-02 星期位映射；不把用户“测试通过”改写成 GPT 的客观细节。
- U-5 仍由 `16` 记录产品决定。只有明确批准为独立的新功能要求后，13 才初始化 R3；在此之前不创建 R3、不生成 R2 Fix2。
- R2 仍未闭环；只有上述 GPT 证据和事项分流完成后，13 才能更新 R2 的最终状态。

### 17.15 GPT 侧 T04/T05 补证与只读收束（2026-09-03，GPT；13）

本节是当前最新的 13 合并记录，只追加本次 GPT 补证，不修改测试计划、GPT 正式报告、用户 06 原文、产品代码、数据库或 `当前代码状态.md`。

#### 17.15.1 测试点最新状态

| 测试点 | 用户结果 | GPT 结果 | 13 当前合并状态 | 说明 |
|---|---|---|---|---|
| `R2-1-T01` | `passed`；用户统一回传本次暂未发现问题 | `passed` | `passed` | 沿用既有入口/隔离证据 |
| `R2-1-T02` | `passed`；T02-C02 日期语义纠正，不成立为产品问题 | `passed` | `passed` | 沿用 U-4 回归核验和用户两份原始证据 |
| `R2-1-T03` | `passed`；用户统一回传本次暂未发现问题 | `passed` | `passed` | 刷新、离开和重进结果沿用既有证据 |
| `R2-1-T04` | `passed`；用户统一回传本次暂未发现问题 | `passed` | `passed` | C03 删除取消/确认、C04 重选/刷新/重进的 GPT 专用截图已补齐 |
| `R2-1-T05` | `passed`；用户统一回传本次暂未发现问题 | `passed` | `passed` | GPT 已补齐固定定义、当前周物化、固定/临时对照及未提交表单离开返回 |
| `R2-1-T06` | `passed`；用户统一回传本次暂未发现问题，另提出 `U-5` | `failed/待核实`（报告层；`O-01/O-02` 不是已确认缺陷） | `📝待补证据/待核实` | `O-01/O-02` 和 `U-5` 仍未完成规格/产品决策收束 |

用户未发现问题的测试按当前最新回传规则记录为“本次暂未发现问题/测试通过”，不要求用户为每个无问题子用例重新制作逐点截图或详细记录；不因此补写用户未提供的页面细节。

#### 17.15.2 T05 GPT GUI 实测事实及证据

- F2 表单保存后页面显示“已保存（物化后本周出现实例）”；固定面板显示 `场次 ×3`、惯常日 `二`，W35 任务清单出现 `v0.3-R2-1-GPT-20260903-F2 固定任务`，进度为 `0/3`，来源标签为 `固定·周二`。
- W35（`2026-08-24 ~ 08-30`）页面同时显示 F1 的 `固定·周一` 和 F2 的 `固定·周二`；W36（`2026-08-31 ~ 09-06`）页面再次显示 F1/F2 两条固定实例及对应周一/周二惯常日安排。
- W35 新增 `v0.3-R2-1-GPT-20260903-TMP1 临时任务` 后，任务清单和“每周固定待安排”均为 3 条；TMP1 没有固定来源标签，F1/F2 保留 `固定·周一/固定·周二` 标签，固定与临时边界可见。
- 打开空白固定任务新建表单但未提交，离开到 2026 年 9 月月指导，再返回周统筹；返回页面固定定义仍为 2 条，没有产生第三条固定定义或额外固定实例。
- 本次 GUI 证据路径：
  - `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-T05-C01-F1创建.png`
  - `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-T05-C01-F2创建.jpg`
  - `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-T05-C02-W35-当前周物化.jpg`
  - `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-T05-C02-W36-周一周二物化.jpg`
  - `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-T05-C03-固定临时对照.jpg`
  - `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-T05-C04-固定表单未提交.jpg`
  - `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-T05-C04-离开月指导.jpg`
  - `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-T05-C04-返回周统筹.jpg`

#### 17.15.3 关闭后只读持久化核对

核对对象为本轮实际入口选择的 `R2-1 / GPT / 20260903` 副本：

- 数据库：`E:\workbuddy-test\R1-3&R2-1\B1-20260829\R2-1\GPT-20260903\userData\workbuddy.db`。
- 前缀：`v0.3-R2-1-GPT-20260903`；快照：`S1`；候选：`QA1`；程序入口日志：`E:\workbuddy-test\R1-3&R2-1\B1-20260829\entry\B1-entry.log`。
- 只读证据：`E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-T05-T06-GPT-20260903-readonly.json`；使用 Node 22 内置 `DatabaseSync` 的 `mode=ro&immutable=1` URI，并执行 `PRAGMA query_only=ON`。
- `flow_fixed_defs`：2 条 active 定义。F1 为 `once`、`targetCount=1`、`weekdayMask=1`；F2 为 `multi`、`targetCount=3`、`weekdayMask=2`；没有未提交表单产生的第三条定义。
- `flow_week_instances`：5 条 active 实例：W35 的 F1/F2、W36 的 F1/F2，以及 W35 的 `origin=temp` TMP1；固定实例带对应 `fixedDefId`，TMP1 的 `fixedDefId` 为空。
- `flow_day_entries`：4 条 `source=habit`、`locked=1` 的日行，日期为 `2026-08-24`、`2026-08-25`、`2026-08-31`、`2026-09-01`，分别关联 F1/F2 的 W35/W36 实例。
- `integrity_check=ok`；`foreign_key_check=[]`；只读查询期间 `query_only=1`；数据库 SHA-256 前后均为 `C4B2C38C2F6FDB723E9BD7CEF013C4965B80A480C1D73B0D98643EA97DA347BC`，`hashUnchanged=true`。

#### 17.15.4 证据完整性与事项分流最新结论

| 项目 | 最新结论 |
|---|---|
| `planStatus` | `ready`；计划存在且标识为 `R2-1` |
| `staticGate/E0` | `passed` |
| 计划原始 `testPointStatus` | `not-tested`；保持不改 |
| 计划原始 `closureStatus` | `not-started`；保持不改 |
| 13 合并层 T04/T05 | GPT 与用户结果均可登记为 `passed`；T05 属覆盖补充结果，不是新缺陷 |
| GPT 侧 T04/T05 证据 | `✅已补齐`；GUI 截图、关闭后只读定义/实例/日行、完整性和前后哈希可定位 |
| 用户侧无问题回传 | `✅按当前规则满足`；用户已明确执行 U01-U06，未发现问题统一按测试通过记录；无问题项不要求逐点用户截图 |
| 用户问题证据 | `✅可定位`；T02-C02 两份 PNG 原始证据保留；`U-5` 是功能调整要求，不是问题证据 |
| `O-01` / `O-02` | 继续留在 R2 原文，分类为 `待核实/规格确认`，不进入 Fix |
| `U-5` | 继续留在 R2，分类为 `非缺陷候选/待用户确认`，交 `16`；未获得独立新事项批准 |
| 当前 R 状态 | `📝待补证据/待核实` |
| 13 合并层 `closureStatus` | `blocked`；阻塞来自 `O-01/O-02` 和 `U-5`，不是用户无问题项未上传截图 |
| `updatedAt` | `2026-09-03T13:21:35+08:00` |
| `updatedBy` | `GPT` |
| 证据完整性 | `GPT T04/T05 ✅；用户回传口径 ✅；R2 全部事项 ❌未闭合` |

#### 17.15.5 当前 `nextAction`

1. `16` 记录 `U-5` 的产品/用户明确决策；在批准为独立新功能要求之前，不创建 R3，不生成 R2 Fix2。
2. 对 `O-01` 核对“当前周存量实例改名是否应同步”的规格和可见 GUI 事实；对 `O-02` 核对 `WD3` 与 `weekdayMask` 的正式星期位定义和预期。两项补证和裁决继续回写本 R，不复制问题。
3. 只有所有待核实/待决策事项收束后，才能判断是否进入 `07`、是否形成统一 Fix；如无代码 Fix 的覆盖事项，则交 `14`，不伪造 QA/S。
4. `R2-1` 通过完整 `Fix → Review → 15 → 14` 或相应关闭路径后，才进入 G1 前置；本次不调用 `05`、`15`、`14`、`G1`。

**本次结论**：R2-1 仍未闭环。T04/T05 的 GPT 侧缺口已补齐，用户无问题项按“未发现问题/测试通过”登记；但 `O-01/O-02` 尚待核实、`U-5` 尚待 `16` 决策，所以不能把当前 R2 写成 `✅当前轮次闭环`，也不能现在开 R3。

### 17.16 环境/产品决策记录（U-5，2026-09-03，16；GPT）

> 本节按 `16-环境与产品处置决策.md` 追加到 R2 唯一闭环文档。U-5 是 R2-1-T06-C05 的用户补充问题 1，用户已明确选择“替代”。本节只记录证据、产品选择和交接，不修改代码、数据库、测试计划、GPT 报告、用户 06 或 `当前代码状态.md`；替代尚未执行，也不表示 R2 或 U-5 已完成验收。

#### 17.16.1 决策卡

| 字段 | 当前记录 |
|---|---|
| 事项 ID | `U-5` |
| 来源标签/测试点 | `用户补充问题 1`；`R2-1-T06-C05` |
| `planStatus` | `ready`；R2-1 计划存在且唯一标识一致 |
| `staticGate/E0` | `passed`；QA1/S1、B1 正式入口、角色目录、前缀和隔离可核对 |
| `testPointStatus` | 用户侧 `R2-1-T06=passed`（用户统一回传未发现问题）；GPT/13 合并层仍为 `failed/待核实`，原因是 `O-01/O-02` 尚未收束；U-5 不是新测试点，不改计划原始台账 |
| `closureStatus` | `in-progress`；16 决策已记录，替代实施、`05` 复审和后续 R3 验证尚未完成 |
| 事项状态 | `🟡已决定替代/待执行`；不认定为 R2 本轮产品缺陷 |
| 用户选择 | `替代` |
| 决策生效时间 | `2026-09-03T13:52:41+08:00` |
| 责任人 | 用户/PM：产品决策与验收；`07/DS`：按批准范围实施，具体实施人由 `07` 确认 |
| 截止期限 | 未提供独立的具体日；由 `14` 在后续计划、由 `05` 在复审记录中补记可核对时间 |
| 复评时间 | `R3` 后续测试计划执行完成并经 `05` 复审后，由 `13` 复评；具体日期由 `14/05` 记录 |
| 恢复条件 | 确认 F1 停用后 W+1 及后续周不再生成新克隆，F1-W0 存量保留，页面计数与列表稳定，且不影响其他固定任务和临时任务；验证不满足时返回 `13` 重新分流 |
| 返回阶段 | `13` |
| `updatedAt` / `updatedBy` | `2026-09-03T13:52:41+08:00` / `GPT` |
| `nextAction` | `13` 更新事项处置总表；按新范围完成后续 R3 唯一闭环/计划交接；计划齐全后交 `07/DS` 执行，再由 `05` 复审和后续计划验证 |

#### 17.16.2 事项事实、用户体验与证据分栏

| 项目 | 记录 |
|---|---|
| 原始事实 | 用户已执行 `R2-1-T06-C05`，并在无问题回传中保留原计划结果；用户提出新的行为边界：停用 F1 后不再克隆到 W+1，保留 F1-W0 存量，页面计数与列表稳定。用户明确不把它认定为 R2 本轮产品缺陷。 |
| 用户主观体验 | 用户把该内容表达为产品行为调整要求，而不是失败、崩溃、数据损坏或不可用问题；用户希望停用后的未来周边界更早生效且页面结果稳定。未把 GPT 对页面/数据库的观察改写成用户体验。 |
| GPT 客观事实 | 原 R2-1 GPT 复测在 `S1/QA1`、`R2-1/GPT-20260831` 副本中完成停用确认：页面显示“已停用（存量保留）”；W37 无新的 F1、F2 保持；刷新、离开和重进后列表/计数稳定；F1 W35/W36 存量仍保留。该证据验证的是原计划的 W+2 边界和停用稳定性，不证明用户副本已验证新的 W+1 边界。 |
| 用户证据 | 用户证据目录为 `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\R2-1相关证据\`；当前仅有 T02-C02 两份 PNG。U-5 没有独立问题截图，符合用户“未发现问题不上传问题证据”的回传口径；不把该目录缺少 U-5 截图写成用户未执行。 |
| 用户 06 材料 | 本次用户 06 的 R2-1 原始回传已在本文 §17.4、§17.14、§17.15 保留；`E:\workspace\用户prompt\06-用户反馈测试问题.md` 是回传规则模板，不是另一个用户结果文件。 |
| 证据等级 | 用户对“替代”的明确选择：`已确认`；原计划 T06-C05 的 GPT GUI/只读事实：`已证实`；新 W+1 行为在用户副本中的独立复测：`未执行/未提供`；因此本节记录产品决定，不写成新行为已经实现。 |

#### 17.16.3 S/副本/正式入口和数据隔离最小核对

| 核对项 | 结果 |
|---|---|
| 测试计划与报告 | 计划：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-1-实测计划.md`；GPT 报告：`E:\workspace\GPT实测记录\v0.3实测内容\第2轮\v0.3-R2-1-GPT实测.md`；二者测试标识均为 `R2-1`。 |
| 当前 S/QA | `S1/QA1`，版本 `0.3.0-qa.1`；`WorkBuddy.exe` SHA-256 为 `EEC94A500952CC83A15A9BE1907B0059D8A6262BE63503FA9E54F94716011874`。 |
| 正式入口 | `E:\workbuddy-test\选择B1实测角色.bat` 选择角色后，由 `E:\workbuddy-test\启动实测.bat` 启动 `E:\workbuddy-test\app-v0.3\WorkBuddy.exe`；B1 入口预检 `allChecksPassed=true`。 |
| C05 行为副本 | `R2-1/GPT-20260831` 独立副本，前缀 `v0.3-R2-1-GPT-20260831`；数据库类别为 `B1-20260829/R2-1/GPT-20260831/userData/workbuddy.db`。 |
| 当前入口复核副本 | 角色选择器记录 `R2-1/GPT/20260903`，前缀 `v0.3-R2-1-GPT-20260903`，实际目录位于 B1 批次下的 GPT 独立日期目录；与 `R2-1/USER`、`R1-3` 角色目录分离。该次只读记录不替代 C05 的行为证据。 |
| 只读与完整性 | `R2-1-T06-C05` 补充核验使用 `DatabaseSync readOnly:true`、URI `mode=ro`、`PRAGMA query_only=ON`；C05 副本前后 SHA-256 均为 `F3A53AE3B7950B000857A50F2FE3E4201441A7BC10DC0181B90C4A7FD389E738`，`integrity_check=ok`、外键核对为空。20260903 补充只读记录另确认 `dataChangedByReadonlyCheck=false`。 |
| 环境阻塞判断 | 未发现启动失败、窗口不可见、真实数据串档或权限阻塞；U-5 的待决事项是行为选择，不是环境阻塞。 |
| 候选替代入口 | `不适用`。本次“替代”对象是 F1 停用后的产品行为边界，不是启动入口；没有提出或批准新的环境入口，不能把任何候选入口写成替代方案。 |

#### 17.16.4 最小环境/数据风险记录

- 最后成功步骤：完成 F1 停用确认，页面显示“已停用（存量保留）”；进入 W37 后未出现新的 F1，F2 保持；刷新、离开到月指导/今日总览再返回后页面计数与列表稳定。
- 可见阻塞：没有环境可见阻塞；当前需要记录的是用户对“W+1 起不再克隆”的产品选择，不把原计划与新要求的差异写成已证实缺陷。
- 受影响范围：`R2-1-T06-C05` 的 F1 停用后未来物化边界，以及后续新功能的测试计划、Fix 和复评；不改变 U-4 的重复选取回归，不把 `O-01/O-02` 并入 U-5。
- 数据风险：16 阶段未执行任何代码、数据库或入口改动；未授权物理删除、清理或迁移数据。后续实现若错误删除 F1-W0、继续生成 W+1、改写其他固定任务或把临时任务误判为固定任务，均属于未满足恢复条件的风险。
- 保持边界：本次明确保留 F1-W0 存量并停止停用后的新 W+1 及后续克隆；“不再克隆”不等于授权删除既有数据。用户原文没有授权删除或改写其他已存在实例，若实现遇到停用前已存在的 W+1，`14/07` 必须在计划和执行记录中单独标明，不得由 GPT 推定删除策略。
- 安全恢复：后续只在新计划绑定的隔离副本和正式 S 中验证；不清理真实用户数据。若验收条件不满足，保留失败事实并返回 `13` 重新分流，不在本阶段直接补数据或绕过门禁。

#### 17.16.5 三种处置选项和用户明确原文

| 选项 | 适用记录 | 当前是否选择 |
|---|---|---|
| `继续排障` | 若是环境或事实不清，应登记排障任务、责任人、期限、复评点和补证步骤；决定前保持 `⏸️待环境/产品决策`，不进入 Fix。 | `未选择`；本事项没有待排障的环境阻塞 |
| `替代` | 将原计划“停用后至 W+2 不再新克隆、W0/W+1 存量保留”的行为边界替代为经用户批准的 W+1 起新边界；由后续计划定义可执行范围和验收，再由 `07/DS` 实施并经 `05` 复审。 | `✅用户明确选择` |
| `删除` | 删除对象、软删除/保留/恢复策略、不可逆风险和验收条件均须另行明确；本事项没有删除对象。 | `未选择` |

**用户/PM 决策字段**：`替代`

**用户明确决定原文**：

> 我明确决定：将 R2-1-T06-C05 的用户补充问题 1（U-5）作为独立的新功能要求，按“替代”处理，不认定为 R2 本轮产品缺陷。原要求为：F1 停用后不再克隆到 W+1；F1-W0 存量保留；页面计数与列表稳定。

#### 17.16.6 批准替代范围、验收和交接

| 项目 | 记录 |
|---|---|
| 被替代行为 | R2-1 计划原口径：F1 停用后不再克隆到 W+2，F1-W0/F1-W+1 存量保留。该原口径在 R2-1-T06-C05 中作为本轮测试预期保留。 |
| 批准的新行为 | F1 停用生效后，不生成 W+1 及后续周的新 F1 克隆；F1-W0 存量保留；页面计数和列表保持稳定。 |
| 允许范围 | 只处理 F1 停用后的未来克隆边界、W0 存量保留和页面稳定性；F2、其他固定任务、临时任务、固定/临时来源区分和既有 U-4 幂等边界不在本次替代范围内。 |
| 保留/迁移边界 | 不物理删除或清理 F1-W0；不凭本决定删除或改写其他既有实例；不得把停用后的“不再克隆”实现成误删存量。任何停用前已存在的 W+1 行，须由后续计划明确其独立验收语义。 |
| 实施责任 | 用户/PM 负责产品决策和验收；`07/DS` 只按本节批准范围实施，具体实施人由 `07` 确认；`16` 不直接调用 `07`。 |
| 验收条件 | 1. F1 停用后 W+1 及后续周不新增 F1 克隆；2. F1-W0 存量保留且无物理删除/数据丢失；3. 当前周、未来周、刷新和离开重进后的页面计数与列表稳定；4. F2、其他固定任务和临时任务不受影响，不产生重复或串档；5. 只读完整性、外键、隔离前缀和安全恢复结果可核对。 |
| 当前是否完成 | `否`。替代决策已批准并记录，但尚未由 `07` 执行、`05` 复审或由后续计划验证。 |
| 是否仍阻断当前 R2 Fix | U-5 不再是“待用户决策”阻塞，但不进入 R2 Fix2，也不授权立即修改代码；当前 R2 仍未闭环，`O-01/O-02` 继续独立待核实。U-5 在后续计划和批准范围齐全前仍不可执行。 |
| 后续交接 | 先交 `13` 更新 U-5 处置总表并保留本节；随后按规则由 `14` 组织独立新功能范围/后续 R3 计划；计划齐全后交 `07/DS` 执行，完成后交 `05` 复审；`R3` 测试计划完成并经 `05` 复审后由 `13` 复评。若恢复条件不满足，回到 `13`，不写成通过。 |

#### 17.16.7 本次 16 结论

- U-5 已完成产品选择记录：用户明确选择 `替代`，并明确要求将其作为独立新功能，不认定为 R2 本轮缺陷。
- 本次没有创建 R3、测试计划、Fix、Review、QA/S、G1 或 C1；没有修改代码、数据库、现有测试计划、GPT 报告、用户 06 或状态文件。
- 现有 R2 文档的 U-5 旧“待用户确认”记录保留为历史事实；本节是当前 16 决策记录，后续由 `13` 更新事项处置总表，不由 `16` 代改总表。
- 当前 R2 仍不能写成 `✅当前轮次闭环`：`O-01/O-02` 仍按原观察编号独立待核实，U-5 虽已完成决策但尚未实施和复评。

### 17.17 R2-1 测试合并后续裁决（2026-09-03，GPT；13）

> 本节是 `16` 对 U-5 作出“替代”决定后的 `13` 测试合并续写。历史 §17.7、§17.9、§17.15 和 §17.16 均保留；本节只建立当前指针、更新事项处置和完成独立 R3 分流。13 未修改测试计划、GPT 正式报告、用户 06 原文、产品代码、数据库或 `当前代码状态.md`。

#### 17.17.1 当前门禁和逐测试点状态

| 字段 | 当前记录 |
|---|---|
| 处理模式 | `R 测试合并` |
| 测试标识 | `R2-1`；计划、GPT 报告和用户 06 标识一致 |
| 测试计划 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-1-实测计划.md`；只读引用，不修改 |
| GPT 正式报告 | `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\v0.3-R2-1-GPT实测.md`；只读引用，不修改 |
| 本次唯一 R2 闭环 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-月目标重复选取-全流程闭环记录.md`；R2 目录中无第二份同轮闭环 |
| `planStatus` | `ready`；只表示 R2-1 计划和 E0 已就绪 |
| `staticGate/E0` | `passed` |
| 计划原始 `testPointStatus` | `not-tested`；计划正文和原始台账保持不变，13 不把 `T01` 改写为通过或 E0 失败 |
| 计划原始 `closureStatus` | `not-started`；计划正文和原始台账保持不变 |
| 13 合并层 `closureStatus` | `blocked`；O-01/O-02 待核实，U-5 已决策但尚未实施/复评 |
| `updatedAt` / `updatedBy` | `2026-09-03T22:00:59+08:00` / `GPT` |
| 当前 R2 状态 | `📝待补证据/待核实`；不能写为 `✅当前轮次闭环` |

计划原始 `testPointStatus=not-tested` 是不可变台账。以下“合并事实状态”只用于 R2 闭环文档的事实分栏，不回写计划；`T01` 在计划台账中仍为 `not-tested`。

| 测试点 | 计划原始 `testPointStatus` | 用户 06 结果 | GPT 结果 | 13 合并事实状态与边界 |
|---|---|---|---|---|
| `R2-1-T01` | `not-tested`（保持） | `passed`；用户统一回传未发现问题 | `passed`；S1 入口、窗口和隔离证据可定位 | 已记录双方执行事实；不改计划台账 |
| `R2-1-T02` | `not-tested`（保持） | `passed`；T02-C02 日期语义纠正，不成立为产品问题 | `passed`；U-4 重复选取回归未重现 | `passed`；保留两份用户 PNG，不把日期纠正写成缺陷 |
| `R2-1-T03` | `not-tested`（保持） | `passed`；用户统一回传未发现问题 | `passed`；刷新、离开重进和只读结果可定位 | `passed` |
| `R2-1-T04` | `not-tested`（保持） | `passed`；用户统一回传未发现问题 | `passed`；C03/C04 专用 GUI 与只读证据已补齐 | `passed`；不新建 Fix |
| `R2-1-T05` | `not-tested`（保持） | `passed`；用户统一回传未发现问题 | `passed`；固定创建、物化、固定/临时对照和未提交表单证据已补齐 | `passed`；属于 COV-R2-01 覆盖结果，不是新缺陷 |
| `R2-1-T06` | `not-tested`（保持） | `passed`；用户统一回传未发现问题，另提出 U-5 | `failed/待核实`（报告层）；O-01/O-02 尚无规格裁决 | `📝待补证据/待核实`；U-5 已转独立 R3，O-01/O-02 仍留 R2 |

#### 17.17.2 当前事项处置总表

> 本表是最新处置指针；§17.9 中的旧“待用户确认”行保留为历史，不覆盖本表。U-5 的批准替代不授权立即执行代码修改，也不把 R2-1 原计划改写为新计划。

| 事项 ID | 类型 | 来源/测试点 | 客观事实 | 用户主观体验 | 证据等级 | 处置类型 | 责任人 | 截止/复评点 | 允许范围/排除范围 | 验收条件 | 阻塞 | 当前状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `U-4` | 既有 R2 代码回归 | `R2-1-T02/T03/T04`；R2 Fix1 | S1/GPT 副本下重复 active 未重现，合法跨周/手写/软删除边界保留 | 用户 06 统一回传未发现问题 | `已证实`；既有 Fix1/Review2 已完成 | `代码 Fix`（沿用 `R2 Fix1`，本次不新建） | DeepSeek 已完成；GPT 合并 | R2-1 事实已合并；不再生成 R2 Fix2 | 仅 U-4 幂等边界；排除 COV、O-01/O-02、U-5 | 既有 Review2 结论保持；不产生回归 | 否 | `✅既有 Fix1/Review2 通过` |
| `COV-R2-01` | 计划覆盖补充 | `R2-1-T05/T06` | 固定定义、物化、编辑、停用和固定/临时范围已纳入 R2-1；T05 GPT 证据已补齐 | 用户 06 统一回传未发现问题 | `已证实`；覆盖结果已合并 | `覆盖补充计划` | GPT/14；用户按计划执行 | R2-1 已完成本轮覆盖事实合并；不另造覆盖 R | 仅固定任务覆盖；排除未确认的 O-01/O-02 产品修复 | 计划覆盖事实和双方结果可追溯 | 否 | `✅本轮覆盖结果已合并；不进 Fix` |
| `O-01` | GPT 产品观察/待核实 | `R2-1-T06-C02/C03` | W0 存量 F1 实例旧标题与 W36 新物化标题不一致 | 用户未报告该现象，未提供主观判断 | `待核实`；已有 GPT GUI/只读事实，缺规格裁决 | `补证/待核实` | GPT + 产品负责人/用户 | 同一 S1 规格/GUI 核对后回写 R2 | 先不改代码；排除未经确认的存量标题同步策略 | 明确当前周改名语义并取得可见事实/决定 | 是 | `留在 R2；不进 Fix，不新建下一 R` |
| `O-02` | GPT 产品观察/待核实 | `R2-1-T06-C02/C03` | 保存 WD3 后页面与只读仍显示 `weekdayMask=1/周一` | 用户未报告该现象，未提供主观判断 | `待核实`；需核对星期位规格 | `补证/待核实` | GPT + 产品负责人/用户 | 核对 WD3/weekdayMask 定义后回写 R2 | 先不改代码；排除仅凭代码阅读作结论 | 星期位映射与计划预期明确，或形成可定位新事项 | 是 | `留在 R2；不进 Fix，不新建下一 R` |
| `U-5` | 用户批准的独立新功能要求 | `用户补充问题 1`；`R2-1-T06-C05`；16 §17.16 | 用户要求 F1 停用后 W+1 及以后不再生成新克隆，F1-W0 存量保留，计数/列表稳定；用户明确不认定为 R2 本轮缺陷 | 用户将其作为行为调整要求，不是失败或数据损坏 | 决策 `已确认`；新行为尚未执行或复评 | `替代` | 用户/PM 决策；14 计划；07/DS 实施；05 复审 | R3 后续计划执行并经 05 复审后由 13 复评 | 只处理 F1 停用后的未来克隆边界和 W0 保留；排除 F2、其他固定/临时任务、U-4、O-01/O-02 | W+1 及以后不新增 F1；W0 保留；刷新/离开重进计数稳定；其他任务不受影响；只读完整性和隔离可核对 | 对 R2 Fix：否；对 R2 最终关闭：待 R3 执行/复评 | `🟡已决定替代；已分流 R3，待 14 计划` |

#### 17.17.3 独立 R3 分流与唯一性

- `U-5` 已由用户/PM 在 R2 §17.16 明确批准为独立新功能要求，符合“独立新事项”分流条件；原始用户事实、T06-C05 结果、GPT 观察、证据路径和决策原文继续保留在 R2，不复制为新的测试结果。
- 当前不存在同 R3 闭环文档、R3 测试计划或冲突目标路径。13 已按 `R 初始化` 建立唯一 R3 骨架：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第3轮\v0.3-R3-停用后固定任务未来周边界-全流程闭环记录.md`。
- R3 骨架只记录 R2/U-5 来源、已批准替代范围、C0/S0 追溯和空白环节；没有写入 R3 测试结果、通过结论、Fix、Review 或用户验收。R3-1 测试计划仍由 `14` 在合法节点创建，13 不创建或续写测试计划。
- R2 的 O-01/O-02 与 U-5 不合并：O-01/O-02 继续留在 R2 做规格/GUI 补证；U-5 由 R3 独立承接。代码修改如后续获准，按流程串行执行。

#### 17.17.4 证据和输入完整性

| 核对项 | 当前结论 |
|---|---|
| 计划/报告标识 | `R2-1` 与计划、GPT 正式报告、§17.4 用户原文一致 |
| 共同 C0/S1 | `C0=34f155a1886c52dae278fbc71d037975646b2563`；当前 R2-1 被测快照为 `S1/QA1`；S1 构建物、入口和隔离路径见 §17.1.1、§17.15.3 |
| GPT 证据 | T01-T05 的 GUI/只读证据可定位；T06 的 O-01/O-02 证据可定位但仍待规格裁决；T04/T05 补证路径见 §17.15.2 |
| 用户 06 原文 | §17.4 逐字保留，覆盖 20 个用户子用例、T02-C02 日期语义纠正、用户补充问题 1 和 U-5 原始调整要求 |
| 用户证据 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\R2-1相关证据\`；仅 T02-C02 两份 PNG，哈希和用途见 §17.2；U-5 无独立问题截图符合无问题回传口径 |
| R3 来源证据 | R2 唯一闭环 §17.16、R2-1 计划、GPT 报告、R2 用户证据目录和 GPT T06 生命周期只读证据；不新增未提供的用户副本证据 |
| 敏感信息 | 当前采纳路径和证据未发现密钥、Cookie、未经脱敏隐私或不必要真实业务内容 |

#### 17.17.5 当前状态和下一动作

| 字段 | 当前值 |
|---|---|
| `planStatus` | `ready`（R2-1；计划原文不变） |
| `staticGate/E0` | `passed` |
| 各测试点 `testPointStatus` | 计划原始台账逐点保持 `T01=not-tested`、`T02=not-tested`、`T03=not-tested`、`T04=not-tested`、`T05=not-tested`、`T06=not-tested`；13 不改写计划台账，实际执行事实见 §17.17.1 独立栏 |
| `closureStatus` | `blocked`（13 合并层；不是计划正文状态） |
| `updatedAt` / `updatedBy` | `2026-09-03T22:00:59+08:00` / `GPT` |
| 证据完整性 | GPT T01-T05 `✅`；用户 06 原文和无问题回传口径 `✅`；T02-C02 两份用户证据 `✅`；O-01/O-02 规格闭环 `❌`；U-5 实施/复评 `❌` |
| R2 当前状态 | `📝待补证据/待核实` |

**`nextAction`**：

1. 由 GPT/产品负责人继续对 `O-01` 的存量标题同步语义和 `O-02` 的 `WD3/weekdayMask` 定义做同一 S1 的规格/GUI 补证；结果回写 R2，不复制问题。
2. 由 `14` 读取 R3 唯一骨架并在合法节点创建 `R3-1` 计划，只覆盖 §17.16 批准的 U-5 替代范围；13 不创建该计划。
3. R3-1 计划齐全后，按批准范围交 `07/DS` 实施，整批交 `05` 复审；复审和后续 R3 实测完成后交 `13` 复评。R2 不生成 `R2 Fix2`。
4. 在 O-01/O-02 和 R2/R3 必要关闭证据完成前，不调用 `15`、不冻结新的 QA/S、不调用 `G1`，也不宣称用户最终验收、C1 或维护期。

**本次 13 结论**：R2-1 的测试事实和证据已更新合并；T01 计划台账保持 `not-tested`，T02-T05 的合并事实通过，T06 因 O-01/O-02 仍待核实而保持阻塞。U-5 已完成“替代”决策并建立唯一 R3 初始化骨架，但替代尚未执行和验证；R2 当前仍为 `📝待补证据/待核实`，不生成 R2 Fix2。

### 17.18 关于 R3-1 创建顺序的纠正（2026-09-03，GPT；13）

> 用户指出：现行被测基线尚未实现 U-5，先创建 R3-1 只会在旧行为上做修复后复测，结果必然不能回答“新要求是否已经实现”。该判断成立。本节是对 §17.17.5“下一步”中 R3-1 先于实现的顺序纠正；§17.17 的历史文字保留，当前适用口径以本节和 R3 文档底部最新正式结论为准。

#### 17.18.1 纠正依据和边界

- R3 的来源是 `16` 已批准的 U-5 产品行为替代，不是 R2-1 已确认的代码失败。因此不能把当前 `C0/S0` 直接拿来作为 U-5 新行为的修复后复测对象。
- `14` 的修复后复测门要求先完成同一 R 的 `Fix → Review → QA/S`，再创建绑定新 QA/S 的后续计划。R3-1 在该节点之前不应创建，也不应标记 `ready`、启动 `E0` 或分配可执行测试点。
- R3-1 仍然需要创建，但它的类型应为 `修复后复测`，用途是验证已实现的 U-5；不是在未实现的旧基线上先行测试。
- 本次只更正文档分流和节点顺序，不修改 R2-1 测试计划、GPT 正式报告、用户 06 原文、用户证据、产品代码、数据库或 `当前代码状态.md`。工作树中如有相关未提交代码或自动化测试，也不能未经 `07` 归属、`complete-fix`、`05` 复审和 `15` 冻结而视为 U-5 已实现。

#### 17.18.2 U-5 的正确交接

U-5 已有 `16` 的明确“替代”决定、允许范围、排除范围和验收条件，现形成 R3 的可执行统一 `R3 Fix1`。该 Fix 是产品行为调整的实现批次，不是测试计划，也不是测试通过结论。

| 项目 | 当前口径 |
|---|---|
| Fix 标识 | `R3 Fix1`；R3 唯一统一批次，不拆成子 Fix |
| 来源 | R2 §17.4 用户原始要求、§17.16 用户批准、本文 §17.18 |
| 允许范围 | 仅 F1 停用生效后的未来克隆边界：W+1 及以后不生成新的 F1 克隆；F1-W0 存量保留；计数、列表、刷新和离开重进保持稳定 |
| 必须保持 | 其他固定任务和临时任务不受影响；同一副本的只读完整性、入口和数据隔离可复核 |
| 排除范围 | F2、其他固定/临时任务、R2 U-4、R2 O-01/O-02、未批准的物理删除或数据清理；不把停用前已经存在的未来行擅自解释为删除授权 |
| 验收条件 | 原始停用操作链路、W0 保留、W+1/W+2 无新 F1、页面稳定、刷新/离开重进、其他任务隔离和只读证据均可复核 |
| 当前状态 | `13` 已形成可执行范围；尚未执行 `07`，尚未产生 Review、QA/S 或 R3 测试结果 |

#### 17.18.3 节点顺序和失败回退

1. `13` 在 R3 唯一闭环中记录并交接可执行的 `R3 Fix1`；本次不调用 `14`。
2. `07/DS` 按批准范围实现 U-5，记录 Fix 执行、自测、构建和 `begin-fix/complete-fix` 机器交接；不能修改计划正文或伪造用户结果。
3. `05` 对整个 `R3 Fix1` 做 `Review1`，按原始行为边界取得 GUI、只读、构建和隔离证据。
4. `05` 通过后交 `15` 核对并冻结新的 QA/S；`05` 通过或 `07` 候选均不等于 QA/S 已冻结。
5. 只有新的 QA/S 冻结后，`14` 才创建唯一 `R3-1` 修复后复测计划，并绑定该 QA/S；计划创建后才进入 GPT/用户实测和 `13` 合并。
6. `07` 实现失败回 `07/13` 补齐 Fix；`05` 复审失败回 `07`，不得先创建 R3-1；`15` 或计划门禁失败分别留在对应节点。R2 不生成 `R2 Fix2`。

#### 17.18.4 当前状态快照

| 字段 | 当前值 |
|---|---|
| R2 `planStatus` | `ready`；R2-1 计划原文不变 |
| R2 `staticGate/E0` | `passed` |
| R2 各测试点 `testPointStatus` | 计划原始台账 `R2-1-T01` 至 `R2-1-T06` 均保持 `not-tested`；13 不回写计划台账 |
| R2 `closureStatus` | `blocked`；O-01/O-02 仍待核实，U-5 尚待后续实现和复评 |
| R2 当前状态 | `📝待补证据/待核实` |
| R3 当前分流 | `R3 Fix1` 可执行；R3-1 尚未到创建节点 |
| 双方证据完整性 | R2-1 计划、GPT 报告、用户 06、用户证据和 R2 来源证据可定位；U-5 新实现、Review、QA/S、R3 双方实测证据尚未产生 |
| `updatedAt` / `updatedBy` | `2026-09-03T22:22:56+08:00` / `GPT` |

**最新 `nextAction`**：R2 的 O-01/O-02 继续按原 R2 路径补证；U-5 不交 `14` 先建计划，而是交 R3 的 `07/DS` 实现 `R3 Fix1`，再经 `05 → 15` 后由 `14` 创建绑定新 QA/S 的 `R3-1` 修复后复测计划。当前不调用 `14`、不调用 `G1`，不宣称 U-5 已修复、用户验收、C1 或维护期。

### 17.19 O-01/O-02 补充 GUI 核验与 R2 当前轮次关闭（2026-09-04，GPT；13）

> 本节是 R2 唯一闭环文档的最新正式指针。§17.17、§17.18 中“O-01/O-02 待核实”和 R2 `blocked` 的历史状态保留；本节记录同一 `R2-1`、同一 `QA1/S1`、同一 GPT 隔离副本的补充复测结果，更新当前状态，不改写测试计划、GPT 正式报告、用户 06、产品代码或用户数据库。

#### 17.19.1 补充核验输入和证据

| 项目 | 当前值 |
|---|---|
| 处理模式 | `R 测试合并` 的同计划补充复测/冲突核对；不创建新计划、新测试点或新 R |
| 测试标识 | `R2-1` |
| 测试点 | `R2-1-T06-C02`、`R2-1-T06-C03` |
| 测试计划 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-1-实测计划.md`；只读引用 |
| GPT 正式报告 | `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\v0.3-R2-1-GPT实测.md`；只读引用 |
| 用户 06 | 本 R §17.4 原文；用户已确认 T06 执行且未发现问题，补充要求 U-5 已按 §17.16/§17.17 分离到 R3 |
| 快照和入口 | `QA1/S1`、`0.3.0-qa.1`；经 B1 正式入口进入 `R2-1/GPT` 隔离副本 |
| GUI 前缀/数据目录 | `v0.3-R2-1-GPT-20260903`；`E:\workbuddy-test\R1-3&R2-1\B1-20260829\R2-1\GPT-20260903\userData` |
| 独立补充证据 | `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-补充核验-T06-C02-C03-20260904.md` |
| 只读证据 | `E:\workspace\GPT实测记录\v0.3实测内容\第2轮\证据\R2-1-T06-C02-C03-GPT-20260904-readonly.json` |

#### 17.19.2 实际 GUI 结果

1. 在 W36（`2026-08-31 ~ 09-06`）重新打开 F1 编辑表单，观察到原状态只有周一被选中；本次明确取消周一、单独选择周三，再保存改名 `v0.3-R2-1-GPT-20260903-F1-R2O-check`。保存反馈为“已保存（物化后本周出现实例）”。
2. W36 周统筹显示 F1 新标题和 `固定·周三`；F2 仍为原标题、场次型和 `固定·周二`。当前周任务没有新增重复项。
3. W37（`2026-09-07 ~ 09-13`）显示 F1/F2 各一条固定实例；F1 使用新标题和周三，F2 保持周二。`2026-09-08` 日规划只出现 F2，`2026-09-09` 日规划出现 F1 新标题并带固定标识。
4. 回到 W37 刷新后，标题、星期标签、实例数量和 F2 对照均保持稳定；关闭 WorkBuddy 后目标窗口已消失。

#### 17.19.3 O-01/O-02 当前裁决

| 事项 | 来源 | 本次可见/只读事实 | 规格对照 | 当前裁决 |
|---|---|---|---|---|
| `O-01` | `R2-1-T06-C02/C03` | F1 definition 已为新标题；W35（`2026-08-24`）历史实例保留旧标题，W36 当前周实例和 W37 未来实例均为新标题；已存在的 `2026-08-31` 周一锁定日行仍保留旧标题 | 计划要求改名影响当前周未删实例，修改星期不回溯重算已有日安排；历史周不追溯、当前周同步、未来周采用新定义与该边界一致 | `✅非缺陷观察，已收束；不进 Fix` |
| `O-02` | `R2-1-T06-C02/C03` | 正确执行“取消周一、选择周三”后，GUI 的固定面板、W36/W37 周统筹和 `2026-09-09` 日规划均为周三；F1 `weekdayMask=4`，F2 `weekdayMask=2` | WD3/周三、weekdayMask 和未来物化结果一致；此前 `weekdayMask=1/周一` 的观察未在本次正确改日操作下复现 | `✅非缺陷观察，已收束；不进 Fix` |

此前 GPT 观察中 O-02 的“仍为周一”来自编辑表单仍保留周一的状态，不能作为“已取消周一并改选周三”的有效复现。补测不把该历史观察删除，而是保留其操作状态、冲突原因和本次可复核的正确结果。

#### 17.19.4 只读完整性与数据边界

| 核对项 | 结果 |
|---|---|
| `queryOnly` | `1` |
| `PRAGMA integrity_check` | `ok` |
| `foreign_key_check` | `[]` |
| 只读前后 SHA-256 | `EEA3FDFC939E6837E3121BDB28D5D16951BD5D22F2DE67695B80AD4E2DACB202`，前后一致 |
| `dataChangedByReadonlyCheck` | `false` |
| 实例/日行 | F1 W36/W37 实例各一条；F1 `2026-09-09` 日行一条；未发现重复、F2 串改或历史行物理删除 |
| 数据边界 | 未读取、复制、修改、清空或删除真实 `%APPDATA%\\workbuddy\\`；只读核对使用 R2-1/GPT 隔离副本 |

#### 17.19.5 最新合并状态

计划原始台账继续保持不可变：`planStatus=ready`、`staticGate/E0=passed`、计划原始 `testPointStatus` 中 `R2-1-T01=not-tested` 以及 T02-T06 的 `not-tested` 均不改写；计划原始 `closureStatus=not-started` 也不改写。以下是 13 合并层的最新事实状态。

| 测试点 | 计划原始 `testPointStatus` | 用户 06 | GPT/13 合并事实 |
|---|---|---|---|
| `R2-1-T01` | `not-tested`（保持） | 用户统一回传未发现问题 | 双方执行事实已合并；合并事实 `passed`，不改计划台账 |
| `R2-1-T02` | `not-tested`（保持） | 未发现问题；T02-C02 日期语义纠正保留 | `passed`；日期语义不成立为缺陷 |
| `R2-1-T03` | `not-tested`（保持） | 未发现问题 | `passed` |
| `R2-1-T04` | `not-tested`（保持） | 未发现问题 | `passed` |
| `R2-1-T05` | `not-tested`（保持） | 未发现问题 | `passed`；覆盖结果不进 Fix |
| `R2-1-T06` | `not-tested`（保持） | 已执行且未发现问题；U-5 已分离 R3 | `passed`；O-01/O-02 已按非缺陷观察收束 |

#### 17.19.6 事项处置总表最新指针

| 事项 ID | 类型/来源 | 处置类型 | 阻塞 | 当前状态 |
|---|---|---|---|---|
| `U-4` | R2 既有回归；T02/T03/T04 | 沿用 `R2 Fix1` | 否 | `✅Fix1/Review2 通过` |
| `COV-R2-01` | 固定任务覆盖；T05/T06 | 覆盖补充结果留痕，不进 Fix | 否 | `✅本轮覆盖事实已合并` |
| `O-01` | GPT 观察；T06-C02/C03 | 补证完成，非缺陷观察 | 否 | `✅已收束；符合当前周/历史/未来标题边界` |
| `O-02` | GPT 观察；T06-C02/C03 | 补证完成，非缺陷观察 | 否 | `✅已收束；WD3/weekdayMask=4/周三一致` |
| `U-5` | 用户补充问题 1；T06-C05 | 已批准替代，独立分流 R3 | 否（对 R2） | `🟡R3 独立承接；不作为 R2 未决条件` |

R2 没有待补证据、待核实、环境阻塞或待用户确认事项；没有代码 Fix、替代或删除事项需要在 R2 生成新的统一 Fix。U-5 的实现和复评属于独立 R3，不回流为 R2 Fix，也不改变本 R 本次已完成的事实合并。

#### 17.19.7 当前状态和下一动作

| 字段 | 当前值 |
|---|---|
| `planStatus` | `ready`（R2-1；计划正文不变） |
| `staticGate/E0` | `passed` |
| 各测试点 `testPointStatus` | 计划原始 `R2-1-T01` 至 `T06` 均为 `not-tested`（保持）；13 合并事实为 T01-T06 `passed` |
| `closureStatus` | `closed`（13 合并层；不是计划正文状态） |
| `updatedAt` / `updatedBy` | `2026-09-04T17:36:24+08:00` / `GPT` |
| 证据完整性 | GPT T01-T06 GUI/只读证据 `✅`；用户 06 原文和统一无问题回传口径 `✅`；T02-C02 用户两份 PNG `✅`；O-01/O-02 规格/GUI/只读补证 `✅`；U-5 已完成决策并明确属于独立 R3 |
| R2 当前状态 | `✅当前轮次闭环` |

**最新 `nextAction`**：

1. R2 不再为 O-01/O-02 调用 `16`、`07`、`05`、`15` 或 `14`；保留本节和独立补充证据作为 R2 关闭依据。
2. U-5 继续沿独立 R3 路径处理；R3 的实现、复审、后续计划和完成状态不作为 R2 关闭条件，也不回写为 R2 测试结果。
3. 版本级 `G1` 仍须等待所有活跃 R 各自关闭后，按独立 G1 计划和双方证据执行；本 R 关闭不等于 `G1`、用户最终验收、`C1` 或维护期。

**本次 13 结论**：R2-1 的 T06-C02/C03 补充 GUI 与只读核验完成。O-01 的历史周/当前周/未来周标题边界符合计划，O-02 的 WD3/星期三与 `weekdayMask=4` 一致；两项均收束为非缺陷观察。R2 所有事项已有明确终态，`closureStatus=closed`，R2 当前为 `✅当前轮次闭环`。R3 仅独立承接 U-5，不影响本 R 关闭；版本仍须后续 G1。
