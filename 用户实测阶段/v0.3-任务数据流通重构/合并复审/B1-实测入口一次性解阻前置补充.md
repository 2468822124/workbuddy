# B1 实测入口一次性解阻前置补充

> 文档性质：B1 当前冻结 S1 的一次性正式实测入口前置补充/勘误记录。
> 本记录不是新的 R 闭环、测试计划、GPT 报告、用户 06 回传、QA/S 快照或产品缺陷记录。
> 本记录不改写 R1-3、R2-1 的业务实测历史事实，也不写入业务实测结果；两份计划的状态字段按本轮决定保留 `🟡待补前置`。

## 入口阶段判定

> **当前入口阶段：B1 当前批次业务实测。**  
> B1 尚未形成完整业务实测结论，但这只是当前结果状态，不是入口阻塞原因。  
> 在 B1 当前批次结束前，R1-3 与 R2-1 的唯一实际操作入口是 `E:\workbuddy-test\启动实测.bat`，角色选择器是 `E:\workbuddy-test\选择B1实测角色.bat`。  
> `E:\workbuddy-test\长期入口\` 仅为 B1 完成后的后续长期实测入口，当前为“已预建、未启用”，不得用于本次 B1 实测。  
> B1 结束后，旧入口保留为历史资产；“切换入口”仅表示后续运行改用长期入口，不表示覆盖、迁移、重命名或删除旧入口。

当前 B1 不运行 B1 完成后的长期入口脚本，不创建新的后续长期 `runId`，不使用 `E:\workbuddy-test\work\...\runId\userData`。现有 `current\active-run.json` 中的 `sealed` 记录属于已封存长期设施，不是本次 B1 运行，也不构成本次 B1 阻塞。

## 一、适用范围与禁止事项

- 仅适用于当前 B1 批次的 `R1-3` 与 `R2-1`，共同绑定已经冻结的 `QA1/S1`。
- 不适用于下一测试轮次，不改变后续版本更新、测试归档或数据库生命周期规则。
- 不检查、不修改、不启动 `E:\workspace\workbuddy`；该目录不是本次被测来源。
- 不创建新 R、缺陷 ID、测试计划或新的 QA/S，不修改产品代码、历史 R 事实、R 计划正文、用户 06 或 GPT 正式报告。

## 二、冻结构建与正式入口

| 项目 | 本次事实 |
|---|---|
| QA/S | `QA1/S1`，版本 `0.3.0-qa.1` |
| S1 构建物 | `E:\workbuddy-snapshots\v0.3.0-qa.1-b1-merge-s1\out` |
| S1 程序哈希 | `EEC94A500952CC83A15A9BE1907B0059D8A6262BE63503FA9E54F94716011874` |
| S1 清单哈希 | `0044A2AE3A0075C3733748006308FD3E5F05B19388C5CDEAF02C584DA94FE581` |
| 正式入口 | `E:\workbuddy-test\启动实测.bat` |
| 角色选择器 | `E:\workbuddy-test\选择B1实测角色.bat`；只写选择状态，不启动 WorkBuddy |
| 入口预检脚本 | `E:\workbuddy-test\B1实测入口预检.ps1` |
| 入口选择状态 | `E:\workbuddy-test\R1-3&R2-1\B1-20260829\entry\selected-role.ini` |

`启动实测.bat` 是唯一实际启动 WorkBuddy 的入口。它拒绝命令行参数，启动前核对 S1 程序哈希和 S1 清单哈希，并根据已登记的计划/角色选择映射 `WORKBUDDY_USER_DATA`。

## 三、一次性数据隔离映射

归档根目录为：

`E:\workbuddy-test\R1-3&R2-1\B1-20260829\`

四个角色副本按实际选择日期建立：

```text
R1-3\GPT-YYYYMMDD\userData
R1-3\USER-YYYYMMDD\userData
R2-1\GPT-YYYYMMDD\userData
R2-1\USER-YYYYMMDD\userData
```

前缀分别为：

```text
v0.3-R1-3-GPT-YYYYMMDD
v0.3-R1-3-USER-YYYYMMDD
v0.3-R2-1-GPT-YYYYMMDD
v0.3-R2-1-USER-YYYYMMDD
```

四个目录不互用；不复制历史 Review/旧轮次数据库；不清空、迁移或覆盖 `E:\workbuddy-test\userData`；不读取、复制、修改、清空或删除 `%APPDATA%\workbuddy\`。

## 四、已执行的非业务核对

- S1 源程序存在，哈希为预期值。
- `E:\workbuddy-test\app-v0.3` 已逐项匹配 S1 清单 100 个文件，未再次覆盖部署。
- 原旧程序备份 `E:\workbuddy-test\app-v0.3-before-qa1-20260829` 保留。
- 初始核对时没有 WorkBuddy 进程。
- `B1实测入口预检.ps1` 于 `2026-08-29T22:17:26.2262830+08:00` 完成四角色启动探针，记录为 `allChecksPassed=true`、`status=entry-preflight-passed-controlled-cleanup-business-prefix-pending`。
- `R1-3/GPT`、`R1-3/USER`、`R2-1/GPT`、`R2-1/USER` 均观察到 `E:\workbuddy-test\app-v0.3\WorkBuddy.exe`，并在各自映射目录创建 `workbuddy.db`；四次 `entryExitCodeAcceptable=true`，结束时没有残留 WorkBuddy 进程。
- 四次探针均使用受控清理结束 GUI 进程；该清理属于预检收尾，不是业务实测结果，也不改变入口对用户传入参数、S1 哈希或旧目录的限制。
- `E:\workbuddy-test\userData` 的预检前后文件快照一致；真实 `%APPDATA%\workbuddy\` 未读取、复制或修改。
- 预检没有业务数据，未观察 GUI 中的 R/角色前缀；同一角色关闭后重进和角色切换后的业务数据隔离仍需由 R1-3/R2-1 `T01` 按计划取证。
- R1-3/R2-1 计划的状态字段保持 `🟡待补前置`；当前入口、数据根和门禁语义按本记录同步，业务实测未开始。

预检证据落点：

`E:\workbuddy-test\R1-3&R2-1\B1-20260829\entry\evidence\B1-entry-once-preflight.json`

入口日志与清单核对落点：

- `E:\workbuddy-test\R1-3&R2-1\B1-20260829\entry\B1-entry.log`
- `E:\workbuddy-test\R1-3&R2-1\B1-20260829\entry\B1-entry-once-preflight.log`
- `E:\workbuddy-snapshots\v0.3.0-qa.1-b1-merge-s1\S1-manifest.sha256`

## 五、解阻状态

入口设施、S1 包绑定、四角色启动、实际数据库落点、旧目录不变和无残留进程等 E0 静态/启动子项已经通过。预检结果中的 `business-prefix-pending` 只表示没有 GUI 业务数据，尚未证明可见 R/角色前缀；同一角色关闭重进与切换后的业务数据隔离属于双方 `T01` 动态取证。

两份计划本轮继续保持 `🟡待补前置`，但该状态是按当前决定保留，不把 B1 业务实测未完成当作入口阻塞原因。E0 通过后，T01 是第一个测试点；T01 未执行只能记录“未测试/待取证”，不能写成通过，也不能反过来称为 T01 启动阻塞。T01 失败时停止依赖它的后续业务测试。

后续独立复核如确认计划可执行，再由当前状态看板和计划正文按门禁规则更新状态；该更新不代表测试通过、用户验收、R 关闭、G1 或 C1。
