# WorkBuddy 项目协作规则

## 项目定位

本目录 `E:\workspace\` 是 WorkBuddy 的项目文档根目录；产品代码通常位于 `E:\workspace\workbuddy\`。除非任务明确要求，不要把治理文档、Prompt、审查记录或测试证据写入代码根。

## 工作前检查

- 先阅读与当前任务相关的 `工作流.md`、`当前代码状态.md`、`当前审查状态.md` 和 `文档索引.md`。
- 涉及开发阶段、用户实测或维护期变更时，优先阅读 `规范类\` 中对应的门禁、命名、文档治理和变更闭环规则。
- 不要猜测项目状态；以文档底部最新正式结论、当前指针、实际代码和测试结果为准。
- 保留用户已有修改，不使用破坏性 Git 操作覆盖、重置或删除未确认的内容。

## Prompt 使用规则

- 已设计好的 Prompt 位于 `E:\workspace\用户prompt\`。总索引和角色边界见 `用户prompt\00-开发单元闭环Prompt使用说明.md`。
- 需要调用 Prompt 时，先确认当前阶段、前置门禁、输入文件的绝对路径和产物落点，再选择对应编号的 Prompt。
- 如果 Prompt 包含 `{{...}}`、`<...>`、`待填写`、`请填写` 或类似用户输入占位符，不得自行臆造内容。
- 调用该 Prompt 前，必须在回传中逐项列出所有待用户填写的字段，并说明每个字段需要什么信息；字段较多时可给出可直接复制的填写模板。
- 若缺少必填内容，先明确告知用户需要补充什么，不要把任务伪装成已完成。
- 优先使用 Prompt 要求的绝对路径、编号、轮次、批次和测试点 ID，不擅自改写编号体系或历史路径。

## 任务执行边界

- 遵守 Prompt 中的角色边界：GPT&Claude、GLM+DS 和用户各自负责的内容不能越权代写或代替验收。
- 所有结论必须区分“已验证事实”“待补证据”“用户决策”和“建议”。
- 修改前先确认目标文件和范围；修改后按项目要求运行相关测试、构建、审查或文档一致性检查。
- 涉及更改文档状态、闭环状态或当前结论的操作，必须同步更新该文档顶部的最新状态字段/摘要；历史章节和历史状态保留，不得用顶部旧状态覆盖底部最新正式结论。
- 不要宣称“通过”“完成”或“已修复”，除非有对应的实际证据。

## 每次任务完成后的回传格式

回传必须简单、清晰、面向下一步，使用以下结构：

### 已完成

- 用 1—3 条说明实际完成的工作。
- 列出关键文件的绝对路径和必要的状态、测试结果或证据。

### 下一步

- 明确说明下一步要做什么、由谁做，以及应调用哪个 Prompt（编号和文件名）。
- 如果下一步不需要用户操作，直接写出将执行的动作。

### 用户需要填写

- 如果下一步 Prompt 没有用户输入，写“无需填写”。
- 如果有用户输入，逐项列出 Prompt 中所有待填写字段，保留字段原名或占位符，并说明填写要求。
- 可以附上可复制格式，例如：

  ```text
  {{版本}} =
  {{功能名}} =
  {{目标文档绝对路径}} =
  ```

- 不要只说“请填写相关信息”，必须告诉用户具体缺哪些字段。

## 推荐回传示例

```text
已完成
- 已完成 R1-1 测试计划核对。
- 已确认文档：E:\workspace\用户实测阶段\vX-功能\第1轮\计划.md
- 测试结果：未运行代码测试；当前仅完成文档核对。

下一步
- 下一步调用 `用户prompt\12-GPT自主实测.md`，由 GPT 执行客观 GUI 实测。

用户需要填写
- 测试计划绝对路径：请提供或确认上述路径。
- 测试标识：填写 `R1-1` 或 `G1`。
```

## 文件与 Git 安全

- 不自动提交、推送或发布，除非用户明确要求。
- 不删除历史记录、证据、Prompt 或用户数据。
- 历史文件原样保留；新规则不得覆盖历史审计事实。
- 发现路径冲突、门禁未满足、证据不足或用户输入缺失时，停止在安全边界并清楚说明阻塞原因。
