# 审查日志 · 阶段 4 — 设置与数据管理

> **说明**：用户粘贴的 03 prompt 残留 `{{阶段3}}` 旧占位符；阶段 3 已 ✅ 关闭。本审查按 `当前状态.md` 实际待审的**阶段 4** 进行。

## 审查元信息
- **阶段 / 批次**：阶段 4（首次实现）
- **审查日期**：2026-08-05
- **审查人**：GLM
- **审查对象**：DeepSeek 阶段 4 产出（11 文件：7 新增 + 4 修改）

## 1. 审查范围
**DeepSeek 产出**：见 `项目日志/阶段4.md`。
**审查方法**：实跑 `npm run build`（7.24s 绿）、`grep` 硬编码色值、`grep` 注入面、读 settings.ipc/llm/data/settings 复合层逐行核查。GUI 运行时项（测试连接实网、dialog 实弹、round-trip 实跑）headless 未验，标注待验。

## 2. 验收点逐项核查（对照 `规格-阶段4-*.md §9`）

| # | 验收点 | 结论 | 证据 |
|---|---|---|---|
| 1 | 通用称呼/晨间时段保存往返；问候反映 | ✅(代码) | SettingsView @blur save → settings:set；TodayView 读 nickname |
| 2 | AI 配置：Base URL/Model 明文、Key 密文往返 | ✅(代码) | settings.ipc SETTINGS_SET 读 `existing.isEncrypted`→对 llmApiKey（seed 时标 true）重加密；safeStorage 链路正确 |
| 3 | 测试连接 成功/失败/超时 ≤10s | ✅(代码) | llm.ts fetch+AbortController(10s)；200/non-200/abort/network 四态 |
| 4 | logger 不输出 apiKey 明文 | ✅(代码) | llm.ts 仅记 status + text.slice(0,200)；Authorization 头从不入 logger |
| 5 | 热点源增删改 + URL 校验 + 持久 + **「立即刷新」** | ❌ | **F2**：CRUD+`/^https?:\/\/.+/` 校验+持久 ✅；但 SettingsView 热点卡与 HotspotSourcesEditor **均无「立即刷新」按钮**，news:refresh 在设置页无触发入口（今日总览热点卡仍有刷新路径，部分覆盖） |
| 6 | 导出：保存框+10业务表+不含news_items/migrations+取消不报错 | ✅(代码) | data.ts BUSINESS_TABLES=10；跳过 news_items/__schema_migrations；cancel→err，UI 忽略不崩 |
| 7 | 导入：打开框+校验+事务还原+round-trip | ⚠️ | 事务 db.transaction DELETE/INSERT ✅；但见 **F1** 列名注入 |
| 8 | 导入校验失败不动 DB | ⚠️ | validatePayload 在事务前 ✅；但 **F1**：仅校验表名白名单+数组，**未校验列名**，「形状合法但列名恶意」的文件能通过校验进入注入 |
| 9 | 导入前内联二次确认（非模态） | ✅(代码) | importConfirm ref + 内联 cfm 条；无 alert/confirm |
| 10 | 保存称呼/AI 配置 → onboarded=true | ✅(代码) | useSettings.save 对 nickname/llmBaseUrl/llmApiKey 触发 set('onboarded','true') |
| 11 | 不配 LLM 亦可访问设置页 | ✅(代码) | SettingsView 无门禁；测试按钮 !canTest 时禁用+提示 |
| 12 | grep 组件无硬编码 hex/rgba | ✅ | 实测 `*.vue` → No matches |
| 13 | build 绿 | ✅ | 7.24s |

**小结：13 项中 ✅10 + ⚠️2 + ❌1。核心问题 F1（注入）+ F2（刷新按钮缺失）。**

## 3. 禁止边界核查（§7-B）
- [x] 无硬编码密钥
- [x] 日志无 apiKey 明文（llm.ts 不传入）
- [x] 无业务数据上传云端（测试连接是出站到用户自配端点，非上传业务数据）
- [x] 软删除：导入为用户主动全量恢复路径，物理 DELETE+INSERT 已显式豁免（规格 §10 声明）
- [x] 无阻断模态（文件选择 OS dialog；覆盖确认内联）

## 4. 错误降级（§7-C）
| 场景 | 期望 | 结论 |
|---|---|---|
| 测试连接失败/超时 | ≤10s 返回可读原因 | ✅(代码) |
| 导入失败 | 事务回滚，不损坏 | ✅(代码) db.transaction；但 F1 注入在事务内，DROP 类语句会提交（事务不防 DROP） |
| 导入取消 | 不报错 | ✅(代码) |

## 5. 视觉核查
- [x] token 化；无硬编码 hex
- [~] 四态：按钮 hover/disabled 到位

## 6. 发现的问题

| # | 严重度 | 问题 | 位置 | 修复要求 |
|---|---|---|---|---|
| F1 | 🟡 MED | **导入列名 SQL 注入**：`data.ts:86-89` `const cols = Object.keys(row)` 后 `INSERT INTO ${table} (${cols.join(', ')}) ...` —— 列名取自导入 JSON 的 key，**未清洗**。表名虽白名单（✓），但列名直接拼入 SQL：伪造备份文件（形状合法、列名含 `a) VALUES(1); DROP TABLE todos;--`）可注入任意 SQL（DROP 等），事务不防 DROP。违反规格 §5.2「白名单防注入」（仅落实表名，漏列名） | `services/data.ts` | 导入前对每行列名清洗：仅允许 `/^[a-zA-Z0-9_]+$/`，含其他字符则整行拒绝（或整次导入拒绝+回滚）。或维护每表允许列白名单比对。验收：构造列名含特殊字符的 JSON → 导入被拒、DB 不变 |
| F2 | 🟡 MED | **热点源「立即刷新」按钮缺失**：规格 §5.3/§5.4/验收#5 明确要求热点源编辑器配「立即刷新热点」按钮调 `news:refresh`，设置页热点卡无此入口 | `components/settings/HotspotSourcesEditor.vue` 或 `SettingsView.vue` | 热点卡内增「立即刷新」按钮 → 调 `api.news.refresh()`（阶段2 通道）→ 完成后非阻塞提示。验收：设置页改源后点刷新→今日总览热点更新 |
| F3 | 🔵 LOW | `data.ts exportAll` 首行 `const win = await import('electron')...` 赋值后从未使用（死代码）；且 electron 已在顶部 `import { dialog }`，动态 import 多余 | `services/data.ts` | 删除该行 |

## 7. 最终结论

- **结论**：🟡 **需修复**
- **依据**：F1 导入注入（安全/数据完整性，规格明确要求防注入）+ F2 刷新按钮（验收#5 未达成）。未达 🔴：无硬编码密钥、apiKey 不入日志、无云端上传、无模态；加密链路/测试连接/导出/onboarded 均正确。
- **下一步**：交 DeepSeek 用 `用户prompt/04-deepseek修复.md` 修 F1（列名清洗，安全优先）+ F2（刷新按钮）+ F3（删死代码）。修完用 `05-glm复审.md` 回复审。**复审通过前不进阶段 5。**
- **肯定项**（保留）：apiKey 密文往返链路正确、测试连接四态+10s 超时、logger 不泄露 key、导入事务+内联确认、onboarded 完整化、零硬编码 hex、不配 LLM 亦可跑——设置地基质量良好，问题集中在导入注入防护与一个漏按钮。

---
### 复审（2026-08-05 · GLM）

| # | 核对方式 | 结论 |
|---|---|---|
| F1 | grep `data.ts:88` `/^[a-zA-Z][a-zA-Z0-9_]*$/` 校验存在；非法列名 throw → 事务回滚 | ✅ |
| F2 | grep `SettingsView:8` destructure refreshNews；`:84` `<button class="btn-refresh" @click="refreshNews">立即刷新` | ✅ |
| F3 | grep `win = await`/`import('electron')` → 零匹配（已删） | ✅ |

**新引入问题**：无。**越界改动**：无。**构建**：`npm run build` 7.92s 绿，SettingsView 16.81KB。

**最终结论**：✅ **通过**（阶段 4 关闭）。
**下一步**：可启动阶段 5（对话式热点搜索 LLM），用 `用户prompt/01-下一阶段启动.md`。
**运行时未验项（累积，建议阶段6打包前 GUI 实跑）**：测试连接三态、导出/导入 round-trip、热点刷新按钮、onboarded 生效——headless 无法起 Electron，链路均代码核查通过。
- 复审日期：—　复审人：GLM
- 逐条核对：待
- 复审结论：—
