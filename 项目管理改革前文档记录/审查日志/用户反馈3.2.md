# 审查日志 · 用户反馈3.2（F3.2-1 方案B + F3.2-2 Tier2 出处标注）

> 对应：`用户最终测试反馈/用户反馈3.2.md` §3 定稿 · `项目日志/用户反馈3.2-修复1.md`（DeepSeek 04）· `当前状态.md` §17 + §2.5追加4 + §5 row28

## 复审（GLM 05，2026-08-09）
- **复审人**：GLM　**方式**：逐行读码 + 全链路 tracing + 独立复跑 `npm test`/`npm run build`/`tsc.node` + 越界/边界 grep（**GLM 未改任何代码**）

### 逐条核对（§3 定稿要求）
| 轨 | 要求 | 实际改动 | 结论 |
|---|---|---|---|
| F3.2-1 B | orphan 分流：status='todo'→软删、'done'→留 | `planRepo.ts:201-221` orphanTodoIds 遍历 → findById → todo 软删(removedOpenCount)/done 留(keptDoneCount+keptIds)；`setGeneratedTodoIds([...resultIds,...keptIds])`（done orphan 留复用池→加回保留完成态） | ✅ 已解决 |
| F3.2-1 B | SyncResult + 计数透传 + 文案 | SyncResult 加 removedOpenCount/keptDoneCount；runSync 透传；PlanningView 文案「N 未完成移除；M 已完成保留」 | ✅ 已解决 |
| F3.2-1 B | 4 例回归 | `tests/planSync.spec.ts` ①删open→软删 ②删done→留 ③删done加回→复用完成态 ④删open加回→新建；含 sourcePlanId 断言 | ✅ 已解决 |
| F3.2-2 | migration 0004 sourcePlanId | `0004_todo_source_plan.ts` addColumnIfMissing 幂等；migrate.ts 注册 v4（事务包裹） | ✅ 已解决 |
| F3.2-2 | Todo 类型 + repo + syncPlanTodos 写入 | types.ts Todo+sourcePlanId + TodoWithSource；todoRepo create/update；syncPlanTodos 新建写 sourcePlanId=plan.id | ✅ 已解决 |
| F3.2-2 | labelTodoSource 5 规则纯函数 | `todoSource.ts` 5 规则优先级（📌/项目/日规划/顺延/手动）+ 项目/计划删除降级；9 例单测 | ✅ 已解决 |
| F3.2-2 | todos:withSource IPC batch | `todos.ipc.ts:118-153` batch 解析 projectNames/planDates，annotate sourceLabel，避免 N 次往返 | ✅ 已解决 |
| F3.2-2 | TodayView tag UI | L100-101/L158-159 `t.sourceLabel` tag（替代 projectId UUID 直显 bug）；`.tag-work` token 无 hex | ✅ 已解决（顺手修 UUID bug） |
| 约束 | migration 规格边界记录 | `当前状态.md` L49/L720 记偏差（用户定稿） | ✅ 已解决 |

### 独立复跑（GLM 实地）
| 验收 | 命令 | 结果 |
|---|---|---|
| vitest 全绿 | `npm test` | ✅ **6 files / 67 passed**（54 存量 + planSync 4 + todoSource 9；含 node:sqlite 实验性 warning，非错） |
| build 绿 | `npm run build` | ✅ 14.68s |
| **tsc.node 严格类型** | `npx tsc -p tsconfig.node.json --noEmit` | ❌ **3 错：2 新引入 + 1 存量**（见下） |

### ❌ 新引入问题（阻断）
**F3.2-2 加 Todo.sourcePlanId 后，2 个既有 `todoRepo.create` 调用点未同步传 sourcePlanId → TS2345：**
1. `src/main/ipc/projects.ipc.ts:91`（TODOS_CREATE，项目任务）— create({...}) 缺 `sourcePlanId`
2. `src/main/services/reminders.ts:18`（ensureTodo，周期提醒）— create({...}) 缺 `sourcePlanId`

- **运行时无害**：todoRepo.create 内部 `sourcePlanId: data.sourcePlanId ?? null`（L77）兜底，项目/提醒 todo sourcePlanId=null（符合「非日规划生成留 null」设计）。
- **类型安全违规**：Todo 类型 sourcePlanId 为必填，Omit<Todo,'createdAt'|'updatedAt'> 要求调用点显式传；tsc.node 严格检查暴露。
- **DS 声明不准**：日志称「零 TS 错误」基于 `npm run build`（electron-vite 不全量类型检查 main），实际 tsc.node 严格检查暴露 2 新错。
- **修复**：两处各补一行 `sourcePlanId: null,`（极简，2 行）。
- 存量基线 `news.ts:22`（非本轮）接受；**本轮新引入 2 错不接受**（与前几轮「新代码 0 错」标准一致）。

### 越界改动（核实可接受）
| 改动 | 性质 | 裁定 |
|---|---|---|
| `period.spec.ts:92` 断言改本地日期 | 既存 flaky 修正（UTC vs 本地跨午夜，凌晨复现）；只改断言对齐实现 todayStr()，不改实现 | ✅ 可接受（最小 flaky 修正，避免回归误报） |
| TodayView projectId UUID → sourceLabel | F3.2-2 出处标注替代 UUID 直显（顺手修丑陋 UUID 显示 bug） | ✅ 范围内 |
| planSync.spec node:sqlite mock | better-sqlite3 ABI(Electron130) vs node(127) 不兼容；vi.mock connection 跑真实 repo SQL(in-memory)，最小 mock 面；0004 存量库实跑双保险 | ✅ 可接受（测试严谨，DS 已记） |

### 已知限制（非阻断，DS 已记）
- **keptDoneCount 每轮重复提示**：done orphan 留 generatedTodoIds → 每次保存重算计入 → 反复提示「M 已完成保留」。方案B 语义副作用，信息性非阻断。LOW（未来可优化：一次性标记或仅新 orphan 提示）。
- **labelTodoSource 项目删除降级**：项目 todo 的项目被删 → 降级「手动·{date}」。语义略怪但可接受（项目已删无法标名）。LOW。

### 复审结论
**🟡 仍需修复（回 DeepSeek 04）。**
- F3.2-1 方案B + F3.2-2 Tier2 逻辑/数据/UI/测试/偏差记录**全部正确落地**，vitest 67/67 + build 绿 + 0004 存量库实跑通过——质量高。
- **唯一阻断**：2 处 TS 类型遗漏（projects.ipc:91 + reminders.ts:18 缺 sourcePlanId）= F3.2-2 类型变更未全同步调用点。运行时无害但类型安全违规 + 本轮回归。

### 剩余项（回 04）
1. `projects.ipc.ts:91` TODOS_CREATE create 调用补 `sourcePlanId: null`
2. `reminders.ts:18` ensureTodo create 调用补 `sourcePlanId: null`
3. 修后 `npx tsc -p tsconfig.node.json --noEmit` 应仅剩 1 存量基线（news.ts:22）；`npm test`+`build` 复跑绿。
4. （非阻断建议）keptDoneCount 重复提示未来优化。

> 修完这 2 行后，F3.2 可 ✅ 通过闭环。逻辑层无需再动。

---

## 复审 2（修复 2 后，2026-08-09）
- **复审对象**：DeepSeek 04 对「复审 🟡 阻断项（2 处 TS 类型遗漏）」的修复。修复说明 `项目日志/用户反馈3.2-修复2.md`。
- **方式**：逐行读码 + 独立复跑 `tsc.node`/`npm test`/`npm run build`（**GLM 未改代码**）

### 逐条核对（复审 🟡 剩余项 + DS 多修项）
| # | 要求 | 实际改动 | 结论 |
|---|---|---|---|
| 1 | projects.ipc:91 TODOS_CREATE 补 sourcePlanId | `projects.ipc.ts:102` `sourcePlanId: null`（项目任务） | ✅ 已解决 |
| 2 | reminders.ts:18 ensureTodo 补 sourcePlanId | `reminders.ts:24` `sourcePlanId: null`（周期提醒） | ✅ 已解决 |
| 3（DS 补，GLM 上轮漏检） | todoRepo quickCreate 同源 create 缺 sourcePlanId | `todoRepo.ts:126` `sourcePlanId: null`（手动 quickCreate） | ✅ 已解决（同根因第 3 处，DS 主动补，合理） |
| 4（DS 补，存量基线） | projects.ipc:38 status `string→ProjectStatus` 窄化（达成验收「tsc 仅剩 news.ts:22」） | `projects.ipc.ts:9-12` `normalizeStatus` helper（合法值原样、非法降级 active）+ L42 调用 | ✅ 已解决（行为：合法值零变化，非法值降级 active 更防御；ProjectStatus 4 值 active/paused/done/dropped 全覆盖，无漏值误降级） |

### 独立复跑（GLM 实地）
| 验收 | 命令 | 结果 |
|---|---|---|
| **tsc.node 严格类型** | `npx tsc -p tsconfig.node.json --noEmit` | ✅ **仅剩 news.ts:22 存量基线**（验收标准达成；projects.ipc:38 + 3 处 sourcePlanId 全清，零新错） |
| vitest | `npm test` | ✅ 6 files / **67 passed** |
| build | `npm run build` | ✅ 2.37s |

### 越界/新引入问题
- **#3 quickCreate**：GLM 上轮只查外部 IPC 调用点，漏 todoRepo 内部 self-call；DS 同模式补 sourcePlanId:null —— 同根因，合理必要。
- **#4 normalizeStatus**：存量基线修复（非本轮引入），但 GLM 验收标准「tsc 仅剩 news.ts:22」隐含需清；helper 行为合法值零变化、非法降级（更防御）。ProjectStatus 4 值全覆盖核实无误降级。可接受。
- **无新引入问题**。无不当越界。

### 复审 2 结论
**✅ 通过（用户反馈3.2 闭环）。**
- 复审 🟡 阻断项（2 处 TS 类型遗漏）+ DS 主动补的同源第 3 处 + 存量 status 窄化 —— 全部正确修复。
- tsc.node 仅剩 news.ts:22 存量基线（验收标准达成）；vitest 67/67 + build 绿；无新引入问题。
- **F3.2-1 方案B + F3.2-2 Tier2 全部交付**，可交用户实测。

### 用户实测要点
1. **F3.2-1 删除传播**：日规划删 open 任务→保存→今日总览该待办消失；删 done→留（历史）；删 done 后加回→完成态保留。
2. **F3.2-2 出处标注**：今日总览每条 todo 末尾 tag（`日规划·{天}`/`项目·{名}`/`手动·{天}`/`顺延·{天}`/`周期提醒`）。
3. **存量数据**：27 条旧 todo（sourcePlanId=null）降级标 `手动·/顺延·`；9 个 orphan/手动重复显形可手清；新建日规划任务才有 `日规划·`。
4. **非阻断已知项**：keptDoneCount 每轮重复提示（方案B 语义副作用，LOW）。
