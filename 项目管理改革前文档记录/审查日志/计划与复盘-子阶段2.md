# 审查日志 · 计划与复盘 — 子阶段 2 计划核心（首次审查）

> **维护**：GLM（审查人员）　**结论三态**：✅ 通过 / 🟡 需修复 / 🔴 驳回
> **审查对象**：DeepSeek 子阶段 2 产出
> **真相源**：`计划与复盘体系实现项目计划/规格-子阶段2-计划核心.md`

---

## 审查元信息
- **功能 / 子阶段**：计划与复盘体系 · 子阶段 2 — 计划核心
- **审查日期**：2026-08-07
- **审查人**：GLM
- **审查方式**：静态逐行核对 + 独立复跑 `npm test`/`npm run build` + grep（require/hex/console/content）+ 幂等算法静态推演。**headless 无法实跑 Electron GUI / 真实 DB**，凡需运行时项均标 ⏳ 待用户 E2E（同子阶段 1 / 阶段 6 口径，不谎报运行时结果）。

## 1. 审查范围
**DeepSeek 产出文件**（4 新增 + 7 修改 = 11）：
- 新增：`planParser.ts`、`planParser.spec.ts`、`planRepo.ts`、`plans.ipc.ts`
- 修改：`shared/types.ts`(+Plan)、`shared/ipc.ts`(+PLAN_*)、`ipc/index.ts`(+registerPlansIpc)、`preload/index.ts`(+api.plans)、`PlanView.vue`(空壳→激活)、`TodayView.vue`(+今日计划链接)、`package.json`(+vitest devDep + "test" script)
- 依赖：`vitest@^4.1.10` devDep 已装

**当前状态.md 快照**：§9 由 DeepSeek 填写，**属实**（文件清单、`npm test` 7/7、build 7.28s、grep 干净均经 GLM 独立复跑确认）。运行时项（幂等 4 场景 / GUI 流）DeepSeek 已如实标「待 E2E」，未谎报。

## 2. 验收点逐项核查（对照规格 §9）

| # | 验收点 | 结论 | 证据 / 复现 |
|---|---|---|---|
| 1 | `Plan` 类型入 `@shared/types` | ✅ | `types.ts:97-108`，含 `generatedTodoIds: string[]` |
| 2 | `parseTasks` 纯函数（`- [ ]` 提取 / `- [x]` 不提取 / 空→[] / 缩进容错） | ✅ | `planParser.ts` 静态正确；TASK_RE `/^(\s*)[-*]\s+\[\s\]\s+(.+?)\s*$/` |
| 3 | **vitest 单测全绿**（≥6 用例） | ✅ | **GLM 独立复跑 `npm test`：7/7 全过（272ms）**，覆盖标准/空/无任务/`- [x]`/缩进/重复行/去空白 7 用例 |
| 4 | `planRepo` CRUD + JSON 往返 + softDelete + 默认过滤软删 | ✅ 静态 / ⏳ E2E | `rowToPlan`↔`JSON.stringify` 往返一致；`findAll`/`findById`/`findByDate` 均 `isDeleted=0` 过滤；softDelete 置位。DB 实跑待 E2E |
| 5 | **幂等同步**：首次建 todos；同内容再存不重复建；改内容增量建/复用 | ✅ 静态推演 / ⏳ E2E | `syncPlanTodos` 按 content 建 `Map<content,Todo[]>` 复用池，shift 取用（每个 todo 复用一次）；新建走 `todoRepo.create`。静态推演 4 场景（首次/同内容/完成态保留/残留）均正确，DB 实跑待 E2E |
| 6 | **完成态保留**：勾完成后再存（行仍 `- [ ]`）→ 该 todo 保持 done+completedAt | ✅ 静态 / ⏳ E2E | 复用路径只取 `id`，**完全不动 todo**（不传 status）→ done 项保持 done。待 E2E |
| 7 | **残留不误删**：删某 `- [ ]` 行后保存 → todo 不被软删、仍可查、UI 提示 | ✅ 静态 / ⏳ E2E | orphan 只 `oldIds.filter` 收集 id，**无 softDelete 调用**；待 E2E。**F4（🔵LOW）**：orphan 计数含已软删项，提示措辞见 §6 |
| 8 | 生成 todos 在 `plan.date=今天` 时流入今日待办卡 | ✅ 静态 / ⏳ E2E | 新建 `planDate=plan.date`；既有 `todoRepo.findByPlanDate(today)` 聚合无需改；待 E2E |
| 9 | 6 个 `plan:*` IPC + ok/err + **logger 不含 plan.content 明文** | ✅ | 6 handler 齐全；**grep 确认新 main 文件 logger 仅记异常对象 `logger.error('plan:xxx error', e)`，无 content 明文**（planRepo/plans.ipc 中 `plan.content` 仅出现在注释与 `parseTasks()` 解析调用，非日志输出） |
| 10 | 解析异常 → content 保原文 / synced:false / PARSE_FAILED / UI 降级 / 不崩 | ✅ 静态 / ⏳ E2E | IPC 先 `create/update`（存 content）→ 再 `runSync`→`syncPlanTodos`（try/catch 返回 synced:false）→ `err('PARSE_FAILED')` → PlanView 回拉 byDate 恢复 id + 显示「⚠️已保存但解析失败」。链路完整；待 E2E |
| 11 | PlanView 选日期/加载/保存分支/内联反馈/禁模态 | ✅ 静态 / ⏳ E2E | `onDateChange`→`loadForDate`；`currentPlanId` 决定 create↔update；feedback 四态（success/warn/info/error）内联 div，**无 alert/confirm/modal**；`saving` 防双击。待 E2E |
| 12 | TodayView「📝 今日计划」链接 → /plan，token 色 | ✅ 静态 / ⏳ E2E | `TodayView.vue` `@click="router.push('/plan')"`；`.plan-link` 全 `var(--accent*)`，无硬编码 hex |
| 13 | 无新迁移 / 无硬编码 hex / 静态 import / 软删 | ✅ | **无新迁移**（0003 子阶段1 已补齐）；grep `#[0-9a-fA-F]{3,8}` 于 PlanView → 无匹配；新 main 文件无 `require(`（todoRepo:86 既有 require 系规格明确越界不动）；删除均 softDelete |
| 14 | `npm run build` 绿，零 TS 错误 | ✅ | **GLM 独立复跑：2.31s 绿**（PlanView/TodayView/MarkdownEditor 正确打包） |
| 15 | 当前状态 §9 + 项目日志 | ✅ | §9 已填（概要/清单/自测/已知项/阻塞）；`项目日志/计划与复盘-子阶段2-计划核心.md` 已写 |

## 3. 禁止边界核查（项目计划 §7-B / 规格 §10）
- [x] 无硬编码密钥（本期无密钥）
- [x] **日志无 plan.content 明文 / 无 API Key**（grep 确认：logger 仅记异常 `e`，不含 content；`redact` 兜底）
- [x] 无业务数据上传云端（纯本地 DB）
- [x] 删除均为软删除（plan.softDelete；PLAN_DELETE **不**软删其生成 todos——符合规格防误删）
- [x] 无阻断式模态弹窗（PlanView feedback 内联）
- [x] **静态 import，无新 require**（planRepo/plans.ipc 均 `import { randomUUID } from 'node:crypto'`；grep 新文件无 require；承袭用户反馈1 + 子阶段1 教训 ✓）
- [x] token.css 唯一色源、无硬编码 hex（grep 干净）
- [x] 图标用 lucide-vue-next（**本子阶段 PlanView/TodayView 未引入新图标**——子阶段1 F1 教训已内化，无重现风险 ✓）

## 4. 错误降级核查（项目计划 §7-C / 规格 §10）
| 场景 | 期望行为 | 实测结论 |
|---|---|---|
| 解析/同步异常 | 先保 content 原文 → 跳过同步 → synced:false → UI「⚠️已保存，N 条解析失败，已保留原文」→ 不崩 | ✅ 静态链路完整（IPC create/update 先于 sync；syncPlanTodos try/catch；runSync→PARSE_FAILED；PlanView applySaveResult 回拉+降级文案）。运行时构造异常待 E2E |
| DB 锁定/损坏 | 全局 safe mode（只读+Toast） | ✅ 沿用既有 connection.ts 机制（本子阶段未改） |
| 残留 todo 失配 | 不自动删，仅提示，用户在项目管理清理 | ✅ 静态（orphan 仅收集 id）；提示精度见 F4 |

## 5. 视觉核查（涉及 UI）
- [x] 还原阶段 0 视觉（token 使用、无硬编码 hex）—— grep PlanView 干净；feedback 四态色用 `var(--accent-soft)`/`var(--warn)`/`var(--danger, var(--warn))` + fallback
- [⏳] 窗口自适应断点 / 四态 —— 待 E2E（静态：PlanView `.field select`/`.date-input`/`.btn-save` 有 `:focus`/`:disabled`；btn-save 缺 `:hover/:focus-visible` —— 🔵 非阻塞，与子阶段1 同款）
- [x] 手帐风（TodayView `.plan-link` token 色、📝 图标文字，与既有卡片风格一致）

## 6. 发现的问题

| # | 严重度 | 问题 | 位置 | 修复要求 |
|---|---|---|---|---|
| F1 | 🔵 LOW | **orphanTodoIds 计数含已软删 todo**：用户在待办卡软删了某计划生成的 todo 后再保存计划，该 id 仍在 `oldIds` → 被 `oldIds.filter(不在 resultIds)` 计入 orphanCount → UI 提示「N 条任务已移出计划，**对应待办仍保留**」对该已软删项不准（它实际已被删）。字面符合规格 §5.3 步骤7「残留=旧 generatedTodoIds 中不在 resultIds 的」，但提示措辞精度有瑕疵。非数据错误（不软删、不误判），场景较边缘（须先软删计划生成项再不改计划保存） | `planRepo.ts:183` `const orphanTodoIds = oldIds.filter(x => !resultIds.includes(x))` | 二选一：① repo 层过滤已软删——`orphanTodoIds` 仅含 `todoRepo.findById(x)?.isDeleted===false` 的；② 或前端文案放宽（「N 条任务已移出计划」去掉「仍保留」绝对化措辞）。建议 ①（更准）。**非阻断** |

## 7. 最终结论
- **结论**：✅ **通过**（子阶段 2 计划核心关闭前置；1 LOW 非阻塞）
- **依据**：13 项验收点静态全部正确，`npm test` 7/7 与 `npm run build` 2.31s 经 GLM 独立复跑确认绿；**禁止边界 §3 全过**（无 require/hex/content 明文/模态/硬编码，静态 import，软删）；错误降级链路静态完整；幂等算法逐行静态推演无误（复用保留完成态 / 残留不删 / 新建双入口）。无 HIGH / MED 问题。唯一 F4（LOW）是 orphan 计数对已软删项的提示精度，字面符合规格且罕见，不阻断关闭。
- **未阻断的肯定**：幂等同步设计扎实（复用池 Map + shift、复用路径零侵入保留完成态）、parser 纯函数 + vitest 引入填补项目测试空白、IPC 保存顺序（先存 content 再 sync）确保崩溃不丢原文、**子阶段1 F1 图标教训已内化（本子阶段零新图标）**——质量高。
- **⏳ 运行时项（待用户 E2E，非本轮阻塞）**：幂等 4 场景真实 DB 实跑（首次建/同内容不重建/完成态保留/残留不删）、0003 迁移在真实 0.1.0 存量库执行、PlanView 日期切换/保存反馈/TodayView「今日计划」链接交互、生成的 todos 流入今日待办卡、PARSE_FAILED 降级实跑。这些随子阶段 1/2 的用户实跑一并在干净 Win11 验。
- **下一步**：✅ 通过 → 可启动**子阶段 3 — 复盘核心**（reviewRepo + CRUD + 昨日复盘卡衔接），用 **01 prompt**（`{{unit}}=子阶段3`、`{{unit名}}=复盘核心`、`{{计划目录}}=计划与复盘体系实现项目计划`）。**F4（LOW）建议**：DeepSeek 可在子阶段 3 实现时顺手修 F4（repo 层 filter 已软删 orphan），或留作维护期 CR——不强制本轮修。
- **给 DeepSeek 的话**：实现整体扎实，幂等算法静态推演零瑕疵，parser 单测到位，子阶段1 图标教训已内化（本子阶段未引入新图标，规避了 F1 重现）。F4 是可选精度优化——orphan 提示对「用户已软删的计划生成项」计数偏大，建议 repo 层 `orphanTodoIds` 排除 `isDeleted` 项（一行 filter），让「对应待办仍保留」措辞名副其实。非阻断，可择机修。
