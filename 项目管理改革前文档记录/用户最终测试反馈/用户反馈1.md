# 用户反馈 1 — 安装后双击图标无法启动

## 反馈元信息
- **反馈编号**：用户反馈1
- **录入日期**：2026-08-06
- **报告人**：江泽锋（用户）
- **对应测试阶段**：B2（首次启动）/ 也阻断 A 系列
- **严重度**：🔴 **阻断**（应用完全无法启动，后续所有测试无法进行）
- **状态**：♻️ 复测中（DeepSeek 主修复+加固已完成，待 GLM 复审 05 + 用户复测；复跑受阻于 Electron/Windows bug #49034）

## 1. 用户报告（原文）
- **操作**：安装到 `E:\workspace test\WorkBuddy`，双击桌面图标（或 exe）
- **预期**：WorkBuddy 窗口打开，显示今日总览
- **实际**：**无任何反应**，无窗口、无错误弹窗、无提示
- **错误信息**：无
- **截图**：（待补）

## 2. 环境
- **OS**：Windows 11
- **安装目录**：`E:\workspace test\WorkBuddy`（**注意：路径含空格 "workspace test"**）
- **测试方式**：NSIS 安装版（WorkBuddy-Setup-0.1.0.exe 装的）
- **是否先经 A1 免安装测试**：未知（关键确认项，见 §4）
- **后台进程**：未确认（关键确认项）
- **网络**：未确认

## 3. 复现步骤
1. 通过 `WorkBuddy-Setup-0.1.0.exe` 安装到 `E:\workspace test\WorkBuddy`。
2. 双击桌面「WorkBuddy」快捷方式（或安装目录内 `WorkBuddy.exe`）。
3. 无任何反应。

## 4. GLM 诊断

### 可能根因（按概率排序）

**① 单实例锁静默退出（最可能，概率 ~60%）**
- 阶段 6 加了 `app.requestSingleInstanceLock()`：若后台已有一个 WorkBuddy 进程（之前 A1 免安装版 `win-unpacked\WorkBuddy.exe` 启动后未完全退出，或卡死），新实例拿不到锁 → 走 `if (!gotLock) { app.quit() }` 分支 → **静默退出，无窗口、无错误**。
- **这与「无任何反应、无错误」的症状完全吻合**（普通崩溃通常会有「已停止工作」对话框或事件日志）。
- 代码位置：`src/main/index.ts:11-13`。

**② better-sqlite3 native 在安装上下文加载失败（概率 ~20%）**
- 安装版与免安装版的资源路径不同，若 .node 解包在安装目录解析异常 → 启动即崩。但通常会触发 Windows 崩溃对话框或事件日志，与「完全无反应」略不符。

**③ 安装路径含空格导致资源/DB 路径解析失败（概率 ~10%）**
- `E:\workspace test\WorkBuddy` 含空格。Electron 一般能处理，但若某处路径未加引号 / `__dirname` 拼接异常，可能加载失败。可能性较低但需排除。

**④ 缺 VC++ 运行时 / better-sqlite3 依赖的系统库缺失（概率 ~10%）**

## 4. GLM 诊断（2026-08-06 二次更新——用户三项自检后）

### 已排除（据新证据）
- ❌ **单实例锁静默退出**：进程能持续运行（"electron (3)"）= 它**拿到了锁**；被拒会 `app.quit()` 不留进程。排除。
- ❌ **better-sqlite3 native 崩溃**：事件查看器**无应用错误、无故障模块**（仅 RestartManager 信息）。native 崩溃必有 .exe 错误事件。排除。
- ❌ **安装路径含空格**：A1 免安装版（无空格路径）**同样失败**。排除。

### 锁定方向：阶段 6「生产加固」回归（`app.isPackaged` 才执行的代码路径）
- 阶段 1–5 全程 dev 模式（`loadURL` 开发服务器），**打包路径（`loadFile` + CSP + 单实例 + Menu 块）从未被实跑**——这是漏网之鱼。
- 症状「进程启动+持续，但窗口永不出现」= `BrowserWindow({show:false})` 的 `ready-to-show` 事件**从未触发** → 窗口永不 show。`window.ts:21-23`。

### 可能根因（重排，按概率）

**① loadFile 路径在打包 asar 内解析失败 → did-fail-load → 无 ready-to-show（~50%）**
- `window.ts:33`：`mainWindow.loadFile(join(__dirname, '../renderer/index.html'))`。
- electron-vite 打包 main 后，`__dirname` 在 asar 内的解析可能与 dev 不同；若路径错 → 渲染进程加载失败 → `did-fail-load` → `ready-to-show` 不触发 → 窗口（show:false）永不显示。
- **完美匹配症状**：进程在跑、无窗口、无崩溃（加载失败是渲染事件非进程崩溃，不入事件查看器）。
- 代码位置：`src/main/window.ts:30-34`。

**② CSP `script-src 'self'` 阻断 file:// 渲染进程脚本（~35%）**
- 阶段 6 加的 CSP（`src/main/index.ts:33-43`）：`script-src 'self'`。
- 打包后渲染从 `file://.../index.html` 加载；**file:// 协议的 origin 是 opaque/null，CSP 的 `'self'` 不匹配 file://** → 内联/模块脚本被拦 → Vue 不挂载。
- 但此因通常致**白屏窗口**（HTML 仍渲染、ready-to-show 触发），与「完全无窗口」略不符——故排第二。

**③ 单实例锁 + 首个隐藏实例（~10%）**
- 若首次启动的实例因 ①/② 窗口没显示但进程持锁，后续每次点击都被拒/聚焦到隐藏窗 → 用户永远只看到僵尸进程。
- 这是 ①/② 的放大效应，非独立根因。

**④ 其他（electron-vite main 打包后 __dirname/externalize 副作用，~5%）**

## ✅ 确认根因（2026-08-06，cmd stderr 抓到）

**用户从 cmd 跑 `npm run dev`，输出：**
```
[INFO] WorkBuddy starting...
[INFO] Opening DB: ...\workbuddy.db
[INFO] DB connected, WAL mode
(node:6972) UnhandledPromiseRejectionWarning: Error: Cannot find module './defaultSettings'
    at Object.seedDefaults (E:\workspace\workbuddy\out\main\index.js:288:36)
    at E:\workspace\workbuddy\out\main\index.js:1272:18
```

**根因（非打包加固，是阶段 1 潜伏 bug）：**
- `src/main/db/repositories/settingsRepo.ts` 的 `seedDefaults()` 用了**动态 `require('./defaultSettings')`**。
- electron-vite 打包 main（SSR bundle）时，动态 `require()` 相对路径**不被解析**：模块内容已内联进 `index.js`，但 `require('./defaultSettings')` 调用仍在运行时找独立文件 `out/main/defaultSettings.js`（不存在）→ 抛 `Cannot find module`。
- `seedDefaults()` 在 `app.whenReady` 内、`createWindow()` **之前**抛错 → `createWindow()` 永不执行 → **窗口从未创建**。进程活（Electron 循环），但无窗口、无崩溃（unhandled rejection 仅警告）。
- **dev + 打包 + 免安装全失败**（共用同一份 `out/main/index.js`）——完美解释全部症状，此前 ①②③④ 嫌疑全部作废。

**为何潜伏到今**：阶段 1–6 所有复审 headless 未实跑（"链路均代码核查通过"），代码核查漏了动态 require。**教训：isPackaged/打包链路必须实跑，不能只查代码。**

## 5. 修复计划（交 DeepSeek，用 04 prompt）

### 主修复（🔴 阻断，必做）
**`src/main/db/repositories/settingsRepo.ts`**：
- 删除 `seedDefaults()` 内的 `const { getDefaultSettings } = require('./defaultSettings')`（动态 require）。
- 文件顶部改静态 import：`import { getDefaultSettings } from './defaultSettings'`。
- `seedDefaults()` 直接用 `getDefaultSettings()`。
- 理由：静态 import 被 electron-vite 正确打包；动态 require 相对路径在 bundle 内解析失败。

### 附带加固（🟡 建议，避免再黑盒）
1. **`src/main/index.ts`** `app.whenReady` 内的启动序列（initDb→migrate→seedDefaults→registerAllIpc→createWindow）包 try/catch：任一抛错 → `logger.error` + 仍尝试 `createWindow()`（显示错误而非静默无窗）。
2. **`src/main/window.ts`**：加 `webContents.on('did-fail-load', ...)` 日志 + 兜底 `show()`（加载失败也显示窗口暴露错误）；防 `show:false` 黑盒。
3. **排查其他动态 require**：grep `require('\.`` 确认无同类（如 data.ts 早期 F3 已删，复查）。

### 验收点（GLM 复审 05 + 用户复测）
- [ ] `npm run dev` → **窗口正常显示今日总览**（cmd 无 UnhandledPromiseRejectionWarning，无 Cannot find module）。
- [ ] `npm run pack:win` → 双击 `dist/win-unpacked/WorkBuddy.exe` → **窗口显示**（cmd 启动无报错）。
- [ ] `npm run dist:win` → 装安装包 → 双击图标 → 窗口显示。
- [ ] 首启后 `%APPDATA%/workbuddy/workbuddy.db` settings 表有默认 8 行（seedDefaults 实际执行）。
- [ ] 若窗口显示后仍有白屏/CSP 违规 → 再查此前嫌疑 ②（CSP file://），作第二轮。
- **严重度**：🔴 阻断（主修复）+ 🟡（加固）。
- **指派**：DeepSeek（04-修复），主修复一行改动 + 加固三项。

## 6. 闭环记录
| 轮次 | 动作 | 结论 | 日期 |
|---|---|---|---|
| 1 | GLM 初诊 | 🔍 待用户确认（任务管理器/A1/事件查看器） | 2026-08-06 |
| 2 | 用户三项自检 | 排除单实例/native/空格；锁定打包专属回归（误判） | 2026-08-06 |
| 3 | GLM 二次诊断 | 嫌疑 ①loadFile / ②CSP（待 cmd stderr） | 2026-08-06 |
| 4 | 用户 cmd 抓 stderr | ✅ **确认根因**：seedDefaults 动态 require → Cannot find module → createWindow 未执行 | 2026-08-06 |
| 5 | DeepSeek 修复（04） | ✅ 主修复+加固3项完成；DeepSeek 称复跑受阻 #49034（**后经 GLM 纠正：实为环境变量 ELECTRON_RUN_AS_NODE=1，非 Electron bug，用户无需重装**）。编译验证：getDefaultSettings 已正确内联 | 2026-08-07 |
| 6 | GLM 复审（05） | ✅ **通过（运行时验证）**：解开 ELECTRON_RUN_AS_NODE 实跑，主进程日志达 `WorkBuddy ready`、无 Cannot find module；全新 DB settings 表=8 行（§5 验收点4 满足）。详见 `审查日志/阶段6-复审1-用户反馈1.md` | 2026-08-07 |
| 7 | 用户复测（dev） | ✅ 用户 cmd 跑 `npm run dev` 成功开窗；已删除旧错误版本 | 2026-08-07 |
| 8 | GLM 重新打包 + 打包实测 | ✅ 无需代码工作（2.2）。GLM 跑 `npm run pack:win` 实测打包 exe（主进程到 WorkBuddy ready、无 did-fail-load、无 CSP 违规）→ `npm run dist:win` 生成新安装包 `WorkBuddy-Setup-0.1.0.exe`（08-07 00:47，asar 含修复）。原诊断候选①loadFile/②CSP 均排除 | 2026-08-07 |
| 9 | 用户安装复测 | ⏳ 待用户双击新安装包 → 安装 → 桌面图标开窗 | — |
