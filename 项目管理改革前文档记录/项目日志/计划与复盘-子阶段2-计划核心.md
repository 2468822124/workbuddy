# 项目日志 · 计划与复盘 — 子阶段 2 计划核心（首次实现）

> **维护**：DeepSeek　**用途**：本单元实现日志

---

## 日志元信息
- **阶段 / 批次**：计划与复盘体系 · 子阶段 2 — 计划核心（首次实现）
- **日期**：2026-08-07
- **实现者**：DeepSeek

## 实现概要
激活「计划」为可用功能：`Plan` 类型入 `@shared/types`；`planParser.parseTasks` 纯函数 + vitest 单测 7 例全绿（新增 vitest devDep + `"test":"vitest run"`）；`planRepo`（CRUD + softDelete + `generatedTodoIds` JSON 往返 + 幂等 `syncPlanTodos`：按 content 复用保留完成态/新建增量/残留不软删仅提示/异常保原文 synced:false）；6 个 `plan:*` IPC + preload 桥（create/update 先写 plan 再同步，失败 PARSE_FAILED）；PlanView 激活（日期选择/按日期加载/保存启用/内联反馈禁模态/防双击）；TodayView「📝 今日计划」链接 → /plan。无新迁移（0003 子阶段1 已补齐 plans 治理字段）。

## 新增 / 修改文件
| 文件 | 类型 | 说明 |
|---|---|---|
| `src/shared/types.ts` | 修改 | +Plan（含 generatedTodoIds: string[]） |
| `src/shared/ipc.ts` | 修改 | +PLAN_GET/LIST/BY_DATE/CREATE/UPDATE/DELETE |
| `src/main/services/planParser.ts` | 新增 | parseTasks 纯函数（TASK_RE 提取未勾 `- [ ]`） |
| `src/main/db/repositories/planRepo.ts` | 新增 | CRUD + softDelete + rowToPlan JSON 往返 + syncPlanTodos 幂等 |
| `src/main/ipc/plans.ipc.ts` | 新增 | 6 通道；runSync 封装（synced→ok / 失败→PARSE_FAILED） |
| `src/main/ipc/index.ts` | 修改 | +registerPlansIpc |
| `src/preload/index.ts` | 修改 | +api.plans 桥 |
| `src/renderer/src/views/PlanView.vue` | 修改 | 空壳→激活（日期/加载/保存/内联反馈） |
| `src/renderer/src/views/TodayView.vue` | 修改 | 今日待办卡 +「📝 今日计划」链接 |
| `tests/planParser.spec.ts` | 新增 | vitest 7 用例（相对路径 import ../src/main/services/planParser） |
| `package.json` | 修改 | +vitest devDep + "test":"vitest run" |

## 关键决策与规格偏差
| 偏差 | 原规格 | 实际做法 | 原因 |
|---|---|---|---|
| 无 | — | — | 严格按 `规格-子阶段2-计划核心.md` 实现 |
| syncPlanTodos 取已有 todos | 规格 §5.3 步骤 3 写 `todoRepo.findByIds(...)` | 用 `todoRepo.findById` 逐 id 查 + `isDeleted` 过滤 | todoRepo 无 findByIds 方法；不越界改既有 repo，语义一致（排除软删） |
| PARSE_FAILED 后前端恢复 | 规格 §5.5「前端据此显示降级提示」 | PlanView 回拉 `plan:byDate` 恢复 plan id + 显示降级文案 | create 失败时前端本不知 plan id；回拉保证后续保存走 update 分支 |

## 自测记录
| 验收点 | 命令/操作 | 结果 |
|---|---|---|
| parser 单测全绿 | `npm test` | ✅ 7/7（标准/空/无任务/`- [x]` 不提取/缩进/重复行/去空白），545ms |
| 构建绿零 TS 错误 | `npm run build` | ✅ 7.28s |
| 新 main 文件无 require | grep `require\(` planRepo/plans.ipc/planParser | ✅ 无匹配（静态 import randomUUID） |
| PlanView 无硬编码 hex | grep `#[0-9a-fA-F]{3,8}` | ✅ 无匹配 |
| 新 main 文件无 console.log | grep | ✅ 无匹配 |

## 已知问题 / 技术债
- 幂等 4 场景（首次建/同内容不重建/完成态保留/残留不删）与 GUI 流（PlanView 日期切换/保存反馈/TodayView 链接）⏳ 待用户 E2E——headless 无法实跑 Electron GUI + 真实 DB，不谎报运行时结果
- vitest 仅覆盖纯函数 parseTasks；repo/IPC/UI 层测试待后续扩展

## 下一步
- 可交 GLM 用 03 prompt 审查（`{{unit}}=子阶段2`、`{{计划目录}}=计划与复盘体系实现项目计划`）；parser 单测可 headless 复跑 `npm test`；幂等/UI 流随用户 E2E
- 后续：子阶段 3（复盘核心）、子阶段 4（LLM 增强）
