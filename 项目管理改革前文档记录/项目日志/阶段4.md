# 阶段 4 实现日志 — 设置与数据管理

## 日志元信息
- **阶段 / 批次**：阶段 4 — 首次实现
- **日期**：2026-08-05
- **实现者**：DeepSeek

## 实现概要
实现设置页(SettingsView 4分节): 通用(称呼/晨间时段)、AI配置(Base URL/Model/Key+测试连接10s超时)、热点源CRUD、JSON全量导出/导入(事务还原)。首启onboarding完整化(保存配置写onboarded=true)。首个触及LLM的阶段(仅用户主动测试)。

## 新增 / 修改文件
| 文件 | 类型 | 说明 |
|---|---|---|
| `src/main/services/llm.ts` | 新增 | testConnection(fetch+AbortController 10s, redact apiKey) |
| `src/main/services/data.ts` | 新增 | exportAll(白名单10表+dialog.save), importAll(校验+事务DELETE/INSERT, 白名单防注入) |
| `src/main/ipc/llm.ipc.ts` | 新增 | llm:test handler |
| `src/main/ipc/data.ipc.ts` | 新增 | data:export, data:import handlers |
| `src/renderer/src/composables/useSettings.ts` | 新增 | load/save/testLlm/export/import + auto onboarded |
| `src/renderer/src/components/settings/HotspotSourcesEditor.vue` | 新增 | newsProviders JSON 数组增删改 |
| `src/renderer/src/views/SettingsView.vue` | 重写 | 4分节设置页(BaseCard×4) |
| `src/shared/types.ts` | 修改 | +LlmTestResult, DataImportResult |
| `src/shared/ipc.ts` | 修改 | +3 IPC channels |
| `src/main/ipc/index.ts` | 修改 | +registerLlmIpc, registerDataIpc |
| `src/preload/index.ts` | 修改 | +llm.test, data.export/import |

## 关键决策与规格偏差
| 偏差 | 原规格 | 实际做法 | 原因 |
|---|---|---|---|
| 导入后刷新 | 导入后刷新 | 页面 hash 跳转 `/today` 重新挂载 | 适合 SPA；无 CLI 重建 |
| newsProvider id | randomUUID | `Date.now().toString(36)` + 手动输入即可唯一 | 简单够用；无冲突 |
| import dialog filter | json 单类型 | 仅 JSON | 符合规格 |

## 自测记录
| 验收点 | 命令/操作 | 结果 |
|---|---|---|
| build 通过 | `npm run build` | ✅ 2.08s, SettingsView 16.21KB |
| 模板语法合法 | fix if 语句 → &&, TS type 移除 | ✅ |
| 无硬编码 hex | grep .vue | 待GLM验证 |
| apiKey 不记日志 | 代码核查(不传入 logger) | ✅ |
| 导入内联确认(非alert) | 代码核查 | ✅ inline confirm bar + importMsg |
| 导入事务回滚 | 代码核查 db.transaction | ✅ |

## 已知问题 / 技术债
- LLM 测试连接需 GUI + 真实网络验证。
- 导出文件路径由用户选——OS dialog 保证不覆盖。
- SettingsView 中 useSettings.save API Key 在 blur 时存入；需确保 settings:set 正确加密(依赖已有 isEncrypted 标记——settingsRepo.set 按 existing.isEncrypted 判断，首次 seed 的 llmApiKey 已标 isEncrypted=true 继)。

## 下一步
✅ 可交 GLM 审查。启动: `cd E:\workspace\workbuddy && npm run dev`。
