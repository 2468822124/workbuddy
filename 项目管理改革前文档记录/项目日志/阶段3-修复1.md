# 阶段 3 修复日志 — 审查问题 F1/F2

## 日志元信息
- **阶段 / 批次**：阶段 3 — 修复批次 1（GLM 审查反馈 F1/F2）
- **日期**：2026-08-05
- **实现者**：DeepSeek (deepseek-v4-pro)

## 实现概要
F1: 详情页任务勾选持久化。F2: projects.ipc.ts 类型清理。

## 修复明细

### F1 [MED] — 详情页任务勾选不持久化
- **文件**：`src/renderer/src/composables/useProjectDetail.ts`, `src/renderer/src/views/ProjectDetailView.vue`
- **改法**：
  - useProjectDetail 新增 `toggleTodo(id)` → 调 `api.todos.toggle(id)` → 成功后将返回的新 Todo 就地替换数组项（`todos.value[i] = r.data`，不可变）
  - ProjectDetailView 模板 `@toggle` 从本地 mutation 闭包改为 `@toggle="toggleTodo"`
  - destructure 新增 `toggleTodo`
- **验证**：build ✅；下一步 GUI 实跑验证（勾选→刷新→仍完成）。

### F2 [LOW] — as any + inline require
- **文件**：`src/main/ipc/projects.ipc.ts`
- **改法**：
  - 顶部 `import { randomUUID } from 'crypto'` + `import { Project, Todo } from '@shared/types'`
  - 移除 2 处 `const { randomUUID } = require('crypto')`
  - `as any` → `as Partial<Project>` / `as Partial<Todo>`
- **验证**：build ✅

## 自测记录
| 验点 | 命令/操作 | 结果 |
|---|---|---|
| build 通过 | `npm run build` | ✅ 2.12s, DetailView 15.36KB |
| toggleTodo 链路 | grep 'api.todos.toggle' in useProjectDetail | ✅ 调 IPC |
| as any 消除 | grep 'as any' projects.ipc.ts | ✅ 零匹配 |
| require crypto 消除 | grep 'require' projects.ipc.ts | ✅ 零匹配 |

## 已知问题 / 技术债
无新增。

## 下一步
✅ 可交 GLM 复审。
