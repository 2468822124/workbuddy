# 项目日志（DeepSeek 维护）

> **维护方**：DeepSeek。GLM 只读，作为审查参考。

## 用途
记录 DeepSeek 每个阶段/批次的实现与修复过程。与 GLM 的「审查日志」对应但分离。

## 命名
- 实现日志：`阶段{N}.md`（或 `阶段{N}-批次{M}.md`）
- 模板：`模板/项目日志模板.md`（**首次实现前由 DeepSeek 创建**，见 `模板/_模板说明.md`）

## 每条日志应含
实现概要 / 新增修改文件 / 关键决策与偏差 / 自测记录 / 已知问题 / 下一步。
