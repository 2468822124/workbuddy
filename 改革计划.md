# 文档结构改革方案（执行手册）

> **本文档是 WorkBuddy 项目文档结构改革的执行蓝本，供执行 LLM 按阶段操作。**
> **制定**：2026-08-12（评审模型）　**审批**：用户已批准　**执行**：后续 LLM（GLM4 系）
> **前身**：本文件替代此前的"文档结构整合实施计划（原版）"。原版是文件夹搬运方案，经评审升级为改革方案（补闭环门禁 + 内容整合机制 + 边界修正）。

---

## 执行铁律（执行 LLM 必须遵守，不可违反）

1. **记录性文件一字不改**：规格（`规格-阶段N-*.md`）、审查日志、项目日志、用户反馈原文——只读、只粘贴，绝不编辑。
2. **活工具不归档**：`用户prompt/`、`变更记录/CHANGELOG.md`、`当前状态.md` 必须留在活跃区。
3. **历史原样粘贴**：整合时旧文档正文 100% 不动，GLM 只加结构元信息（环节标题、来源文件、日期、门禁徽标）；**后续新增内容**才按模板续写。
4. **每阶段开始前告知用户**；不可逆操作（移动/覆盖）前再次确认。
5. **路径不靠 sed 全局替换**：记录性文件保持原引用，靠 `文档索引.md` 新旧路径映射表兜底。

---

## Context（为什么改革）

用户痛点有二：①文件夹冗余，一次任务散落多处；②**一个阶段是否真正闭环是未知状态**。
原版计划做对了"5 大类分类 + 全流程单文档"的方向，但评审发现 6 处问题，经与用户多轮确认后形成本方案：

| # | 原版做法 | 升级为 | 依据 |
|---|---|---|---|
| **C1** | 把 `用户prompt/`、`CHANGELOG` 一并归档 | 活工具保留活跃；**prompt 还需适配新结构**（独立阶段） | 用户确认 |
| **C2** | 12 份全流程文档"怎么生成"完全空白 | **GLM 编排**：历史原样粘贴 + 新增按模板续写 + 试点先行 | 用户确认 |
| **C3** | `sed` 全局替换所有 .md | 记录性文件**一字不改**；路径靠映射表兜底；仅改规范/索引/README | 用户授权边界 |
| **H1** | 模板仍叫"项目计划模板.md" | 改名"**XX部分编写模板**" + 子分类随"部分"语义 | 用户既定 |
| **H2** | "闭环"是主观段落 | **三层门禁**：开发部分=GLM 审查过即闭环；实测=用户实测无问题才闭环 | 用户确认 |
| **H3** | 实测与开发边界模糊 | **三段式分治**：开发(GLM关)→实测(用户关)→增量(反馈修复) | 用户确认 |

---

## 一、确认的改革骨架（5 条）

1. **5 大类文件夹**：模板 / 规范类 / 开发阶段 / 用户实测阶段 / 增量开发
2. **三段式分治**：开发阶段（功能构建）→ 用户实测阶段（功能验收）→ 增量开发（反馈/新需求修复）
3. **全流程单文档**：每个阶段一份文档，整合规格+日志+审查+修复+复审；**GLM 编排**（历史粘贴入位 + 来源标注，DS 仅在原文缺失时补该段）
4. **三层闭环门禁**：开发部分闭环（GLM）→ 功能闭环（进实测前提）→ 实测闭环（用户）
5. **活工具保留**：`用户prompt/`、`CHANGELOG`、`当前状态.md` 不归档

---

## 二、修正后的文件夹结构

```
E:\workspace\
├── 模板/                        # H1：改名"部分编写模板"+ 子分类
│   ├── 01_计划部分/  项目计划部分编写模板.md
│   ├── 02_日志部分/  项目日志部分编写模板.md
│   ├── 03_审查部分/  审查部分编写模板.md
│   └── 04_反馈部分/  用户实测反馈部分编写模板.md
├── 规范类/                      # 原"项目规范"重命名（仅改可改的文件）
│   ├── 文档治理总纲.md
│   ├── 变更闭环规则.md
│   ├── 命名规则.md
│   └── 阶段闭环门禁规则.md       # 【新】H2 门禁落地为规范文档
├── 开发阶段/
│   ├── v0.1-今日总览与项目管理/   # 子目录名建议去全角括号
│   └── v0.2-计划与复盘体系/
├── 用户实测阶段/
│   ├── v0.1-开发阶段实测/
│   └── v0.2-计划与复盘实测/
├── 增量开发/                     # H3：按版本/功能分子目录
│   ├── v0.1-用户反馈修复/
│   └── v0.2-参照完整性/
├── 项目管理改革前文档记录/       # 只归"真历史"，不含活工具
│   ├── 开发阶段项目计划/  计划与复盘体系实现项目计划/
│   ├── 审查日志/  项目日志/  用户最终测试反馈/
│   └── 初级文档/
│   # ⚠️ 不归档：用户prompt/、变更记录/、当前状态.md
├── 用户prompt/                  # 【保留活跃】+ 适配更新（阶段七）
├── 变更记录/CHANGELOG.md        # 【保留活跃】
├── 当前状态.md                  # 【保留】重构为 5 字段精简看板
├── 当前审核状态.md              # 【新】GLM 审核看板
└── 文档索引.md                  # 更新 + 新旧路径映射表
```

---

## 三、三层闭环门禁（裁决权分明，治"闭环未知"痛点）

| 门禁 | 条件 | 裁决权 | 状态徽标 |
|---|---|---|---|
| **① 开发部分闭环** | GLM 审查/复审通过 + 验收点全 ✅ + 无遗留 HIGH/CRITICAL（**不涉实测**） | **GLM** | 🟡实现中 → 🟡待审 → ✅开发闭环 |
| **② 功能闭环** | 该功能**所有开发部分**均已 ✅ 开发闭环 | GLM（自动） | ✅开发闭环 → 🔜待实测 |
| **③ 实测闭环** | **用户实测该功能无问题**（发现问题→转增量开发→回归，直到无问题） | **用户** | 🔜待实测 → ✅验收闭环 |

**关键**：开发阶段闭环**不需要**用户实测介入；实测闭环**只认用户**。两道闸分开，"是否闭环"从此可判定。
门禁落地为 `规范类/阶段闭环门禁规则.md`；每个全流程文档顶部固定一栏「当前门禁状态」。

---

## 四、全流程单文档生成规则（C2 落地——核心）

**核心原则（分两种来源，文档是"活的"）：**
- **历史已有内容** → 旧文档**原样粘贴**，不修改/改写/润色/仲裁；GLM 只加**结构元信息**（环节标题、来源文件、日期、门禁徽标），正文一字不动。与"禁止修改记录性文件"一致。
- **后续新增内容**（新一轮复审/实测反馈/增量修复等）→ 按 `模板/XX部分编写模板` 格式在文档内**追加续写**。

> 即：先把历史粘贴入位，之后该阶段每有新动作，就套对应模板往下续写一节，直到彻底闭环。**模板服务"新增"，历史不套模板。**

| 决策项 | 规则 |
|---|---|
| **编排者** | **GLM**（按环节顺序把各旧文档原文粘入对应章节 + 标注来源文件/日期）；DS 默认无需补，仅当某环节旧文档原文缺失时由 DS 补该段 |
| **信息源映射** | 规格←`规格-阶段N.md` · 日志←`项目日志/阶段N.md` · 审查←`审查日志/阶段N.md` · 修复←`项目日志/阶段N-修复M.md` · 复审←`审查日志/...复审.md` |
| **出入处理** | **不仲裁**。旧文档间若有出入（如规格写 A、日志实际做 B），各自原样贴在对应环节，演进关系由"环节顺序+时间"自然体现 |
| **允许的编排元信息** | 环节标题、来源文件名、日期、状态徽标 —— 这类 GLM 可加；**正文内容一字不动** |
| **记录顺序** | 文档顶部「当前状态摘要」(5 字段) + 下方环节**时间正序**（规格→日志→审查→…→闭环） |
| **实测-增量-回归循环** | 回归测试**追加回原实测批次文档**（不另开）；只有全新一轮完整实测才开新批次 |
| **试点先行** | 先做 **v0.2-子阶段1（基建）** 一份，跑通"粘贴整合→门禁→闭环"全链路，用户认可后再批量 |

> ⚠️ **工作量提示**：12 份历史阶段文档（v0.1×7 + v0.2×5）由 GLM 粘贴编排，工作量集中在"找齐+对位"，需分多次会话完成，**非原版所称 85 分钟**。

---

## 五、模板重构与改名（H1）

- **改名**为"**XX部分编写模板**" + 4 个子分类：
  - `模板/01_计划部分/项目计划部分编写模板.md`
  - `模板/02_日志部分/项目日志部分编写模板.md`
  - `模板/03_审查部分/审查部分编写模板.md`
  - `模板/04_反馈部分/用户实测反馈部分编写模板.md`
- **用途定位**：模板只服务**后续新增环节**的书写（套格式续写）；历史环节是粘贴保真，不套模板。
- 原模板（项目计划模板.md 等）内容可作为新模板的基础，但需对齐"部分编写"语义（是文档内一个环节，不是独立文档）。

---

## 六、当前状态 / 当前审核状态 两个看板

**`当前状态.md`（5 字段精简，DS 维护，每次覆盖）**
- 阶段 / 当前状态 / 最近一次 DS 行动详情 / 下一步行动 / 关键问题

**`当前审核状态.md`（新建，GLM 审核看板）**
- 当前审核队列 / 当前审核焦点 / 待复审项目 / 最近审核记录

> 约定：`当前状态.md` 只跟踪**进行中**的阶段；已闭环阶段以全流程文档顶部门禁状态为准，不再进看板，避免双写不一致。

---

## 七、执行步骤（10 阶段，顺序已修正：先归档→再重命名→内容整合）

- **阶段 0｜备份**：仅备份文档层 `.md`（**不复制 `workbuddy/` 代码库**）
- **阶段 1｜归档真历史**：移动历史文件夹到 `项目管理改革前文档记录/`（含：开发阶段项目计划、计划与复盘体系实现项目计划、审查日志、项目日志、用户最终测试反馈、初级文档；**不含** `用户prompt/`、`变更记录/`）
- **阶段 2｜规范类重命名**：`项目规范/` → `规范类/`；新建 `阶段闭环门禁规则.md`；仅更新规范/索引/README（**不 sed、不碰记录性文件**）
- **阶段 3｜模板重构**：建 4 个子分类 + 模板改名"XX部分编写模板"（**告知用户后执行**）
- **阶段 4｜建空壳结构**：开发阶段/用户实测阶段/增量开发 各版本子目录 + `_README`
- **阶段 5｜【试点】GLM 整合 v0.2-子阶段1**：跑通全流程文档 + 门禁状态栏，提交用户验收（**告知用户后执行**）
- **阶段 6｜【批量】GLM 整合其余 11 个阶段**（分多次会话）
- **阶段 7｜用户 prompt 适配更新**：00-09 中引用的路径/模板名跟进新结构（**告知用户后执行**）
- **阶段 8｜更新 `文档索引.md`**：新结构 + 新旧路径映射表
- **阶段 9｜验证**：结构/链接/门禁状态/试点文档完整性

---

## 八、验证方式（阶段 9）

1. `ls -R` 核对新结构（模板/规范类/开发阶段/用户实测阶段/增量开发）目录正确
2. 检查 `用户prompt/`、`CHANGELOG` **仍在活跃区**（未被归档）
3. 抽查 1 份记录性文件（如某 `规格-阶段N.md`）**内容未被改动**
4. 试点文档 v0.2-子阶段1：顶部门禁状态栏正确、各环节齐全、与旧文档信息一致
5. `文档索引.md` 新旧路径映射表可双向跳转

---

## 九、关键文件清单

- `E:\workspace\改革计划.md` —— 本文件（已完成定稿）
- `E:\workspace\规范类\阶段闭环门禁规则.md` —— **新建**（H2 门禁落地）
- `E:\workspace\规范类\_README.md` / `模板\_模板说明.md` / `文档索引.md` —— 更新（可改类）
- `E:\workspace\当前审核状态.md` —— **新建**（GLM 审核看板）
- `E:\workspace\当前状态.md` —— 重构为 5 字段精简看板
- `E:\workspace\用户prompt\00-09` —— 适配更新（阶段七）

---

## 十、角色分工（执行期）

- **GLM**：全流程文档**编排**（旧文档原样粘贴入位 + 来源标注）、维护门禁规则/索引/模板/审核看板、prompt 适配
- **DeepSeek**：仅当某环节旧文档原文缺失时补该段；维护 `当前状态.md`/`命名规则.md`
- **用户**：实测闭环的唯一裁决者；改革方案每阶段审批

---

## 附：环境与操作注意

- 执行环境为 Windows + Git Bash；中文路径与全角括号在 bash 下需转义，**建议优先用 PowerShell 或文件管理器操作**，降低翻车率。
- 备份不要 `cp -r` 整个 workspace（会连带 `workbuddy/` 代码库，体积大）；只备份文档层。
- 历史文档归档后仍是"整合原料源"，必须保持可读，不得删除。
