# 用户 Prompt · 08 维护期 CR — GLM+DS 执行（给 GLM+DS；文件名保留兼容）

> **用法**：阶段 6 关闭后，GPT 在 `变更开发/` 内起 CR 全流程闭环记录（写「变更请求」环节）后，把本文件复制粘贴给 **GLM+DS**。替换 `{{CR-NNN}}`、`{{简述}}`、`{{功能目录}}`。
> **与 02/04/07 的区别**：02 是构建期子阶段实现、04 修审查问题、07 修用户实测反馈；**08 是维护期按 CR 改代码**——唯一依据是 CR 全流程记录的「变更请求」环节。
> **维护期判定**：见 `当前代码状态.md`——阶段 6 标"✅ 全部关闭（含用户实跑 E2E）"才用本 prompt，否则仍用构建期 02/04/07。

> **阶段门禁卡**：开启条件 = 项目已正式进入维护期；CR 编号唯一且已批准；变更请求、验收点、允许/排除范围、原失败场景和版本影响齐全。完成状态 = 变更日志与原失败场景复跑完成，`closureStatus=in-progress`、`nextAction=09`、状态为“待 09 审查”；失败回退 = 请求不完整回 CR 发起/补充，实施失败留在 `08`，不得调用 `09` 或改走 `04/07`。

---

你是 GLM+DS 代码执行团队中的实际执行者。项目经理 GPT 已在维护期起 **CR-{{CR-NNN}}：{{简述}}**（写在全流程闭环记录的「变更请求」环节），请按其修复计划严格实现。

## CR 实施开始卡（执行前填写）

```text
CR：{{CR-NNN}}
维护期状态：{{阶段 6 用户 E2E 已关闭 / 项目已进入维护期}}
变更请求来源：{{CR 全流程文档绝对路径}}
允许范围/排除范围：{{}}
原失败场景与验收条件：{{}}
前置状态：planStatus={{ready}} · staticGate={{passed}} · closureStatus={{in-progress}}
启动时间/实际执行者：{{}} / {{GLM / DS / GLM+DS}}
联合执行分工与 `当前代码状态.md` 唯一写入者：{{单人执行写“不适用”；联合执行写文件/命令分工和 GLM 或 DS}}
```

**CR 全流程文档落点**：`变更开发/vX-{功能名}/CR-{{CR-NNN}}-{{简述}}-全流程闭环记录.md`

**权威依据（按优先级）**：
1. 该 CR 全流程记录的「变更请求」环节（**唯一修复依据**：修复计划 + 验收点 + 范围）
2. `当前代码状态.md`（实现中持续更新）
3. `规范类/命名规则.md`（命名）
4. `规范类/技术栈规范.md`（**强制遵守**：分层边界/数据层铁律/IPC 契约/新依赖评审；违反即驳回）
5. `规范类/文档治理总纲.md §2`（**双根**：文档操作一律回文档根 `E:\workspace\`，勿在 `workbuddy\` 下另建文档）
5. `项目管理改革前文档记录/开发阶段项目计划/规格-阶段0-视觉.md`（若涉及 UI）+ `项目管理改革前文档记录/初级文档/`（§9 禁止边界、§10 降级）

**硬约束（违反即驳回，不进审查细节）**：
- 禁硬编码密钥；禁日志输出业务明文 / API Key（脱敏 logger）。
- 删除一律软删除；禁业务数据上传云端；禁阻断式模态弹窗。
- `token.css` 是唯一色源，组件内禁硬编码 hex；`better-sqlite3` 配 electron-rebuild；图标用 `lucide-vue-next`。
- **只修「变更请求」环节指出的问题**，不扩大范围、不擅自重构未要求的部分；偏差在 `当前代码状态.md` 记录原因。

**完成义务（缺一不可）**：
1. 按「变更请求」环节的修复计划实现；逐项自测其验收点。
2. **复跑门（强制）**：实跑「变更请求」环节的原始失败场景，确认不再复现——**不能只查代码**（教训：动态 `require` 因 headless 未实跑潜伏 6 阶段）。
3. 在该 CR 全流程记录内**续写「变更日志」环节**（按 `模板/02_日志部分/变更日志部分编写模板.md` 格式）——写清改了哪些文件、对应修复计划哪条、复跑结果。**不再使用 `项目日志/` 文件夹。**
4. 同文档更新「闭环记录」环节（GLM+DS 行：真实执行者、结论 + 日期；门禁状态 → 待审查）。
5. 更新 `当前代码状态.md`（修复摘要、影响范围、复审所需信息）。
6. 若 CR 指定版本 bump，同步 `package.json` `version`（按 `规范类/变更闭环规则.md §5`）。

完成后在变更日志和闭环记录中写明 `planStatus`、`staticGate`、各验收点 `testPointStatus`、`closureStatus`、`updatedAt`、`updatedBy`、证据和 `nextAction=09`，再报告「CR-{{CR-NNN}} 修复完成，待 GPT 审查」，告诉我：
- 改了哪些文件（对应修复计划哪条）；
- 复跑 CR 失败场景的结果（命令 + 输出摘要）；
- GPT 该如何复审（给具体命令/操作）。

我会用 **09 prompt（GPT 审查）** 启动审查。
