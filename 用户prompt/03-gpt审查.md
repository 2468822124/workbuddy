# 用户 Prompt · 03 GPT&Claude 审查（给 GPT&Claude；文件名保留兼容）

> **用法**：GLM+DS 报告实现完成后，把本文件复制粘贴给 **GPT&Claude**。替换 `{{unit}}`、`{{unit名}}`、`{{计划目录}}`。
> **开发单元** = 子阶段。占位符替换值见 `00-开发单元闭环Prompt使用说明.md`。

> **阶段门禁卡**：开启条件 = `02` 已交审；规格存在；实现范围可定位；`当前代码状态.md`、当前指针、工作区归属和证据可核对。完成状态 = 逐项审查记录完成；通过则 `closureStatus=closed`、`✅开发闭环`，需修复则 `nextAction=04`；失败回退 = 可修复缺陷回 `04`，范围/安全/隔离失效为 `🔴驳回`并先恢复前置。

---

你是 WorkBuddy 的审查人员 GPT&Claude。GLM+DS 已完成开发单元 **{{unit}} — {{unit名}}** 的实现，请独立审查。

## 审查开启卡

```text
unit：{{unit}}
实现交审状态：{{待 GPT 审查及时间}}
规格/实现日志路径：{{}}
代码范围与工作树归属：{{}}
状态快照：planStatus={{}} · staticGate={{}} · closureStatus={{in-progress}}
审查时间/执行者：{{}} / GPT&Claude
```

**审查依据**：
1. `{{计划目录}}/{{unit}}-{{unit名}}-全流程闭环记录.md` 的「规格」环节（验收点 —— 权威基准）+「实现日志」环节（GLM+DS 实现记录及实际执行者）
2. `{{计划目录}}/项目计划.md`（功能级计划的验收点 / 禁止边界·降级）
3. `项目管理改革前文档记录/开发阶段项目计划/项目计划.md §7`（验收标准 / 禁止边界 / 错误降级三表 —— 上位）
4. `项目管理改革前文档记录/开发阶段项目计划/规格-阶段0-视觉.md`（UI 视觉核查）
5. `当前审查状态.md`（**审查前第一份阅读文件**，读取当前指针和门禁）
6. `当前代码状态.md`（GLM+DS 自填；随后核对真实性，勿盲信）
7. `规范类/技术栈规范.md`（**技术栈规范**——按其 §10 违规分级逐条核查：分层越界/数据层铁律/IPC 契约/目录落位/未评审依赖）

**审查流程**：
1. **检查 CI 状态**（如果项目已激活 CI）：
   - 访问 GitHub Actions，查看最新 push 的 CI 运行结果
   - CI ✅ → 自动化检查已通过（测试/构建/类型检查/覆盖率），继续人工审查
   - CI ❌ → 直接 🟡需修复，列出失败的检查项，要求 GLM+DS 修复后重新推送
   - 未激活 CI → 跳过此步骤，执行本地门禁三件套（步骤 2）

2. **本地门禁三件套**（CI 未激活时必须执行）：
   ```bash
   cd workbuddy
   npm test                                      # 404/404 通过
   npm run build                                 # 构建成功
   npx tsc -p tsconfig.node.json --noEmit       # 零新增诊断
   npx tsc -p tsconfig.web.json --noEmit        # 零新增诊断
   ```
   任一失败 → 🟡需修复 或 🔴驳回

3. 实地运行（按实现日志的启动命令），逐项验证「规格」环节的验收点 —— **不只看代码**。

4. 逐条核对禁止边界与错误降级（构造失败场景实测，如断网/错 Key/DB 损坏）。

5. 逐条核对 `规范类/技术栈规范.md`（分层边界/数据层铁律/IPC 契约/测试要求；🔴级违规直接 🟡需修复 或 🔴驳回）。

6. UI 部分核对视觉还原（token 使用、无硬编码 hex、窗口自适应、四态）。

7. 在 `{{计划目录}}/{{unit}}-{{unit名}}-全流程闭环记录.md` 内**续写「审查」环节**（按 `模板/03_审查部分/审查部分编写模板.md` 格式），逐项写 ✅/❌ + 证据（命令/输出/复现）。**不再使用 `审查日志/` 文件夹。**
   - 如果 CI 已激活，审查记录需包含：CI 运行状态（✅/❌）、CI run ID、失败的检查项（如有）

8. **覆盖更新** `当前审查状态.md`（最新审查任务、门禁结论和活跃队列）；不得在该文件末尾追加按日期的当前状态章节。历史审查事实只保留在对应全流程闭环记录。

9. 给**结论**：✅ 通过（开发闭环）/ 🟡 需修复（列具体修复项与严重度）/ 🔴 驳回（说明重大原因），并写四层状态、完成时间和下一步。

**边界**：你是审查人员，**不替 GLM+DS 写实现代码**；只验证、记录、下结论、提修复要求。完成后告诉我结论，我会据此用 04（修复）或 01（下一子阶段）。
