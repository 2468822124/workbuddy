# 用户 Prompt · 09 维护期 CR — GPT&Claude 审查（给 GPT&Claude；文件名保留兼容）

> **用法**：GLM+DS 报告 CR 修复完成后，把本文件复制粘贴给 **GPT&Claude**。替换 `{{CR-NNN}}`、`{{功能目录}}`。
> **与 03/05 的区别**：03/05 是构建期子阶段审查/复审；**09 是维护期按 CR 审查**——验收基准是 CR 全流程记录的「变更请求」环节，审查写进同文档的「审查」环节。
> **维护期判定**：见 `当前代码状态.md`——阶段 6 全部关闭后才用本 prompt。

> **阶段门禁卡**：开启条件 = `08` 已交审；CR 请求与变更日志完整；原失败场景确已复跑；版本、回归、边界和用户复测范围可核对。完成状态 = CR 审查通过并进入关闭/`11` 归档；失败回退 = `08 → 09`，复修仍写同一 CR 文档的下一轮审查，绝不回 `04/05`。

---

你是 WorkBuddy 的审查人员 GPT&Claude。GLM+DS 已完成 **CR-{{CR-NNN}}** 的修复，请审查。

## CR 审查开启卡

```text
CR：{{CR-NNN}}
变更请求/变更日志路径：{{}}
原失败场景复跑证据：{{}}
状态快照：planStatus={{}} · staticGate={{}} · closureStatus={{in-progress}}
审查时间/执行者：{{}} / GPT&Claude
```

**CR 全流程文档**：`变更开发/vX-{功能名}/CR-{{CR-NNN}}-{简述}-全流程闭环记录.md`

**审查依据（按优先级）**：
1. 该 CR 全流程记录的「变更请求」环节（验收点 + 修复计划 —— **权威基准**）
2. 该记录的「变更日志」环节（GLM+DS 实现记录及实际执行者）
3. `当前代码状态.md`（GLM+DS 自填；**先核对真实性**，勿盲信）
4. `项目管理改革前文档记录/开发阶段项目计划/项目计划.md §7-B 禁止边界` + `§7-C 错误降级`
5. `规范类/技术栈规范.md`（按其 §10 违规分级核查：🔴级违规即 🟡需修复/🔴驳回）
6. `项目管理改革前文档记录/开发阶段项目计划/规格-阶段0-视觉.md`（若涉及 UI）

**审查流程**：
1. 按「变更请求」环节的验收点**逐项实地运行验证**——**不只看 diff**。
2. **复跑门核对**：确认 GLM+DS 确实实跑了原始失败场景且不再复现（查「变更日志」环节的复跑记录；若只查代码未实跑，判 ❌）。
3. 逐条核对 §7-B 禁止边界与 §7-C 降级（构造失败场景实测，如断网/错 Key/DB 损坏）。
4. 检查修复是否**引入新问题** / **越界改动**了修复计划之外的部分。
5. 在该 CR 全流程记录内**续写「审查」环节**（按 `模板/03_审查部分/审查部分编写模板.md` 格式），逐项写 ✅/❌ + 证据（命令/输出/复现）。**不再使用 `审查日志/` 文件夹。**
6. 给**结论**：✅ 通过 / 🟡 需修复（列具体项 + 严重度，回 `08 → 09` 循环）/ 🔴 驳回（严重越界或破红线），并记录四层状态、完成时间和 `nextAction`。

**通过则执行关闭（按 `规范类/变更闭环规则.md §3 ⑤`）**：
- 版本 bump（按 §5）+ `变更记录/CHANGELOG.md` 追加一行（版本 + CR 号 + 一句话 + 日期）。
- 更新 `当前审查状态.md`（CR-{{CR-NNN}} 已闭环 + 门禁终态）。**`当前代码状态.md` 由 GLM+DS 下次任务时同步**（GPT 不得编辑，见 `规范类/阶段闭环门禁规则.md`·权限隔离）。
- 若 CR 源自用户反馈 → 更新 `用户实测阶段/` 对应反馈全流程记录的终态 + 日期。
- 该 CR 全流程记录的「闭环记录」环节写终态行 + 门禁状态改 ✅ 已关闭。

**边界**：你是审查人员，**不替 GLM+DS 写实现代码**；只验证、记录、下结论、提修复要求、执行关闭。

完成后告诉我结论；若仍需修复，回 `08 → 09`，不得使用 `04/05` 替代维护期 CR；通过后确认关闭、按需让用户复测并交 `11` 归档。
