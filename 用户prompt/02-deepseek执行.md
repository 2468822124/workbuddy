# 用户 Prompt · 02 GLM+DS 执行（兼容文件名）

> **用法**：GPT 在本子阶段全流程闭环记录内写完「规格」环节后，把本文件复制粘贴给 **GLM、DS 或二者联合执行会话**。替换 `{{unit}}`、`{{unit名}}`、`{{计划目录}}`。
> **文件名兼容**：文件名保留 `deepseek` 以避免既有路径断链；正文现行执行角色为 GLM+DS。
> **GLM 前置**：GLM 第一次承担代码任务前，必须先完成 `用户prompt/02A-GLM代码熟悉.md` 的一次性只读熟悉。
> **开发单元** = 子阶段。占位符替换值见 `00-开发单元闭环Prompt使用说明.md`。

> **阶段门禁卡**：开启条件 = `01` 已在同一全流程文档写入已确认规格、验收点、批准代码范围和工作区归属；完成状态 = 实现日志、自测和代码状态完整，`closureStatus=in-progress`、`nextAction=03`、代码状态为“待 GPT 审查”；失败回退 = 规格缺失回 `01`，实现偏差/失败留在 `02` 并明确补充，不得直接标记待审。`02` 不修改 `当前审查状态.md`、测试计划或历史事实。

---

你是 GLM+DS 代码执行团队中的实际执行者。项目经理 GPT 已在开发单元 **{{unit}} — {{unit名}}** 的全流程闭环记录内写好「规格」环节，请严格实现。你不能对自己的实现作 GPT 审查结论。

## 实现开始卡（执行前填写）

```text
unit：{{unit}}
规格来源：{{全流程闭环文档绝对路径}} · 规格环节/版本：{{}}
允许代码范围：{{}}
排除范围：{{}}
工作区归属：{{本次允许变更路径；无关变更不得处理}}
前置状态：planStatus={{ready}} · staticGate={{not-applicable}} · closureStatus={{in-progress}}
启动时间/实际执行者：{{}} / {{GLM / DS / GLM+DS}}
联合执行分工与 `当前代码状态.md` 唯一写入者：{{单人执行写“不适用”；联合执行写文件/命令分工和 GLM 或 DS}}
```

**权威依据（按优先级）**：
1. `{{计划目录}}/{{unit}}-{{unit名}}-全流程闭环记录.md` 的「规格」环节（实现/验收基准）
2. `{{计划目录}}/项目计划.md`（功能级计划，看本子阶段的范围/边界）
3. `项目管理改革前文档记录/开发阶段项目计划/规格-阶段0-视觉.md` + `今日总览-样张.html`（视觉，UI 必须遵守）
4. `当前代码状态.md`（GLM+DS 维护的第一份阅读文件，了解当前任务）
5. `当前审查状态.md`（GPT 维护的审查工作台，了解审查要求）
6. `规范类/命名规则.md`（命名规范 —— 若不存在，**先创建**再遵守）
7. `规范类/技术栈规范.md`（**技术栈规范——强制遵守**：分层边界/数据层铁律/IPC 契约/目录落位/测试要求/依赖评审；违反 §10 🔴级即驳回）
8. `规范类/文档治理总纲.md §2`（**双根**：文档操作一律回文档根 `E:\workspace\`，勿在 `workbuddy\` 下另建文档）
9. `项目管理改革前文档记录/初级文档/`（v0.3 原始定义；`§9 禁止边界`、`§10 错误降级` 必读）

**硬约束（违反即驳回，不进入审查细节）**：
- 禁硬编码密钥；禁日志输出业务明文 / API Key（必须用脱敏 logger）。
- 删除一律软删除（`isDeleted`，查询默认过滤）；禁业务数据上传云端。
- 禁阻断式模态弹窗；`token.css` 是唯一色源，**组件内禁硬编码 hex**。
- `better-sqlite3` 必须配 electron-rebuild；图标用 `lucide-vue-next` 组件（**勿用 `<i data-lucide>`**）。
- **遵守 `规范类/技术栈规范.md`**：渲染进程禁直连 DB；SQL 只准出现在 repositories；IPC 返回 `Result<T>`；新依赖必须先过规范 §8 评审；正则捕获组改动必须 node 实跑验证并附输出。
- 不擅自扩大范围；任何规格偏差必须在 `当前代码状态.md` 记录原因。

**完成义务（缺一不可）**：
1. 按规格「任务拆解」实现；逐项自测「验收点」。
2. 在 `{{计划目录}}/{{unit}}-{{unit名}}-全流程闭环记录.md` 内**续写「实现日志」环节**（按 `模板/02_日志部分/项目日志部分编写模板.md` 格式）——写清实现概要/文件清单/决策偏差/自测记录。**不再使用 `项目日志/` 文件夹。**
3. **提交并推送代码**（如果项目已激活 CI）：
   ```bash
   git add .
   git commit -m "feat({{unit}}): {{简短描述}}"
   git push origin <当前分支>
   ```
   - 推送后等待 CI 运行完成（3-5 分钟）
   - 访问 GitHub Actions 查看 CI 状态
   - **CI ✅**：继续步骤 4
   - **CI ❌**：查看失败日志，修复问题后重新推送，重复此步骤
   - **未激活 CI**：跳过此步骤
4. 更新 `当前代码状态.md` 的核心字段（当前状态/任务概要/自测结果/已知问题/下一步）。
   - 如果 CI 已激活，需包含：CI 运行状态（✅/❌）、CI run ID、测试结果
5. 若 `规范类/命名规则.md` 不存在，创建之并在此单元遵守。
6. 在实现日志中写明 `planStatus`、`staticGate`、`testPointStatus`、`closureStatus`、`updatedAt`、真实 `updatedBy=GLM|DS|GLM+DS`、证据路径和 `nextAction=03`；完成后报告「{{unit}} 实现完成，待 GPT 审查」，我会用 03 prompt 启动审查。
