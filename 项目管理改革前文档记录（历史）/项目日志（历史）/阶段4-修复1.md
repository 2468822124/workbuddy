# 阶段 4 修复日志 — 审查问题 F1/F2/F3

## 日志元信息
- **阶段 / 批次**：阶段 4 — 修复批次 1（GLM 审查反馈）
- **日期**：2026-08-05
- **实现者**：DeepSeek (deepseek-v4-pro)

## 实现概要
F1: 导入列名 SQL 注入防护。F2: 热点源「立即刷新」按钮。F3: 死代码清理。

## 修复明细

### F1 [MED] — 导入列名 SQL 注入防护
- **文件**：`src/main/services/data.ts`
- **改法**：importAll 中 `Object.keys(row)` 后逐列校验 `/^[a-zA-Z][a-zA-Z0-9_]*$/`，遇非法列名 `throw Error`（事务回滚），不触 DB。
- **验证**：build ✅；逻辑：合法 JSON（正常列名）→ 导入成功；恶意列名 JSON → throw + 回滚 + 不动 DB。

### F2 [MED] — 热点源「立即刷新」按钮
- **文件**：`src/renderer/src/composables/useSettings.ts` + `src/renderer/src/views/SettingsView.vue`
- **改法**：useSettings 新增 `refreshNews()`（调 `api.news.refresh`，复用阶段 2 通道）；SettingsView 热点卡头增「立即刷新」按钮。
- **验证**：build ✅；按钮可见于热点源卡片右上角。

### F3 [LOW] — 死代码清理
- **文件**：`src/main/services/data.ts`
- **改法**：删 `exportAll` 中 `const win = await import('electron')...` 行（未用 + 多余动态 import）。
- **验证**：build ✅。

## 自测记录
| 验点 | 命令/操作 | 结果 |
|---|---|---|
| build 通过 | `npm run build` | ✅ 2.25s, SettingsView 16.81KB |
| col 注入防护 | grep 校验正则 | ✅ `/^[a-zA-Z][a-zA-Z0-9_]*$/` |
| 刷新按钮存在 | grep SettingsView.vue btn-refresh | ✅ |

## 已知问题 / 技术债
无新增。

## 下一步
✅ 可交 GLM 复审。
