# 审查日志 · 阶段 6 — 打包 .exe（终审）

## 审查元信息
- **阶段 / 批次**：阶段 6（首次实现，**终审交付**）
- **审查日期**：2026-08-06
- **审查人**：GLM
- **审查对象**：DeepSeek 阶段 6 产出（electron-builder 配置 + 生产加固 + NSIS 安装包 + 安装说明）

## 1. 审查范围
**DeepSeek 产出**：
- `electron-builder.yml`（新增）、`安装说明.md`（新增）、`package.json`（修改，+脚本，未加 productName）
- `src/main/index.ts`（修改，+单实例锁 + isPackaged 加固）、`src/main/window.ts`（修改，+devtools 守卫）
- `dist/WorkBuddy-Setup-0.1.0.exe`（85 MB）、`dist/win-unpacked/WorkBuddy.exe`（181 MB）

**当前状态.md 快照**：阶段状态已更新为「阶段 6 待终审」；§2 实现概要/文件清单/偏差/自测/运行说明/已知项完整；§3/§4 已按本阶段重写。**真实性核对**：§2.5 自测结论与 GLM 独立复核一致，无虚报。

**审查方法**：独立复核——产物存在性 + asar 解包检查（源码泄漏/密钥/主入口）+ package.json 完整性 + 编译产物含加固代码 grep + 源码正确性逐行读 + build 复跑。**⚠️ 终审核心（实跑安装包 + §8 端到端 E2E）headless 无法起 Electron，必须由用户江泽锋在真实 Windows 11 上执行**——本审查提供逐项清单移交，不冒认已跑。

## 2. 验收点逐项核查（对照 `规格-阶段6-*.md §9`）

| # | 验收点 | 结论 | 证据 / 复现 |
|---|---|---|---|
| 1 | pack:win 产出 win-unpacked/WorkBuddy.exe，启动不崩 | ✅(产物)/⚠️(启动待用户) | 产物存在 181 MB；**.node 解包正确（见#3）是启动不崩的必要条件已满足**；但「双击实跑不崩」须用户在 Win11 双击 `dist/win-unpacked/WorkBuddy.exe` 确认（headless 无法起 GUI） |
| 2 | dist:win 产出 NSIS 安装包 | ✅ | `ls dist/*.exe` → WorkBuddy-Setup-0.1.0.exe 85 MB + .blockmap |
| 3 | asar 内 better-sqlite3 .node 已解包 | ✅ | `find dist/win-unpacked/resources -name '*.node'` → `app.asar.unpacked/node_modules/better-sqlite3/build/Release/better_sqlite3.node`（在 app.asar **外**） |
| 4 | 安装包 per-user 无需管理员 | ✅(配置) | electron-builder.yml `nsis.perMachine:false`；运行时由用户安装时确认 |
| 5 | 冷启动<3s / 晨间<5s / 内存<150MB | ⚠️待用户 | headless 无法测；须用户实跑观任务管理器 |
| 6 | 全流程 E2E §8.4 | ⚠️待用户 | headless 无法起 GUI；**移交用户清单见 §7** |
| 7 | 断网/错 Key 降级 §7-C | ⚠️待用户 | 降级代码链路阶段 2/5 已核查通过；运行时由用户构造场景确认 |
| 8 | DB 密文 / 日志无明文 §8.6 | ✅(代码)/⚠️(实跑) | safeStorage 链路阶段 1/4 已验；用户可用 SQLite 工具查 `%APPDATA%/workbuddy/workbuddy.db` settings 表 llmApiKey 确认密文 |
| 9 | DB 路径 = %APPDATA%/workbuddy/（productName 未污染） | ✅ | `grep productName package.json` → 零匹配；productName 仅在 electron-builder.yml；app.getName()=workbuddy 不变 |
| 10 | 生产加固：无菜单 / 单实例 / devtools 不开 | ✅(代码) | 编译产物 grep：`requestSingleInstanceLock`+`Content-Security-Policy`+`setApplicationMenu`（3 命中）+ `devtools-opened`/`closeDevTools`（2 命中）；源码逐行核查逻辑正确 |
| 11 | CSP 生效（无违规阻断 / Fonts 加载） | ✅(代码)/⚠️(实跑) | CSP 串完整（default/script/style/font/img/connect/object/base 八指令）；`script-src 'self'` 无 unsafe-inline/eval；运行时由用户开 DevTools console 确认无违规 |
| 12 | 无硬编码密钥（包内 grep 无明文） | ✅ | asar 解包扫描无 .env/secret/credential；safeStorage 链路不变 |
| 13 | 安装说明.md 含 SmartScreen 绕过 | ✅ | `grep 更多信息\|仍要运行` → 2 命中；含首启/数据位置/卸载 |
| 14 | npm run build 绿 | ✅ | 独立复跑 `npm run build` → 2.19s 绿，零 TS 错误 |

**额外独立核查（规格外，安全/整洁）**：
- **asar 内容**：解包后顶层 = `node_modules/` + `out/` + `package.json`；**`src/` 未泄漏**（仅编译 out/）；主入口 `out/main/index.js` 存在；`electron-builder.yml` 未被打入包。✅
- **@electron/rebuild 成功**：build 日志 `installing native dependencies → finished moduleName=better-sqlite3 arch=x64`。✅

**小结：14 项 = ✅8（产物/配置/代码核查）+ ⚠️6（GUI 运行时，移交用户）。无 ❌。**

## 3. 禁止边界核查（项目计划 §7-B）
- [x] 无硬编码密钥（asar 扫描无明文；safeStorage 链路阶段 1/4/5 已验）
- [x] 日志无业务明文/API Key（logger redact 兜底；packaged console 无终端即静默）
- [x] 无业务数据上传云端（DB 本地 userData；RSS/LLM 出站为用户自配端点/公开 RSS）
- [x] 软删除（打包阶段不改数据行为）
- [x] 无阻断式模态（应用内不变；安装向导为 OS 原生 NSIS）
- [x] 未签名明示（安装说明.md 写清 SmartScreen，不谎称已签名）

## 4. 错误降级核查（项目计划 §7-C）
| 场景 | 期望 | 实测结论 |
|---|---|---|
| better-sqlite3 native 加载失败（头号坑） | 不崩 | ✅(必要条件) .node 解包至 app.asar.unpacked + @electron/rebuild 成功；**实跑确认待用户双击 exe** |
| 单实例/SQLite 双开 | 第二实例聚焦已窗 | ✅(代码) requestSingleInstanceLock + second-instance 重聚焦 |
| CSP 阻断 renderer | 仅白名单源 | ✅(代码) 八指令 CSP；主进程 RSS/LLM 不受约束 |
| 安装签名失败 | 未签名跳过 | ✅ signAndEditExecutable:false + 日志「no signing info, signing is skipped」 |

## 5. 视觉核查
- 本阶段无新 UI（打包阶段）；既有 token 化/无硬编码 hex 不变（阶段 0–5 已验）。
- 应用图标 = electron 默认（规格 §5.5 允许的退路；待用户替换）。

## 6. 发现的问题

| # | 严重度 | 问题 | 位置 | 修复要求 |
|---|---|---|---|---|
| F1 | 🔵 LOW | `author` 字段缺失 → electron-builder 告警；NSIS「发布者」显示未知 | `package.json` | 可选：加 `"author": "江泽锋"`（或占位）消告警、让发布者名可读 |
| F2 | 🔵 LOW | 冗余 `electron-rebuild` 独立 devDep + `rebuild` 脚本；electron-builder 25 已内嵌 @electron/rebuild（build 日志明确告警「consider to remove excess dependency」），独立包已废弃 | `package.json` | 可选：移除独立 `electron-rebuild`，`rebuild` 脚本改用 `electron-builder install-app-deps`（官方推荐） |

**无 HIGH / MED / CRITICAL。** 构建链路、native rebuild、生产加固、asar 整洁、密钥安全均正确。

## 7. 最终结论

- **结论**：✅ **通过（构建与静态核查全部通过，零阻断缺陷）**
- **⚠️ M6 交付为「条件性通过」**：本审查**未**、**无法**在 headless 环境实跑安装包与 §8 端到端 E2E。**M6 正式交付须由用户江泽锋在真实 Windows 11 上完成下列清单**，全部通过方可宣告交付：
  1. **启动不崩**：双击 `dist/win-unpacked/WorkBuddy.exe`（或装 WorkBuddy-Setup-0.1.0.exe），确认窗口正常显示、无 better-sqlite3 崩溃（**头号坑实跑证据**）。
  2. **冷启动 < 3s**；晨间数据 < 5s（允许骨架屏）。
  3. **空闲内存 < 150MB**（任务管理器观 WorkBuddy.exe 空载）。
  4. **全流程 E2E（§8.4）**：配 RSS→晨间拉热点→总览；建项目→建 todo(planDate)→总览待办/交接→勾选完成；配 LLM→对话搜索→跳转；导出 JSON→卸载重装→导入还原。
  5. **降级（§7-C）**：断网→热点卡「点击重试」+缓存保留；错 Key→红色状态条；不崩。
  6. **DB 密文**：SQLite 工具查 `%APPDATA%/workbuddy/workbuddy.db` settings 表，llmApiKey 非明文。
  7. **单实例**：二次双击图标→聚焦已有窗口（不开第二进程）。
  8. **CSP**：临时开 DevTools（Ctrl+Shift+I 若被守卫关，可注释守卫临时验）→ Console 无 CSP 违规；Google Fonts 正常。
  9. **累积补验（阶段 1–5 headless 未验项）**：测试连接三态（成功/失败/超时≤10s）、导出/导入 round-trip、真实 LLM 对话全链路、六类错误态、布局像素/四态。
- **F1/F2 处理**：非阻塞，可在用户 E2E 后择机清理（加 author + 移除冗余 electron-rebuild）。
- **下一步**：交用户实跑上述清单。全过 → **M6 交付达成 🎯**；若有项失败 → 用 `04-deepseek修复.md` 修。
- **给 DeepSeek 的话**：构建链路质量高，better-sqlite3 头号坑（asarUnpack + rebuild）正确攻克，productName 漂移坑成功规避，生产加固到位。F1/F2 为可选整洁项。

---
### 复审 1 — 用户反馈1 修复（2026-08-07）
- **复审日期**：2026-08-07　**复审人**：GLM
- **复审记录**：见 [`阶段6-复审1-用户反馈1.md`](阶段6-复审1-用户反馈1.md)（独立成文，含运行时实测）
- **逐条修复核对**：§5 主修复 ✅ / 加固1 ✅ / 加固2 ✅ / 加固3 ✅（均**运行时验证**：实跑主进程日志达 `WorkBuddy ready`，全新 DB settings 表 = 8 行）
- **新引入问题**：R1/R2（🔵 LOW，非阻断）；并纠正 DeepSeek 的「#49034」误诊（实为环境变量 `ELECTRON_RUN_AS_NODE=1`，非 Electron bug）
- **复审结论**：✅ 通过（用户反馈1 启动阻断已解决并验证；阶段 6 §7 完整 E2E 清单仍待用户真实机器复测）
