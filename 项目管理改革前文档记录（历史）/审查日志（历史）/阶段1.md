# 审查日志 · 阶段 1 — 工程地基

## 审查元信息
- **阶段 / 批次**：阶段 1（首次实现）
- **审查日期**：2026-08-05
- **审查人**：GLM
- **审查对象**：DeepSeek 阶段 1 产出（`E:\workspace\workbuddy\`，42 文件）

## 1. 审查范围
**DeepSeek 产出**：见 `项目日志/阶段1.md` 文件清单（main/preload/renderer/shared 全层）。
**当前状态.md 真实性核对**：§2.5 自测表多项与实测不符（见下 ❌ 项），**不可盲信**——已逐项独立验证。

**审查方法**（诚实声明）：
- ✅ 实跑：`npm run build`（两次均绿，renderer 271KB）、`grep` 硬编码色值、`node` 路径解析实证。
- 🔍 代码核查（无头环境无法起 Electron GUI）：迁移/DDL、IPC、加密、redact、软删除、安全模式、CSS 媒体查询。
- ⚠️ 未做 GUI 运行时验证（侧边栏折叠动画、首启条实际渲染、冷启动秒数）——标注于相应项。

## 2. 验收点逐项核查（对照 `规格-阶段1-工程地基.md §12`）

| # | 验收点 | 结论 | 证据 |
|---|---|---|---|
| 1 | dev 冷启动 < 3s 无报错 | ⚠️ 待运行时验 | `npm run build` 1.8s 绿；代码无明显阻塞；GUI 秒数未实跑 |
| 2 | 迁移自动执行/表全/version=1 | ✅ | `0001_init.ts` 建 11 表；`migrate.ts` 事务化 + `__schema_migrations` |
| 3 | llmApiKey 密文往返正确 | ✅ | `settingsRepo.set` 用 `.run()` 正确；`crypto.ts` safeStorage 包装；isEncrypted 标记驱动 |
| 4 | window.api 有类型提示 | ❌ | **F3**：`useApi.ts:1` 路径错。node 实证：`../../../../preload/index`→`workbuddy\preload\index`(不存在)；应为 `../../../preload/index`→`src\preload\index`(存在)。`import type` 被 esbuild 剥离故 build 过，但 tsc/vue-tsc 下 `ApiType` 解析失败→`window.api` 实际为隐式 any |
| 5 | 侧边栏 ≤1180px 折叠/无横向滚动 | ✅(代码) | `AppSidebar.vue:214 @media(max-width:1180px)` 折叠 68px；`tokens.css body overflow-x:hidden`。未实跑 |
| 6 | tokens.css 唯一色源/组件无硬编码 hex | ❌ | **F2**：`grep` 实测 `AppSidebar.vue:107` 含 `#D98C6A`（brand-mark 渐变辅色） |
| 7 | 首启为非阻塞提示条 | ❌ | **F1**：`index.ts:19` `seedDefaults()` 在 IPC 前播种 `nickname=''`；`ipc/index.ts:24-26` `isFirstLaunch` 判 `get('nickname')===undefined` → 永远 false → 首启条**永不显示**。DeepSeek 自填 ✅ 不实 |
| 8 | logger 不输出 apiKey 明文 | ✅ | `logger.ts` redact 正则 `/apiKey\|key\|secret\|token\|password/i` 覆盖；逐层递归替换 `***REDACTED***` |
| 9 | DB 损坏→只读安全模式不崩溃 | ✅(代码) | `connection.ts` try readonly fallback + `isReadonlyMode`；`App.vue` 据此显安全模式条。未实跑 |

**小结：9 项中 ✅4 + ⚠️1 + ❌3（#4/#6/#7）。3 项验收点未达成，且 DeepSeek 自填均为 ✅。**

## 3. 禁止边界核查（项目计划 §7-B）
- [x] 无硬编码密钥
- [x] 日志无业务明文/API Key（redact 生效）
- [x] 无业务数据上传云端（Phase1 无任何网络出站代码）
- [x] 软删除强制（project/todo repo 查询均 `WHERE isDeleted=0`；DDL 默认 0）
- [x] 无阻断式模态弹窗（首启条/安全模式条均为非模态横条）

## 4. 错误降级核查（项目计划 §7-C）
| 场景 | 期望 | 结论 |
|---|---|---|
| LLM 失败 | 停重试+日志+状态条 | N/A（Phase1 无 LLM 调用） |
| 热点失败 | 缓存+不阻塞 | N/A（Phase2） |
| DB 锁定/损坏 | 重建→只读安全模式+Toast | ✅ 代码核查到位（待运行时验证） |

## 5. 视觉核查（阶段 0）
- [x] 大部分组件走 `var(--*)` token
- [ ] **无硬编码 hex**：❌ F2（`#D98C6A`）；另 `App.vue:53` `rgba(176,86,75,.12)` 裸色值（F5，rgba 非 hex 但违精神）
- [x] 窗口自适应断点正确（1180 折叠 / tokens.css 兜底），未实跑
- [~] 四态：nav/button 的 hover/active/disabled 到位；focus ring 仅部分（Phase1 无输入框，可接受）

## 6. 发现的问题（按严重度）

| # | 严重度 | 问题 | 位置 | 修复要求 |
|---|---|---|---|---|
| F1 | 🟡 MED | 首启检测逻辑失效：`seedDefaults()` 先于判断播种 nickname，导致 `isFirstLaunch` 恒 false，首启条永不显示 | `main/index.ts:19` + `main/ipc/index.ts:22-31` | 引入独立 `onboarded` 标记（**不**在 seedDefaults 播种）。`isFirstLaunch` 改判 `get('onboarded')?.value !== 'true'`。用户点「知道了」或完成引导后 `set('onboarded','true')`。验证：全新 DB→条显示→点关闭→重启不再显示 |
| F3 | 🟡 MED | `useApi.ts` 导入路径多一层 `..`，目标不存在；type-only 被 esbuild 剥离故 build 过，但类型实际失效 | `renderer/src/composables/useApi.ts:1` | 改 `../../../preload/index`（node 实证存在）。**更稳**：在 `electron.vite.config.ts` renderer alias 加 `'@preload'`，改用别名。修后跑 `npx vue-tsc --noEmit`（无则 tsc -p tsconfig.web.json）确认 `ApiType` 解析、`window.api` 非隐式 any |
| F2 | 🟡 MED | 组件硬编码 hex `#D98C6A`，违反"tokens.css 唯一色源" | `AppSidebar.vue:107` | tokens.css 新增 `--accent-light:#D98C6A`，组件改 `var(--accent-light)`。顺带 F5：新增 `--danger-soft`，`App.vue:53` 改用之。验证：`grep -rn '#[0-9a-fA-F]\{3,6\}' src/renderer/src --exclude-dir=styles` 为空 |
| F4 | 🟡 MED（latent） | `create()` 用 `.get()` 执行 INSERT，返回 undefined（INSERT 不返回行）。Phase1 无调用方未暴露，Phase3 一用即错 | `db/repositories/projectRepo.ts:17-26`、`todoRepo.ts` 同 | INSERT 加 `RETURNING *` + `.get()`（SQLite 3.35+，better-sqlite3 v11 支持）；或 `.run()` 后 `this.findById(id)` 回查。验证：最小脚本调 create() 断言返回含 id/createdAt |
| F5 | 🔵 LOW | safe-mode 条裸 `rgba(176,86,75,.12)` | `App.vue:53` | 随 F2 一并消除（`--danger-soft`） |

## 7. 最终结论

- **结论**：🟡 **需修复**
- **依据**：9 项验收点中 3 项未达成（#4 类型安全 / #6 无硬编码 hex / #7 首启检测），且 DeepSeek 自填表与实测不符。另 1 处 latent 正确性 bug（F4）须在 Phase3 前修。未达 🔴：基础架构（三层进程/迁移/加密/redact/软删除/安全模式/构建）方向正确、无安全或数据风险。
- **下一步**：交 DeepSeek 用 `用户prompt/04-deepseek修复.md` 修 F1/F2/F3/F4/F5（均为小改）。修完用 `05-glm复审.md` 回我复审。**复审通过前不进阶段 2。**
- **肯定项**（保留）：redact 脱敏、软删除强制、WAL+外键、safeStorage 加密、安全模式降级、IPC 错误信封、tokens 体系搭建——地基质量良好，问题集中在细节与自测真实性。

---
### 复审（2026-08-05 · GLM）

**逐条核对**（独立验证，不只看 diff）：
| # | 修复项 | 核对方式 | 结论 |
|---|---|---|---|
| F1 | 首启 onboarded 逻辑 | 读 `ipc/index.ts:25-26`（判 `get('onboarded')`）+ `defaultSettings.ts`（确认**无** onboarded 种子）+ `App.vue:17-20`（「知道了」写 onboarded）。端到端逻辑：全新DB→未seed→true→条显；关闭→存→重启→false | ✅ 已解决 |
| F2 | 硬编码 hex | `grep '#[0-9a-fA-F]{3,6}\|rgba?\(' *.vue` → **No matches**；`tokens.css:29 --accent-light` 已建 | ✅ 已解决 |
| F3 | useApi 路径 | `node` 路径解析 → `src/preload/index` **exists:true** | ✅ 已解决 |
| F4 | create() RETURNING | grep → `projectRepo.ts:20` + `todoRepo.ts:38` 均 `RETURNING *` + `.get()` | ✅ 已解决 |
| F5 | safe-mode rgba | `App.vue:58 background: var(--danger-soft)`；`tokens.css:32 --danger-soft` 已建 | ✅ 已解决 |

**新引入问题**：无。
**越界改动**：无。仅触及 5 项相关文件；`defaultSettings.ts` 正确未动（onboarded 故意不播种，scope 克制）。
**构建**：`npm run build` 1.79s 绿，renderer 271.52KB。

**最终结论**：✅ **通过**（阶段 1 关闭）。
**下一步**：可启动阶段 2（晨间唤醒 + 热点 + 今日总览），用 `用户prompt/01-下一阶段启动.md`。
**运行时未验项（留待阶段2/集成时）**：F4 create() 首次实际调用、GUI 首启条渲染、冷启动秒数、侧边栏折叠动画——headless 环境无法起 Electron，代码核查均通过。
