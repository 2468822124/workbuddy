# 审查日志 · 阶段 3 — 项目管理（含 todos）

## 审查元信息
- **阶段 / 批次**：阶段 3（首次实现）
- **审查日期**：2026-08-05
- **审查人**：GLM
- **审查对象**：DeepSeek 阶段 3 产出（`workbuddy/`，18 文件：11 新增 + 7 修改）

## 1. 审查范围
**DeepSeek 产出**：见 `项目日志/阶段3.md`。
**当前状态.md 真实性核对**：自测表基本属实，但**遗漏任务勾选不持久化**（见 F1）。

**审查方法**（诚实声明）：
- ✅ 实跑：`npm run build`（2.12s 绿）、`grep` 硬编码色值、`grep` toggle 持久化链路、读核心源码核查。
- 🔍 代码核查（无头环境无法起 Electron GUI）：CRUD、看板 DnD、列表、详情、IPC、软删除、内联确认、数据闭环。
- ⚠️ 未做 GUI 运行时验证：拖拽实际效果、视图切换、详情交互、窗口自适应——均代码核查通过，标注待验。

## 2. 验收点逐项核查（对照 `规格-阶段3-*.md §9`）

| # | 验收点 | 结论 | 证据 |
|---|---|---|---|
| 1 | 项目 CRUD（含软删，DB 留存） | ✅(代码) | projects.ipc 5 handler；projectRepo.create/update/softDelete；listWithCounts/getWithCounts 过滤 isDeleted=0 |
| 2 | 看板 4 列 + 拖拽改 status 持久 + 列头计数 | ✅(代码) | KanbanBoard/Column/Card DnD；changeStatus 乐观更新+回滚调 `projects:update`；运行时未验 |
| 3 | 列表视图 + 看板↔列表切换 + 偏好持久化 | ✅(代码) | ProjectListView + viewMode 存 localStorage `wb_proj_view` |
| 4 | 详情页编辑 + 任务 CRUD + **勾选完成** + 软删 | ❌ | **F1**：`ProjectDetailView.vue:107` `@toggle` 仅本地 `todo.status=` 翻转，**无 IPC**；`useProjectDetail` 无 `toggleTodo`、未调 `api.todos.toggle`。→ 勾选不持久，刷新还原。违反"勾选完成" |
| 5 | 数据闭环：项目任务 planDate=today → 今日总览 | ✅(代码) | todos:create 带 planDate；阶段2 todos:today 按 planDate 过滤（与 projectId 无关）天然命中 |
| 6 | 项目不存在 id → 友好 EmptyState，不崩溃 | ✅(代码) | useProjectDetail notFound → EmptyState + 返回按钮 |
| 7 | 「关联笔记」显示「敬请期待」空状态 | ✅(代码) | ProjectDetailView 底部 EmptyState icon=Library |
| 8 | 删除为内联确认条，非模态 | ✅(代码) | ProjectsView del-bar / DetailView delConfirm / TaskRow confirmDelete，均 3s 超时；无 alert/confirm/dialog |
| 9 | `grep` 组件无硬编码 hex/rgba | ✅ | 实测 `*.vue` → **No matches**；新增 `--ok-soft/--warn-soft/--danger-faint` token |
| 10 | 软删除强制 + 删除项目不级联删任务 | ✅(代码) | 所有查询 `isDeleted=0`；project softDelete 仅标记，todos 不动（projectId 外键 ON DELETE SET NULL 不触发） |
| 11 | 输入长度限制 + `{{}}` 渲染（无 v-html） | ✅(代码) | IPC 校验 name≤80/desc≤500/content≤200；模板文本插值；未见 v-html |
| 12 | 窗口自适应 | ✅(代码) | 看板 `overflow-x:auto`；列表/面板流式；运行时未验 |
| 13 | build 绿无 TS 错 | ✅ | `npm run build` 2.12s；ProjectsView 12.43KB + DetailView 15.23KB |

**小结：13 项中 ✅12 + ❌1（#4）。唯一未达成为 F1。**

## 3. 禁止边界核查（项目计划 §7-B）
- [x] 无硬编码密钥
- [x] 日志无业务明文/apiKey（IPC 错误仅记 message）
- [x] 无业务数据上传云端（本阶段纯本地 CRUD）
- [x] 软删除强制（project/todo 查询 `isDeleted=0`；无物理删除）
- [x] 无阻断式模态弹窗（删除/编辑均内联）

## 4. 错误降级核查（项目计划 §7-C）
| 场景 | 期望 | 结论 |
|---|---|---|
| 项目不存在 | EmptyState + 不崩溃 | ✅(代码) |
| 拖拽失败/不支持 | 编辑表单 status 下拉兜底 | ✅(代码) ProjectForm 编辑模式含 status select |
| 乐观更新失败 | 回滚 + 非阻塞提示 | ✅(代码) changeStatus 失败还原 old status |

## 5. 视觉核查（阶段 0）
- [x] token 化：组件全走 `var(--*)`；新增 3 token
- [x] 无硬编码 hex（grep 实测零匹配）
- [x] 状态色映射正确（active→accent / paused→warn / done→ok / dropped→text-muted）
- [~] 四态：按钮/行 hover、卡片 hover lift 到位；focus ring 部分

## 6. 发现的问题

| # | 严重度 | 问题 | 位置 | 修复要求 |
|---|---|---|---|---|
| F1 | 🟡 MED | **详情页任务勾选不持久化**：`ProjectDetailView.vue:107` 的 `@toggle` 仅本地翻转 `todo.status`，未调任何 IPC；`useProjectDetail` 无 `toggleTodo`、未引 `api.todos.toggle`（阶段 2 已有该通道）。→ 勾选完成状态刷新后丢失，违反验收"勾选完成"。另：该处直接 `todo.status=` 属 mutation，违 common/coding-style 不可变原则 | `composables/useProjectDetail.ts` + `views/ProjectDetailView.vue` | (1) `useProjectDetail` 增 `toggleTodo(id)`：调 `api.todos.toggle(id)`，成功后**就地替换**数组项（`todos.value[i] = r.data`，不可变更新）。(2) `ProjectDetailView` 模板 `@toggle="toggleTodo"`（删掉本地 mutation 闭包）。验收：详情页勾选 → 刷新仍在；切到今日总览该任务也为已完成 |
| F2 | 🔵 LOW | `projects.ipc.ts` update handler 用 `partial as any` 绕过类型；多处 `require('crypto')` 内联 | `main/ipc/projects.ipc.ts` | 顶部 `import { randomUUID } from 'crypto'`；partial 用具体 `Partial<Record<...>>` 或 `Partial<Project>` |

## 7. 最终结论

- **结论**：🟡 **需修复**
- **依据**：13 验收点中 #4（详情页勾选完成持久化）未达成（F1）。其余 12 项代码核查通过。未达 🔴：无安全/数据/§9 问题，CRUD/看板/列表/数据闭环/视觉/软删除均正确，问题集中在 toggle 持久化一处。
- **下一步**：交 DeepSeek 用 `用户prompt/04-deepseek修复.md` 修 F1（+F2 顺手）。修完用 `05-glm复审.md` 回我复审。**复审通过前不进阶段 4。**
- **肯定项**（保留）：看板 DnD 乐观更新+回滚、HTML5 原生拖拽零依赖、listWithCounts 单查询聚合、内联删除确认（无模态）、数据闭环天然命中、零硬编码色值、输入校验、notFound 兜底——项目管理骨架质量良好。

---
### 复审（2026-08-05 · GLM）

**逐条核对**（独立验证，不只看 diff）：
| # | 修复项 | 核对方式 | 结论 |
|---|---|---|---|
| F1 | 详情页勾选持久化 | grep：`useProjectDetail.ts:62-63` `toggleTodo` 调 `api.todos.toggle(todoId)`；:71 导出；`ProjectDetailView.vue:14` destructure 含 `toggleTodo`；:107 `@toggle="toggleTodo"`。链路 TaskRow emit toggle(id)→toggleTodo→IPC toggle→不可变 `todos.value[i]=r.data` | ✅ 已解决 |
| F2 | as any + require 清理 | grep：`projects.ipc.ts:2` `import { randomUUID } from 'crypto'`；`as any` 零匹配；`require(` 零匹配 | ✅ 已解决 |

**新引入问题**：无。
**越界改动**：无。仅触及 useProjectDetail.ts / ProjectDetailView.vue / projects.ipc.ts 三文件，scope 克制。
**构建**：`npm run build` 2.33s 绿，DetailView 15.36KB。
**遗留**：无。阶段 0 登记的「已完成 x/y 计数响应式」、阶段 2 复审遗留的 `as NewsItem[]` nit 仍存（无害），均不影响阶段 3 验收。

**最终结论**：✅ **通过**（阶段 3 关闭）。
**下一步**：可启动阶段 4（设置 + 数据管理），用 `用户prompt/01-下一阶段启动.md`。
**运行时未验项（累积，建议打包前 GUI 实跑）**：项目看板拖拽、详情任务 CRUD、勾选持久化实际刷新、数据闭环（项目任务流入今日总览）、列表/详情窗口自适应——headless 无法起 Electron，链路均代码核查通过。
