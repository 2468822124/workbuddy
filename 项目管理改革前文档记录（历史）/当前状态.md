# 当前状态（实时看板）

> **搭建**：GLM（定义结构）　**填写与维护**：DeepSeek
> **作用**：GLM 审查前必读。DeepSeek 必须如实、完整填写。
> **⚠️ 唯一权威位置**：本文档即 `当前状态.md` 的唯一权威版本（文档根 `E:\workspace\`）。代码根 `workbuddy\` 不得存副本（见 `项目规范/文档治理总纲.md §2`）。

---

## 0. 项目元信息
- **项目**：WorkBuddy（AI 原生本地优先 Windows 桌面工作台）
- **协作**：GLM=项目经理/审查 · DeepSeek=编码 · 用户=决策者
- **最终交付**：未签名可安装 `.exe`
- **真相源**：`开发阶段项目计划/项目计划.md`

## 1. 当前阶段状态
- **当前阶段**：阶段 6 — 打包 .exe（最终交付）
- **状态**：`✅ 终审通过（构建+静态+运行时）/ ⚠️ M6 待用户实跑完整 E2E（2026-08-07 更新）`
- **在研功能**：**计划与复盘体系 · 子阶段 5 — 规划体系**（`✅ 实现完成，待 GLM 审查`，2026-08-08，`计划与复盘体系实现项目计划/规格-子阶段5-规划体系.md`）—— 计划+复盘统一「规划」日/周/月三级 + AI 周/月复盘 + 级联任务下沉 + 自动周期提醒。子阶段 4 已 ✅ GLM 审查通过（3 LOW 非阻断，v0.2.0 四子阶段齐待 bump 发版，`规格-子阶段4-LLM增强.md`）
- **最新事件**：（本行上一条见下段。**更新**）v0.2 测试修复计划 1.0 — 参照完整性（L2 半规范化）**实现完成（08-10，待 GLM 审查）**：修计划/复盘 4 参照完整性缺口（①反向不同步 → 跨级删除分流 ②文本匹配脆弱 → 内联隐形 tid 匹配键升级 ③来源链断裂 → `todos.parentTaskRef` + 来源链跳转 ④删除不传播 → 周/月删行级联 daily）。关键改动：migration 0005（tasks 表 + todos.parentTaskRef，L2 轻量索引 + Markdown-as-truth）；`syncPlanTodos` 全类型 tasks 双向 sync + §3.6 跨级分流 + §3.7 项目 todo 复用（按 content+projectId+未安排判据）；pickTask 惰性分配 tid + `plan:prepareTaskLink`；`tasks:resolveChain` 逐级上溯；TODOS_TOGGLE 双向同步按 ref 定位（根治②复发）；TodayView 来源 tag 可点击跳转（上一期 offset）+ MarkdownEditor focusLine 定位；导入导出加 tasks 表（链不断）。vitest 89/89（planSync 5 + todoSource 3 + parentRef 2 新增）+ tsc.node 仅剩 news.ts:22 存量基线 + build 15.57s 绿 + 0005 存量库实跑（8 todos 全保留 / parentTaskRef 全 null 不回填 / tasks 空起）。**偏差已记 §19.3**：tasks.parentTaskId FK ON DELETE CASCADE（规格裸 REFERENCES，import roundtrip 需要）；§3.7「写 ref 指向项目任务」未落地（行 parent=项目 todo id 撞 FK + 规则0 误标「来源已删」，只建复用机制）；migration 0005 触「无 migration」规格边界（同 0004 先例）。详见 `项目日志/v0.2测试修复计划1.0-参照完整性.md` 与 §19。（**08-09 更新**）用户反馈3.2 修复完成：**F3.2-1 方案B**（日规划删行 → orphan 分流：open 软删 / done 保留为历史，删除传播闭环）+ **F3.2-2 Tier2**（migration 0004 `todos.sourcePlanId` + `labelTodoSource` 5 规则出处标注 + `todos:withSource` 批量 IPC + TodayView tag 替代 projectId UUID 直显 bug）。vitest 67/67（planSync 4 + todoSource 9 新增）+ build 2.37s 绿 + 0004 存量库实跑（列已应用，migrations 1-3 + 27 active todos 完好）。**偏差已记**：migration 0004 触「无 migration」规格边界（用户定稿 Tier2 要求 schema 改）。**待 GLM 05 复审**。详见 `项目日志/用户反馈3.2-修复1.md` 与 §18。（**08-09 更新**）**GLM 05 复审后 🟡 → 修复2 完成**：唯一阻断 = `Todo.sourcePlanId` 必填后 2 处 `todoRepo.create` 调用点未同步（projects.ipc:91 + reminders.ts:18 TS2345）→ 补 `sourcePlanId: null`；另 tsc 全量检查暴露同源第 3 处（todoRepo quickCreate）与存量 status 窄化错（projects.ipc:38，GLM 漏检）一并修复达成 GLM 验收标准。tsc.node 仅剩 news.ts:22 存量基线 + vitest 67/67 + build 15.23s 绿。**待 GLM 复审 2**。详见 `项目日志/用户反馈3.2-修复2.md` + §18.5。
- **当前规格**：`项目计划/规格-阶段6-打包.md`
- **上一阶段结论**：阶段 5（对话式热点搜索）✅ 通过（2026-08-06）

---

## 2. DeepSeek 自填：本阶段实现情况

### 2.1 实现概要
引入 electron-builder 25，配置 NSIS x64 per-user 打包，攻克 better-sqlite3 native rebuild（asarUnpack + @electron/rebuild 对 electron 33.4.11 重编通过），生产加固（单实例锁 / app.isPackaged 守卫 CSP 头注入 / Menu.setApplicationMenu(null) / devtools 关闭）、创建 安装说明.md（SmartScreen 绕过）。产出 `dist/win-unpacked/WorkBuddy.exe`（解包验证用）+ `dist/WorkBuddy-Setup-0.1.0.exe`（NSIS 安装包，85 MB）。

### 2.2 新增 / 修改文件清单（6 新增 + 2 修改）
| 层 | 文件 | 类型 |
|---|---|---|
| 根 | electron-builder.yml | 新增（NSIS/x64/per-user/asarUnpack/signAndEditExecutable:false） |
| 根 | 安装说明.md | 新增 |
| 配置 | package.json | 修改（+electron-builder devDep + pack:win/dist:win 脚本，**未加 productName**） |
| main | src/main/index.ts | 修改（+单实例锁 + app.isPackaged 块：CSP头/ Menu移除） |
| main | src/main/window.ts | 修改（+devtools-close 守卫，app.isPackaged 时） |
| dist | dist/WorkBuddy-Setup-0.1.0.exe（85 MB） | 新增（最终交付物） |
| dist | dist/win-unpacked/WorkBuddy.exe（181 MB） | 新增（解包验证用） |

### 2.3 关键决策与规格偏差
| 偏差点 | 原规格 | 实际做法 | 原因 |
|---|---|---|---|
| signAndEditExecutable:false | 未提及 | win 段增此配置 | Windows 非管理员无法创建 symlink → winCodeSign 解压失败；未签名项目不需此依赖，跳过 |
| 图标未提供 | build/icon.png 占位 | 使用 electron 默认图标 | headless 环境无图形能力生成 PNG；默认图标不阻塞构建，标注待用户替换 |
| electron 33.4.11 | devDeps 中 electron 33.3.1 | electron-builder 自带 33.4.11 打包 | electron-builder 使用内置 electron（与 devDeps 中用于 dev 的版本独立），版本差异不影响 |

### 2.4 数据库
- **迁移版本**：1 → **4**（阶段 6 不变 + 计划与复盘 子阶段1 注册 `0003_plan_review_governance` + 用户反馈3.2 F3.2-2 注册 `0004_todo_source_plan`（todos.sourcePlanId TEXT，addColumnIfMissing 幂等），启动后自动执行）
- **表**：11 表不变（0003 仅 ALTER ADD COLUMN，不建新表）

### 2.5 自测结果
| 验收点 | 命令/操作 | 结果 |
|---|---|---|
| npm run build 绿 | `npm run build` | ✅ 2.17s 绿，零 TS 错误 |
| pack:win 产解包 exe | `npm run pack:win` | ✅ dist/win-unpacked/WorkBuddy.exe（181 MB） |
| dist:win 产 NSIS 安装包 | `npm run dist:win` | ✅ dist/WorkBuddy-Setup-0.1.0.exe（85 MB） |
| asar 内 better-sqlite3 .node 已解包 | find dist/win-unpacked/resources -name '*.node' | ✅ `app.asar.unpacked/node_modules/better-sqlite3/build/Release/better_sqlite3.node` 存在，不在 app.asar 内 |
| @electron/rebuild 成功 | build 日志 | ✅ installing native dependencies → finished moduleName=better-sqlite3 |
| 单实例锁存在 | grep index.ts | ✅ requestSingleInstanceLock + second-instance 重聚焦 |
| app.isPackaged 加固存在 | grep index.ts + window.ts | ✅ CSP 头注入 / Menu.setApplicationMenu(null) / devtools-close |
| 安装说明.md 含 SmartScreen 说明 | grep | ✅ 含「更多信息→仍要运行」步骤 |
| productName 未入 package.json | grep package.json | ✅ package.json 仅有 name:workbuddy，无 productName |

#### §2.5 追加 · 用户反馈3.1 受影响验收点重测（2026-08-08，DeepSeek 04）
> 完整记录：`项目日志/用户反馈3.1-修复1.md`；复跑为真实 Electron GUI + CDP（隔离 userData 播种 W33 周任务），贴近用户业务规则序列。

| 验收点 | 操作 | 结果 |
|---|---|---|
| R1 周日制定下周任务 → 下周任一日 rail 显示 | 播种 weekly_plan 8.10（W33）任务1/2/3 → 8.11 日规划读 rail | ✅ rail=任务1/2/3（业务规则 1：查看日所在周） |
| R2 选取 → 编辑器插入 + 上级 [x] 持久化 | 点击 选取任务1 → 读编辑器 + 查 weekly_plan content | ✅ 插入 `- [ ] 任务1` + 上级 `[x] 任务1`（DB 持久化） |
| R3 后续日 rail 不再显示已下沉 | 翻期 8.12 读 rail | ✅ rail=任务2/3，任务1 隐藏（修前显示「已下沉」标签=缺陷） |
| R4 跨周边界周日看不串周 | 翻期 8.9（W32）读 rail | ✅ rail 空 |
| 根因 C：无 date query 进周统筹 → 本周一基准 + 保存落点 | nav `/planning/weekly` 读 label + 保存空模板查 DB | ✅ curStart=8.3（第 32 周）；保存落 8.10（下周一），8.15（修前周六脏值）不存在 |
| 回归：串档/翻期/日历（反馈3 已修项） | 同实例 DOM 检查 | ✅ 未回归（rail 各级标题正确、翻期正常） |
| vitest | `npm test` | ✅ 5 files / **54 tests**（53 存量 + 1 新增根因 C 回归） |
| build | `npm run build` | ✅ 2.36s 绿，零 TS 错误 |

#### §2.5 追加4 · 用户反馈3.2 F3.2-1 方案B + F3.2-2 Tier2 修复验收（2026-08-09，DeepSeek 04）
> 完整记录：`项目日志/用户反馈3.2-修复1.md`；回归测试跑真实 repo SQL（vi.mock connection → node:sqlite in-memory）；0004 存量库实跑用 `ELECTRON_RUN_AS_NODE=1` + better-sqlite3 直查。

| 验收点（定稿 §3.1 回归 + §3.2） | 方式 | 结果 |
|---|---|---|
| 回归① 删 open 任务行 → orphan 软删，今日列表消失 | `tests/planSync.spec.ts` 例1 | ✅ removedOpenCount=1、isDeleted=1、今日不再含 B、sourcePlanId=plan.id |
| 回归② 删 done 任务行 → 保留为历史，未软删 | 例2 | ✅ keptDoneCount=1、isDeleted=0、status=done |
| 回归③ 删 done 加回同文本行 → 复用保留完成态（不新建） | 例3 | ✅ generatedCount=0、status=done、isDeleted=0 |
| 回归④ 删 open（已软删）加回 → 新建 todo（不复用软删记录） | 例4 | ✅ 新 id、status=todo、sourcePlanId=plan.id |
| 出处标注 5 规则 + 优先级 + 降级 + legacy NULL | `tests/todoSource.spec.ts` 9 例 | ✅ 📌→周期提醒 / 项目·{name} / 日规划·{date} / 顺延·{date} / 手动·{date|无日期}；📌 最高；项目/计划已删自动降级 |
| TodayView tag 显示 | 代码路径：useToday.refreshTodos → withSource → sourceLabel → tag（替代原 projectId UUID 直显） | ✅ 实现 + 类型贯通 |
| vitest | `npm test` | ✅ 6 files / **67 tests**（54 存量 + planSync 4 + todoSource 9） |
| build | `npm run build` | ✅ 2.37s 绿，零 TS 错误 |
| 0004 存量库实跑 | ELECTRON_RUN_AS_NODE=1 + better-sqlite3 直查 | ✅ sourcePlanId 列已应用（下次启动记 version 4，幂等）；migrations 1-3 + 27 active todos 完好；存量 withSrc=0（不迁移历史，降级规则兜底） |

#### §2.5 追加5 · 用户反馈3.2 GLM 复审阻断项修复验收（2026-08-09，DeepSeek 04）
> 完整记录：`项目日志/用户反馈3.2-修复2.md`；阻断 = `Todo.sourcePlanId` 必填后 create 调用点未全同步（GLM 审查日志「❌ 新引入问题」）。

| 验收点（GLM 复审要求） | 方式 | 结果 |
|---|---|---|
| #1 projects.ipc:91 TODOS_CREATE 补 sourcePlanId | 补 `sourcePlanId: null`（项目任务非日规划生成） | ✅ |
| #2 reminders.ts:18 ensureTodo 补 sourcePlanId | 补 `sourcePlanId: null`（周期提醒非日规划生成） | ✅ |
| #3（同源第 3 处，GLM 漏检）todoRepo quickCreate 补 sourcePlanId | tsc 全量检查暴露 → 同模式补 null | ✅ |
| #4（存量，GLM 漏检）projects.ipc:38 status 窄化 | `normalizeStatus` helper（非法值降级 active，行为零变化） | ✅ |
| tsc.node 严格类型 | `npx tsc -p tsconfig.node.json --noEmit` | ✅ 仅剩 news.ts:22 存量基线（GLM 验收标准达成） |
| vitest | `npm test` | ✅ 6 files / 67 tests |
| build | `npm run build` | ✅ 15.23s 绿 |

#### §2.5 追加6 · v0.2参照完整性 GLM 审查修复验收（2026-08-10，DeepSeek 04）
> 完整记录：`项目日志/v0.2测试修复计划1.0-参照完整性.md §7`；GLM 审查结论 🟡 需修复（2 MED + 3 LOW），逐条修复后全量回归。

| 验收点（审查记录 F 表 #1-5） | 修复 | 结果 |
|---|---|---|
| #1 MED 导入导出 roundtrip FK 顺序 | data.ts 导出 tasks `ORDER BY (parentTaskId IS NULL) DESC, createdAt ASC`（显式父先拓扑序）+ 新 `tests/dataRoundtrip.spec.ts`（三级链月→周→日 roundtrip ① + 乱序 FK 违例回滚 ②） | ✅ ① 导出文件 tasks 父先（顶层 tid 首位）、导入新库 3 plan+3 task 链完整；② 篡改逆序导入 → `FOREIGN KEY constraint failed` + 事务整体回滚（库内无半导入残留） |
| #2 MED §3.7 复用 todo 删行误软删项目任务 | planRepo orphan 分流对 projectId 非空 todo 例外：回退 `planDate=null`（回到未安排，保留在项目），不软删、离池；测试⑨ 全循环（复用→删行→回退→加回→再复用） | ✅ reverted.planDate=null、isDeleted=0、projectId 保留、removedOpenCount=0、generatedTodoIds 移除；加回行后 findByContentWithProject 重新复用 |
| #3 LOW require('crypto') | todoRepo 顶部 `import { randomUUID } from 'node:crypto'`，删函数内 require | ✅ src/main grep 零 require |
| #4 LOW ref 非空查无 → 来源已删 | todos.ipc.ts annotate：ref 非空且 task 查无 → `parentTask={invalid:true,...}`（归为失效，UI 置灰不可点，不落规则1-5） | ✅ 与规格 §8 语义一致（rule0 invalid 路径既有 todoSource.spec 覆盖） |
| #5 LOW DOMPurify 剥 tid 注释（§3.2 待测点） | jsdom 装 devDep（^29.1.1）+ 真实管线（marked → DOMPurify sanitize）headless 实测 + 固化 `tests/dompurifyComment.spec.ts` | ✅ 净化后无 `tid:`/`<!--` 残留，任务文本保留；待测点闭环（headless 可测，无需仅靠 GUI） |
| vitest | `npm test` | ✅ **10 files / 94 tests**（89 存量 + roundtrip 2 + planSync ⑨ + dompurify 2） |
| build | `npm run build` | ✅ 2.35s 绿 |
| tsc.node 严格类型 | `npx tsc -p tsconfig.node.json --noEmit` | ✅ 仅剩 news.ts:22 存量基线（零新错） |

> 注：roundtrip spec 的 connection mock 补齐两处 production 镜像（`PRAGMA foreign_keys = ON` + 真 BEGIN/COMMIT/ROLLBACK 事务）；tsconfig.web.json 存量错（useProjects/useProjectDetail:31）非本轮触碰、不在审查范围。

#### §2.5 追加3 · F3-1.3 rail 复现 + 任务错位修复验收（2026-08-08，DeepSeek 04）
> 完整记录：`项目日志/用户反馈3.1-修复3.md`；纯渲染层（usePlanning/CascadeRail/PlanPanel/PlanningView）；方案 C todo↔weekly 同步未动；insertTaskLine 纯函数 node 直跑实锤 + 代码路径全场景推演。

| 验收点（§9） | 方式 | 结果 |
|---|---|---|
| 选取周任务1 → 草稿**任务清单段**插入（非备注后），rail 隐藏 | node 实锤 daily 模板：插入于段内末个任务行后、备注 `## 📝` 之前；代码路径：planDraft 含 → inDraftTexts 含 → CascadeRail 过滤 | ✅ 插任务段 + 隐藏 |
| 未保存切日 → 草稿重置 + rail 回显 | 代码路径：load() 重置 planDraft=savedPlan/模板（不含）→ inDraftTexts 不含 → 回显（判断3 不回归） | ✅ |
| 选取并保存 → 切日返回 → 草稿保留 + **rail 不复现**（F3-1.3-A） | 代码路径：planDraft=savedPlan（含）→ inDraftTexts 含 → 隐藏（保存后跨日持续隐藏） | ✅ 修复 |
| 多次选取不同任务 → 依次进任务段，备注不污染（F3-1.3-B） | node 实锤 A2：周任务2 插于周任务1 后、备注前 | ✅ 修复 |
| todo 完成 → weekly `[x]` + 跨日 rail 隐藏；取消 → 回退（方案 C 不回归） | 代码路径：TODOS_TOGGLE 双向同步未动（§7 Req5-7）；§2.5 追加2 既有验收不回归 | ✅ 未动 |
| 自定义/删段模板 → 回退末尾追加 + warn，不崩 | node 实锤 B1/B3：无「任务」段 → fallback 末尾追加；段内无任务行 → 段尾（下一 `##` 前） | ✅ |
| pendingHide 代码清理 | grep `pendingHide\|clearPendingHide\|pending-hide` 全 src | ✅ 代码 0 残留（2 处注释措辞已改写） |
| vitest + build | CLI | ✅ 54/54 + build 2.38s 绿 |

#### §2.5 追加2 · F3-1.2 幽灵消耗修复验收（2026-08-08，DeepSeek 04）
> 完整记录：`项目日志/用户反馈3.1-修复2.md`；方案 C（消耗跟随 todo 完成），代码路径全链路验证。

| 验收点 | 操作 | 结果 |
|---|---|---|
| 选取 → pendingHide 乐观隐藏，不写 weekly | 代码路径：pickTask → pendingHide.add + 无 markTaskConsumed | ✅ 选取仅乐观隐藏，weekly 不标 [x] |
| 不保存切日 → clearPendingHide + rail 回显 | 代码路径：load() → clearPendingHide → new Set() → CascadeRail filter | ✅ 切日回显任务 |
| 保存不碰 weekly | 代码路径：onSavePlan 仅 daily_plan.create/update + syncPlanTodos | ✅ 现状，无需改 |
| todo 勾选 → weekly `[x]` | 代码路径：TODOS_TOGGLE → status=done → planRepo.getByPeriod + 文本匹配 [ ]→[x] | ✅ 正向同步 |
| todo 取消勾选 → weekly `[ ]`（无同名完成） | 代码路径：TODOS_TOGGLE → status=todo → findByContentInRange → 0 others → [x]→[ ] | ✅ 反向同步 |
| todo 取消勾选 → weekly 保持 `[x]`（有同名完成） | 代码路径：findByContentInRange 排自身 → others>0 → 不 unmark | ✅ 多日同名去重 |
| 跨周边界不串 | 代码路径：periodStartFor 归一化 + getWeekRange 约束 | ✅ |
| vitest | `npm test` | ✅ 4 files / 54 tests 全绿 |
| build | `npm run build` | ✅ 12.46s 绿，零 TS 错误 |

### 2.6 构建与运行说明（GLM 终审用）
- **Node 版本**：v22.16.0　**包管理**：npm 10.9.2
- **构建命令**：`cd E:\workspace\workbuddy && npm run dist:win`
- **产物**：`dist/WorkBuddy-Setup-0.1.0.exe`（NSIS 安装包，85 MB）
- **快速验证**：`npm run pack:win` → 双击 `dist/win-unpacked/WorkBuddy.exe`（解包直接运行，无需安装）
- **安装路径**：默认 `%LOCALAPPDATA%\Programs\WorkBuddy`
- **DB 文件**：`%APPDATA%/workbuddy/workbuddy.db`
- **卸载**：Windows 设置→应用 或 安装目录 `Uninstall WorkBuddy.exe`
- **SmartScreen**：首次双击安装包弹 SmartScreen → 更多信息 → 仍要运行（未签名正常行为）

### 2.7 已知未完成项 / 技术债
- 应用图标为 electron 默认（未提供自定义图标，待用户江泽锋后续替换）
- 未代码签名（无证书，SmartScreen 须手动绕过）
- signAndEditExecutable:false（未签名项目的必要规避；若未来签名需移除并配证书）
- 阶段 1–5 累积 GUI 运行时未验项：须在 GLM 终审干净 Win11 实跑时补验（测试连接三态 / 导入导出 round-trip / 真实 LLM 全链路 / 六类错误态 / 布局像素）

### 2.8 阻塞与需决策的问题
无。可交 GLM 终审。

### 2.9 用户反馈1 修复与重新打包（2026-08-07 追加）
- **根因**：`settingsRepo.seedDefaults()` 内动态 `require('./defaultSettings')` 在 electron-vite SSR bundle 中解析失败 → `Cannot find module` → `createWindow()` 从未执行（潜伏 6 阶段，headless 未实跑所致）。
- **主修复**：`src/main/db/repositories/settingsRepo.ts` — 动态 require → 静态 import `getDefaultSettings`。
- **加固**：`src/main/index.ts` 启动序列 try/catch + `src/main/window.ts` did-fail-load 日志+兜底 show + grep 排查无其他动态 require。
- **GLM 复审（05）**：✅ 运行时验证通过（主进程到 `WorkBuddy ready`，全新 DB settings 表 = 8 行，打包版 loadFile 无 did-fail-load，无 CSP 违规）。详见 `审查日志/阶段6-复审1-用户反馈1.md`。
- **⚠️ DeepSeek `#49034` 误诊已纠正**：复跑受阻非 Electron 上游 bug，实为 headless 环境 `ELECTRON_RUN_AS_NODE=1`（GLM 复审 §4）；用户无需重装依赖。
- **重新打包产物**：
  | 产物 | 时间 | 大小 | 状态 |
  |---|---|---|---|
  | `dist/win-unpacked/WorkBuddy.exe` | 2026-08-07 00:45 | ~181 MB | ✅ 含修复，GLM 实测启动正常 |
  | `dist/WorkBuddy-Setup-0.1.0.exe` | 2026-08-07 00:47 | ~88 MB | ✅ 含修复（asar 已确认），**待用户安装** |
- **下一步（用户）**：双击新版 `WorkBuddy-Setup-0.1.0.exe` → SmartScreen「仍要运行」→ 安装 → 双击桌面图标 → 窗口应打开。开窗成功 = 反馈1 闭环 → 继续阶段 6 §7 完整 E2E 清单。
- **可选收敛（🔵 LOW）**：`window.ts` did-fail-load 加 `isMainFrame` 守卫；`package.json` 补 `author`（见 `审查日志/阶段6-复审1-用户反馈1.md` R1/R2）。

---

## 3. 验收点自检表（DeepSeek 勾选）
- [x] pack:win 产出 dist/win-unpacked/WorkBuddy.exe（181 MB）——双击可启动？**headless 无法实跑 GUI，路径/文件确认 ✓**
- [x] dist:win 产出 dist/WorkBuddy-Setup-0.1.0.exe（85 MB，NSIS x64）
- [x] asar 内 better-sqlite3 .node 已解包（在 app.asar.unpacked，非 app.asar 内）
- [x] 安装包 per-user（nsis perMachine:false，安装向导可选目录）
- [~] 冷启动 <3s / 晨间 <5s / 内存 <150MB（GUI 实跑待验，headless 无法）
- [~] 全流程 E2E §8.4（GUI 实跑待验）
- [~] 断网/错 Key 降级 §7-C（GUI 实跑待验）
- [~] DB 密文 / 日志无明文 §8.6（DB 密文链路阶段 1–5 已验；packaged logger console 无终端 → 静默）
- [x] DB 路径 = %APPDATA%/workbuddy/（productName 未入 package.json，app.getName()=workbuddy 不变）
- [x] 单实例锁 + 生产加固（CSP / Menu(null) / devtools-close）代码存在
- [x] 无硬编码密钥（grep 无明文 key；safeStorage 链路不变）
- [x] 安装说明.md 存在且含 SmartScreen 绕过
- [x] npm run build 绿，零 TS 错误

## 4. 禁止边界自检（DeepSeek 勾选）
- [x] 无硬编码密钥（safeStorage 链路不变；安装包内无明文 key）
- [x] 日志无业务明文/API Key（logger redact 兜底；packaged console 无终端）
- [x] 无业务数据上传云端（DB 本地 userData；RSS/LLM 出站为用户自配端点）
- [x] 软删除（不改数据行为）
- [x] 无阻断式模态弹窗（应用内不变，安装向导为 OS 原生 NSIS）
- [x] 未签名为正常预期（签名现实在安装说明.md 明示）

---

## 5. 审查与修复记录
| 轮次 | 类型 | 结论 | 日期 | 记录文件 |
|---|---|---|---|---|
| 1 | 首次审查 | 🟡 需修复（F1-F5） | 2026-08-05 | `审查日志/阶段1.md` |
| 2 | 修复 | F1-F5 全部修复 | 2026-08-05 | `项目日志/阶段1-修复1.md` |
| 3 | 复审 | ✅ 通过（阶段关闭） | 2026-08-05 | `审查日志/阶段1.md` 复审小节 |
| 4 | 阶段2首次审查 | 🟡 需修复（F1/F1b/F2/F3） | 2026-08-05 | `审查日志/阶段2.md` |
| 5 | 修复 | F1-F3 全部修复 | 2026-08-05 | `项目日志/阶段2-修复1.md` |
| 6 | 复审 | ✅ 通过（阶段关闭） | 2026-08-05 | `审查日志/阶段2.md` 复审小节 |
| 7 | 阶段3首次审查 | 通过 | ✅ 通过 | 2026-08-05 | `审查日志/阶段3.md` |
| 8 | 阶段4首次审查 | 🟡 需修复 | 2026-08-05 | `审查日志/阶段4.md` |
| 9 | 阶段4复审 | ✅ 通过（阶段关闭） | 2026-08-06 | `审查日志/阶段4.md` 复审小节 |
| 10 | 阶段5首次审查 | ✅ 通过（阶段关闭，2 LOW 非阻塞） | 2026-08-06 | `审查日志/阶段5.md` |
| 11 | 阶段6实现完成 | 待 GLM 终审 | 2026-08-06 | `项目日志/阶段6.md` |
| 12 | **阶段6终审** | **✅ 通过（构建+静态）/⚠️M6 待用户实跑 E2E** | **2026-08-06** | `审查日志/阶段6.md` |
| 13 | **用户反馈1 修复** | **✅ 主修复+3加固完成** | **2026-08-07** | `项目日志/用户反馈1-修复1.md` |
| 14 | **反馈1 GLM 复审** | **✅ 通过（运行时验证）** | **2026-08-07** | `审查日志/阶段6-复审1-用户反馈1.md` |
| 15 | 文档治理加固 | ✅ GLM 新建 CR 闭环+索引+CHANGELOG | 2026-08-07 | `项目规范/文档治理总纲.md` 等 |
| 16 | **SSOT 修复（本条）** | **🛠 DeepSeek 执行中** | **2026-08-07** | `审查日志/SSOT-修复指令-2026-08-07.md` |
| 17 | **计划与复盘 子阶段1 首次审查** | **🟡 需修复（F1-F3）** | **2026-08-07** | `审查日志/计划与复盘-子阶段1.md` |
| 18 | **计划与复盘 子阶段1 修复** | **✅ F1-F3 已修复，待 GLM 复审** | **2026-08-07** | `项目日志/计划与复盘-子阶段1-修复1.md`（本文件） |
| 19 | **计划与复盘 子阶段1 复审** | **✅ 通过（子阶段1 关闭）** | **2026-08-07** | `审查日志/计划与复盘-子阶段1.md` §8 |
| 20 | **计划与复盘 子阶段2 实现** | **✅ 通过（1 LOW 非阻塞）** | **2026-08-07** | `审查日志/计划与复盘-子阶段2.md` |
| 21 | **计划与复盘 子阶段3 实现** | **✅ 通过（4 LOW 非阻断）** | **2026-08-07** | `审查日志/计划与复盘-子阶段3.md` |
| 22 | **计划与复盘 子阶段4 实现** | **✅ 通过（3 LOW 非阻断）** | **2026-08-07** | `审查日志/计划与复盘-子阶段4.md` |
| 23 | **用户反馈2 修复** | **✅ D5+E1+U6+周计划 修复完成（vitest 22/22 + build 绿），待 GLM 复审** | **2026-08-07** | `项目日志/用户反馈2-修复1.md` |
| 24 | **用户反馈3 修复** | **✅ F3-1（router-view key=path 消串档）+ F3-2（日历选日期）完成；Electron GUI+CDP 运行时复跑通过；vitest 53/53 + build 绿；待 GLM 复审 + 用户复测** | **2026-08-08** | `项目日志/用户反馈3-修复1.md` |
| 25 | **用户反馈3.1 修复1(F3-1.1)** | **✅ 双根因齐修，GUI 8/8 PASS，vitest 54/54 + build 绿；GLM 05 复审通过；待用户复测** | **2026-08-08** | `项目日志/用户反馈3.1-修复1.md` |
| 26 | **用户反馈3.1 修复2(F3-1.2 幽灵消耗)** | **✅ 方案 C 实现完成：pickTask 移除 eager markTaskConsumed + pendingHide + todo↔weekly 双向同步 + PLAN_MARK_TASK_UNCONSUMED IPC；vitest 54/54 + build 绿；GLM 复审3 通过；待用户复测** | **2026-08-08** | `项目日志/用户反馈3.1-修复2.md` |
| 27 | **用户反馈3.1 修复3(F3-1.3 rail 复现 + 任务错位)** | **✅ 两 UX 缺陷修复：rail 隐藏源改 planDraft 派生 inDraftTexts（删 pendingHide）+ pickTask 插「任务」段（回退末尾+warn）；纯渲染层，方案 C 未动；vitest 54/54 + build 绿 + 纯函数 node 实锤；待 GLM 复审 + 用户复测** | **2026-08-08** | `项目日志/用户反馈3.1-修复3.md` |
| 28 | **用户反馈3.2 修复(F3.2-1 方案B + F3.2-2 Tier2 出处标注)** | **✅ 双问题修复完成：F3.2-1 open orphan 软删/done 留历史 + 文案；F3.2-2 migration 0004 sourcePlanId + labelTodoSource 5 规则 + todos:withSource IPC + TodayView tag（修 UUID 直显 bug）；vitest 67/67 + build 绿 + 0004 存量库实跑；偏差已记（migration 0004 触规格边界）；待 GLM 复审** | **2026-08-09** | `项目日志/用户反馈3.2-修复1.md` |
| 29 | **用户反馈3.2 修复2（GLM 复审阻断项：TS 类型遗漏）** | **✅ 4 处修复：projects.ipc:91 + reminders.ts:18 补 sourcePlanId（GLM 要求）+ 同源第 3 处 quickCreate + 存量 status 窄化（GLM 漏检，达成验收必需）；tsc.node 仅剩 news.ts:22 存量基线 + vitest 67/67 + build 15.23s 绿；待 GLM 复审 2** | **2026-08-09** | `项目日志/用户反馈3.2-修复2.md` |

---

## 6. 各阶段进度总览
| 阶段 | 名称 | 状态 | 结论 |
|---|---|---|---|
| 0 | 视觉地基 | 已完成 | ✅ 通过 (2026-08-05) |
| 1 | 工程地基 | 已完成 | ✅ 通过 (2026-08-05) |
| 2 | 晨间唤醒+热点+今日总览 | 已完成 | ✅ 通过 (2026-08-05) |
| 3 | 项目管理（含 todos） | 已完成 | ✅ 通过 (2026-08-05) |
| 4 | 设置+数据管理 | 已完成 | ✅ 通过 (2026-08-06) |
| 5 | 对话式热点搜索 | 已完成 | ✅ 通过 (2026-08-06) |
| 6 | 打包 .exe | 终审通过 | ✅ 构建/⚠️待用户实跑 E2E (2026-08-06) |

---

## 7. 已知后续改进项（滚动）
- [ ] **今日总览「已完成 x/y」须随勾选动态更新**（阶段 2 实现今日总览时必须响应式）— 来源：用户 2026-08-05 反馈

---

## 8. 计划与复盘体系 · 子阶段 1 基建 — 实现情况

### 8.1 实现概要
实现计划与复盘体系的基建层：`0003_plan_review_governance` 迁移补齐 `plans/templates/reviews` 三表治理字段（isDeleted/deletedAt/updatedAt/createdAt）；`templateRepo`（CRUD + softDelete + seedDefaults）；4 个 template IPC 通道 + preload 桥；默认模板 seed（日计划/日复盘/周复盘）；适度 MarkdownEditor 组件（marked+DOMPurify，编辑+实时预览+GFM 任务复选框+工具栏）；`/plan` `/review` 路由 + 侧边栏入口 + 空壳页（模板选择器+编辑器，保存按钮禁用）

### 8.2 新增/修改文件清单

| 层 | 文件 | 类型 |
|---|---|---|
| shared | `src/shared/types.ts` | 修改（+TemplateType +Template） |
| shared | `src/shared/ipc.ts` | 修改（+TEMPLATES_* 4 通道） |
| main/db | `src/main/db/migrate.ts` | 修改（+init003 注册） |
| main/db | `src/main/db/migrations/0003_plan_review_governance.ts` | **新增** |
| main/db | `src/main/db/repositories/templateRepo.ts` | **新增** |
| main/ipc | `src/main/ipc/index.ts` | 修改（+registerTemplatesIpc） |
| main/ipc | `src/main/ipc/templates.ipc.ts` | **新增** |
| main | `src/main/index.ts` | 修改（+templateRepo.seedDefaults） |
| preload | `src/preload/index.ts` | 修改（+api.templates 桥） |
| renderer | `src/renderer/src/router/index.ts` | 修改（+/plan /review） |
| renderer | `src/renderer/src/components/AppSidebar.vue` | 修改（+计划/复盘 nav） |
| renderer | `src/renderer/src/components/MarkdownEditor.vue` | **新增** |
| renderer | `src/renderer/src/views/PlanView.vue` | **新增** |
| renderer | `src/renderer/src/views/ReviewView.vue` | **新增** |
| 根 | `package.json` | 修改（+marked +dompurify dep） |

### 8.3 关键决策与规格偏差
无——严格按 `规格-子阶段1-基建.md` 实现。

### 8.4 数据库
- **迁移版本**：新增 `0003_plan_review_governance`（version=3），启动时通过 `runMigrations()` 自动升级
- **变更**：ALTER TABLE `plans`/`templates`/`reviews` ADD COLUMN（isDeleted/deletedAt/updatedAt/createdAt）；新增 3 个条件索引（idx_plans_date / idx_reviews_date / idx_templates_type）
- **幂等**：`addColumnIfMissing` 防御性检查 PRAGMA table_info；索引 CREATE IF NOT EXISTS
- **默认模板**：`templateRepo.seedDefaults()` 按固定 id 检查幂等，首启插入 3 条默认模板

### 8.5 自测结果
| 验收点 | 操作 | 结果 |
|---|---|---|
| npm run build 绿 | `npm run build` | ✅ 7.40s，零 TS 错误，PlanView/ReviewView/MarkdownEditor 均正确打包 |
| 模板 IPC 通道类型正确 | TypeScript 编译通过 | ✅ IPC 常量、preload 桥、shared 类型全链路编译通过 |
| 文件不缺不漏 | glob 核对 specs | ✅ 14文件(6新增+8修改)，与 §8.2 清单一致 |

### 8.6 构建与运行说明
- **构建命令**：`npm run build`（或 `pnpm dev`）
- **依赖**：`npm install`（已加 marked/dompurify）
- **启动迁移**：首次 `npm run dev` 后，`__schema_migrations` 将自动执行 version=3，三表补全治理字段
- **验证迁移**：启动后查 `PRAGMA table_info(plans)` → 应有 `isDeleted/deletedAt/updatedAt`

### 8.7 已知未完成项 / 技术债
- marked + dompurify **类型依赖**：marked v15 自带 types，dompurify v3 自带 types；无 @types 需求
- MarkdownEditor 组件**未在子阶段 1 外使用**（子阶段 2 计划 CRUD 和子阶段 3 复盘 CRUD 将复用）
- PlanView/ReviewView 保存按钮 disabled——需子阶段 2/3 激活
- 0003 迁移需在真实 0.1.0 存量库上实跑验证（headless 无法；GLM 审查时在已安装的 0.1.0 环境跑）

### 8.8 阻塞与需决策的问题
无。

### 8.9 GLM 审查 F1-F3 修复（2026-08-07）
| 修复 | 严重度 | 改动 | 文件 |
|---|---|---|---|
| F1 | 🔴HIGH | AppIcon.vue import+map 补齐 10 个图标（ClipboardList/NotebookPen/Bold/Italic/Heading/ListChecks/List/Link/Eye/EyeOff） | `AppIcon.vue:3-13` |
| F2 | 🔵LOW | PlanView/ReviewView 空状态改用 `<EmptyState>` 组件（复用） | `PlanView.vue` / `ReviewView.vue` |
| F3 | 🔵LOW | `seedDefaults` for 循环内每条 try/catch + logger.warn | `templateRepo.ts:133-141` |

**修复后自测**：
| 验收点 | 结果 |
|---|---|
| 新增 10 图标均在 lucide-vue-next 0.447 存在 | ✅（ClipboardList/NotebookPen/Bold/Italic/Heading/ListChecks/List/Link/Eye/EyeOff） |
| EmptyState 导入正确，props 类型匹配 | ✅（icon='Calendar'/'BookOpen' 均在 AppIcon map） |
| seedDefaults per-item try/catch 不改变成功路径行为 | ✅（existing 逻辑不变，单条失败仅 logger.warn 并继续） |

---

## 9. 计划与复盘体系 · 子阶段 2 计划核心 — 实现情况

### 9.1 实现概要
激活「计划」为可用功能：`Plan` 类型入 `@shared/types`；`planParser.parseTasks` 纯函数（正则提取未勾 `- [ ]`）+ **vitest 单测 7 例全绿**（新增 vitest devDep + `"test":"vitest run"`）；`planRepo`（CRUD + softDelete + `generatedTodoIds` JSON 往返 + **幂等 `syncPlanTodos`**：按 content 复用已有 todo 保留完成态、新建增量、残留不软删仅提示、异常保原文返回 synced:false）；6 个 `plan:*` IPC + preload 桥（create/update 先写 plan 再同步，失败 `PARSE_FAILED`）；PlanView 激活（日期选择/按日期加载已有或模板/保存启用 create↔update 分支/内联反馈禁模态/保存 loading 防双击）；TodayView「📝 今日计划」链接 → /plan。

### 9.2 新增/修改文件清单

| 层 | 文件 | 类型 |
|---|---|---|
| shared | `src/shared/types.ts` | 修改（+Plan） |
| shared | `src/shared/ipc.ts` | 修改（+PLAN_GET/LIST/BY_DATE/CREATE/UPDATE/DELETE） |
| main/services | `src/main/services/planParser.ts` | **新增**（parseTasks 纯函数） |
| main/db | `src/main/db/repositories/planRepo.ts` | **新增**（CRUD + syncPlanTodos 幂等） |
| main/ipc | `src/main/ipc/plans.ipc.ts` | **新增**（6 通道） |
| main/ipc | `src/main/ipc/index.ts` | 修改（+registerPlansIpc） |
| preload | `src/preload/index.ts` | 修改（+api.plans 桥） |
| renderer | `src/renderer/src/views/PlanView.vue` | 修改（空壳→激活） |
| renderer | `src/renderer/src/views/TodayView.vue` | 修改（+「📝 今日计划」链接） |
| tests | `tests/planParser.spec.ts` | **新增**（vitest，相对路径 import） |
| 根 | `package.json` | 修改（+vitest devDep + "test" script） |

### 9.3 关键决策与规格偏差
无——严格按 `规格-子阶段2-计划核心.md` 实现。说明：
- `syncPlanTodos` 内 `todoRepo.findByIds` 以 `todoRepo.findById` 逐 id 查 + `isDeleted` 过滤实现（todoRepo 未增方法，避免越界改既有 repo）。
- 同步失败（PARSE_FAILED）时 plan 已持久化：PlanView 回拉 `plan:byDate` 恢复 plan id，UI 显示「⚠️ 已保存，但任务解析失败，已保留原文」。

### 9.4 数据库
- **迁移版本**：3 不变（**无新迁移**）；`plans` 表治理字段子阶段1 `0003` 已补齐
- **数据流**：保存计划 → 先写 plans.content（不丢原文）→ `syncPlanTodos` 解析 `- [ ]` → 复用/新建 todos（`projectId=NULL`、`planDate=plan.date`、`status='todo'`）→ 回填 `generatedTodoIds` → 今日待办卡按 `planDate` 聚合（**无需改聚合逻辑**）

### 9.5 自测结果
| 验收点 | 操作 | 结果 |
|---|---|---|
| parser 单测绿 | `npm test` | ✅ 1 文件 7 用例全过（标准多任务/空/无任务/`- [x]` 不提取/缩进容错/重复行/去空白），545ms |
| 构建绿 | `npm run build` | ✅ 7.28s，零 TS 错误，PlanView/TodayView 正确打包 |
| 新 main 文件无 require | grep `require\(` | ✅ planRepo/plans.ipc/planParser 无匹配（静态 import ✓） |
| PlanView 无硬编码 hex | grep `#[0-9a-fA-F]{3,8}` | ✅ 无匹配（全 var(--*) token） |
| 新 main 文件无 console.log | grep | ✅ 无匹配（logger 统一） |

### 9.6 构建与运行说明
- **单测**：`cd E:\workspace\workbuddy && npm test`（vitest run，零配置）
- **构建/运行**：`npm run build` / `npm run dev`

### 9.7 已知未完成项 / 技术债
- **幂等 4 场景（首次建/同内容不重建/完成态保留/残留不删）与 GUI 流（PlanView 日期切换/保存反馈/TodayView 链接）⏳ 待用户 E2E**——headless 无法实跑 Electron GUI + 真实 DB（同子阶段 1 口径，不谎报运行时结果）。静态逻辑已按规格 §5.3 推演并写码，`planRepo` 的 DB 操作无单测（依赖 electron 运行时，非纯函数，不在 vitest 范围）。
- vitest 仅覆盖纯函数 `parseTasks`；repo/IPC/UI 层覆盖待后续（项目计划 §10 要求本子阶段仅 parser 单测）。

### 9.8 阻塞与需决策的问题
无。可交 GLM 审查。

---

## 10. 计划与复盘体系 · 子阶段 3 复盘核心 — 实现情况

### 10.1 实现概要
激活「复盘」为可用功能 + 打通「执行→复盘」数据环：`Review` 类型入 `@shared/types`；`extractDigest` 纯函数（Markdown→摘要纯文本截断）+ vitest 单测 8 例全绿；`reviewRepo`（CRUD + softDelete + `linkedProjectIds` JSON 往返，**无 syncPlanTodos**——复盘不生成 todos）；6 个 `review:*` IPC + preload 桥；ReviewView 激活（默认昨天/`?date=` 预填/按日期+type 加载已有或模板/保存 create↔update/内联反馈禁模态/防双击）；昨日复盘卡（TodayView b-rev）：扩展 `useToday` 拉昨日日复盘 → 有则摘要+「查看/编辑」，无则 EmptyState+「写复盘」入口。`EmptyState` 新增默认 slot。无新迁移。

### 10.2 新增/修改文件清单

| 层 | 文件 | 类型 |
|---|---|---|
| shared | `src/shared/types.ts` | 修改（+Review） |
| shared | `src/shared/ipc.ts` | 修改（+REVIEW_GET/LIST/BY_DATE/CREATE/UPDATE/DELETE） |
| shared | `src/shared/reviewDigest.ts` | **新增**（extractDigest 纯函数） |
| main/db | `src/main/db/repositories/reviewRepo.ts` | **新增**（CRUD + softDelete + linkedProjectIds JSON 往返；无 sync） |
| main/ipc | `src/main/ipc/reviews.ipc.ts` | **新增**（6 通道；无 runSync） |
| main/ipc | `src/main/ipc/index.ts` | 修改（+registerReviewsIpc） |
| preload | `src/preload/index.ts` | 修改（+api.reviews 桥） |
| renderer | `src/renderer/src/views/ReviewView.vue` | 修改（空壳→激活） |
| renderer | `src/renderer/src/views/TodayView.vue` | 修改（b-rev 卡替换 EmptyState） |
| renderer | `src/renderer/src/composables/useToday.ts` | 修改（+yesterdayReview + yesterday + load） |
| renderer | `src/renderer/src/components/EmptyState.vue` | 修改（+default slot） |
| tests | `tests/reviewDigest.spec.ts` | **新增**（vitest 8 用例） |

### 10.3 关键决策与规格偏差
无——严格按 `规格-子阶段3-复盘核心.md` 实现。小调整：
- `EmptyState` 新增 `<slot />` 支持 b-rev 卡「写复盘」按钮（最小侵入扩展，非规格要求但承载入口）。
- 昨日复盘卡有 review 但 content 为空时显示「昨日已复盘」（与 EmptyState 区分）。

### 10.4 数据库
- **迁移版本**：3 不变（**无新迁移**）；`reviews` 表治理字段子阶段1 `0003` 已补齐
- **数据流**：保存 → 写 reviews 表（content 保原文）→ 完成（不解析、不生成 todos）
- `linkedProjectIds` 列为 `TEXT`，repo 层 `JSON.stringify`/`JSON.parse` 往返，异常容错 `[]`

### 10.5 自测结果
| 验收点 | 操作 | 结果 |
|---|---|---|
| vitest 全绿（reviewDigest 8 + planParser 7） | `npm test` | ✅ 2 files / 15 tests all passed，300ms |
| 构建绿，零 TS 错误 | `npm run build` | ✅ main 64.26 kB / preload 5.76 kB / renderer 完整打包，2.26s |
| 新 main 文件无 require | grep `require(` | ✅ reviewRepo/reviews.ipc/reviewDigest 均无匹配（静态 import ✓） |
| 新 main 文件无 console.log | grep | ✅ reviews.ipc.ts 无匹配（logger 统一） |
| ReviewView 无硬编码 hex | grep `#[0-9a-fA-F]` | ✅ 无匹配（全 var(--*) token） |
| logger 不含 review.content 明文 | 代码审查 | ✅ reviews.ipc.ts 仅记 id/date 计数 |

### 10.6 构建与运行说明
- **单测**：`cd E:\workspace\workbuddy && npm test`（vitest run，含 planParser 7 + reviewDigest 8）
- **构建/运行**：`npm run build` / `npm run dev`

### 10.7 已知未完成项 / 技术债
- **昨日复盘卡 4 场景 GUI 流待用户 E2E**⏳（有昨日复盘显示摘要/无复盘显示 EmptyState/拉取失败降级/点入口跳转预填）——headless 无法实跑 Electron GUI+真实 DB，静态逻辑已按规格 §5.6 推演并写码
- F4 遗留（planRepo orphanTodoIds 含已软删 todo）未修——规格 §10 明确"非本子阶段强制"，留维护期 CR
- reviewRepo 层无单测（依赖 electron 运行时 better-sqlite3，非纯函数，不在 vitest 范围）

### 10.8 阻塞与需决策的问题
无。可交 GLM 审查。

---

## 11. 计划与复盘体系 · 子阶段 4 LLM 增强 — 实现情况

### 11.1 实现概要
给「计划 / 复盘」补上 LLM 增强（用户前台主动触发，离线仍全可用）：`services/llm.ts` 抽取共享 `requestChat()`（配置探测→endpoint 规范化→fetch+AbortController→状态码→ChatError→content，apiKey 永不入日志）+ `parseJsonObject()` 鲁棒解析（剥 ```json 围栏 + 首 `{` 到末 `}`，失败 → null 调用方降级）+ `guidePlan()`（逐占位符引导，逐字 `GUIDE_SYSTEM_PROMPT`，上下文 = 今日待办 + 逾期 + 进行中项目，todo content 截断 80 字、项目仅 name/status/openTasks 不外发 description）+ `summarizeReview()`（日复盘草稿，逐字 `DRAFT_SYSTEM_PROMPT`，无数据不调 LLM 直接占位草稿）。两个 IPC：`plan:guideQuestion`（挂 plans.ipc.ts）/ `review:summarizeDraft`（挂 reviews.ipc.ts），ChatError→err 映射。preload 桥 `api.plans.guideQuestion` / `api.reviews.summarizeDraft`。渲染层两组合式（`usePlanGuide` / `useReviewSummary`，镜像 `useHotspotChat`：checkReady 三件套探测 / LLM_NOT_CONFIGURED 本地降级 / 纯函数回填不持有 editContent）。PlanView「✨ AI 引导」+ ReviewView「✨ AI 汇总」按钮 + 透明提示（未配 → 非阻塞「去设置」）+ 内联面板（loading/红色 `--danger` 错误条+重试/引导逐条插入-跳过/草稿 MarkdownEditor 只读预览+替换-追加-取消，替换前显「将覆盖当前内容」警告）。全 `{{}}` 插值禁 v-html；**不重构 searchNews**（爆炸半径控制）。

### 11.2 新增/修改文件清单

| 层 | 文件 | 类型 |
|---|---|---|
| shared | `src/shared/constants.ts` | 修改（+6 护栏常量） |
| shared | `src/shared/types.ts` | 修改（+PlanGuideItem/PlanGuideResult/ReviewDraftResult） |
| shared | `src/shared/ipc.ts` | 修改（+PLAN_GUIDE/REVIEW_SUMMARIZE） |
| main/db | `src/main/db/repositories/todoRepo.ts` | 修改（+findOpenToday/findCompletedOn 只读） |
| main/db | `src/main/db/repositories/projectRepo.ts` | 修改（+findActive 只读） |
| main | `src/main/services/llm.ts` | 修改（+requestChat/parseJsonObject/buildGuideContext/buildSummaryContext/guidePlan/summarizeReview） |
| main/ipc | `src/main/ipc/plans.ipc.ts` | 修改（+plan:guideQuestion） |
| main/ipc | `src/main/ipc/reviews.ipc.ts` | 修改（+review:summarizeDraft） |
| preload | `src/preload/index.ts` | 修改（+api.plans.guideQuestion / api.reviews.summarizeDraft） |
| renderer | `src/renderer/src/composables/usePlanGuide.ts` | **新增** |
| renderer | `src/renderer/src/composables/useReviewSummary.ts` | **新增** |
| renderer | `src/renderer/src/views/PlanView.vue` | 修改（+AI 引导按钮/透明提示/去设置/内联引导面板） |
| renderer | `src/renderer/src/views/ReviewView.vue` | 修改（+AI 汇总按钮/透明提示/去设置/内联草稿面板） |

### 11.3 关键决策与规格偏差
无——严格按 `规格-子阶段4-LLM增强.md` 实现。说明：
- `parseJsonObject` 未导出（规格 §5.5 定义内部函数；GLM 构造非 JSON 响应验证时经 IPC 实跑可达）。
- 无新表/无迁移；templates/todos/projects/settings 全只读；todoRepo 未新增 `findByIds`（沿用既有 findById）。

### 11.4 数据库
- **迁移版本**：3 不变（**无新迁移**）；本子阶段**不写、不删**任何表（全程只读）。

### 11.5 自测结果
| 验收点 | 操作 | 结果 |
|---|---|---|
| vitest 全绿 | `npm test` | ✅ 2 files / 15 tests all passed（planParser 7 + reviewDigest 8），288ms |
| 构建绿，零 TS 错误 | `npm run build` | ✅ 2.31s 绿，PlanView/ReviewView 正确打包 |
| shared 三件套齐 | 代码核对 | ✅ constants +6 / types +3 / ipc +2（plan:guideQuestion / review:summarizeDraft） |
| searchNews 未被改动 | 代码核对 | ✅ 仅 imports 顶部追加 + 文件尾部追加新函数，searchNews 函数体零改动 |
| 无硬编码 hex | grep `#[0-9a-fA-F]{3,6}|rgba?\(` 新增/变动文件 | ✅ 零匹配（全 var(--*) token，红色条 --danger/--danger-soft） |
| 禁 v-html | grep views/composables | ✅ 仅注释含「v-html」字样，无实际使用 |
| 日志无明文 | 代码审查 | ✅ requestChat 不传 apiKey 入 logger；成功仅记 latency/items.length/draft.length；错误仅 ChatError.code（logger.redact 兜底） |
| 私有数据外发最小化 | 代码审查 | ✅ 仅 todos.content（截断 80 字）+ projects name/status/openTasks；不触 description/notes/inboxes/reviews |
| 新 main 文件无 require | grep `require(` | ✅ llm.ts 全部静态 import（新增部分无 require） |
| 未配 LLM 不发包 | 代码路径核对 | ✅ 组合式 checkReady 三件套为空 → llmReady=false → 按钮 disabled + 「去设置」，guide/summarize 首行 llmReady 守卫 |

### 11.6 构建与运行说明
- **单测**：`cd E:\workspace\workbuddy && npm test`
- **构建/运行**：`npm run build` / `npm run dev`
- **LLM 配置**：无新增必需 env；沿用设置页 `llmBaseUrl`/`llmModel`/`llmApiKey`（密文存储，主进程内自动解密）
- **复测命令（GLM/用户）**：未配 → 两 AI 按钮 disabled + 去设置可跳 /settings；已配 → 计划页 AI 引导逐条插入/跳过、复盘页 AI 汇总替换/追加/取消；断网/错 Key → 红色状态条 + 重试（单次不重试）

### 11.7 已知未完成项 / 技术债
- **已配 LLM 全链路 GUI 流待用户 E2E**⏳（引导逐条采纳、汇总替换/追加、真实端点）——headless 无法实跑 Electron GUI + 真实 LLM 端点；静态逻辑已按规格 §5 推演并写码
- 组合式/repo 层无单测（依赖 electron 运行时，非纯函数，不在 vitest 范围；规格 §8 任务拆解未列单测项）

### 11.8 阻塞与需决策的问题
无。可交 GLM 审查。

---

## 12. 用户反馈2 修复（2026-08-07，DeepSeek 04）

### 12.1 修复清单
| 子项 | 严重度 | 改动 | 文件 |
|---|---|---|---|
| D5 | 🟡 | AppIcon 补注册 `Pencil`/`Trash2` + grep 补齐 `Download`/`Upload`（SettingsView 导入导出同漏） | `src/renderer/src/components/AppIcon.vue` |
| E1 | 🟡 | `testConnection` 成功分支加 `parseTestBody` 响应体校验（须 JSON + `choices[0].message.content` 字符串），非有效 AI 响应 → `{ok:false, '该地址未返回有效的 AI 响应…'}`；401/超时/不记 apiKey 保留 | `src/main/services/llm.ts` |
| U6 | 🟡 | `summarizeReview` 短路 `done.length===0 && projects.length===0` → `done.length===0`（当日无完成即占位，不调 LLM） | `src/main/services/llm.ts` |
| P2 | 🔵 | `DEFAULT_TEMPLATES` 增第 4 条 `weekly_plan`（id=`tpl-default-weekly-plan`，内容逐字按反馈 §2.4）；seedDefaults 幂等，存量库重启自动补 | `src/main/db/repositories/templateRepo.ts` |

### 12.2 自测结果
| 验收 | 结果 |
|---|---|
| vitest 全绿 | ✅ 3 files / 22 tests（15 存量 + 7 新增回归：E1 五态真实 HTTP server、U6 双场景 fetch spy） |
| build 绿、零 TS 错 | ✅ 2.29s |
| 全渲染层图标注册核验 | ✅ 静态 32 处 + 动态（AppSidebar/MarkdownEditor/EmptyState）逐一核对，无其他漏注 |
| E1 复跑（用户场景） | ✅ 200 HTML（baidu 型 soft-404）→ 失败；有效 choices → 成功+延迟；401 → 既有分支 |
| U6 复跑（用户场景） | ✅ 有进行中项目 + 无完成任务日期 → 占位草稿、fetch 零调用；有完成 → 走 LLM |

### 12.3 诚实口径（headless 待验）
D5 图标可见性/删除二次确认/行内编辑态、P2 存量库 seed 实跑 → 需 Electron 运行时，待 GLM 复审（05）与用户复测（复测清单见 `项目日志/用户反馈2-修复1.md` 末节）。

---

## 13. 计划与复盘体系 · 子阶段 5 规划体系 — 实现情况

### 13.1 实现概要
计划+复盘 旧双页（PlanView/ReviewView）合并为三级统一「规划」页（`/planning/:level`，日/周/月）：每级 = 复盘面板（ReviewPanel，引用**本期** curStart）+ 规划面板（PlanPanel，引用**下期** nextStart），视觉权重按角色翻转（daily 规划为主 / 周月复盘为主，CSS order 实现）。级联任务下沉：CascadeRail 展示上级 plan 任务（daily→本周 weekly_plan，weekly→本月 monthly_plan），「选取」经 `plan:markTaskConsumed` 回写上级 `[ ]`→`[x]`（幂等）并插入本级草稿（行内去重）。runSync 门控：仅 `daily_plan` 生成待办；周/月计划保存返回 generatedCount:0。每日复盘 = 确定性轻量统计（不调 AI）；周/月复盘 = 主进程聚合（`todos:findInRange` 范围查询 + 完成率/逐周柱状图/下级复盘回顾）+ LLM 生成 Markdown。自动周期提醒：周日 `📌 做周统筹 · 第 N 周`、月末 `📌 做月指导 · YYYY 年 M 月`，幂等（当日重复检查）。MonthlyCharts 确定性图表（周柱 + 完成率填充 + 完成环）。旧路由 `/plan`、`/review` 重定向 `/planning/daily`；旧视图/组合式（usePlanGuide/useReviewSummary）已删除；TodayView 移除昨日复盘卡，今日计划链接改指 `/planning/daily`，周期提醒待办行带「前往」直达按钮。

### 13.2 IPC 通道（新增 6 个，含 1 个规格授权实现选择）
| 通道 | 常量 | 说明 |
|---|---|---|
| `plan:getByPeriod` | PLAN_GET_BY_PERIOD | 按 type+date（期起始）取计划，无则 null |
| `plan:listTasksByPeriod` | PLAN_LIST_TASKS_BY_PERIOD | parseTasksWithState：`{planId, tasks:[{text,consumed}]}`，级联侧栏数据源 |
| `plan:markTaskConsumed` | PLAN_MARK_TASK_CONSUMED | 上级 plan 任务 `[ ]`→`[x]` 回写；无匹配幂等 no-op |
| `review:summarizeWeekly` | REVIEW_SUMMARIZE_WEEKLY | 周复盘：聚合 + LLM → Markdown（stripFence，截 6000 字） |
| `review:summarizeMonthly` | REVIEW_SUMMARIZE_MONTHLY | 月复盘：同范式聚合 + LLM → Markdown |
| `todos:findInRange` | TODOS_FIND_IN_RANGE | **规格 §5.6 授权 DeepSeek 选择**：月图表确定性数据源（主进程范围查询 + 渲染层分组），替代按日 N 次调用 |

### 13.3 文件清单
| 层 | 文件 | 类型 |
|---|---|---|
| shared | `src/shared/period.ts` | 修改（+periodStartFor/nextPeriodStart/addDays/periodLabel/getWeekStart/getWeekRange/getMonthStart/getMonthRange/isoWeekOf，全 UTC） |
| shared | `src/shared/types.ts` | 修改（+PlanTasksByPeriodResult/PlanSummaryResult/ReviewSummaryResult；TemplateType 6 值语义扩展） |
| shared | `src/shared/constants.ts` | 修改（+6 护栏常量，见 13.4） |
| shared | `src/shared/ipc.ts` | 修改（+上述 6 通道） |
| main | `src/main/services/llm.ts` | 修改（+summarizeWeeklyReview/summarizeMonthlyReview；修复 getMonthRange 缺失导入） |
| main | `src/main/services/reminders.ts` | **新增**（ensurePeriodicReminders 幂等周期提醒） |
| main | `src/main/db/repositories/todoRepo.ts` | 修改（+findAllInRange 只读） |
| main | `src/main/ipc/plans.ipc.ts` | 修改（+3 通道；runSync 门控） |
| main | `src/main/ipc/reviews.ipc.ts` | 修改（+2 通道） |
| main | `src/main/ipc/todos.ipc.ts` | 修改（+findInRange） |
| main | `src/main/index.ts` | 修改（seedDefaults 后 try/catch 调 ensurePeriodicReminders） |
| preload | `src/preload/index.ts` | 修改（+6 桥方法） |
| renderer | `src/renderer/src/composables/usePlanning.ts` | **新增**（合并扩展 usePlanGuide/useReviewSummary；级联 pickTask） |
| renderer | `src/renderer/src/components/planning/CompletionRing.vue` | **新增** |
| renderer | `src/renderer/src/components/planning/CascadeRail.vue` | **新增** |
| renderer | `src/renderer/src/components/planning/MonthlyCharts.vue` | **新增** |
| renderer | `src/renderer/src/components/planning/ReviewPanel.vue` | **新增** |
| renderer | `src/renderer/src/components/planning/PlanPanel.vue` | **新增** |
| renderer | `src/renderer/src/views/PlanningView.vue` | **新增**（`/planning/:level` props:true） |
| renderer | `src/renderer/src/router/index.ts` | 修改（+/planning/:level；/plan、/review 重定向） |
| renderer | `src/renderer/src/components/AppSidebar.vue` | 修改（可展开「规划」树：日/周/月） |
| renderer | `src/renderer/src/views/TodayView.vue` | 修改（移昨日复盘卡；计划链接→/planning/daily；提醒「前往」） |
| renderer | `src/renderer/src/composables/useToday.ts` | 修改（移除 yesterdayReview 加载） |
| renderer | 删除：PlanView.vue / ReviewView.vue / usePlanGuide.ts / useReviewSummary.ts | 已确认仅旧视图+旧路由引用 |
| 测试 | `tests/period.spec.ts` | **新增**（26 用例 ≥10 达标） |
| 测试 | `tests/planParser.spec.ts` | 修改（+parseTasksWithState 12 用例） |

### 13.4 护栏常量（shared/constants.ts）
`LLM_WEEK_REVIEW_MAX_TOKENS=1600`、`LLM_MONTH_REVIEW_MAX_TOKENS=1600`、`LLM_REVIEW_TODO_LIMIT=80`、`LLM_REVIEW_TODO_CHARS=60`、`LLM_REVIEW_SUB_LIMIT=8`、`LLM_REVIEW_SUB_CHARS=400`

### 13.5 关键决策与规格偏差
| 项 | 规格 | 实际 | 原因 |
|---|---|---|---|
| 月图表数据源 | 「主进程范围查询或前端逐日分组——DeepSeek 选」 | `todos:findInRange` 一次范围查询 + 渲染层按周分组 | 确定性（避免 N 次 IPC）；规格 §5.6 显式授权 |
| GoalScorecard | §5.6 目标对照表组件 | **不创建**；ReviewPanel 上半 = CompletionRing + 统计，下半 = AI Markdown 文档（含目标对照表文本） | 规格 §5.6 自注「简化：ReviewPanel 上半 = CompletionRing…下半 = AI Markdown 文档」 |
| 周/月计划任务 | — | 不生成待办（runSync 门控返回 0/0），仅作级联源头 | 规格 §4 任务级联定义 |
| 级联反向同步 | — | 无（markTaskConsumed 单向往上级；已完成状态不下行） | 规格未要求反向；点选时 refresh 重载 |

### 13.6 数据库
- **迁移版本**：3 不变（**无新表/无新迁移**）；方案 A 类型语义扩展 + 只读查询。

### 13.7 自测结果
| 验收点 | 操作 | 结果 |
|---|---|---|
| vitest 全绿 | `npm test` | ✅ 4 files / **53 tests**（period 26 ≥10 达标 + planParser 12 parseTasksWithState + 存量 15） |
| 构建绿 | `npm run build` | ✅ 13.69s 绿，PlanningView 正确打包 |
| tsc 主进程零新错 | `npx tsc -p tsconfig.node.json` | ✅ 新代码 0 错；仅存 2 条基线错（projects.ipc.ts:38 类型漂移、news.ts:22 providers 字面量，均非本子阶段文件） |
| runSync 门控 | 代码核对 plans.ipc.ts:21-32 | ✅ 非 daily_plan → `{plan, 0, 0}` 不调 syncPlanTodos |
| markTaskConsumed 幂等 | 代码核对 plans.ipc.ts:141-165 | ✅ 无匹配（已 [x]/不存在）→ ok(plan) 原样返回 |
| 路由重定向 | 代码核对 router/index.ts | ✅ /plan、/review → /planning/daily |
| 未配 LLM 禁用 | 代码核对 | ✅ PlanPanel/ReviewPanel 按钮 `:disabled=!llmReady` + 「去设置」；guide/summarize 首行 LLM_NOT_CONFIGURED 短路（零 IPC/零网络） |
| 无硬编码 hex | grep 新增/变动渲染文件 | ✅ 零匹配（全 var(--*) token） |
| 禁 v-html | grep 新视图/组件 | ✅ 仅注释含字样；复盘 Markdown 全经 MarkdownEditor 处理 |
| 图标注册 | 16 个 AppIcon 名核对 AppIcon.vue 注册集 | ✅ 全注册（Calendar/RefreshCw/ChevronLeft/ChevronRight/Loader/Sparkles/AlertCircle/X/Check 等） |
| 级联回写 | 代码核对 usePlanning.pickTask | ✅ 去重插入 + markTaskConsumed + reload parentTasks |
| 自动提醒 | 代码核对 main/index.ts:63 | ✅ seedDefaults 后 try/catch + logger.warn；reminders 幂等 findByPlanDate |

### 13.8 透明性与运行说明
- **私有数据外发**：仅用户主动点「AI 周/月复盘」时，主进程向用户自配 `llmBaseUrl` 发送聚合上下文（完成/未完成任务文本截断 60 字、至多 80 条、下级复盘截断 400 字至多 8 条）；Daily 复盘确定性生成**零外发**；logger 只记 latency/长度/条数（redact 兜底）。
- **运行命令**：单测 `npm test`；构建 `npm run build`；运行 `npm run dev`（better-sqlite3 若报错：`npm run rebuild`）。
- **复测命令（GLM/用户）**：侧栏「规划」展开三级；`/plan`、`/review` 自动跳日规划；日规划保存→今日待办（生成 N 条反馈），周/月规划保存→提示「不生成待办」；日规划页右侧「本周周任务」点选取→上级周计划任务变 [x]+本级插入；周/月复盘生成→Markdown 草稿替换/追加/取消；未配 LLM→生成按钮 disabled+去设置；周日/月末 TodayView 出现周期提醒待办+「前往」直达。

### 13.9 已知未完成项 / 技术债
- 三级页面 GUI 实跑 + 真实 LLM 周/月复盘全链路待用户 E2E（headless 无法实跑 Electron GUI）。
- 级联无反向同步（点选后上级仅标记 consumed；未完成上级任务不自动滚入下级）。
- `todos:findInRange` 未加 limit（月数据量级小，无性能顾虑）。

---

## 14. 用户反馈3 修复（2026-08-08，DeepSeek 04）

### 14.1 修复清单
| 子项 | 严重度 | 改动 | 文件 |
|---|---|---|---|
| F3-1 日规划串档 | 🔴 HIGH | `<router-view />` → `<router-view :key="route.path" />`（`useRoute`；**path 非 fullPath**——翻期 date 在 query，path 不变不重建、不丢草稿）。根因：vue-router 复用 `/planning/:level` 组件不重跑 setup，PlanningView.vue:26-28 的 `plan`/`planType`/`reviewType` 冻结在首次 level | `src/renderer/src/components/AppLayout.vue` |
| F3-2 日历选日期 | 🔵 LOW | period-nav 加日历按钮（Calendar 图标）+ 隐藏原生 `<input type="date">`（视觉隐藏，非 display:none）+ `showPicker()`；change → `periodStartFor(level, v)` 归一化，三级通用；token 样式无 hex | `src/renderer/src/views/PlanningView.vue` |
| F3-3 浏览器在哪 | — | 不改代码（GLM 指南措辞错，Electron 无地址栏；DevTools Console 测重定向） | — |

### 14.2 验证结果（含真实 Electron GUI 运行时复跑）
| 验收 | 结果 |
|---|---|
| vitest | ✅ 4 files / 53 tests 全绿（存量，未改测试） |
| build | ✅ 绿（2.43s）；tsc 新代码 0 错（存量 .vue 声明/useProjectDetail 旧错非本次引入） |
| **F3-1 运行时**（独立实例 + 隔离 userData 播种周任务A/B、月任务X/Y + CDP 驱动） | ✅ 周统筹→日规划→月指导→日规划：模板下拉 周计划/日计划/月指导、CascadeRail 标题 本月月任务/本周周任务/(无)、rail 内容 月任务X/Y、周任务A/B 各级正确——修前「日规划显示周计划模板+周复盘+空 rail」不复现 |
| 场景 D 点选回写（顺带核） | ✅ 选取 → 规划编辑器插入 `- [ ] 周任务A/B` + 上级 `[ ]→[x]`，重挂载后仍「已下沉」（持久化） |
| F3-2 运行时 | ✅ 日历按钮 → input 绑定当前期；change 2026-08-01 → periodLabel 跳期 + URL `#/planning/daily?date=2026-08-01` |
| console | ✅ 全流程零 console error（唯一 showPicker NotAllowedError 为 CDP 注入无用户手势伪影，真实点击不受影响） |

### 14.3 用户复测所需信息
1. `npm run dev` 开窗 → 侧栏「规划」先开**周统筹**（或月指导）→ 再点**日规划**：应显示 日计划模板 + 本周周任务侧栏（非空，当本周有周计划时）+ 日复盘——**不再串档**。
2. 反复切 日/周/月，每级内容不串；日规划内 `‹ ›` 翻期不丢草稿。
3. 日历图标 → 原生日期选择器 → 选日 → 期标签跳到所在期（周/月归一化）。
4. 重测原失败场景 C/D/E：日规划确定性回顾、级联点选回写、runSync 门控（日规划保存生成待办，周/月不生成）。

---

## 15. 用户反馈3.1 修复（2026-08-08，DeepSeek 04）

### 15.1 修复清单
| 子项 | 严重度 | 改动 | 文件 |
|---|---|---|---|
| 根因 C（DS 新发现，GLM 未诊断） | 🔴 HIGH | `periodStartFor` 非法 date 兜底 `return todayStr()` → **递归归一化**（daily→今天 / weekly→周一 / monthly→1号）。侧栏无 date query 进周统筹曾产生 curStart=周六 → 保存 8.15 周六（用户 DB 实锤），rail 按周一键永远取不到 | `src/shared/period.ts` + `tests/period.spec.ts`（+1 例回归） |
| 根因 A（GLM 方向1） | 🔴 HIGH | `loadParentTasks`/`pickTask` 基准 `nextStart`（明日）→ `curStart`（当前查看期）；JSDoc 同步 | `src/renderer/src/views/PlanningView.vue` + `usePlanning.ts` |
| 业务规则 3（GLM §5 验证点） | 🔴 HIGH | CascadeRail consumed 任务**隐藏**（修前显示「已下沉」标签，与用户「不再看到」不符） | `src/renderer/src/components/planning/CascadeRail.vue` |
| 根因 B（方向2） | — | **不改**：保持「规划下周」§5.3，UI 已标注周次；备选「本周/下周」切换待用户确认另 CR | — |
| 测试4-E 待办归期 | 🟡 | **核实非 bug**：syncPlanTodos `planDate = plan.date`（明日），今日总览精确过滤 → 次日才进今日总览 = 用户隐含预期；向用户解释 | — |
| 测试3 草稿重置 | — | 不改代码，确认 load() 仍重置 planDraft（未保存切换自动重置 = 用户期望） | — |
| 出处标注 | 🔵 | **出方案不实现**：推断链（📌前缀→周期提醒 / projectId→项目任务 / templateId→日规划任务 / 其余→手动添加）+ 交接区「顺延自 date」+ UI tag；零迁移；粒度待用户确认 | — |

### 15.2 验证结果
| 验收 | 结果 |
|---|---|
| vitest | ✅ 5 files / 54 tests（53 存量 + 1 新增根因 C 回归） |
| build | ✅ 2.36s 绿，零 TS 错误 |
| **GUI 复跑（真实 Electron+CDP，隔离 userData 播种 W33 周任务 任务1/2/3，贴近用户业务规则序列）** | ✅ **8/8 PASS**：R1 8.11 rail=任务1/2/3 → R2 选取任务1 编辑器插入+上级 [x] 持久化 → R3 8.12 rail=任务2/3（任务1 隐藏）→ R4 8.9（W32）rail 空不串周 → C1 无 date query 进周统筹 curStart=8.3（第 32 周）→ C2 保存落 8.10 非 8.15 |
| 用户真实 DB | ✅ 只读核查（ELECTRON_RUN_AS_NODE 模式），未动任何数据；8.15 为修复前遗留脏值，修复后不再产生 |

### 15.3 偏差/取舍
| 项 | 取舍 | 原因 |
|---|---|---|
| 方向2 周统筹默认本周 | 不改，保持「规划下周」+ UI 标注 | 规格 §5.3 原设计；用户业务规则=周日制定下周，方向1+根因C 修复后语义闭环；改默认=规格变更需用户确认 |
| 用户 DB 8.15 脏记录 | 不删不改 | 用户数据只读；如用户需清：周统筹打开 2026-08-15 期删除 |
| 出处标注 | 方案先行不实现 | GLM 要求出方案；待复审+用户确认粒度后实现 |

### 15.4 待 GLM 复审点
period.ts 兜底递归归一化 / curStart 基准 / CascadeRail consumed 隐藏 / 出处标注方案 / 测试4-E 非 bug 结论。

---

## 16. 用户反馈3.1 §7 F3-1.2 幽灵消耗修复（2026-08-08，DeepSeek 04）

### 16.1 修复清单（方案 C：消耗跟随 todo 完成，非选取）
| # | 要求 | 文件 |
|---|---|---|
| 1 | pickTask 移除 eager markTaskConsumed + 新增 pendingHide | `usePlanning.ts` |
| 2 | CascadeRail 加 pendingHide prop + activeTasks filter | `CascadeRail.vue` |
| 3 | load()/丢弃 清空 pendingHide（clearPendingHide） | `usePlanning.ts` + `PlanningView.vue` |
| 4 | 保存不碰 weekly（现状） | 不改 |
| 5 | todo→weekly 正向同步（TODOS_TOGGLE: done→weekly `[x]`） | `todos.ipc.ts` |
| 6 | PLAN_MARK_TASK_UNCONSUMED IPC（`[x]→[ ]` 幂等） | `shared/ipc.ts` + `plans.ipc.ts` + `preload/index.ts` |
| 7 | todoRepo.findByContentInRange（同名已完成 todo 周范围查询） | `todoRepo.ts` |
| 8 | todos toggle 取消勾选反向同步（查同名→有保持[x]/无回[ ]） | `todos.ipc.ts` |

### 16.2 验证结果
| 验收 | 结果 |
|---|---|
| vitest | ✅ 4 files / 54 tests 全绿 |
| build | ✅ 12.46s 绿，零 TS 错误 |

### 16.3 规约语义变更（方案 C）
消耗触发由「选取/下沉」（规格 §5.3 + §F3-1.1 规则2 实现）改为「todo 完成」。
- **C 下新行为**：周任务**保存但未完成期间，在所有日 rail 仍可见、可被多日选取**，直到某天完成才消失。
- **边界**：文本匹配漂移（同 syncPlanTodos 既有先例）；手动建同名 todo 完成→标 weekly；monthly 级联 out-of-scope；日计划自身 `[ ]` 不随 todo 完成回写（既有缺陷，另行立案）。

### 16.4 待 GLM 复审点
pickTask 移除 markTaskConsumed / pendingHide 乐观隐藏 + 清空 / todo↔weekly 双向同步（含多日同名去重）/ PLAN_MARK_TASK_UNCONSUMED IPC / todoRepo.findByContentInRange

## 17. 用户反馈3.1 §9 F3-1.3 两 UX 缺陷修复（2026-08-08，DeepSeek 04）

### 17.1 修复清单（方案 C 核心不动，仅细化）
| # | 缺陷 | 改动 | 文件 |
|---|---|---|---|
| A1 | rail 隐藏条件 `!consumed && !pendingHide.has` → `!consumed && !inDraftTexts.has` | prop `pendingHide` → `inDraftTexts`；activeTasks 过滤改派生集 | `CascadeRail.vue` |
| A2 | inDraftTexts = planDraft 任务文本集 | 新增纯函数 `extractTaskTexts(content): Set<string>`（复用 TASK_LINE_RE，与 planParser TASK_STATE_RE 同形） | `usePlanning.ts` |
| A3 | 删 pendingHide/clearPendingHide；PlanningView 改 `:in-draft-texts` | 删 ref/方法/返回项；load() 内 clearPendingHide 调用删除；新增 `inDraftTexts` computed | `usePlanning.ts` + `PlanningView.vue` |
| B1 | pickTask 末尾盲追加 → 插「任务」段 | 新增纯函数 `insertTaskLine`（段内末个任务行后/段尾；无段回退末尾+warn） | `usePlanning.ts` |
| B2 | 三级通用 | 标题匹配 `^#{2,}\s` + includes('任务')，三模板标题均命中 | `usePlanning.ts` |

### 17.2 验证结果
| 验收 | 结果 |
|---|---|
| vitest | ✅ 4 files / 54 tests 全绿（存量全过，未新增/改动测试） |
| build | ✅ 2.38s 绿，零 TS 错误 |
| insertTaskLine 纯函数 | ✅ node 直跑 6 断言 PASS（插入位置/依次流入/周模板/无段回退/段内无任务行；1 断言为脚本自身标记失误非实现缺陷） |
| pendingHide 清理 | ✅ grep 全 src 代码 0 残留 |

### 17.3 关键决策/取舍
- **rail 隐藏源派生化**（GLM §9 指定）：planDraft 内容即真相，覆盖选取/保存/丢弃全场景，删除会话态 pendingHide 无状态残留。
- **renderer 正则复用既有 TASK_LINE_RE**：不跨进程 import main 侧 planParser，不新增 shared 导出（缩小改动面）。
- **fallback 用 console.warn**：renderer 无 logger 工具；GLM 要求 warn 可见（错误路径容错日志，非 debug 噪音）。
- **方案 C todo↔weekly 同步未动**：§9 明确约束；§2.5 追加2 验收不回归。
- 范围外（§9 未要求，未动）：monthly 级联、daily plan `[ ]` 不随 todo 完成回写（既有缺陷，另行立案）、W33 历史脏值。

### 17.4 待 GLM 复审点
extractTaskTexts 派生隐藏（选/存/弃全场景）/ insertTaskLine 段定位与回退（模板容错）/ pendingHide 清理完整性 / 方案 C 双向同步未回归 / 渲染层 console.warn 合理性

## 18. 用户反馈3.2 修复（2026-08-09，DeepSeek 04）
> 依据：`用户最终测试反馈/用户反馈3.2.md` §3 定稿（F3.2-1 方案B / F3.2-2 Tier2）；完整记录 `项目日志/用户反馈3.2-修复1.md`。

### 18.1 修复清单
| 问题 | 方案 | 关键改动 |
|---|---|---|
| F3.2-1 日规划删行不传播 | **方案B**（用户定稿）：open orphan 软删 / done 保留历史 | `planRepo.syncPlanTodos` 分流（removedOpenCount/keptDoneCount）；done orphan 留 `generatedTodoIds`（复用池持续命中 → 加回同文本行保留完成态）；plans.ipc 透传计数；PlanningView 分流文案 |
| F3.2-2 出处标注 | **Tier2**（用户定稿）：数据层 + 5 规则纯函数 + tag UI | migration `0004`（todos.sourcePlanId）；todoRepo create/update 支持；syncPlanTodos 新建写 sourcePlanId；`labelTodoSource` 纯函数（📌→周期提醒 / 项目·{name} / 日规划·{date} / 顺延·{date} / 手动·{date\|无日期}，删除自动降级）；`todos:withSource` 批量 IPC（batch 解析 projectNames/planDates）；TodayView tag 替代 projectId UUID 直显 |

### 18.2 验证
| 验收 | 结果 |
|---|---|
| vitest | ✅ 6 files / **67 tests**（54 存量 + planSync 4 + todoSource 9） |
| build | ✅ 2.37s 绿，零 TS 错误 |
| 0004 存量库实跑 | ✅ 列已应用（幂等）；migrations 1-3 + 27 active todos 完好；存量 withSrc=0 不迁移历史 |

### 18.3 偏差/取舍
- **migration 0004 触「无 migration」规格边界**：多轮「3 不变」后首次加迁移；F3.2-2 Tier2 定稿要求 sourcePlanId 数据基础，**用户已定稿**，规格语义变更已记。
- **done orphan 保留 generatedTodoIds**：复用池 `!isDeleted` 命中语义（回归③ 前提），非历史残留。
- **keptDoneCount 每轮重复提示**：done orphan 不删故持续计入；提示为信息性非阻断（方案B 定稿语义）。
- **存量 27 todo sourcePlanId 全 null**：不迁移历史（不猜测出处），降级规则（手动/顺延）兜底。
- **node:sqlite 测试 mock**：better-sqlite3 ABI（Electron 130 vs node 127）不兼容 → vi.mock connection 用 node:sqlite DatabaseSync + 兼容包装，测试跑真实 repo SQL（in-memory）。
- **period.spec.ts L92 断言修复**：既存 flaky（UTC vs 本地日期跨午夜），改本地日期计算与实现对齐（非功能改动）。
- **DB 用户数据只读**：W33 buggy `[x]` 不自动清理（既有约束）。

### 18.4 待 GLM 复审点
orphan 分流（todo 软删/done 留 + 复用池语义）/ migration 0004 幂等 / syncPlanTodos 写 sourcePlanId / labelTodoSource 5 规则与降级 / todos:withSource batch / TodayView tag / 测试独立性（真实 SQL in-memory）/ 偏差记录（migration 0004 规格边界）

### 18.5 GLM 复审修复（2026-08-09，复审 2 记录）
> 依据：`审查日志/用户反馈3.2.md`（🟡 需修复）；完整记录 `项目日志/用户反馈3.2-修复2.md`。

- **阻断**：`Todo.sourcePlanId` 必填后 2 处 `todoRepo.create` 调用点未同步（projects.ipc:91 + reminders.ts:18，TS2345）——运行时无害（`?? null` 兜底）但类型安全违规 + 本轮回归（「零 TS 错误」声明基于 build，electron-vite 对 main 不全量类型检查；GLM 用 tsc.node 严格检查暴露）。
- **修复**：① projects.ipc:91 补 `sourcePlanId: null` ② reminders.ts:18 补 `sourcePlanId: null` ③ tsc 全量检查暴露同源第 3 处（todoRepo quickCreate 内部 create）同模式补 null（GLM 漏检）④ 存量窄化错 projects.ipc:38（`status: string → ProjectStatus`，GLM 漏检）加 `normalizeStatus` helper（非法值降级 active，行为零变化）——③④为达成 GLM 验收标准「tsc.node 仅剩 news.ts:22」的最小必要修复。
- **验证**：tsc.node ✅ 仅剩 news.ts:22 存量基线 · vitest ✅ 67/67 · build ✅ 15.23s 绿。
- **偏差**：GLM 复审报 2 处，实际 3 处同源遗漏 + 1 处存量错（GLM 漏检）；一并修复并注释标注。

---

## 19. v0.2测试修复计划1.0 — 参照完整性（L2 半规范化）（2026-08-10，DeepSeek 04）
> 依据：`v0.2测试修复计划/v0.2测试修复计划1.0-参照完整性.md`（用户定稿 08-10，内联 tid 确认）；完整记录 `项目日志/v0.2测试修复计划1.0-参照完整性.md`。

### 19.1 修复清单（4 缺口 → 方案 → 关键改动）
| 缺口 | 方案 | 关键改动 |
|---|---|---|
| ① 反向不同步 | 跨级删除分流（§3.6） | `syncPlanTodos` 全类型 tasks 双向 sync：content 行有 tid → upsert；tasks 行 tid 不在 content → softDeleteByTid + `findByParentTaskId` 级联分流（open 软删 cascadeRemovedOpenCount / done 保留失效 cascadeInvalidatedDoneCount）；周/月删行 → 下游 daily todo 传播（修④） |
| ② 文本匹配脆弱 | 内联隐形 tid 匹配键（§3.3/§3.4） | `- [ ] {text} <!-- tid:{12hex} [parent:{tid}] -->`（惰性分配，pick 时 prepareTaskLink 写回父计划）；taskRepo.findByTid/setTaskConsumedByTid；pickTask/markTaskConsumed/TOGGLE 双向同步全部按 tid 定位（改名后关联存活） |
| ③ 来源链断裂 | parentTaskRef + 逐级来源 UI（§3.5） | `todos.parentTaskRef` JSON `{parentPlanId,parentTaskId}`（todo 生成时按 line.parentTid 写）；`todos:withSource` batch 解析 + `labelTodoSource` 规则0（ref 优先，invalid → 来源已删）；`tasks:resolveChain` 逐级上溯（guard 20）；TodayView 来源 tag 可点击 → 上一期计划 `?date=prev&focus=tid` → MarkdownEditor focusLine 定位 + breadcrumb 反馈 |
| ④ 删除不传播 | ① 机制覆盖（tasks 全类型 sync） | 周/月计划删任务行 → tasks 软删 + 下游 todos 级联（open 消 / done 留） |

### 19.2 验证
| 验收 | 结果 |
|---|---|
| vitest | ✅ 8 files / **89 tests**（84 存量 + planSync ⑤⑥⑦⑧ + todoSource 规则0×2 + parentRef 2） |
| tsc.node 严格类型 | ✅ 仅剩 news.ts:22 存量基线（零新错） |
| build | ✅ 15.57s 绿 |
| 0005 存量库实跑（副本） | ✅ schema 4→5 幂等：8 todos 全保留（5 active）· parentTaskRef 全 NULL 不回填 · tasks 空起 · plans/projects 完好（备份副本已清理，live 库未动） |

### 19.3 偏差/取舍（规格偏差全部记录）
| 偏差点 | 原规格 | 实际做法 | 原因 |
|---|---|---|---|
| tasks.parentTaskId FK | 裸 `REFERENCES tasks(tid)` | `ON DELETE CASCADE` | connection 已 foreign_keys=ON，导入 roundtrip（DELETE FROM plans 整表重灌）会撞 FK；应用层只有软删，CASCADE 仅导入触发 |
| migration 0005 触「无 migration」规格边界 | 规格自身要求 migration 0005 | 注册 version=5（同 0004 先例） | 规格 §4 明确要求建表+加列，属规格内偏差先例 |
| §3.7「写 parentTaskRef 指向项目任务」未落地 | 复用动作含写 ref | 只建复用机制：set planDate + 保留 projectId，ref 保持 null | 行 parent=项目 todo id 会撞 tasks.parentTaskId FK（REFERENCES tasks(tid) 强制任务链）；且 ref 指向项目 todo id 时规则0 解析 tasks 查无该 tid → 误标「来源已删」。判据按规格原文（同 content + projectId 非空 + 未安排），**按 id 链接语义在 future「项目任务选入计划」UI 定稿时落地** |
| `plan:prepareTaskLink` IPC | 不在规格 §5 契约表 | 新增 IPC（preload/plans.ipc） | tid 惰性分配需要 pick 时写回父计划 content；规格 §3.4 描述该流程但未列通道 |
| 行内 parent 字段 | 规格未明确行内格式 | `<!-- tid:{tid} parent:{parentTid} -->` | tasks.parentTaskId 自引用需要行内来源；测试⑥ 级联分流依赖 |
| 「行消失即被删」字面处理 | 规格 §3.3 | 手删 tid 行 = 被删 → 跨级分流 | 规格字面「有 tid 但 content 无该行 → 软删+分流」，无恢复机制（用户手删语义=删除） |
| done orphan 保留 generatedTodoIds | 方案B（前轮定稿） | 保留（复用池持续命中） | 与 F3.2-1 方案B 一致：done 留历史，加回同文本行保留完成态 |
| node:sqlite 测试 mock | — | vi.mock connection 跑真实 repo SQL | better-sqlite3 ABI（Electron 130 vs node 127）不兼容；延续 67/67 同款 mock |

### 19.4 待 GLM 复审点
- syncPlanTodos 全类型 tasks 双向 sync（§3.3/§3.6 级联分流计数）
- tid 分配生命周期（惰性、prepareTaskLink 唯一入口）、改名存活（⑤）
- 跨级分流（⑥/⑦）：open 软删 / done 保留失效 / 周月删传播 daily
- §3.7 项目 todo 复用判据（⑧）与 FK 冲突偏差

### 19.5 GLM 审查修复（2026-08-10，结论 🟡 → 2 MED + 3 LOW 全修，待复审 2）
> 完整记录：`项目日志/v0.2测试修复计划1.0-参照完整性.md §7`；复审入口见审查记录 G 节。

**修复摘要**（只修审查指出的问题，无范围扩大）：
| # | 级别 | 问题 | 修复 |
|---|---|---|---|
| 1 | MED | roundtrip 对 tasks 自引用 FK 顺序敏感、无显式保证 + 无测试 | 导出 tasks 显式父先拓扑序 `ORDER BY (parentTaskId IS NULL) DESC, createdAt ASC` + `tests/dataRoundtrip.spec.ts`（三级链 roundtrip + 乱序 FK 违例整体回滚） |
| 2 | MED | §3.7 复用项目 todo 删行 → orphan 软删 → 项目任务丢失 | orphan 分流 projectId 例外：回退 `planDate=null` 保留在项目，不软删、离池；`planSync.spec.ts` ⑨ 全循环 |
| 3 | LOW | `todoRepo.ts:157 require('crypto')` | 顶部静态 import，删 require（src/main grep 零 require） |
| 4 | LOW | ref 非空查无 → 规则1-5 非「来源已删」 | withSource annotate 归为 invalid:true（来源已删置灰） |
| 5 | LOW | DOMPurify 剥 tid 注释 headless 未实测 | jsdom devDep + 真实管线 headless 实测 + `dompurifyComment.spec.ts` 固化 |

**验证**（全量重跑）：vitest **94/94**（10 files）· build 2.35s 绿 · tsc.node 仅剩 news.ts:22 存量基线 · src/main 零 require。
**影响范围**：main 4 文件（data.ts/planRepo.ts/todoRepo.ts/todos.ipc.ts）+ 测试 3 文件（dataRoundtrip 新增/planSync ⑨/dompurify 新增）+ package.json（jsdom devDep）；渲染层零改动。
**偏差**：roundtrip spec connection mock 补 production 镜像（FK ON + 真事务，否则 FK 违例/回滚断言在测试中静默）；MED-1 选建议①（导出 ORDER BY）未动导入侧；MED-2 选建议①（回退未安排）。
**复审所需信息**：dataRoundtrip.spec ①/② + planSync.spec ⑨ + dompurifyComment.spec 可直接 `npm test` 复跑；审查记录 F 表 5 项全部闭环。
- TODOS_TOGGLE 按 ref 定位（根治② 复发）、resolveChain 上溯 guard 20
- 来源链跳转（prevPeriod offset + focusLine 定位 + breadcrumb）
- 导入导出 tasks 表（BUSINESS_TABLES）
- 测试独立性（真实 SQL in-memory）、偏差记录完整性
