# 阶段 4 实施规格 — 设置与数据管理

> **编写**：GLM　**执行**：DeepSeek　**状态**：待实现
> **前置**：阶段 1/2/3 均 ✅ 通过
> **定位**：稳 —— 打通 LLM 配置入口与备份恢复；首个触及 LLM 的阶段（仅用户主动测试连接）。
> **视觉基准**：`规格-阶段0-视觉.md`（token 体系、BaseCard、表单风格）

---

## 0. 元信息
- **阶段**：阶段 4 — 设置与数据管理
- **快/稳**：稳
- **复用**：阶段 1 `settingsRepo`（get/set/getAll/seedDefaults）、safeStorage 自动加密 `llmApiKey`、`settings:get/set/getAll` IPC。**无新表/无迁移。**

## 1. 阶段目标
用户可在设置页配置称呼/晨间时段/AI（Base URL/Model/Key，含测试连接）/热点源，并可全量导出导入 JSON 数据。应用不配 LLM 亦能正常使用（设置页可访问，测试按钮未配置时禁用）。

## 2. 范围
**含**：
- 通用设置：称呼（nickname）、晨间时段（morningStart/morningEnd）
- AI 配置：Base URL / Model / API Key（密文），「测试连接」按钮（10s 超时）
- 热点源管理：newsProviders 列表的增/删/改（名称/RSS URL），保存后可触发一次刷新
- 数据管理：JSON 全量导出（系统保存对话框）/ 导入（系统打开对话框 + 校验 + 事务还原）
- 首启引导完整化：保存称呼或 AI 配置时写 `onboarded=true`（阶段 1 首启条此后不再显示）

**不含（推迟）**：
- 多套 LLM 配置切换 / 模型列表自动拉取（超范围）
- 加密备份文件（明文 JSON 导出，由用户自行保管；密文仅指库内 apiKey 存储）
- 自动定时备份

## 3. 技术栈新增
无新依赖。HTTP 用 Node 22 全局 `fetch` + `AbortController`（10s 超时）。文件对话框用 Electron `dialog`。

## 4. 目录结构（本阶段新增/变动）
```
src/
├─ main/
│  ├─ services/
│  │  ├── llm.ts              # 新增：testConnection（ping，10s 超时）
│  │  └── data.ts             # 新增：exportAll / importAll（dialog + 事务还原）
│  └─ ipc/
│     ├── llm.ipc.ts          # 新增：llm:test
│     └── data.ipc.ts         # 新增：data:export / data:import
├─ shared/
│  ├─ types.ts                # 增 LlmTestResult / DataExportPayload
│  └─ ipc.ts                  # 增 3 通道
└─ renderer/src/
   ├─ views/SettingsView.vue  # 重写：分节设置页
   ├─ components/settings/
   │  ├── HotspotSourcesEditor.vue  # 新增：newsProviders 列表 CRUD
   │  └── SectionCard.vue           # 新增：设置分节容器（可选，复用 BaseCard 亦可）
   └─ composables/useSettings.ts    # 新增：聚合加载/保存/测试
```

## 5. 核心设计

### 5.1 AI 配置与测试连接（`services/llm.ts`）
```
testConnection(cfg: {baseUrl, model, apiKey}): Promise<LlmTestResult>
  - 规范化 baseUrl：去尾部斜杠，拼 '/chat/completions'
  - 请求体：{ model, messages:[{role:'user',content:'ping'}], max_tokens:1, stream:false }
  - 头：{ 'Authorization': 'Bearer <apiKey>', 'Content-Type':'application/json' }
  - AbortController 超时 10s
  - 200 → { ok:true, latencyMs }
  - 非 200 / 网络错 / 超时 → { ok:false, message }（message 不含 apiKey；超时提示「连接超时(>10s)」）
  - **禁记录 apiKey**：logger 仅记 status / message（redact 已覆盖，但此处显式不传入）
```
> §9 token 红线：用户主动触发（foreground），不适用；且 `max_tokens:1` 极小。

### 5.2 数据导出/导入（`services/data.ts`）
**导出**：
```
exportAll(): 调 dialog.showSaveDialog(defaultName=workbuddy-backup-YYYYMMDD.json)
  → 用户选路径后，组装 payload：
    { version:1, exportedAt:ISO, tables:{ projects:[...], todos:[...], settings:[...],
      inboxes, plans, templates, reviews, notes, books, workout_logs } }
  → 逐表 SELECT * （业务表白名单 10 个；**跳过 news_items 缓存 + __schema_migrations 元表**）
  → fs.writeFile UTF-8 JSON（pretty，2 空格）
  → 返回 { ok:true, path } ；用户取消 → { ok:false, message:'cancelled' }
```
**导入**：
```
importAll(): 调 dialog.showOpenDialog(filters=[json])
  → 读文件 → JSON.parse → 校验：
      必须有 version(number) 与 tables(object)
      tables 仅含白名单 10 表，每表为 row 对象数组
      校验失败 → 返回 err，**不动 DB**
  → 通过后 db.transaction 内逐表：DELETE FROM <table>（白名单，防注入）→ INSERT 每行
      （列名取 Object.keys(row)，占位符按列数；全用 bound param）
  → 任意异常 → 事务回滚，返回 err，**现有数据不损坏**
  → 成功 → 返回 { ok:true, counts:{projects:n, todos:n, ...} }
  → 重新 seedDefaults 不覆盖已有（与阶段1一致）；导入后设置项以导入为准
```
> **§9 豁免声明**：导入是**用户主动全量恢复**路径（数据恢复，与 §9 模态例外同源），物理替换表内容（DELETE+INSERT）而非软删除。仅由用户在 UI 明确触发（文件选择 + 内联二次确认）。`news_items` 不导入（可由晨间重新拉取）。

### 5.3 热点源编辑（HotspotSourcesEditor）
- 读 `newsProviders`（JSON 数组 `{id,name,type:'rss',url}`）→ 列表行（name + url + 删除）。
- 新增行：name/url 输入 + 添加（id 用 `randomUUID` 或时间戳 slug；url 需 http(s):// 校验）。
- 编辑：行内改 name/url。
- 保存：前端重组数组 → `settings:set('newsProviders', JSON.stringify(arr))`。
- 「立即刷新热点」按钮 → 调 `news:refresh`（阶段 2 通道）。

### 5.4 设置页结构（SettingsView）
单页滚动，分节（每节 BaseCard）：
1. **通用**：称呼（nickname 文本）、晨间时段（morningStart/morningEnd，`<input type="time">`）。保存按钮。
2. **AI 配置**：Base URL、Model（文本）、API Key（password 输入，密文存）、「测试连接」按钮（未填 baseUrl/model 时禁用 + 提示）。保存 + 测试分开。
3. **热点源**：HotspotSourcesEditor + 「立即刷新」。
4. **数据管理**：导出按钮 / 导入按钮（导入前内联二次确认「将覆盖现有全部业务数据」）。

### 5.5 首启引导完整化
- SettingsView 保存「称呼」或「AI 配置」成功后，调 `settings:set('onboarded','true')`。
- 阶段 1 首启条 `isFirstLaunch` 据此判断 → 配置过即不再显示。

## 6. 数据模型变更
**无新表/无迁移。** 复用 `settings`（key-value）。新增共享类型：
```ts
export interface LlmTestResult { ok: boolean; message?: string; latencyMs?: number }
export interface DataExportPayload {
  version: number
  exportedAt: string
  tables: Record<string, Record<string, unknown>[]>
}
export interface DataImportResult { ok: boolean; counts?: Record<string, number>; message?: string }
```

## 7. IPC 契约（`shared/ipc.ts` 新增）
| 通道 | 入参 | 返回 `Result<T>` | 说明 |
|---|---|---|---|
| `llm:test` | `{baseUrl, model, apiKey}` | `LlmTestResult` | 用传入值（非已存）测试，10s 超时；不记 apiKey |
| `data:export` | — | `{ path: string }` | 弹保存对话框 + 写文件；取消返 err 'cancelled' |
| `data:import` | — | `DataImportResult` | 弹打开对话框 + 校验 + 事务还原 |

（热点源 CRUD 走已有 `settings:get/set`，无需新通道。）

preload 增：`llm.test(cfg)`、`data.export()`、`data.import()`。

## 8. 任务拆解（DeepSeek 执行顺序）
1. `shared/types.ts` 增 3 类型；`shared/ipc.ts` 增 3 通道常量。
2. `services/llm.ts`：`testConnection`（fetch + AbortController 10s + redact）。
3. `services/data.ts`：`exportAll`（dialog.save + 白名单 SELECT + writeFile）、`importAll`（dialog.open + 校验 + 事务 DELETE/INSERT + 白名单防注入）。
4. `main/ipc/llm.ipc.ts` + `data.ipc.ts`；注册到 `ipc/index.ts`。
5. `preload/index.ts`：暴露 `llm.test` / `data.export` / `data.import`，带类型。
6. `composables/useSettings.ts`：`load()`（settings:getAll）、`save(key,value)`、`testLlm(cfg)`、`exportData()`、`importData()`、`saveProviders(arr)`、`refreshNews()`。
7. `components/settings/HotspotSourcesEditor.vue`（newsProviders 列表 CRUD + 校验 + 保存 + 刷新）。
8. `views/SettingsView.vue` 重写（4 分节 + 保存/测试/导出/导入交互 + 内联确认 + onboarded 写入）。
9. 侧边栏「设置」对 `/settings` 激活（阶段 1 已就绪，确认即可）。
10. 自测全部验收点；更新 `当前状态.md` 与 `项目日志/`。

## 9. 验收点（GLM 评审依据）
- [ ] 通用：称呼 + 晨间时段保存往返；保存后今日总览问候称呼随之变化（跨阶段）。
- [ ] AI 配置：Base URL/Model 明文存、API Key **密文存**（DB 中非明文）；往返正确。
- [ ] 「测试连接」：成功/失败/超时三种 ≤10s 返回；失败给可读原因；未填必要项时按钮禁用 + 提示。
- [ ] **logger 不输出 apiKey 明文**（grep 测试连接代码 + 主进程日志，无明文 key）。
- [ ] 热点源：增/删/改 + URL 格式校验（http(s)://）；保存后持久；「立即刷新」触发 `news:refresh`，今日总览热点更新。
- [ ] 导出：弹系统保存框 → 生成 JSON 文件；含 10 业务表数据；不含 news_items/__schema_migrations；用户取消不报错。
- [ ] 导入：弹系统打开框 → 选合法 JSON → 事务还原；round-trip（导出→清库→导入）数据一致。
- [ ] 导入校验失败（非 JSON / 缺字段 / 非白名单表）→ 返回错误 + **不动 DB**。
- [ ] 导入前内联二次确认「覆盖现有数据」（非系统模态 alert/confirm）。
- [ ] 保存称呼或 AI 配置 → 写 `onboarded=true` → 重启后首启条不再显示。
- [ ] 不配 LLM 亦可访问设置页、保存通用/热点/数据；仅「测试连接」需配置。
- [ ] `grep` 组件无硬编码 hex/rgba（仅 tokens.css）。
- [ ] `npm run build` 绿，无 TS 错误。

## 10. 禁止边界 & 错误降级（承袭并具化）
- **§9 日志**：`llm:test` 严禁记录 apiKey；错误仅记 status/message（redact 兜底 + 显式不传入）。
- **§9 token 红线**：测试连接为用户主动触发（foreground），`max_tokens:1`；红线不适用。
- **§9 软删除豁免**：导入为用户主动全量恢复路径，物理 DELETE+INSERT 替换业务表，已显式声明豁免；`news_items` 不导入。
- **导入失败降级**：事务回滚 + 非阻塞错误提示，现有数据不损坏。
- **无应用模态弹窗**：文件选择用 OS 原生 `dialog`（非应用模态，必需）；覆盖确认用**内联二次确认**，禁 alert/confirm。
- **输入校验**：baseUrl 需 http(s)://；model 非空；provider url http(s)://；前端拦截 + IPC 复校。
- **无硬编码 hex**：复用 tokens.css。
- **XSS**：所有用户输入（baseUrl/model/provider name）渲染用 `{{}}`，禁 v-html。

## 11. 交付与运行说明
- 启动：`cd E:\workspace\workbuddy && npm run dev`。
- DeepSeek 完成后在 `当前状态.md` 补：实现概要、文件清单、自测（含测试连接成功/失败/超时三态、round-trip 导入导出）、已知项。
- 运行时验证提示：GLM 复审会实跑 dev，核对设置保存往返、测试连接（需真实可用 LLM 端点，或用错误端点验失败/超时态）、热点源增删、导出/导入 round-trip、onboarded。
