# 规格 · 子阶段 1 — 基建（计划与复盘体系）

> **编写**：GLM　**执行**：DeepSeek　**状态**：待实现
> **真相源**：`计划与复盘体系实现项目计划/项目计划.md`（§5 数据模型 / §9 子阶段 / §11 验收 / §12 禁止边界·降级）。
> **上位**：`开发阶段项目计划/项目计划.md §7`、`初级文档/`（v0.3 §9/§10）。

---

## 0. 元信息

| 项 | 值 |
|---|---|
| 子阶段 | 子阶段 1 — 基建 |
| 编写 | GLM |
| 执行 | DeepSeek |
| 状态 | 待实现 |
| 前置依赖 | Day-1 GA `0.1.0`；本功能 `项目计划.md` 已立项 |
| 本子阶段定位 | **稳** —— 迁移 + 仓储 + 编辑器是后续子阶段 2/3/4 的基石，必须先稳；本子阶段交付「可见的空壳 + 可用基建」，不含计划/复盘业务逻辑 |

> ⚠️ **迁移版本号订正**：本功能 `项目计划.md §5` 原写 `0002_plan_review_governance`，但 `migrate.ts` 中 `0002` 已被 `0002_news_items` 占用。**本子阶段一律用 `0003_plan_review_governance`**。项目计划文档随后由 GLM 同步订正。

---

## 1. 单元目标

交付「可见的空壳 + 可用基建」：`0003` 迁移跑通（补齐三表治理字段）→ `templateRepo` + IPC 可用 → 3 条默认模板 seed → 适度 Markdown 编辑器组件 → `/plan` `/review` 路由 + 侧边栏入口 + 空壳页（选模板、编辑器渲染，但**不保存**）。

**一句话可演示**：打开「计划」页 → 选「日计划」模板 → 编辑器渲染出手帐风模板（含 `- [ ]` 复选框、工具栏、实时预览），但保存按钮禁用并提示「子阶段 2 上线」。

---

## 2. 范围

**含**：
1. `0003_plan_review_governance` 迁移：为 `plans`/`templates`/`reviews` 补齐 `isDeleted`/`deletedAt`/`updatedAt`（及 `templates`/`reviews` 的 `createdAt`）+ 3 个索引。
2. `templateRepo`（仓储 CRUD + softDelete + `seedDefaults`）+ `@shared` 类型。
3. `templates.ipc.ts` 4 个通道，注册入 `ipc/index.ts`，preload 桥暴露。
4. 默认模板 seed：日计划 / **周计划** / 日复盘 / 周复盘（镜像用户 Obsidian SOP，含 `{{占位符}}` 与 `- [ ]`）。> 周计划为 2026-08-07 用户反馈2（方案 B）补入。
5. `MarkdownEditor.vue` 适度编辑器（编辑 + 实时预览 + `- [ ]` 渲染 + 工具栏），手帐风，可复用。
6. `/plan` `/review` 路由 + 侧边栏入口启用 + 空壳页（模板选择器 + 编辑器 + 禁用保存）。

**不含（推迟）**：
- `planRepo`/`reviewRepo` 与计划/复盘 CRUD、`- [ ]` 解析→todos（子阶段 2/3）。
- 昨日复盘卡衔接（子阶段 3）。
- LLM 引导式提问 / 复盘汇总（子阶段 4）。
- 模板高级可视化编辑器、移动端。（周计划模板已于 2026-08-07 用户反馈2 补入默认 seed；月计划仍推迟——需扩 `TemplateType` + 迁移。）

---

## 3. 技术栈与本子阶段新增依赖

| 依赖 | 版本 | 用途 |
|---|---|---|
| `marked` | 最新稳定（DeepSeek 锁定，写入 `package.json`） | Markdown → HTML（开启 GFM，支持任务列表） |
| `dompurify` | 最新稳定 | 预览 HTML 的 XSS 净化 |

> **复用核查（先做）**：项目当前无 Markdown 库（`package.json` 仅 better-sqlite3/lucide-vue-next/rss-parser/vue/vue-router）。若实现中发现已有等价渲染能力则复用；否则引入上表两项。`marked`/`dompurify` 均**静态 import**（禁动态 `require`，承袭用户反馈 1 教训）。

沿用既有：`better-sqlite3`、`lucide-vue-next`（图标，勿用 `<i data-lucide>`）、`vue-router`、设计 token（`token.css` 唯一色源）。

---

## 4. 目录结构（本子阶段新增/变动）

```
workbuddy/src/
├── main/
│   ├── db/
│   │   ├── migrate.ts                              ← 变动：注册 init003
│   │   ├── migrations/0003_plan_review_governance.ts ← 新增（导出 init003）
│   │   └── repositories/templateRepo.ts            ← 新增
│   └── ipc/
│       ├── index.ts                                ← 变动：registerTemplatesIpc()
│       └── templates.ipc.ts                        ← 新增
├── renderer/src/
│   ├── router/index.ts                             ← 变动：加 /plan /review
│   ├── components/
│   │   ├── AppSidebar.vue                          ← 变动：navItems 加 计划/复盘
│   │   └── MarkdownEditor.vue                      ← 新增（可复用组件）
│   └── views/
│       ├── PlanView.vue                            ← 新增（空壳页）
│       └── ReviewView.vue                          ← 新增（空壳页）
└── shared/
    ├── types.ts                                    ← 变动：加 Template / TemplateType
    └── ipc.ts                                      ← 变动：IPC 加 TEMPLATES_*；preload 桥（若在此声明）加 templates
```

> `shared` 的确切文件名以现有 `@shared/types`、`@shared/ipc` 别名指向为准（`todoRepo` 用 `@shared/types`、`todos.ipc` 用 `@shared/ipc`）。

---

## 5. 核心设计

### 5.1 迁移 `0003_plan_review_governance`（幂等）

- 导出 `init003(): void`，注册：`{ version: 3, name: '0003_plan_review_governance', up: init003 }` 于 `migrate.ts` 的 `migrations[]`。
- **幂等写法**：SQLite `ALTER TABLE ADD COLUMN` 无 `IF NOT EXISTS`，故实现 `addColumnIfMissing(table, col, defn)` 辅助函数——先 `PRAGMA table_info(table)` 查列是否存在，再决定是否 `ALTER`。索引用 `CREATE INDEX IF NOT EXISTS`。确保部分应用/重跑不报错。
- `init003` 内部用 `getDb()`（不自行开事务；`runMigrations` 已包事务）。

### 5.2 templateRepo（沿用 `todoRepo` 对象字面量 + prepared + `RETURNING *` 模式）

方法：
- `findAll(type?: TemplateType): Template[]` — `WHERE isDeleted=0`，可选 `AND type=?`，`ORDER BY type, name`。
- `findById(id): Template | undefined`
- `findByType(type): Template[]`
- `findDefaultByType(type): Template | undefined` — `isDefault=1 AND isDeleted=0`。
- `upsert(data): Template` — 有 `id` 则 `INSERT ... ON CONFLICT(id) DO UPDATE`（或先查后更/插），回填 `updatedAt`；`isDefault`/`isDeleted` 布尔↔0/1 映射。
- `softDelete(id): boolean` — `UPDATE ... SET isDeleted=1, deletedAt=?, updatedAt=? WHERE id=? AND isDeleted=0`；**默认模板禁删**（IPC 层拦截，见 7）。
- `seedDefaults(): void` — **幂等**：按 `name`（或固定 `id`）检查是否存在，缺则插入 3 条默认模板（§5.4）；已存在不覆盖用户改动。

id 生成：`import { randomUUID } from 'node:crypto'`（**静态 import**，勿用内联 `require('crypto')`）。

### 5.3 MarkdownEditor.vue（适度编辑器，非 WYSIWYG）

- **Props**：`modelValue: string`（v-model）；`readonly?: boolean=false`；`placeholder?: string`；`toolbar?: boolean=true`；`showPreview?: boolean=true`。
- **Emits**：`update:modelValue`。
- **结构**：`<textarea>`（源码，绑定 modelValue）+ 预览面板（`marked(modelValue, {gfm:true})` → `DOMPurify.sanitize(html)` → `v-html`）；工具栏按钮（粗体/斜体/标题/任务/列表/链接）在光标处插入对应 Markdown 语法。
- **任务列表**：marked GFM 将 `- [ ]`/`- [x]` 渲染为 `<input type=checkbox disabled>`；预览态只读。
- **样式**：手帐风，全部用 `token.css` 变量（`--bg-*`/`--text-*`/`--r-*`/`--s-*` 等），**组件内禁硬编码 hex**；编辑/预览左右分栏，窄窗可堆叠或切 tab。
- **XSS**：所有预览 HTML 必须经 `DOMPurify.sanitize` 后再 `v-html`。

### 5.4 默认模板 seed 内容（镜像 Obsidian SOP：plan→execute→review→adjust）

`type` 取值：`daily_plan` / `weekly_plan` / `daily_review` / `weekly_review`（在 `@shared/types` 定义 `TemplateType` 联合类型）。

**日计划**（`daily_plan`，`isDefault=1`）：
```markdown
# 📅 日计划 · {{date}}

## 🎯 今日重点
{{focus}}

## ✅ 任务清单
- [ ] 
- [ ] 
- [ ] 

## 🕒 时间安排
- 上午：
- 下午：
- 晚上：

## 📝 备注

```

**周计划**（`weekly_plan`，`isDefault=1`，2026-08-07 用户反馈2 补入）：
```markdown
# 🗓️ 周计划 · {{week}}

## 🎯 本周重点
{{focus}}

## ✅ 本周任务
- [ ]
- [ ]
- [ ]

## 📅 分日安排
- 周一：
- 周二：
- 周三：
- 周四：
- 周五：
- 周末：

## 📝 备注

```

**日复盘**（`daily_review`，`isDefault=1`）：
```markdown
# 🌙 日复盘 · {{date}}

## ✅ 今日完成
- 

## 📈 关键进展
{{progress}}

## 💡 反思与收获
- 做得好的：
- 可改进的：

## ➡️ 明日重点
- 

```

**周复盘**（`weekly_review`，`isDefault=1`）：
```markdown
# 🗓️ 周复盘 · {{week}}

## 📊 本周回顾
- 计划完成情况：
- 关键成果：

## 🔍 深度反思
{{reflection}}

## 🎯 下周重点
- [ ] 
- [ ] 

## 📝 备注

```

> `{{date}}`/`{{focus}}`/`{{progress}}`/`{{reflection}}`/`{{week}}` 为占位符，供子阶段 4 引导式提问；本子阶段仅原样展示。

### 5.5 路由 + 侧边栏 + 空壳页

- **路由**（`router/index.ts`，沿用 hash history + 懒加载）：
  - `{ path: '/plan', name: 'plan', component: () => import('@/views/PlanView.vue') }`
  - `{ path: '/review', name: 'review', component: () => import('@/views/ReviewView.vue') }`
- **侧边栏**（`AppSidebar.vue` `navItems`，在 `/projects` 之后插入）：
  - `{ path: '/plan', label: '计划', icon: 'ClipboardList' as const, disabled: false }`
  - `{ path: '/review', label: '复盘', icon: 'NotebookPen' as const, disabled: false }`
  - 图标用 `lucide-vue-next` 已有图标名（`ClipboardList`/`NotebookPen`；若不存在则换 `ListTodo`/`History`）。
- **空壳页 `PlanView.vue` / `ReviewView.vue`**：
  - 顶部：日期选择（默认今天，仅展示）+ 模板选择器（`template:list`：Plan 取 `daily_plan`/`weekly_plan`；Review 取 `daily_review`/`weekly_review`）。
  - 主体：`MarkdownEditor`（选中模板的 `content` 为初始 `modelValue`；用户可编辑）。
  - 底部：「保存」按钮 `disabled` + tooltip「计划保存与解析将在子阶段 2 上线」/「复盘保存将在子阶段 3 上线」。
  - 无模板时显示 EmptyState（复用现有 EmptyState 组件样式）。

---

## 6. 数据模型变更

**新迁移 `0003_plan_review_governance`**（DDL；`addColumnIfMissing` 包装 `ALTER`，索引 `IF NOT EXISTS`）：

```sql
-- plans：补 isDeleted/deletedAt/updatedAt（createdAt 已存在）
ALTER TABLE plans     ADD COLUMN isDeleted INTEGER NOT NULL DEFAULT 0;
ALTER TABLE plans     ADD COLUMN deletedAt TEXT;
ALTER TABLE plans     ADD COLUMN updatedAt TEXT NOT NULL DEFAULT '';

-- templates：补 isDeleted/deletedAt/createdAt/updatedAt（isDefault 已存在）
ALTER TABLE templates ADD COLUMN isDeleted INTEGER NOT NULL DEFAULT 0;
ALTER TABLE templates ADD COLUMN deletedAt TEXT;
ALTER TABLE templates ADD COLUMN createdAt TEXT NOT NULL DEFAULT '';
ALTER TABLE templates ADD COLUMN updatedAt TEXT NOT NULL DEFAULT '';

-- reviews：补 createdAt/updatedAt/isDeleted/deletedAt（原仅有 id/date/type/content/linkedProjectIds）
ALTER TABLE reviews   ADD COLUMN createdAt TEXT NOT NULL DEFAULT '';
ALTER TABLE reviews   ADD COLUMN updatedAt TEXT NOT NULL DEFAULT '';
ALTER TABLE reviews   ADD COLUMN isDeleted INTEGER NOT NULL DEFAULT 0;
ALTER TABLE reviews   ADD COLUMN deletedAt TEXT;

CREATE INDEX IF NOT EXISTS idx_plans_date     ON plans(date)     WHERE isDeleted=0;
CREATE INDEX IF NOT EXISTS idx_reviews_date   ON reviews(date)   WHERE isDeleted=0;
CREATE INDEX IF NOT EXISTS idx_templates_type ON templates(type) WHERE isDeleted=0;
```

- **Day-1 启用**：是（本子阶段第一步即执行）。
- **向后兼容**：纯加列（`ADD COLUMN ... DEFAULT`），存量 0.1.0 库（`__schema_migrations` version=2）平滑升级到 3，既有数据不动。
- **shared 类型**（`@shared/types`）：
  ```ts
  export type TemplateType = 'daily_plan' | 'weekly_plan' | 'daily_review' | 'weekly_review'
  export interface Template {
    id: string
    name: string | null
    type: TemplateType | null
    content: string | null
    isDefault: boolean
    isDeleted: boolean
    deletedAt: string | null
    createdAt: string
    updatedAt: string
  }
  ```
  （`plans`/`reviews` 的 TS 接口在本子阶段可一并补齐于 `@shared/types`，供子阶段 2/3 用；字段对齐迁移后表结构。）

---

## 7. IPC 契约（新增通道）

沿用 `todos.ipc.ts` 模式：`import { IPC, ok, err } from '@shared/ipc'`；`registerTemplatesIpc()`；`ipcMain.handle`；`try/catch` + `logger.error` + `err('INTERNAL'/'NOT_FOUND'/'INVALID', msg)`；返回 `ok(data)`。**logger 不得输出模板 `content` 正文**。

`@shared/ipc` 的 `IPC` 新增（通道名沿用 `domain:action` 风格）：

| 通道常量 | 通道名 | 入参 | 返回（信封内 data） | 说明 |
|---|---|---|---|---|
| `IPC.TEMPLATES_LIST` | `templates:list` | `{ type?: TemplateType }` | `Template[]` | 排除软删；可选 type 过滤 |
| `IPC.TEMPLATES_GET` | `templates:get` | `{ id: string }` | `Template \| null` | |
| `IPC.TEMPLATES_UPSERT` | `templates:upsert` | `Template`（无 id 则内部生成） | `Template` | 新增/编辑；校验 `type` 合法 |
| `IPC.TEMPLATES_DELETE` | `templates:delete` | `{ id: string }` | `{ ok: true }` | 软删；**默认模板（isDefault=1）禁删** → `err('INVALID', '默认模板不可删除')` |

注册：`ipc/index.ts` 的 `registerAllIpc()` 加 `registerTemplatesIpc()`。
Preload 桥：沿用 `window.api.todos.*` 模式，暴露 `window.api.templates.{list,get,upsert,delete}`（具体 preload 文件以现有桥位置为准）。

---

## 8. 任务拆解（DeepSeek 执行顺序）

1. **迁移**：新建 `migrations/0003_plan_review_governance.ts`（`init003` + `addColumnIfMissing` 辅助）；在 `migrate.ts` 注册 version=3。在干净库与存量 0.1.0 库各跑一次验证（看 `__schema_migrations` 与 `PRAGMA table_info`）。
2. **类型**：`@shared/types` 加 `TemplateType`/`Template`（并补 `Plan`/`Review` 接口供后续）。
3. **仓储**：`repositories/templateRepo.ts`，按 §5.2 实现；逐方法自测（含 findAll 默认过滤软删、softDelete、seedDefaults 幂等）。
4. **IPC**：`@shared/ipc` 加 `TEMPLATES_*`；新建 `ipc/templates.ipc.ts`；`ipc/index.ts` 注册；preload 桥暴露 `window.api.templates.*`。
5. **Seed**：在主进程启动初始化序列（与 `settingsRepo.seedDefaults()` 同处）调用 `templateRepo.seedDefaults()`；首启落库 3 条，重启幂等。
6. **编辑器组件**：装 `marked`+`dompurify`；实现 `MarkdownEditor.vue`（§5.3），含工具栏、GFM 任务复选框、DOMPurify 净化、手帐风、响应式。
7. **路由 + 侧边栏**：`router/index.ts` 加 `/plan` `/review`；`AppSidebar.vue` `navItems` 加「计划」「复盘」（启用）。
8. **空壳页**：`PlanView.vue`/`ReviewView.vue`（§5.5）：模板选择器（走 `templates:list`）+ `MarkdownEditor` + 禁用保存按钮（tooltip 提示子阶段 2/3）+ EmptyState。
9. **自测全部 §9 验收点**；更新 `当前状态.md`（§2 全字段、迁移版本=3、自测、运行说明）；在 `项目日志/计划与复盘-子阶段1-基建.md` 写实现日志。

---

## 9. 验收点（GLM 评审依据，可勾选可验证）

- [ ] `0003` 在干净库执行成功；`PRAGMA table_info` 确认三表新列齐全；`__schema_migrations` 记录 `version=3`
- [ ] `0003` 在存量 0.1.0 库（version=2）升级成功，既有 projects/todos/news 数据完好
- [ ] `0003` 幂等：对已加列的库再调 `init003` 不报错（`addColumnIfMissing` 生效）
- [ ] `templateRepo`：findAll 默认过滤 `isDeleted=1`；softDelete 置位后 findAll 不可见；upsert 往返正确；`isDefault`/`isDeleted` 布尔↔0/1 正确
- [ ] 默认模板 seed：首启后 `templates` 表含 **4 条**（日计划/周计划/日复盘/周复盘，`isDefault=1`）；重启不重复；用户改过的模板不被覆盖。> 周计划为用户反馈2 补入（id `tpl-default-weekly-plan`），存量库重启自动补。
- [ ] 4 个 IPC 通道工作；统一 `ok/err` 信封；构造异常时返回 `err` 且 `logger` 记录（**日志不含模板 content 正文**）
- [ ] 默认模板禁删：`templates:delete` 对 `isDefault=1` 返回 `err('INVALID')`
- [ ] `MarkdownEditor`：编辑→预览实时同步；`- [ ]`/`- [x]` 渲染为复选框；工具栏按钮插入正确语法；`readonly` 时只读；窄窗布局不溢出
- [ ] **XSS**：模板/编辑内容含 `<script>` 或 `<img onerror>` 时，预览经 DOMPurify 净化，不执行
- [ ] `/plan` `/review` 路由可访问；侧边栏「计划」「复盘」启用、`active` 高亮正确、窄窗图标轨正常
- [ ] 空壳页：能选模板并在编辑器渲染；「保存」按钮禁用且 tooltip 文案正确；无模板时 EmptyState
- [ ] 视觉：编辑器/空壳页手帐风，用 token 变量、**无硬编码 hex**；禁纯黑 #000/纯白 #FFF 高对比
- [ ] 图标用 `lucide-vue-next` 组件（无 `<i data-lucide>`）；无 `console.log`/调试残留
- [ ] 导入导出 JSON 往返一致（三表含新字段；默认模板行可导出/导入）
- [ ] 冷启动 `<3s`（不退化）；空闲内存 `<150MB`
- [ ] `当前状态.md` 已更新（迁移版本=3 等）；`项目日志/计划与复盘-子阶段1-基建.md` 已写

---

## 10. 禁止边界 & 错误降级（承袭项目计划 §7-B/§7-C，本子阶段具化）

**禁止边界**：
- 禁硬编码密钥（本期无密钥）；**禁日志输出模板/计划/复盘正文或 API Key 明文**。
- 删除一律软删除（`isDeleted=1`），默认模板禁删；禁业务数据上传云端。
- 禁阻断式模态弹窗；`token.css` 唯一色源，**组件内禁硬编码 hex**。
- `better-sqlite3` 配 electron-rebuild；图标用 `lucide-vue-next` 组件。
- **静态 import**（禁动态 `require` 相对模块——承袭用户反馈 1 教训）；`randomUUID` 用 `import { randomUUID } from 'node:crypto'`。

**错误降级**：
- **迁移失败**：`runMigrations` 已 try/catch+throw；`0003` 用 `addColumnIfMissing` 保证幂等；失败则应用进入既有安全模式（DB 只读 + Toast），不崩溃。
- **Markdown 渲染异常**：`marked`/`DOMPurify` 抛错时捕获，预览面板降级显示「预览不可用，仅显示源码」，不白屏。
- **Seed 失败**：`seedDefaults` 单条失败不阻塞启动（try/catch + logger），空壳页显示 EmptyState。

---

## 11. 交付与运行说明

- **启动**：`pnpm dev`（renderer）/ `pnpm electron:dev`（main，实际以 `当前状态.md §2.6` 为准）；`better-sqlite3` 改动后跑 `pnpm rebuild`。
- **DeepSeek 须在 `当前状态.md` 补充**：迁移版本=3、新增 IPC 通道清单（`templates:list/get/upsert/delete`）、新增文件清单、seed 行为、自测结果（含干净库/存量库/幂等/XSS 四类）、运行/复测命令。
- **GLM 审查入口**：实现完成后用 03 prompt（按 `00-开发单元闭环Prompt使用说明.md` 替换 `{{unit}}=子阶段1`、`{{计划目录}}=计划与复盘体系实现项目计划`）启动审查，GLM 依 §9 验收点 + §10 边界逐条实地核。
