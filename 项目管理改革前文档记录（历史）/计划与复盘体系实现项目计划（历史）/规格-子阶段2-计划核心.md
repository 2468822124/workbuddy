# 规格 · 子阶段 2 — 计划核心（计划与复盘体系）

> **编写**：GLM　**执行**：DeepSeek　**状态**：待实现
> **真相源**：`计划与复盘体系实现项目计划/项目计划.md`（§5 数据模型 / §6.2 解析幂等 / §9 子阶段 / §11 验收 / §12 禁止边界·降级）。
> **前置**：子阶段 1（基建）✅ 已通过关闭（`审查日志/计划与复盘-子阶段1.md` §8）。

---

## 0. 元信息

| 项 | 值 |
|---|---|
| 子阶段 | 子阶段 2 — 计划核心 |
| 编写 | GLM |
| 执行 | DeepSeek |
| 状态 | 待实现 |
| 前置依赖 | 子阶段 1（0003 迁移、templateRepo、MarkdownEditor、/plan 路由与 PlanView 空壳、AppIcon 图标均已就绪并复审通过） |
| 本子阶段定位 | **稳** —— 解析幂等与 todo 双入口一致性是数据正确性核心，必须稳；UI 为既有空壳的增量激活 |

---

## 1. 单元目标

激活「计划」为可用功能：写计划 → 保存 → 自动解析 `- [ ]` → 生成 todos（幂等）→ 流入今日待办卡。一句话可演示：**选今天、写日计划（含 `- [ ]` 任务）、保存 → 今日待办卡出现这些任务；改计划再存不重复建；在待办卡勾选完成后再存，完成态保留。**

---

## 2. 范围

**含**：
1. `Plan` 类型（`@shared/types`）。
2. `planRepo`（CRUD + softDelete + `generatedTodoIds` JSON 往返）。
3. `planParser`（纯函数：提取未勾 `- [ ]` 任务）+ **vitest 单测**。
4. 幂等同步（`syncPlanTodos`：解析→复用/新建 todo→回填 `generatedTodoIds`，保留完成态）。
5. `plan:*` IPC（create/update/get/list/delete）+ preload 桥；create/update 触发解析+同步。
6. PlanView 激活：日期选择、加载已有/空白模板、**保存启用**（create/update）、保存反馈内联、生成 todo 数提示。
7. 今日计划入口：PlanView 默认今天；TodayView 今日待办卡旁加「📝 今日计划」链接 → `/plan`。

**不含（推迟）**：
- 复盘 CRUD 与昨日复盘卡（子阶段 3）。
- LLM 引导式提问（子阶段 4）。
- 计划的周视图/历史列表页、计划模板的增删管理 UI、计划与项目关联。
- 残留 todo 的清理 UI（本子阶段仅提示，见 §5.3）。

---

## 3. 技术栈与本子阶段新增依赖

| 依赖 | 版本 | 用途 |
|---|---|---|
| `vitest` | 最新稳定（devDep） | `planParser` 纯函数单测（项目计划 §10 要求「解析单测」；项目当前无测试运行器） |

- `vitest` 零配置即可跑（`planParser.ts` 无 `@`/`@shared` 别名依赖，测试文件用**相对路径** import，避开别名解析）。
- 新增 npm script：`"test": "vitest run"`。
- 沿用：`better-sqlite3`、`marked`/`dompurify`（子阶段1已装）、`lucide-vue-next`、token.css。

---

## 4. 目录结构（本子阶段新增/变动）

```
workbuddy/src/
├── main/
│   ├── db/repositories/
│   │   └── planRepo.ts                       ← 新增（CRUD + syncPlanTodos）
│   ├── services/
│   │   └── planParser.ts                     ← 新增（纯函数 parseTasks）
│   └── ipc/
│       ├── plans.ipc.ts                      ← 新增
│       └── index.ts                          ← 变动（registerPlansIpc）
├── preload/index.ts                          ← 变动（+api.plans 桥）
├── shared/types.ts                           ← 变动（+Plan）
├── shared/ipc.ts                             ← 变动（+PLAN_* 通道）
├── renderer/src/
│   ├── views/
│   │   ├── PlanView.vue                      ← 变动（空壳→激活：日期/保存/反馈）
│   │   └── TodayView.vue                     ← 变动（+「今日计划」链接 → /plan）
│   └── composables/
│       └── usePlan.ts                        ← 新增（计划加载/保存封装，可选；PlanView 也可直调 api）
└── tests/planParser.spec.ts                  ← 新增（vitest；相对路径 import ../src/main/services/planParser）
```

> 测试文件放 `workbuddy/tests/`（新建目录）；不进 `src/`，不进打包。

---

## 5. 核心设计

### 5.1 Plan 类型（`@shared/types`）

```ts
export interface Plan {
  id: string
  date: string | null              // 'YYYY-MM-DD'
  type: string | null              // 'daily_plan' | 'weekly_plan'（与模板 type 对齐；字符串留宽）
  content: string | null           // Markdown 原文
  generatedTodoIds: string[]       // 解析生成的 todo id（DB 内存 JSON 字符串，repo 层 ↔ 数组）
  templateId: string | null
  isDeleted: boolean
  deletedAt: string | null
  createdAt: string
  updatedAt: string
}
```

### 5.2 planParser（纯函数，`services/planParser.ts`）

```ts
const TASK_RE = /^(\s*)[-*]\s+\[\s\]\s+(.+?)\s*$/   // 未勾选任务行

/** 提取所有未勾选任务行文本（去空白）。纯函数，无 DB/无副作用。*/
export function parseTasks(markdown: string): string[] {
  if (!markdown) return []
  const out: string[] = []
  for (const line of markdown.split(/\r?\n/)) {
    const m = line.match(TASK_RE)
    if (m) out.push(m[2].trim())
  }
  return out
}
```

- 只提取 `- [ ]` / `* [ ]`；**不**提取 `- [x]`（已勾选）。
- 缩进容错（行首任意空白）。
- 非法/空输入 → 返回 `[]`，不抛。

### 5.3 幂等同步（`planRepo.syncPlanTodos(planId)`，DB 操作）

**算法**（承袭项目计划 §6.2）：
```
1. 读 plan（含 content、generatedTodoIds、date）
2. parsed = parseTasks(plan.content)
3. existingTodos = todoRepo.findByIds(JSON.parse(plan.generatedTodoIds||'[]'))  // 排除软删
4. 按 content 建索引 available = Map<content, todo[]>（每个 todo 可被复用一次）
5. resultIds = []
   for task in parsed:
       const pool = available.get(task)
       if (pool && pool.length) { reuse = pool.shift(); resultIds.push(reuse.id) }   // 复用：保留其 status/completedAt
       else { t = todoRepo.create({ id: randomUUID(), content: task, status:'todo',
                planDate: plan.date, projectId: null, isDeleted:false, deletedAt:null, completedAt:null })
              resultIds.push(t.id) }
6. plan.generatedTodoIds = resultIds；planRepo 更新 plan（回填 updatedAt）
7. 「残留 todo」= 旧 generatedTodoIds 中不在 resultIds 的（用户删了某 `- [ ]` 行）→ **不软删**，仅由 repo 返回 { plan, orphanTodoIds } 供 UI 提示
```

- **复用即保留完成态**：被复用的 todo 若已 `done`，不重置（不传 status → create 不触发；复用路径不改 status）。
- **不自动软删残留**：防误删（用户可能只是临时改文档）。UI 提示「N 条任务已移出计划，对应待办仍保留，可在项目管理清理」。
- 解析/同步 `try/catch`：异常 → **保原文（plan.content 已先存）**、跳过同步、返回 `{ synced: false, error }`，不崩溃（§10 降级）。

> 顺序：保存计划时**先**写 plan.content（确保不丢），**再** `syncPlanTodos`。

### 5.4 planRepo（`db/repositories/planRepo.ts`，沿用 todoRepo/templateRepo 模式）

方法：
- `findAll(type?)` / `findByDate(date)` / `findById(id)` —— `WHERE isDeleted=0`。
- `create(data)` —— 插入；`generatedTodoIds` 存 `JSON.stringify(data.generatedTodoIds||[])`。
- `update(id, data)` —— 更新 content/type/date/templateId + `generatedTodoIds`（数组→JSON）+ updatedAt。
- `softDelete(id)`。
- `setGeneratedTodoIds(id, ids)` —— 同步回填专用（内部用）。
- `syncPlanTodos(id)` —— 见 §5.3，返回 `{ plan, orphanTodoIds, synced, error? }`。
- `rowToPlan`：`generatedTodoIds` 字符串 → 数组（`JSON.parse`，异常→`[]`）；`isDeleted`/`isDefault` 布尔↔0/1。

id 生成：`import { randomUUID } from 'node:crypto'`（**静态 import**；勿用 `require('crypto')`——参见 todoRepo.quickCreate 既有写法，不在本子阶段改动，但**新代码必须静态**）。

### 5.5 IPC（`plans.ipc.ts` + `@shared/ipc` 加 `PLAN_*`）

沿用 ok/err 信封 + logger（**禁记 plan.content 明文**）。create/update 内部：先写 plan → 再 `syncPlanTodos` → 返回 `{ plan, generatedCount, orphanCount }`。

| 通道 | 入参 | 返回 data |
|---|---|---|
| `PLAN_GET` | `{ id }` | `Plan \| null` |
| `PLAN_LIST` | `{ from?, to?, type? }` | `Plan[]` |
| `PLAN_BY_DATE` | `{ date }` | `Plan \| null`（该日期最新一条） |
| `PLAN_CREATE` | `{ date, type?, content, templateId? }` | `{ plan, generatedCount, orphanCount }` |
| `PLAN_UPDATE` | `{ id, content, date?, type? }` | `{ plan, generatedCount, orphanCount }`（触发幂等再同步） |
| `PLAN_DELETE` | `{ id }` | `{ ok }`（软删；**不**软删其生成的 todos） |

- `PLAN_UPDATE` 幂等：同内容再存 → generatedCount=0（无新建）；改内容 → 增量建/复用。
- 失败信封：`err('INTERNAL'/'NOT_FOUND'/'PARSE_FAILED', msg)`；`PARSE_FAILED` 时 plan 仍已存（content 保原文），前端据此显示降级提示。

### 5.6 PlanView 激活（变动 `PlanView.vue`）

- 顶部：`<input type="date">`（默认今天）；变更 → 调 `PLAN_BY_DATE`：有则载入其 content/type；无则载入选定模板（沿用子阶段1模板选择器）。
- 主体：MarkdownEditor（不变）。
- 底部：**「保存」按钮启用**（替换子阶段1 disabled）。点击 → 有 `currentPlanId` 则 `PLAN_UPDATE`，否则 `PLAN_CREATE`。
- 反馈（**内联，禁模态**）：保存后显示状态行：`✅ 已保存，生成 N 条待办（流入今日待办卡）` 或 `⚠️ 已保存，但 N 条任务解析失败，已保留原文` 或 `ℹ️ N 条任务已移出计划，待办仍保留`。
- 保存按钮 loading 态防双击。

### 5.7 今日计划入口（变动 `TodayView.vue`）

- 今日待办卡标题区加一个轻量链接「📝 今日计划」→ `router.push('/plan')`（手帐风，token 色，无硬编码 hex）。
- 计划生成的 todos 因 `planDate=today` 自动被现有 `todoRepo.findByPlanDate(today)` 聚合进今日待办卡——**无需改聚合逻辑**。

---

## 6. 数据模型变更

**无新迁移**。`plans` 表治理字段已在子阶段1 `0003` 补齐（id/date/type/content/generatedTodoIds/templateId/createdAt/updatedAt/isDeleted/deletedAt）。

- `generatedTodoIds` 列为 `TEXT`，存 `JSON.stringify(string[])`；repo 层做数组↔字符串转换，异常容错为 `[]`。
- 生成的 todos 复用现有 `todos` 表：`projectId=NULL`、`planDate=plan.date`、`status='todo'`；与项目管理详情页创建的 todos 形成**双入口**，统一被今日待办卡按 `planDate` 聚合。

---

## 7. IPC 契约（新增通道，见 §5.5 表）

`@shared/ipc` 的 `IPC` 新增：`PLAN_GET`/`PLAN_LIST`/`PLAN_BY_DATE`/`PLAN_CREATE`/`PLAN_UPDATE`/`PLAN_DELETE`（通道名 `plan:*`）。
preload：`api.plans.{get,list,byDate,create,update,delete}`（沿用 `api.todos` 模式）。
注册：`ipc/index.ts` 的 `registerAllIpc()` 加 `registerPlansIpc()`。

---

## 8. 任务拆解（DeepSeek 执行顺序）

1. **类型**：`@shared/types` 加 `Plan`。
2. **parser**：`services/planParser.ts`（`parseTasks` 纯函数）；装 `vitest` devDep + `package.json` 加 `"test":"vitest run"`；写 `tests/planParser.spec.ts`，用例：标准多任务 / 空 / 无任务 / `- [x]` 不提取 / 缩进容错 / 重复行各自提取。
3. **repo**：`planRepo.ts`（CRUD + `syncPlanTodos` 幂等 + `rowToPlan` JSON 往返）；静态 import `randomUUID`。
4. **IPC**：`@shared/ipc` 加 `PLAN_*`；`plans.ipc.ts`；`ipc/index.ts` 注册；preload `api.plans`。
5. **PlanView 激活**：日期选择 + 按日期加载 + 保存启用 + 内联反馈（生成数/解析失败/残留）。
6. **TodayView 链接**：今日待办卡旁「📝 今日计划」→ `/plan`。
7. **自测**：`npm test` 绿（parser）、`npm run build` 绿；手动跑幂等场景（见 §9）；更新 `当前状态.md`（§8 追加子阶段2 小节）；写 `项目日志/计划与复盘-子阶段2-计划核心.md`。

---

## 9. 验收点（GLM 评审依据，可勾选可验证）

- [ ] `Plan` 类型入 `@shared/types`
- [ ] `planParser.parseTasks` 纯函数：标准 `- [ ]` 提取；`- [x]` 不提取；空/无任务/非法→`[]` 不抛；缩进容错
- [ ] **vitest 单测**：`npm test` 全绿（标准/空/无任务/`- [x]` 不提取/缩进/重复行 ≥6 用例）
- [ ] `planRepo` CRUD 正确；`generatedTodoIds` JSON 往返（数组↔字符串）一致；softDelete 置位；`findAll` 默认过滤软删
- [ ] **幂等同步**：首次保存建 todos（`projectId=NULL`、`planDate=plan.date`、`status=todo`）；同内容再保存**不重复建**（generatedCount=0）；改内容增量建/复用
- [ ] **完成态保留**：在今日待办卡勾完成某 todo → 再保存计划（该行仍 `- [ ]`）→ 该 todo 保持 `done` + `completedAt`
- [ ] **残留不误删**：删除计划中某 `- [ ]` 行后保存 → 对应 todo **不被软删**，仍可查；UI 有残留提示
- [ ] 生成的 todos 在 `plan.date=今天` 时流入今日待办卡（`todoRepo.findByPlanDate(today)` 命中）
- [ ] 6 个 `plan:*` IPC 工作；ok/err 信封；**logger 不含 plan.content 明文**
- [ ] 解析异常 → plan content 保原文、同步跳过、返回 `PARSE_FAILED`/`synced:false`、UI 降级提示，不崩溃
- [ ] PlanView：选日期→加载已有/空白模板；保存（create/update）正确分支；内联反馈显示生成数；禁模态
- [ ] TodayView「📝 今日计划」链接存在 → `/plan`，手帐风（token 色，无硬编码 hex）
- [ ] 无新迁移；无硬编码 hex（grep 新文件干净）；静态 import（无新 `require`）；删除均软删
- [ ] `npm run build` 绿，零 TS 错误
- [ ] `当前状态.md` §8 追加子阶段2；`项目日志/计划与复盘-子阶段2-计划核心.md` 已写

---

## 10. 禁止边界 & 错误降级（承袭项目计划 §7-B/§7-C，本子阶段具化）

**禁止边界**：
- **日志禁输出 plan.content 正文**与 API Key 明文（logger 仅记异常/计数）。
- 删除一律软删除（plan 软删；生成的 todos **不**随计划软删——防误删）。
- 禁业务数据上云；禁阻断式模态弹窗（保存反馈用内联状态行）。
- **静态 import**：`planRepo`/`planParser` 用 `import { randomUUID } from 'node:crypto'`；**不得新增 `require(...)`**（todoRepo.quickCreate 的既有 `require('crypto')` 不在本子阶段改动范围——勿越界改）。
- token.css 唯一色源，组件内禁硬编码 hex；图标用 `lucide-vue-next`（新图标须同步注册进 AppIcon.vue——承袭子阶段1 F1 教训）。

**错误降级**：
- **解析/同步异常**：先保 plan.content 原文 → 跳过同步 → 返回 `synced:false` → UI「⚠️ 已保存，N 条任务解析失败，已保留原文」；不崩溃。
- **DB 锁定/损坏**：沿用全局安全模式（只读 + Toast）。
- **生成的 todo 与计划文档失配（残留）**：不自动删，仅提示，由用户在项目管理清理。

---

## 11. 交付与运行说明

- **启动/构建**：`npm run dev` / `npm run build`；**单测**：`npm test`（vitest run）。
- **DeepSeek 须在 `当前状态.md` 补充**（§8 追加子阶段2 小节）：新增 IPC 通道清单、新增文件清单、`npm test` 输出摘要、幂等 4 场景（首次建/同内容不重建/完成态保留/残留不删）自测结果、运行/复测命令。
- **GLM 审查入口**：实现完成后用 03 prompt（`{{unit}}=子阶段2`、`{{计划目录}}=计划与复盘体系实现项目计划`）启动审查，GLM 依 §9 验收点 + §10 边界逐条核（parser 单测可 headless 跑 `npm test`；幂等与 UI 流待用户 E2E）。
