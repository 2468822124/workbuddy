# R1-3 补充核验：T03-C03 用户副本最终 UI 与 active/软删除记录

## 核验范围

- 事项：`U-7` / `R1-3-T03-C03`。
- QA/快照/版本：`QA1` / `S1` / `0.3.0-qa.1`。
- 主要用户副本：`B1-20260829/R1-3/USER-20260901/workbuddy.db`，与用户 2026-09-01 原始截图同一测试副本、同一用户前缀。
- 主要用户证据：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\R1-3相关证据\R1-3-T03-C03（1）.png` 至 `R1-3-T03-C03（7）.png`。
- 用户补充证据：`R1-3-T03-C03（补充1）.png` 至 `（补充8）.png`。补充截图显示前缀为 `v0.3-R1-3-USER-20260902`，属于跨自然日后新建的日期副本，作为同场景的独立 UI 佐证，不与 `USER-20260901` 的数据库行合并。
- 核验方式：对两个副本分别使用 `readOnly=true`、`immutable=1` 和 `PRAGMA query_only=ON` 只读读取；未写入、更新、删除、重置或修复数据库。

## 用户 UI 事实核对

主要 `USER-20260901` 截图按用户实际时间顺序显示：

1. W-1 复盘趋势页执行转下周并确认，出现成功反馈，W0 能看到同名承接。
2. 返回 W-1 周统筹后，原 H1 仍以普通未完成项显示，转周及其他操作入口仍可用。
3. W0 中同名承接仍以普通未完成项显示；回到 W-1 复盘趋势页时才显示灰化/“已转下周”的历史态。

2026-09-02 的补充截图在独立 `USER-20260902` 副本复现了同一 UI 分层：复盘确认后，W-1 周统筹仍显示普通未完成项且转周按钮可用；W0 可见一条 active 承接；再次从 W-1 周统筹操作后，页面才显示“已转下周”灰化状态。

这组事实确认的是跨页面 UI 未收敛，不把用户关于“被分成两条数据”的推测写成数据库事实。

## `USER-20260901` 只读记录

来源：`E:\workspace\GPT实测记录\v0.3实测内容\第1轮\证据\R1-3-补充核验-T03-C03-USER副本.md`。

| 对象 | 只读事实 |
|---|---|
| W-1 源 H1 | `flow_week_instances.id=2`，`weekStart=2026-08-24`，`carriedFrom=null`，`isDeleted=0`；1 条 active 源 |
| W0 承接历史 | `id=3`、`id=4`、`id=5` 均为 `weekStart=2026-08-31`、`carriedFrom=2`；其中 `id=3`、`id=4` 为 `isDeleted=1`，`id=5` 为 `isDeleted=0` |
| 当前 active 承接 | `1` 条 |
| 软删除承接历史 | `2` 条 |
| 数据完整性 | `integrity_check=ok`；`foreign_key_check` 为空 |
| 只读前后哈希 | `4419DFBD5BFBAFF02914DF708957154A0095C0F3C3D91B0283A677D819BF3EC4`，未变化 |

结论：没有确认 2 条同时 active 的 W0 承接；存在 2 条同源软删除历史。该历史不单独改写为新的数据缺陷，但说明成功后的重复可操作入口需要与 UI 生命周期一起验收。

## `USER-20260902` 补充副本只读记录

来源：`E:\workspace\GPT实测记录\v0.3实测内容\第1轮\证据\R1-3-补充核验-T03-C03-USER-20260902-readonly.json`。

| 对象 | 只读事实 |
|---|---|
| W-1 源 H1 | `id=1`，`weekStart=2026-08-24`，`carriedFrom=null`，`isDeleted=0` |
| W0 承接 | `id=2`，`weekStart=2026-08-31`，`carriedFrom=1`，`isDeleted=0` |
| 当前 active 承接 | `1` 条 |
| 软删除承接历史 | `0` 条 |
| 数据完整性 | `integrity=ok`；`foreignKeyCheck=[]`；`queryOnly=1` |
| 只读前后哈希 | `C2F11B30D70F814904AFA3332AC56B0751C98B7933B522E004060A9A438E6B42`，未变化 |

## GPT 补充结论

- `U-7` 的 UI 偏离已证实：复盘页成功态、W-1 周统筹页和 W0 页面之间没有在一次转周动作后统一收敛。
- active/软删除核对已完成：两个合法用户日期副本均没有发现 2 条同时 active 的承接；`USER-20260901` 的 2 条软删除历史如实保留。
- 事项仍归入 R1 既有 `U-3 UI` 回归范围；不创建独立新问题 ID，不把用户推测写成已确认根因。
- 证据支持进入统一 `R1 Fix2` 的 UI/入口生命周期范围；Fix 只需修复跨页面状态同步、成功后入口不可重复和既有 `carriedFrom` 一条 active 承接的验收，不得借修复删除历史证据或改库造证。
