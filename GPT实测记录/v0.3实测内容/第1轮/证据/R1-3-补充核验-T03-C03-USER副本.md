# R1-3 补充核验：T03-C03 用户副本

## 核验元信息

- 核验目的：按 13 的 `R 测试合并` 要求，对用户 06 回传的 `R1-3-T03-C03` 在同一 S1、同一 R1-3 用户测试副本和同一用户前缀下做只读持久化补证。
- 核验时间：`2026-09-01T13:23:52.654Z`（北京时间 `2026-09-01 21:23:52.654 +08:00`）。
- QA/快照/版本：`QA1` / `S1` / `0.3.0-qa.1`。
- 数据库标识：`B1-20260829/R1-3/USER-20260901/workbuddy.db`。
- 实际用户前缀：`v0.3-R1-3-USER-20260901`。
- 只读方法：Node 22 `node:sqlite`，`DatabaseSync` 以 `readOnly=true` 打开 `immutable=1` URI，并执行 `PRAGMA query_only=ON`；未插入、更新、删除、重置或修复任何数据。

## 用户事实对应

- 用户证据：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\R1-3相关证据\R1-3-T03-C03（1）.png` 至 `R1-3-T03-C03（7）.png`。
- 用户可见事实：复盘趋势 W-1 中原项显示“已转下周”；回到 W-1 周统筹后，同一 H1 仍以普通未完成项显示，转周按钮和其他操作按钮仍可用；W0 页面中 H1 仍显示未完成并位于待安排区域。用户“被分成两条数据”保留为推测，不改写为已确认事实。
- 计划预期：W-1 原 H1 保留但显示“已转下周”、划线/变灰或等价收敛状态；转周入口和确认条消失或不可操作；W0 只出现一条 CARRY1，并保留 `carriedFrom` 关系。

## 只读结果

- `integrity_check`：`ok`。
- `foreign_key_check`：空。
- H1 W-1 源：`id=2`，`weekStart=2026-08-24`，`carriedFrom=null`，`isDeleted=0`，1 条 active 源记录。
- H1 W0 承接历史：3 条，均为 `weekStart=2026-08-31`、`carriedFrom=2`：
  - `id=3`：`isDeleted=1`，创建 `2026-09-01T11:39:43.583Z`，删除 `2026-09-01T11:40:21.282Z`；
  - `id=4`：`isDeleted=1`，创建 `2026-09-01T11:42:17.751Z`，删除 `2026-09-01T11:46:43.781Z`；
  - `id=5`：`isDeleted=0`，创建 `2026-09-01T11:46:46.265Z`，当前唯一 active 承接。
- 按该 H1 标题和 `carriedFrom=2` 统计：`activeCarryRows=1`，`deletedCarryRows=2`，不是 2 条同时 active 的 W0 承接。
- 原数据库 SHA-256：`4419DFBD5BFBAFF02914DF708957154A0095C0F3C3D91B0283A677D819BF3EC4`。
- 只读核验后数据库 SHA-256：`4419DFBD5BFBAFF02914DF708957154A0095C0F3C3D91B0283A677D819BF3EC4`；`hashUnchanged=true`。

## 核验边界与结论

- 该结果客观确认：用户副本中存在 1 条 H1 源、1 条当前 active 的 W0 承接，以及 2 条同源、后续被软删除的承接历史；未确认 2 条同时 active 的重复承接。
- 用户截图客观显示的 W-1 周统筹 UI 未收敛，不能被“当前 active 承接只有 1 条”抵消；UI 状态和持久化结果必须分栏记录。
- 该事项暂定为当前 R 的 `待核实` 问题候选，证据等级为 `已证实（数据行数/关系）+ 待核实（UI 与承接历史的根因及是否应阻止重复操作）`；在问题级证据和分类完成前不得进入 Fix。
