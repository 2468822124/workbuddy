# R1-3 补充核验：T02-C01 用户副本

## 核验元信息

- 核验目的：按 13 的 `R 测试合并` 要求，对用户 06 回传的 `R1-3-T02-C01` 在同一 S1、同一 R1-3 用户测试副本和同一用户前缀下做只读持久化补证。
- 核验时间：`2026-09-01T13:23:52.654Z`（北京时间 `2026-09-01 21:23:52.654 +08:00`）。
- QA/快照/版本：`QA1` / `S1` / `0.3.0-qa.1`。
- 数据库标识：`B1-20260829/R1-3/USER-20260901/workbuddy.db`。
- 实际用户前缀：`v0.3-R1-3-USER-20260901`；标题中的完整前缀为 `[v0.3-R1-3-USER-20260901]`。
- 只读方法：Node 22 `node:sqlite`，`DatabaseSync` 以 `readOnly=true` 打开 `immutable=1` URI，并执行 `PRAGMA query_only=ON`；未插入、更新、删除、重置或修复任何数据。

## 用户事实对应

- 用户证据：
  - `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\R1-3相关证据\R1-3-T02-C01（1）.png`
  - `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\R1-3相关证据\R1-3-T02-C01（2）.png`
- 截图事实：月目标页显示 `[v0.3-R1-3-USER-20260901]-M1 月目标`；进入 W36（`2026-08-31 ~ 09-06`）“本周核心推进目标”区域后，选择器只显示“或从月目标选取…”占位项，数量显示 `0`，未显示 M1。
- 计划预期：从月目标选择 M1 生成 W1；M1、W1、I2 应出现在对应列表，且 W1 保留 M1 关联。

## 只读结果

- `integrity_check`：`ok`。
- `foreign_key_check`：空。
- `flow_month_goals` 前缀行：1 条，`id=1`，`month=2026-09`，标题为 `[v0.3-R1-3-USER-20260901]-M1 月目标`，`isDeleted=0`。
- `flow_week_focus` 前缀行：0 条；核验时没有可持久化识别的 W36 周核心目标记录。
- `flow_week_instances` 前缀行：I2 记录 1 条，`weekStart=2026-08-31`、`kind=multi`、`targetCount=3`、`isDeleted=0`。该行证明后续 I2 测试数据仍在同一用户副本，不替代对 W1 关联的判断。
- 原数据库 SHA-256：`4419DFBD5BFBAFF02914DF708957154A0095C0F3C3D91B0283A677D819BF3EC4`。
- 只读核验后数据库 SHA-256：`4419DFBD5BFBAFF02914DF708957154A0095C0F3C3D91B0283A677D819BF3EC4`；`hashUnchanged=true`。

## 核验边界与结论

- 该结果与用户截图一致支持：M1 已成功创建并持久化，但本次核验时没有 W36 的 `flow_week_focus` 记录。
- 只读数据不能单独证明“选择器没有列出 M1”的具体代码根因；GUI 截图是该可见事实的主要证据，数据库结果是同副本的持久化补证。
- 事项暂定为 `待核实` 的用户实测偏离/问题候选，不能据此直接认定回归、独立新增或进入 Fix。
