# R1-3 补充核验：用户补充问题 1（T02-C03 用户副本）

## 核验元信息

- 核验目的：按 13 要求核对用户指出的计划脚本设计问题；该条使用已有 `R1-3-T02-C03`，不是新测试点或新子用例。
- 核验时间：`2026-09-01T13:23:52.654Z`（北京时间 `2026-09-01 21:23:52.654 +08:00`）。
- QA/快照/版本：`QA1` / `S1` / `0.3.0-qa.1`。
- 数据库标识：`B1-20260829/R1-3/USER-20260901/workbuddy.db`；实际用户前缀为 `v0.3-R1-3-USER-20260901`。
- 只读方法：Node 22 `node:sqlite`，`DatabaseSync` 以 `readOnly=true` 打开 `immutable=1` URI，并执行 `PRAGMA query_only=ON`；未写入或重置用户数据。

## 用户事实对应

- 用户证据：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\R1-3相关证据\R1-3-T02-C03（1）.png` 至 `R1-3-T02-C03（10）.png`。
- 用户时间顺序：D0、D+1、D+2 分别安排并完成 I2 一场；周统筹显示 `3/3`；回到 D0 删除后任务重新可选，周统筹恢复 `2/3`。
- 用户判断：软件行为符合每天安排一场、跨三天完成三场的实际模型；计划脚本把三次完成写成同一天连续完成，属于测试计划/脚本设计问题候选，不作为产品缺陷，不进入 Fix。

## 只读结果

- `integrity_check`：`ok`；`foreign_key_check`：空。
- I2 周实例：`flow_week_instances.id=1`，`weekStart=2026-08-31`，`kind=multi`，`targetCount=3`，`isDeleted=0`。
- 当前 active I2 日安排共 3 条，日期分别为 `2026-09-01`、`2026-09-02`、`2026-09-03`，均指向 `weekInstanceId=1`。
- 对应当前 active 日完成凭据共 3 条，均为 `kind=check`，分别指向上述 3 条 active 日安排；与 `targetCount=3` 一致。
- 用户删除/重建 D0 或 D+2 日安排产生的历史软删除行仍保留；该历史保留不被解释为本次计划设计判断之外的产品缺陷，也不改写用户的主观验收。
- 原数据库与只读核验后 SHA-256 均为 `4419DFBD5BFBAFF02914DF708957154A0095C0F3C3D91B0283A677D819BF3EC4`；`hashUnchanged=true`。

## 核验边界与结论

- 只读结果支持“跨三天各一场、最终 3/3”的客观执行事实。
- 本核验不把用户未在同一天连续点击三次写成失败，也不把当前计划脚本改写为已修订；计划设计修订应由 14 在合法后续计划节点处理。
- 该条为 `用户补充问题 1` 来源标签下的计划脚本设计/覆盖候选，不进入代码 Fix，不创建新测试点、子用例或新问题来源 R。
