# v0.3-R1-2-GPT实测

> **维护方**：GPT（项目经理 / GUI 实测人员）
> **报告性质**：GPT 根据 `R1-2` 测试计划执行的可见窗口实测记录
> **本报告边界**：本报告记录 `R1-2` 计划的前置核对和可见窗口实测；如前置资料存在冲突，停止业务实测，不以代码阅读、历史截图或其他构建物结果替代本轮 GUI 证据。

## 一、报告元信息

- **报告标题**：`v0.3-R1-2-GPT实测`
- **测试计划类型**：`R 轮次计划`
- **测试标识**：`R1-2`
- **实测版本**：`v0.3`
- **功能名称**：`任务数据流通重构`
- **实测日期**：`2026-08-23`
- **实测人员**：GPT
- **记录状态**：`⏸️停止（需补充信息）`
- **本次结论**：`⏸️停止（需补充信息）`
- **测试计划路径**：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\v0.3-R1-2-实测计划.md`
- **上一份同标识 GPT 报告**：`无同标识报告；历史参考为 E:\workspace\GPT实测记录\v0.3实测内容\v0.3GPT实测 1.md 和 E:\workspace\GPT实测记录\v0.3实测内容\v0.3GPT实测 2.md，不替代本轮证据`
- **关联轮次闭环文档**：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\v0.3-R1-项目投影模板与周统筹创建-全流程闭环记录.md`
- **实测门禁**：`C0 首次实测`
- **候选代码状态**：`C0`
- **实测快照 ID**：`S0`

## 二、实测前置

### 2.1 测试计划读取摘要

- **计划状态**：`✅可执行`
- **计划唯一标识**：`R1-2`，与文件名和本报告一致。
- **版本/构建标识**：`0.3.0-c0`
- **package.json 版本字段**：只读核对 C0 提交中的 `workbuddy/package.json`，为 `0.3.0-c0`。
- **C0/QA 来源**：`C0`
- **实测快照 ID**：`S0`
- **来源提交号**：`34f155a1886c52dae278fbc71d037975646b2563`
- **来源 Tag**：`v0.3.0-c0`，Tag 解析到上述提交。
- **来源工作树指纹**：计划声明 `C0 提交已归档；本计划及实测文档当前未提交`。只读核对当前 Git 工作树有 3 条非忽略状态记录；未提交、未清理，也未作为被测代码启动。
- **构建目录**：`E:\workbuddy-snapshots\v0.3.0-c0-r1-2-s0\out`
- **构建物哈希**：`WorkBuddy.exe SHA-256 B9F75D623F13069DCA8CE042737924905B62EE2BDD6418527C26365B5019B127`
- **S0 清单哈希**：`S0-manifest.sha256 SHA-256 F7A30AB853F2F1478B4EC25C12FA64FCF45784033115D2E32F6948F00D1BD2AF`
- **快照冻结时间**：`2026-08-22 13:15:33`
- **构建物规模**：S0 独立构建目录只读核对为 100 个文件、335398344 bytes。
- **测试包核对**：`E:\workbuddy-test\app-v0.3` 为 100 个文件、335398344 bytes；测试包 `WorkBuddy.exe` SHA-256 与 S0 一致，为 `B9F75D623F13069DCA8CE042737924905B62EE2BDD6418527C26365B5019B127`。
- **本次 GPT 范围**：计划列出的客观状态、持久化、刷新/重进、派生数据、历史回归、错误降级、兼容、导入导出和隔离安全。
- **共同核心链路**：`R1-2-T01` 至 `R1-2-T06`。
- **明确不测试项**：正式版本发布、C1 Tag、真实用户数据迁移、计划未列出的全新功能；原因见测试计划第七节。
- **报告落点**：`E:\workspace\GPT实测记录\v0.3实测内容\第1轮\v0.3-R1-2-GPT实测.md`
- **证据落点**：`E:\workspace\GPT实测记录\v0.3实测内容\第1轮\证据\`

### 2.2 门禁与快照核对

#### C0 首次实测门

- [x] **开发单元闭环**：`当前审查状态.md` 记载阶段5、阶段6和用户反馈1当前开发闭环；阶段1至阶段4闭环记录的最新门禁也为 `✅开发闭环`。用户验收不被此项替代。
- [x] **C0 提交、Tag 和版本字段**：提交 `34f155a1886c52dae278fbc71d037975646b2563` 存在；`v0.3.0-c0` 指向该提交；C0 `package.json` 版本为 `0.3.0-c0`。
- [x] **S0 冻结**：独立构建目录、WorkBuddy.exe 哈希、清单哈希、文件数量、字节数和冻结时间均可只读核对。
- [x] **独立被测包**：计划指定的测试包哈希与 S0 构建物一致。
- [x] **程序入口**：`E:\workbuddy-test\启动实测.bat` 和 `E:\workbuddy-test\app-v0.3\WorkBuddy.exe` 均存在；启动方式明确为可见窗口双击或执行批处理入口。本轮因资料冲突未执行。
- [x] **测试数据隔离**：`E:\workbuddy-test\userData\` 存在且当前 0 个文件；真实用户数据仅按计划使用 `%APPDATA%\workbuddy\` 作为禁止触碰边界，本轮未读取、复制、修改或删除。
- [x] **重置方式**：`E:\workbuddy-test\重置实测数据.bat` 存在；本轮未执行，未启动 WorkBuddy。

#### 前置核对结果

快照、构建、版本、入口和目录隔离条件已具备。但业务实测前置资料存在未解决的产品契约冲突，按规则停止，不进入 WorkBuddy 可见窗口操作。

### 2.3 依据与矛盾

| 依据 | 路径 | 读取摘要 | 是否存在矛盾 | 处理 |
|---|---|---|---|---|
| 测试计划 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\v0.3-R1-2-实测计划.md` | 计划类型为 R，唯一标识 `R1-2`，绑定 `C0/S0`，测试点为 `R1-2-T01` 至 `R1-2-T06`。 | 有，继承的用户流程与当前决策不一致 | 启动前停止 |
| 当前审查状态 | `E:\workspace\当前审查状态.md` | 第15、18、58行附近仍写 UF4 待用户/PM 选择 A/B；第78至82行又写用户已选择方案 B。 | 有，单文件内部冲突 | 需要统一当前有效状态 |
| 当前代码状态 | `E:\workspace\当前代码状态.md` | 第25至26、38、56行附近写 UF4 已裁决为方案 B，必须显式点击“加入今日”。 | 与用户流程冲突 | 需要确认是否为当前有效契约 |
| 开发单元闭环记录 | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\v0.3-R1-项目投影模板与周统筹创建-全流程闭环记录.md` | 当前 R1 续写区保留“待补证据/待用户实测”；前文第258至281行仍记 UF4 待裁决；第675行以后又记录 C0-01 方案 B。 | 有，当前/历史段落未形成唯一有效值 | 需要确认并修正文档治理状态 |
| 用户测试流程 | `E:\workspace\用户打回重构\任务数据流通重构（v0.3）\用户测试流程.md` | 第224至229行要求项目任务计划日期设为今天，并预期按计划日期投影到今日总览；步骤未要求先点击“加入今日”。 | 与方案 B 冲突 | 必须明确 A 或 B |
| 技术栈规范 | `E:\workspace\规范类\技术栈规范.md` | 技术选型、分层、IPC、数据安全和测试门禁规范已读取；未发现与本次停止原因相关的冲突。 | 无 | 保留为依据 |

### 2.4 需补充信息

本轮停止原因不是 GUI 缺陷，而是业务实测前置文档没有唯一一致的测试预期。需要用户或开发者确认并统一以下值：

1. **UF4 当前有效产品语义只能二选一**：
   - **方案 A**：项目任务的计划日期为今天时，自动投影到今日总览；需明确改为明天、改回今天、清空日期时的边界预期，并确认是否需要代码修复。
   - **方案 B**：必须显式点击“加入今日”才生成今日投影；需将 `用户测试流程.md` 第3.2节预期改为显式加入今日，并将 `当前审查状态.md` 顶部状态、R1 闭环当前摘要中的“待裁决”更新为已裁决方案 B。
2. **需确认当前有效文档值**：`当前审查状态.md` 第15、18、58行附近的“UF4待裁决”，与同文件第78至82行“已选择方案B”哪个为当前有效状态。
3. **需确认本 R1-2 测试点 T04 的预期**：计划要求项目渠道汇聚，但未在稳定测试点表中明示 UF4 的“仅日期”与“加入今日”分支；确认后再执行该分支和证据命名。
4. **历史 GPT 报告路径缺失**：闭环记录引用 `E:\workspace\GPT实测记录\v0.3实测内容\v0.3GPT实测 1.md`，该文件当前不存在，导致历史 F2/F4/F6 的原始 GPT 报告无法独立读取；需确认正确路径或补充该文档后再做深度回归。

在上述值统一前，不得猜测、临时替换启动命令或扩大测试范围；本轮未启动业务程序。

## 三、运行环境与隔离

- **操作系统**：Windows 11 家庭中文版，10.0.22631，x64。
- **窗口尺寸**：未启动 WorkBuddy，未产生窗口尺寸或页面状态证据。
- **启动方式**：计划指定双击或执行 `E:\workbuddy-test\启动实测.bat`；本轮未执行。
- **程序入口**：`E:\workbuddy-test\app-v0.3\WorkBuddy.exe`；文件存在，未启动。
- **测试专用目录**：`E:\workbuddy-test\`。
- **测试数据目录**：`E:\workbuddy-test\userData\`，只读核对为 0 个文件。
- **真实用户数据目录核对**：未读取、未进入、未复制、未修改、未删除；仅保留 `%APPDATA%\workbuddy\` 作为禁止触碰边界。
- **网络状态**：未核对；业务实测已在网络场景前停止。
- **重置命令**：`E:\workbuddy-test\重置实测数据.bat`；未执行。
- **统一测试前缀**：计划绑定 `[v0.3-R1-2-20260822]`；本轮未写入业务数据。
- **启动输出摘要**：未启动，因此无启动输出或 GUI 状态可记录。当前 WorkBuddy 进程数为 0。
- **报告路径**：`E:\workspace\GPT实测记录\v0.3实测内容\第1轮\v0.3-R1-2-GPT实测.md`。
- **证据目录**：`E:\workspace\GPT实测记录\v0.3实测内容\第1轮\证据\`，目录存在且本轮保持 0 个文件。
- **被测快照**：`S0`；未运行，因前置冲突停止。

## 四、可见窗口与操作合规

- [ ] 用户可见 WorkBuddy 窗口完成点击、键盘、选择、导航、确认/取消、拖动和滚动。本轮未启动，未执行。
- [ ] 原生文件对话框、日期选择器、系统确认框。本轮未进入，未执行。
- [x] 每个业务测试点未执行；不存在等待页面稳定后仍记录为通过的情况。
- [x] 未直接调用业务 IPC、预加载 API、HTTP、SQL/SQLite 或内部函数替代用户操作。
- [x] 未通过脚本插入、修改或删除业务数据；测试数据目录保持空。
- [x] 未修改产品代码、测试程序、数据库、`当前代码状态.md`、`当前审查状态.md` 或用户测试计划。
- [x] 未生成截图；没有敏感信息证据需要删除或重新截取。

## 五、逐项实测记录

> 以下六个测试点均属于计划范围，但因第2节前置文档冲突，在业务窗口启动前停止。没有把代码复审、历史 GUI 记录或构建核对写成测试点通过。

### R1-2-T01 启动、隔离、首次使用、窗口自适应

- **计划范围**：双方共同；GPT 负责启动事实、隔离和窗口状态。
- **前置与测试数据**：计划要求 S0 可启动、测试数据为空；空目录已只读核对。
- **操作**：未执行启动脚本、首次进入、创建前缀数据、重启或调整窗口。
- **预期反馈**：计划要求程序正常进入、首次状态清晰、窗口可用，数据只写入测试目录。
- **实际反馈**：未产生页面、文本、数量、跳转或等待结果。
- **证据**：无截图；证据目录保持空。前置核对见本报告第2节。
- **结论**：`⚠️待用户实测`（本轮因前置文档冲突未执行，不代表用户已完成）。
- **严重度**：`ℹ️待用户确认`
- **依赖/备注**：需先统一 UF4 文档值；恢复后按计划使用可见窗口执行。

### R1-2-T02 月目标、周统筹、固定/临时任务

- **计划范围**：双方共同；GPT 负责状态、关联、持久化和 UF1/UF2 回归。
- **前置与测试数据**：依赖 T01；本轮未创建测试数据。
- **操作**：未创建月目标、周核心目标、固定任务或临时/场次任务，未执行完成、取消、删除或重进。
- **预期反馈**：计划要求关联清晰、一次型/场次型状态正确、确认可取消、重进后创建和更新结果保持。
- **实际反馈**：未产生 GUI 反馈或持久化结果。
- **证据**：无截图；证据目录保持空。
- **结论**：`⚠️待用户实测`（本轮前置停止）。
- **严重度**：`ℹ️待用户确认`
- **依赖/备注**：依赖 T01；不把历史 UF1/UF2 复审结果移植到 S0 本轮。

### R1-2-T03 周任务、日规划 rail、今日任务

- **计划范围**：双方共同；GPT 负责 rail、完成态、场次和持久化及 UF3 回归。
- **前置与测试数据**：依赖 T02 有可用任务；本轮无业务数据。
- **操作**：未创建或完成周任务，未查看日规划 rail，未安排今日任务，未执行 UF3 模板或离开后重进。
- **预期反馈**：任务在正确日期/rail 展示，完成态和模板反馈可读，来源、日期、状态和模板操作一致。
- **实际反馈**：未产生 GUI 反馈或持久化结果。
- **证据**：无截图；证据目录保持空。
- **结论**：`⚠️待用户实测`（本轮前置停止）。
- **严重度**：`ℹ️待用户确认`
- **依赖/备注**：依赖 T02；不把历史 UF3 GUI 复审结果移植到 S0 本轮。

### R1-2-T04 四渠道汇聚到今日总览

- **计划范围**：双方共同；GPT 负责来源、派生、完成状态和 T04 相关安全核对。
- **前置与测试数据**：依赖 T02/T03；本轮未写入项目、提醒、手动待办或周/日任务。
- **操作**：未执行四渠道加入/生成、总览查看、完成/移出或重进。
- **预期反馈**：计划要求来源可辨识、总览不重复、完成态与源数据一致，移出/完成语义正确。
- **实际反馈**：未产生 GUI 反馈或派生数据结果。
- **证据**：无截图；证据目录保持空。
- **结论**：`⚠️待用户实测`（本轮前置停止）。
- **严重度**：`ℹ️待用户确认`
- **依赖/备注**：T04 的项目渠道还依赖 UF4 A/B 唯一预期，需先确认仅设日期与点击“加入今日”的测试分支。

### R1-2-T05 完成凭据、复盘统计、趋势、封账、转下周

- **计划范围**：双方共同；GPT 负责凭据、统计、趋势和跨周持久化。
- **前置与测试数据**：依赖 T02-T04 有完成和未完成数据；本轮无业务数据。
- **操作**：未添加/查看凭据，未复盘、查看统计/趋势、封账或转下周，未刷新/重进。
- **预期反馈**：计划要求统计与实际数据一致，跨周动作可解释且可恢复，无丢失或重复。
- **实际反馈**：未产生 GUI 反馈或数据结果。
- **证据**：无截图；证据目录保持空。
- **结论**：`⚠️待用户实测`（本轮前置停止）。
- **严重度**：`ℹ️待用户确认`
- **依赖/备注**：依赖 T02-T04；不能以开发记录中的测试结果替代本轮 S0 窗口证据。

### R1-2-T06 导入导出、旧入口、错误降级、数据安全

- **计划范围**：GPT/用户；GPT 负责隔离副本、导入导出、旧入口和错误降级核对。
- **前置与测试数据**：计划要求先有导出测试数据和可恢复隔离副本；本轮未生成导出文件，也未制造数据库异常。
- **操作**：未执行导入/导出、旧入口访问、空/非法/不可达输入或损坏/只读数据库场景。
- **预期反馈**：计划要求错误可读、不崩溃、不白屏、无无限重试，合法数据可恢复，非法场景不清空、不重复写入、不串档。
- **实际反馈**：未产生 GUI 反馈、错误文本或数据结果。
- **证据**：无截图；证据目录保持空。未操作数据库副本。
- **结论**：`⚠️待用户实测`（本轮前置停止）。
- **严重度**：`ℹ️待用户确认`
- **依赖/备注**：依赖前置资料统一；数据库损坏/只读场景仅在计划副本和恢复步骤可核对后执行。

## 六、取消、错误降级与数据一致性

本节所有业务操作均未执行：删除、封账、转周、导入确认/取消、空输入、非法输入、不可达地址、旧入口、损坏/只读数据库均无本轮 GUI 结果。没有截图、没有页面错误文本、没有数据变更，也没有把“未操作”写成安全通过。

| 测试点 ID | 场景 | 操作 | 预期 | 实际 | 页面/错误反馈 | 数据是否保持 | 证据 | 结论 | 严重度 |
|---|---|---|---|---|---|---|---|---|---|
| `R1-2-T02` | 删除/完成取消 | 未执行 | 计划要求取消后原数据不变，确认后只产生预期变化 | 无结果 | 无结果 | 测试目录仍为空 | 无 | `⚠️待用户实测` | `ℹ️待用户确认` |
| `R1-2-T05` | 封账/转周取消与确认 | 未执行 | 计划要求可读确认、可恢复且不重复 | 无结果 | 无结果 | 测试目录仍为空 | 无 | `⚠️待用户实测` | `ℹ️待用户确认` |
| `R1-2-T06` | 导入/错误降级/数据库异常 | 未执行 | 计划要求可读错误、不崩溃、不清空、不串档 | 无结果 | 无结果 | 未操作数据库 | 无 | `⚠️待用户实测` | `ℹ️待用户确认` |

## 七、已通过项

本轮没有已通过的业务测试点。快照哈希、目录数量、入口文件和隔离边界是实测前置核对事实，不构成 `R1-2-T01` 至 `R1-2-T06` 的 GUI 通过证据。

## 八、发现问题

本轮没有通过 GUI 复现的产品缺陷，因为程序未启动、没有业务操作。以下是阻断业务实测的前置文档矛盾，不作为产品缺陷关闭或升级：

| 问题 ID | 测试点 ID | 来源 | 严重度 | 问题/实际 | 复现条件 | 影响 | 证据 | 建议去向 |
|---|---|---|---|---|---|---|---|---|
| `P-01` | `R1-2-T04` | 前置资料冲突 | `ℹ️待用户确认` | 用户测试流程写“计划日期为今天即投影”，而当前代码状态和 C0-01 写“必须点击加入今日”；当前审查状态、R1 闭环记录和测试计划未形成唯一一致的 UF4 预期。 | 只读对比测试计划、当前审查状态、当前代码状态、R1 闭环记录和用户测试流程即可复核，无需启动程序。 | T04 预期反馈、操作分支和通过标准不唯一；按规则在启动前停止。 | 本报告第2.3、2.4节；无 GUI 截图。 | 待用户或开发者确认并统一 UF4 语义、相关文档和 T04 预期后，再按同一 C0/S0 继续执行。 |

## 九、待用户实测项与未测试项

### 待用户实测项

| ID | 原因 | 用户操作 | 预期 | 必须回传 | 证据落点 |
|---|---|---|---|---|---|
| `U-R1-2-T01` | 本轮前置停止；需用户完成共同启动和窗口体验 | 按计划启动 S0，首次进入、调整窗口、重启 | 可启动、数据隔离、窗口可用 | 实际状态、等待条件和截图 | `用户实测阶段\v0.3-任务数据流通重构\第1轮\R1-2相关证据\` |
| `U-R1-2-T02` | 本轮前置停止；需用户完成创建、编辑、完成、取消和删除体验 | 按计划执行月目标到周统筹链路 | 语义清晰，确认可取消，重进一致 | 实际结果、阻塞和证据 | 同上 |
| `U-R1-2-T03` | 本轮前置停止；需用户完成日规划、rail、模板和重进体验 | 按计划执行周任务到今日任务链路 | 日期、来源、完成态和模板反馈一致 | 实际结果和证据 | 同上 |
| `U-R1-2-T04` | 本轮前置停止；还需确认 UF4 A/B 语义 | 按统一后的流程执行四渠道总览 | 来源可辨识、不重复、不误删 | 实际结果、UF4 选择和证据 | 同上 |
| `U-R1-2-T05` | 本轮前置停止；需用户完成跨页面复盘体验 | 按计划执行凭据、复盘、趋势、封账和转周 | 统计可解释，跨周可恢复 | 实际结果和证据 | 同上 |
| `U-R1-2-T06` | 本轮前置停止；需用户完成可见错误和导入导出体验 | 按计划执行合法/非法导入、旧入口和错误降级 | 可读错误、不崩溃、不清空、不串档 | `用户prompt\06-用户反馈测试问题.md` 回传及证据 | 同上 |

### 未测试项

| 测试点 ID | 原因 | 依赖/恢复条件 |
|---|---|---|
| `R1-2-T01` | 前置文档冲突，启动前停止 | 统一 UF4 A/B 语义及文档状态后，使用同一 S0 重测 |
| `R1-2-T02` | 依赖 T01，未启动 | T01 完成且无阻断 |
| `R1-2-T03` | 依赖 T02，未创建任务 | T02 完成且无阻断 |
| `R1-2-T04` | 依赖 T02/T03，且 UF4 预期不唯一 | 统一项目投影语义后执行 |
| `R1-2-T05` | 依赖 T02-T04，未生成数据 | 上游链路完成且无阻断 |
| `R1-2-T06` | 依赖测试数据导出和恢复副本，未准备操作 | 前置统一并确认隔离副本/恢复步骤 |

## 十、回归项

| 历史问题 ID | 原始复现步骤 | 修复后预期 | 本轮实际操作 | 实际反馈 | 证据 | 状态 |
|---|---|---|---|---|---|---|
| `UF1` | 周统筹从月目标选取或手动新增核心目标 | 创建成功、标题/月目标关联/周归属正确，重进保持，无 `weekStart/title` 错误 | 未执行；前置停止 | 无结果 | 无 | 无法完成复测 |
| `UF2` | 周任务创建一次型和场次型任务并记录场次 | 一次型成功；场次型保持 `0/3 → 1/3 → 3/3`，重进保持 | 未执行；前置停止 | 无结果 | 无 | 无法完成复测 |
| `UF3` | 日规划创建含 2 项的模板并查看列表/反馈 | 列表显示、项数和成功反馈可见，无空红条；模板后续链路可继续 | 未执行；前置停止 | 无结果 | 无 | 无法完成复测 |
| `UF4` | 设置项目任务日期、查看今日总览，再执行“加入今日”边界 | A 或 B 需先定稿；当前文档无法确定唯一预期 | 未执行；发现前置冲突 | 无结果 | 无 | 无法完成复测，需先裁决 |
| `GPT F2` | 历史复盘统计/完成率回归 | 统计与任务事实一致 | 未执行 | 无结果 | 不复用历史证据 | 无法完成复测 |
| `GPT F4` | 历史未关闭目标样式回归 | 目标状态和样式符合计划 | 未执行 | 无结果 | 不复用历史证据 | 无法完成复测 |
| `GPT F6` | 清空 AI Key 后错误反馈回归 | 错误可读，不残留误导状态，不影响确定性任务流 | 未执行 | 无结果 | 不复用历史证据 | 无法完成复测 |

## 十一、正式 GPT 结论

`⏸️停止（需补充信息）`

- **依据**：测试计划和唯一标识匹配；C0/S0、独立构建、哈希、入口、版本和隔离目录均可只读核对。但 `当前审查状态.md`、`当前代码状态.md`、R1 闭环记录和 v0.3 用户测试流程对 UF4 项目投影语义没有唯一一致的预期。
- **已通过项数量**：`0` 个业务测试点。
- **发现问题**：无本轮 GUI 复现缺陷；前置文档阻断 `P-01`，级别 `ℹ️待用户确认`。
- **待用户实测项**：`R1-2-T01` 至 `R1-2-T06` 的用户范围，以及统一 UF4 语义后的共同链路。
- **未测试项**：`R1-2-T01` 至 `R1-2-T06` 的 GPT 业务范围和回归项 UF1、UF2、UF3、UF4、GPT F2/F4/F6。
- **用户验收状态**：尚未由本报告替代；用户仍需按同一测试计划实际操作后使用 `用户prompt\06-用户反馈测试问题.md` 回传。
- **版本级 C1 状态**：本报告不形成 C1；不宣称用户实测闭环、用户验收通过或项目进入维护期。
- **下一步**：先确认 UF4 方案 A/B 并统一相关文档，再在同一 `C0/S0` 独立构建中重新执行本计划；不清理当前工作树既有未提交状态。

## 十二、记录完整性检查

- [x] 计划路径和测试标识一致：R 计划为 `R1-2`，测试点均使用 `R1-2-T01` 至 `R1-2-T06`。
- [x] 报告落在计划指定路径；证据目录存在且本轮保持空。
- [x] 没有把代码阅读、自测、历史截图或其他构建物结果冒充本轮 GUI 证据。
- [x] 启动方式、程序入口、测试数据隔离、真实用户数据边界和只读核对结果已记录；窗口和业务状态因停止未产生。
- [x] 没有把未执行测试写成通过；依赖前置冲突的测试点均列为待实测/未测试。
- [x] 前置文档冲突已列出文件、冲突内容、需确认值和恢复条件。
- [x] 没有宣称用户验收闭环、功能用户实测闭环或维护期。
- [x] 本轮没有修改产品代码、测试程序、数据库、当前状态文档或用户实测计划。

---

## 十三、R1-2 续测补充（2026-08-25）

> 本节是同一 `R1-2`、同一 `C0/S0` 的继续实测记录。上文的 `⏸️停止（需补充信息）` 是 2026-08-23 前置核对阶段的历史状态；本节记录补充资料核对后的实际 GUI 结果，并以本节最新结论为准，不删除或改写原始中止记录。
>
> UF4 按 `C0-01` 的现行方案 B 执行：项目任务仅设置计划日期不会自动生成今日投影，必须在项目详情显式点击“加入今日”。旧文档中的待裁决文案不在本节被改写。

### 13.1 续测前置与运行事实

- **测试标识**：`R1-2`；计划、报告和测试点 ID 一致。
- **候选/快照**：`C0 / S0`，版本/构建标识 `0.3.0-c0`。
- **来源提交/Tag**：`34f155a1886c52dae278fbc71d037975646b2563` / `v0.3.0-c0`。
- **构建物**：`E:\workbuddy-test\app-v0.3\WorkBuddy.exe`；SHA-256 `B9F75D623F13069DCA8CE042737924905B62EE2BDD6418527C26365B5019B127`，与计划绑定的 S0 一致。
- **测试数据**：使用 `E:\workbuddy-test\userData\`，统一前缀为 `[v0.3-R1-2-GPT-20260825]`。未读取、复制、修改或删除真实 `%APPDATA%\workbuddy\`。
- **程序状态**：最终关闭后 `WorkBuddy` 进程数为 `0`；测试副本 `workbuddy.db` 仍存在，末次只读核对大小为 `237568` bytes。导出文件和计划指定非法文件均存在。
- **启动反馈**：使用计划环境变量 `WORKBUDDY_USER_DATA=E:\workbuddy-test\userData` 启动时，`BOOT1`、`I2` 和 `D1` 均按前次结果恢复；每次 GUI 动作后等待约 `400-800ms`，再获取新窗口状态。
- **敏感信息**：没有读取或记录真实 API Key；截图中的 Key 仅为界面掩码/占位显示。

### 13.2 逐项 GUI 结果

#### R1-2-T01 启动、隔离、首次使用、窗口自适应

- **操作**：按测试环境启动 S0，进入今日总览；保存带前缀的 `BOOT1`，调整可见窗口尺寸，关闭并在隔离环境下重开。
- **预期**：程序进入今日总览，BOOT1 和测试数据持久化，窗口可用，数据只写入测试副本。
- **实际**：在带 `WORKBUDDY_USER_DATA` 的启动下，BOOT1、前缀任务和页面均正常恢复，已有证据为 `R1-2-T01-C01-启动.png`、`R1-2-T01-C02-BOOT1.png`、`R1-2-T01-C03-窗口.png`、`R1-2-T01-C04-重进.png`。
- **额外边界复现**：关闭隔离启动的程序后，从文件资源管理器直接双击同一 `WorkBuddy.exe`。窗口显示另一组非本轮前缀数据（“朋友”、`v0.3日规划手动任务-已改名`、`1` 项顺延和 `Agent 已为你抓取 5 条`），而不是 BOOT1/I2/D1。随后关闭该窗口，并用明确的测试环境变量重新启动，前缀数据恢复。直接启动窗口证据：`R1-2-T01-C05-直接启动隔离.png`。
- **结论**：`❌缺陷`；正确环境启动的功能结果可见，但直接 exe 启动无法证明使用测试副本，见问题 `F-ISO-01`。
- **严重度**：`🔴阻断`。

#### R1-2-T02 月目标、周统筹、固定/临时任务

- **操作**：创建并核对 M1；从 M1 创建 W1；手动创建 W2、I1 一次型、I2 场次型 `3` 次；在 W-1 创建 H1；执行完成/取消完成、跳过取消/确认、W2 删除取消/确认；离开并重进周统筹。
- **预期**：关联、周归属、一次型/场次型语义和确认/取消结果正确，重进不丢失、不重复。
- **实际**：M1/W1/W2/I1/I2/H1 均按计划出现；I2 目标次数为 `3`；取消路径保持原状态，确认路径只产生预期变化。W2 已按用户明确确认完成删除，本节不重复删除；重进后 I2/H1 状态保持。
- **证据**：`R1-2-T02-C01-月目标创建.png`、`R1-2-T02-C02-核心目标.png`、`R1-2-T02-C03-任务类型.png`、`R1-2-T02-C04-历史任务.png`、`R1-2-T02-C05-完成.png`、`R1-2-T02-C05-完成取消.png`、`R1-2-T02-C06-跳过取消确认.png`、`R1-2-T02-C07-删除取消.png`、`R1-2-T02-C07-删除确认.png`、`R1-2-T02-C07-删除完成.png`、`R1-2-T02-C08-周统筹重进.png`。
- **结论**：`✅通过`（在隔离启动环境中有完整 GUI 证据）。
- **严重度**：不适用。

#### R1-2-T03 周任务、日规划 rail、今日任务

- **操作**：从 rail 选取 I2 到 D0；创建 D1；创建两项 TPL1；执行模板套用取消、部分套用和日任务完成/取消、跳过取消/确认；执行空模板错误；离开并重进日规划。
- **预期**：来源、日期、锁定态、模板项数和错误反馈清晰；取消不改变数据；重进保持一致。
- **实际**：I2 以周任务来源进入当日任务；D1 可识别且保留备注；TPL1 显示两项，套用分支和空模板场景均有可见反馈；重进后 I2/D1/TPL1 状态可见，没有空白错误条或数据重复。
- **证据**：`R1-2-T03-C01-rail选取.png`、`R1-2-T03-C02-D1创建.png`、`R1-2-T03-C03-模板创建.png`、`R1-2-T03-C04-模板套用取消.png`、`R1-2-T03-C05-模板部分套用.png`、`R1-2-T03-C06-日任务操作.png`、`R1-2-T03-C07-跳过取消确认.png`、`R1-2-T03-C08-日规划重进.png`、`R1-2-T03-C09-空模板错误.png`。
- **结论**：`✅通过`。
- **严重度**：不适用。

#### R1-2-T04 四渠道汇聚到今日总览

- **操作**：创建项目任务 P1 和提醒 PT1；验证项目任务仅设置 D0 日期时不自动投影；在项目详情显式点击“加入今日”；返回总览核对来源；执行移出今日取消/确认；对 R1 提醒按计划核对前置。
- **预期**：按方案 B 显式加入今日后生成项目投影；来源可辨识；移出今日不删除源任务；提醒前置缺失时不得伪造通过。
- **实际**：项目来源回写可见；仅设置日期时未自动进入今日总览；点击“加入今日”后项目任务出现；移出取消保持投影，确认后移除今日投影但源任务仍在；离开/重进后结果保持。R1 提醒没有可用的计划前置，本轮未伪造提醒结果，记录为待补前置。
- **证据**：`R1-2-T04-C01-项目任务创建.png`、`R1-2-T04-C02-日期不自动投影.png`、`R1-2-T04-C03-加入今日.png`、`R1-2-T04-C04-项目回写.png`、`R1-2-T04-C05-移出今日.png`、`R1-2-T04-C06-提醒前置.png`、`R1-2-T04-C07-今日重进.png`。
- **结论**：`✅通过（适用渠道）`；R1 提醒为 `⚠️待用户实测/待补前置`，不影响已验证的项目、手动和周/日任务分支。
- **严重度**：R1 前置 `ℹ️待用户确认`。

#### R1-2-T05 完成凭据、复盘统计、趋势、封账、转下周

- **操作**：将 I2 从 `1/3` 完成到 `3/3`；打开并更新一条完成凭据；进入当前周复盘查看完成率、完成/未完成/跳过/未安排、日计划明细、周任务和近 8 周趋势；进入 W-1 查看只读封账和 H1；对 H1 先取消再确认“转下周”；保存周感想/月感想；离开到今日总览后返回复盘和周统筹。
- **预期**：每次场次只增加一次；凭据可更新；统计与任务事实一致；历史周只读；转周取消不改数据，确认后有可读反馈且不重复；感想和状态重进保持。
- **实际**：I2 达到 `3/3` 并出现凭据入口；更新后显示“凭据已更新”，I2 保持完成；复盘显示完成率、状态统计、日明细、近 8 周趋势和顺延清单；W-1 显示“历史周·只读展示”，H1 为未完成；取消转周后 H1 保留，再确认后显示“已转至下周”，返回 W35 后任务数量由 `2` 变为 `3`，转入任务可见；感想、凭据、统计和 H1 状态重进保持。
- **证据**：`R1-2-T05-C01-场次完成.png`、`R1-2-T05-C02-完成凭据-编辑.png`、`R1-2-T05-C02-完成凭据.png`、`R1-2-T05-C03-复盘趋势-概览.png`、`R1-2-T05-C03-复盘趋势.png`、`R1-2-T05-C04-历史周封账-概览.png`、`R1-2-T05-C04-历史周封账.png`、`R1-2-T05-C05-转下周-取消确认条.png`、`R1-2-T05-C05-转下周.png`、`R1-2-T05-C06-周月感想.png`、`R1-2-T05-C07-今日总览.png`、`R1-2-T05-C07-复盘重进-复盘.png`、`R1-2-T05-C07-复盘重进.png`。
- **结论**：`✅通过`。
- **严重度**：不适用。

#### R1-2-T06 导入导出、旧入口、错误降级、数据安全

- **C01 导出**：在设置的数据管理点击“导出 JSON”，通过原生“另存为”进入 `R1-2-exports`，保存 `R1-2-user-export-20260825.json`。返回设置；只读核对文件存在、大小 `10352` bytes。证据：`R1-2-T06-C01-导出.png`。结论：`✅通过`。
- **C02 导入取消**：点击“导入 JSON”，观察到“确认覆盖全部业务数据？”，点击取消；确认回到普通设置页，未打开文件选择器，前缀任务仍可访问。证据：`R1-2-T06-C02-导入取消.png`。结论：`✅通过`。
- **C03 合法导入**：再次确认覆盖，在原生打开窗口选择 C01 生成的合法副本并打开；文件窗口关闭，约 `800ms` 后返回今日总览，I2/D1 等前缀数据可见且未出现重复。未在稳定截图中保留短暂的“导入成功” toast，客观成功依据为合法文件被接受、页面返回和数据保持。证据：`R1-2-T06-C03-合法导入.png`。结论：`✅通过（数据结果；成功 toast 未单独留存）`。
- **C04 非法导入**：选择计划指定的 `R1-2-invalid.json`；返回设置并显示完整文字“导入失败：格式无效：请选择合法的 WorkBuddy 备份文件”。页面未崩溃、未白屏，今日任务和 D1 仍保持。证据：`R1-2-T06-C04-非法导入.png`。结论：`✅通过`。
- **C05 旧入口责任边界**：展开可见侧栏“规划”，只看到“日规划/周统筹/月指导/复盘趋势”四个正式入口，没有旧 Markdown 页面或用户可见旧入口。本轮没有猜测、构造地址或使用开发者工具访问 `/plan`、`/review`。证据：`R1-2-T06-C05-旧入口边界.png`。结论：`ℹ️非本轮范围`；专项旧 hash 路由仍需有授权的专项环境验证。
- **C06 刷新/重进**：使用同一测试环境变量重新启动，今日总览恢复 BOOT1、I2、D1 和 `2` 项任务；随后清空测试副本 Key，再次关闭/用同一环境变量重开，任务仍可见。证据：`R1-2-T06-C06-导入重进.png`、`R1-2-T06-F6-重进.png`。结论：`✅通过（隔离启动方式明确时）`。
- **结论**：`❌缺陷`，C01-C04/C06 的适用 GUI 链路完成，但隔离启动边界失败，见 `F-ISO-01`。
- **严重度**：`🔴阻断`。

### 13.3 GPT F6 回归补充

- **历史依据**：在 `GPT实测记录\v0.3实测内容（历史）\v0.3GPT实测 1.md` 中，F6 原始问题为“无效配置测试后清空 Key，上一条 401 文案仍残留”。
- **本轮操作**：设置页中只使用虚构 Key；输入后测试连接按钮仍保持禁用，无法在本轮通过 GUI 生成 401；随后清空输入并观察未配置状态。
- **实际反馈**：未配置状态下“测试连接”按钮为禁用；页面没有残留 401 文案、技术堆栈或系统模态框；离开到今日总览后 I2/D1 仍可见；隔离环境重启后仍保持。
- **证据**：`R1-2-T06-F6-清空Key.png`、`R1-2-T06-F6-任务可用.png`、`R1-2-T06-F6-重进.png`。
- **状态**：`无法完成完整复测`。本轮关闭了“未配置态无残留错误、确定性任务流可用”子结果，但没有生成上一条 401 后再清空的前置，因此不宣称历史 F6 已关闭。

### 13.4 取消、错误降级与数据一致性汇总

| 测试点/场景 | 实际结果 | 证据 | 结论 |
|---|---|---|---|
| `R1-2-T02` 完成/跳过/删除取消与确认 | 取消保持原状态，确认只产生计划中的状态变化；W2 已按用户确认删除 | `R1-2-T02-C05` 至 `C07` | `✅通过` |
| `R1-2-T03` 模板套用取消、空模板 | 取消不新增任务；空模板有可读页面反馈 | `R1-2-T03-C04`、`C09` | `✅通过` |
| `R1-2-T05` 转周取消与确认 | 取消保留 H1；确认显示“已转至下周”，当前周出现转入任务 | `R1-2-T05-C05` | `✅通过` |
| `R1-2-T06` 导入取消 | 覆盖确认可取消，未打开文件选择器且原数据保持 | `R1-2-T06-C02-导入取消.png` | `✅通过` |
| `R1-2-T06` 非法文件 | 显示“导入失败：格式无效：请选择合法的 WorkBuddy 备份文件”，数据和页面保持可用 | `R1-2-T06-C04-非法导入.png` | `✅通过` |
| 数据库损坏/只读 | 未执行；当前安全范围没有建立由本轮创建并可恢复的数据库副本，未进行文件替换或破坏性操作 | 无 | `⚠️待用户实测` |

### 13.5 发现问题

| 问题 ID | 测试点 ID | 来源 | 严重度 | 问题/实际 | 复现条件 | 影响 | 证据 | 建议去向 |
|---|---|---|---|---|---|---|---|---|
| `F-ISO-01` | `R1-2-T01`、`R1-2-T06` | 环境/隔离边界 | `🔴阻断` | 直接双击 `E:\workbuddy-test\app-v0.3\WorkBuddy.exe` 时，进程未继承 `启动实测.bat` 设置的 `WORKBUDDY_USER_DATA`，窗口加载另一数据源而非本轮测试前缀数据。 | 先用隔离环境启动并关闭，再从文件资源管理器直接双击同一 exe；随后比较今日总览文本和任务数量。使用同样的 exe 但显式设置计划环境变量启动后，前缀数据恢复。 | 无法证明直接 exe 启动未访问真实用户库，可能导致串档或真实数据边界失效；按计划安全门禁阻断全部依赖该启动方式的正式结论。 | 直接启动窗口证据：`R1-2-T01-C05-直接启动隔离.png`，可见 `v0.3日规划手动任务-已改名`、`1` 项顺延、`Agent 已为你抓取 5 条`；隔离重启证据：`R1-2-T06-F6-重进.png`。 | `07` 修复/复审：让正式 exe 启动也明确绑定测试 userData，或将 wrapper 作为唯一可执行入口并阻止无环境启动；修复后重新核对真实库边界并执行 R1-3。 |

### 13.6 已通过项

- `R1-2-T01`：隔离环境变量明确时的启动、BOOT1 持久化和窗口操作；不包含直接 exe 启动边界。
- `R1-2-T02`：月目标、周目标、一次型/场次型任务、确认/取消/删除和重进。
- `R1-2-T03`：rail、日任务、模板套用/取消/错误和重进。
- `R1-2-T04`：项目/手动/周日任务适用渠道；UF4 采用方案 B，R1 提醒因前置缺失单列待补。
- `R1-2-T05`：完成凭据、统计趋势、历史周只读、转周、感想和重进。
- `R1-2-T06-C01` 至 `C04`、`C06`：导出、导入取消、合法导入、非法错误降级和隔离方式正确时的重进。
- `GPT F6` 的未配置态子结果：按钮禁用、无残留 401/技术错误、任务页可用；完整历史复现仍未关闭。

### 13.7 待用户实测项与未测试项

| 项目 | 状态/原因 | 恢复条件 |
|---|---|---|
| 用户范围 `R1-2-T01` 至 `R1-2-T06` | 仍需用户亲自完成，GPT 证据不能替代用户体验和验收 | 用户按同一计划执行并通过 `06-用户反馈测试问题.md` 回传 |
| `R1-2-T04` R1 提醒 | 缺少计划要求的提醒前置，未伪造数据 | 提供隔离测试前置后单独执行 |
| `R1-2-T06-C05` `/plan`、`/review` 等旧 hash 路由 | 当前用户可见窗口没有旧入口；未使用开发者工具或猜测地址 | 由授权专项路由环境验证，或提供用户可见历史链接 |
| `GPT F4` | 本轮未进入历史月目标样式专项场景 | 准备对应隔离数据并按历史复现步骤执行 |
| `GPT F6` 完整链路 | 未生成 401 后再清空 Key 的前置；只验证了清空后的安全降级 | 提供可控虚构错误配置流程并重新执行 |
| 损坏/只读数据库 | 未执行破坏性文件替换，当前没有本轮建立的恢复副本 | 由用户确认测试副本、备份和恢复步骤后执行 |

### 13.8 续测结论（覆盖旧中止状态）

`🔴驳回`

- **依据**：同一 S0 在明确 `WORKBUDDY_USER_DATA` 的隔离启动下完成了 T02-T06 的大部分业务 GUI 证据，导入取消/合法/非法错误和跨重进数据均可观察；但直接双击计划指定的 exe 会进入另一数据源，无法证明真实数据边界安全。
- **已通过项数量**：`R1-2-T02`、`R1-2-T03`、`R1-2-T05`，以及 `R1-2-T04` 的适用渠道；`R1-2-T06-C01` 至 `C04`、`C06`。`R1-2-T01` 和 `R1-2-T06` 测试点整体不关闭，因为包含阻断级隔离问题。
- **发现问题**：`F-ISO-01 🔴阻断`；直接 exe 启动未继承隔离数据环境，可能串档/触碰真实用户边界。
- **待用户实测项**：用户范围全部六个测试点、R1 提醒前置、旧 hash 路由专项、数据库损坏/只读、GPT F4 和 GPT F6 完整前置。
- **后续**：回到 `07` 修复/复审 `F-ISO-01`，确保所有允许的启动方式都绑定测试 userData；修复后使用新的 R1-3 或计划指定的后续轮次复测。不得把本节写成用户验收闭环、G1 通过或 C1。

### 13.9 续测记录完整性

- [x] 本节使用同一计划路径、测试标识 `R1-2` 和稳定测试点 ID。
- [x] S0、来源提交、构建目录、构建物哈希、测试数据目录和环境变量均已记录。
- [x] 业务操作通过用户可见 WorkBuddy 窗口和原生文件对话框完成；未调用业务 IPC、HTTP、SQL/SQLite、隐藏 DOM 或内部函数。
- [x] 每个 GUI 动作后重新获取状态并等待页面稳定；直接 exe 错误数据源被单独记录，未写成通过。
- [x] 截图已落在 `GPT实测记录\v0.3实测内容\第1轮\证据\`，未包含真实 Key、Cookie 或私人数据。
- [x] W2 删除仅执行了用户已确认的既有分支，本续测没有重复删除。
- [x] 未修改产品代码、数据库文件、当前状态文档或测试计划；最终 WorkBuddy 进程为 `0`。
- [x] 本节明确区分 GPT 通过项、发现问题、待用户实测项和未测试项，不宣称用户验收、G1 或 C1。
