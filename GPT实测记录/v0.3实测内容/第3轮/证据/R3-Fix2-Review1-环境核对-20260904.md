# R3 Fix2 Review1 环境核对索引

- **复审标识**：`Review1`
- **复审模式**：用户实测 R
- **对应 Fix**：`R3 Fix2`；机器命名空间 `B3/R3/Fix2-only`
- **核对时间**：`2026-09-04T19:36:21+08:00`
- **核对者**：GPT

## 候选与机器交接

- 唯一候选：`E:\workbuddy 复审\B3\R3-Fix2-only\`
- 正式启动包装：`E:\workbuddy 复审\B3\R3-Fix2-only\启动复审-R3-Fix2.bat`
- 候选程序：`E:\workbuddy 复审\B3\R3-Fix2-only\app\WorkBuddy.exe`
- 独立数据目录：`E:\workbuddy 复审\B3\R3-Fix2-only\userData\`
- `review-meta.json`：`status=completed`、`candidateType=r-only`、`batchId=B3`、`rId=R3`、`fixId=Fix2`、`reviewStatus=not-reviewed`、`restoreStatus=verified`、`nextAction=05-r-only-review`
- `candidate-source-manifest.json` 与 `post-fix-manifest.json` 均列出 155 个源文件；8 个 Fix2 变更文件及其 post-fix 哈希已登记。Fix 完成后的开发区恢复为 begin-fix 基线，故恢复后的工作树哈希不能替代候选源哈希。
- `build-manifest.json` 独立复算：预期/实际均为 100 个文件、335,402,370 bytes，逐文件 SHA-256 差异 0；`WorkBuddy.exe` SHA-256=`512e0b1e861b6f03b2e53f3a26613f8fe48520dfb6c4892f525343ad9ec76866`；manifest SHA-256 与 `review-meta.build.manifestSha256` 一致：`fb21d0be4feffbbfb18a037df54cd7fee97cdd7420497af1f26ca75167512cdf`。

## GUI 启动核对

1. 已通过上述正式启动包装启动候选；候选进程路径均为 `...\B3\R3-Fix2-only\app\WorkBuddy.exe`。
2. 当前运行会话为 `CodexSandboxOffline`，没有可用的交互式 Windows 桌面/Explorer；WorkBuddy 主进程及子进程均为 `MainWindowHandle=0`、无可见标题。
3. 可用 GUI 控制端只有 `about:blank`，不能接入该 Electron 桌面窗口；因此没有进行任何 IPC/API 代替操作，也没有把进程存在、代码阅读或自动化测试当作 GUI 通过。
4. 候选启动后只初始化了空的独立数据库，未创建 F1/F2 业务数据；不能在当前环境真实执行创建、确认停用、跨周访问、刷新/离开重进或用户主观验收。

## 只读数据库核对

- 关闭仅属于该候选的进程后，以 Node `node:sqlite` `readOnly=true` 打开 `E:\workbuddy 复审\B3\R3-Fix2-only\userData\workbuddy.db`，并设置 `PRAGMA query_only=ON`。
- `query_only=1`、`integrity_check=ok`、`foreign_key_check=[]`。
- 空候选库表计数：`__schema_migrations=9`、`settings=8`；`flow_fixed_defs=0`、`flow_week_instances=0`、`flow_day_entries=0`，没有可用于业务验收的 F1/F2 数据。
- 只读打开前后 `workbuddy.db` SHA-256 均为 `14948FA5E2D8F2D2F9D9F9349A2626F275B36C870D724B183D7C9216D676F20E`，`unchanged=true`；当时 `workbuddy.db-wal=412032` bytes、`workbuddy.db-shm=32768` bytes。

## Review1 结论边界

本次仍处于 `05` 修复复审阶段。由于 Review1 未通过，尚未进入用户实测阶段；以下验收项的状态统一表示为 `⏸️待环境补证（05复审未完成）`，不表示等待用户执行 R3-1。

| 验收项 | 结论 | 原因 |
|---|---|---|
| U5-01 | `⏸️待环境补证（05复审未完成）` | 未能在 GUI 创建/确认 F1、形成 W0 存量并停用 |
| U5-02 | `⏸️待环境补证（05复审未完成）` | 未能在候选 GUI 首次/重复访问 W+1、W+2 |
| U5-03 | `⏸️待环境补证（05复审未完成）` | 未能在 GUI 执行刷新、离开和重进后的页面稳定性核对 |
| U5-04 | `⏸️待环境补证（05复审未完成）` | 未能创建 F2/临时任务并观察对照隔离 |
| U5-05 | `⏸️待环境补证（05复审未完成）` | 未能在 GUI 构造并观察停用前已存在的未来行 |
| U5-06 | `⏸️待环境补证（05复审未完成）` | 候选目录/构建与空库只读检查通过，但业务数据隔离和关闭后业务持久化无法验证 |

本索引只证明候选构建清单、启动尝试和空库只读安全检查；不证明 U-5 行为已修复，也不替代用户 06、R3-1 测试计划或后续 GUI 复审。

## 最小恢复条件

在可交互的 Windows 桌面上，使用同一 `启动复审-R3-Fix2.bat`、同一候选程序和同一独立 `userData`，按 R3 §21.5 重新取得 U5-01～U5-06 的 GUI 稳定态、取消/确认反馈、W0/W+1/W+2 列表与计数、F2/临时任务对照、停用前未来行保留、关闭后只读持久化和数据库前后哈希证据。补齐后由 05 使用未占用的下一正式复审编号继续复审；若实际发现代码问题，再回 `07`，不得由本次环境阻断直接生成 Fix 或 15 结论。
