# R3 Fix2 Review2 环境核对索引

- **复审标识**：`Review2`
- **复审模式**：用户实测 R
- **对应 Fix**：`R3 Fix2`；事项 `U-5`；机器命名空间 `B3/R3/Fix2-only`
- **核对时间**：`2026-09-05T00:01:53+08:00` 至 `2026-09-05T00:02:37+08:00`
- **核对者**：GPT
- **前置 Review**：`Review1` 已于 R3 权威文档 §23 记录为当前环境阻断；本次使用下一正式编号 `Review2`，未覆盖历史记录。

## 候选与机器交接

- 唯一候选：`E:\workbuddy 复审\B3\R3-Fix2-only\`
- 正式启动包装：`E:\workbuddy 复审\B3\R3-Fix2-only\启动复审-R3-Fix2.bat`
- 候选程序：`E:\workbuddy 复审\B3\R3-Fix2-only\app\WorkBuddy.exe`
- 独立数据目录：`E:\workbuddy 复审\B3\R3-Fix2-only\userData\`
- `review-meta.json`：`status=completed`、`candidateType=r-only`、`batchId=B3`、`rId=R3`、`fixId=Fix2`、`reviewStatus=not-reviewed`、`restoreStatus=verified`、`nextAction=05-r-only-review`
- 候选源基线：`candidateBaseCommit=a6aa7307bb6426486059dfc01b1d17297ff3312e`；候选变更仍为 8 个已登记文件；恢复后工作树指纹仍为 `98d9cbcd029b97eadf14037e19ffbc80dd4bbdb6c702174a5b3cf99e8582287e`，恢复后树指纹仍为 `ba4e5fba17935348b81760e28711f3fdf31456784a6dca8ad79123607982e44e`。
- 构建清单辅助核对：100 个文件、335,402,370 bytes；`WorkBuddy.exe` SHA-256=`512E0B1E861B6F03B2E53F3A26613F8FE48520DFB6C4892F525343AD9EC76866`；`build-manifest.json` SHA-256=`FB21D0BE4FEFFBBFB18A037DF54CD7FEE97CDD7420497AF1F26CA75167512CDF`；`candidate-source-manifest.json` SHA-256=`E6EC13E3BA52A32971834F6882C3DCD2F56949E2B86BFAFB93A38C1A769ACC8D`。

## 启动与 GUI 事实

1. 通过正式 `启动复审-R3-Fix2.bat` 启动候选。`WorkBuddy` 进程短暂出现，`MainWindowTitle` 为空、`MainWindowHandle=0`，随后退出；核对结束时没有 `WorkBuddy` 或 `electron` 进程。
2. 直接前台启动同一候选时也未出现可见窗口；进程输出过 `Lock file can not be created! Error code: 5`、Windows 加密存取失败和网络变更通知初始化失败，随后退出。这些是当前启动环境输出，不能单独归因于 Fix2 产品代码。
3. 当前可用浏览器控制端仍只有 `about:blank`，不能接入该 Electron 桌面窗口；没有使用 IPC/API、代码阅读或自动化测试替代 GUI 复审。
4. 因无可交互 Windows 桌面，无法在正式入口执行 F1 创建/确认/停用、W0/W+1/W+2 导航、刷新/离开重进、F2/临时任务对照或页面主观验收。

## 只读数据库事实

- 只读打开：`E:\workbuddy 复审\B3\R3-Fix2-only\userData\workbuddy.db`，`mode=ro`，并设置 `PRAGMA query_only=ON`。
- `query_only=1`；`integrity_check=ok`；`foreign_key_check=[]`。
- 当前候选库为空：`flow_fixed_defs=0`、`flow_week_instances=0`、`flow_day_entries=0`；没有 F1、F2、临时任务、W0 存量或停用前未来行可用于业务验收。
- 只读打开前后 `workbuddy.db` SHA-256 均为 `14948FA5E2D8F2D2F9D9F9349A2626F275B36C870D724B183D7C9216D676F20E`，`unchanged=true`。

## U-5 逐项门禁结果

| 验收项 | 当前事实 | Review2 结论 |
|---|---|---|
| `U5-01` | 未能在 GUI 创建/确认 F1、形成 W0 存量并停用；无业务行 | `⏸️待环境补证`；`🔴阻断` |
| `U5-02` | 未能在 GUI 首次/重复访问 W+1/W+2；07 自动化结果不能替代当前 GUI 证据 | `⏸️待环境补证`；`🔴阻断` |
| `U5-03` | 未能在 GUI 执行刷新、离开重进或核对计数/列表稳定态 | `⏸️待环境补证`；`🔴阻断` |
| `U5-04` | 空库中没有 F2/其他固定任务/临时任务，无法取得对照证据 | `⏸️待环境补证`；`🔴阻断` |
| `U5-05` | 空库中没有停用前未来行，无法核对保留、展示和软删除边界 | `⏸️待环境补证`；`🔴阻断` |
| `U5-06` | 候选路径、构建、独立 userData、只读完整性和空库前后哈希可核对；业务数据隔离和关闭后的业务持久化不可核对 | `⏸️待环境补证`；`🔴阻断` |

## 结论与恢复条件

正式结论：`🔴驳回（当前环境阻断，核心范围无法可靠验证）`。

本次不是对 Fix2 代码实现失败、替代决策失败或数据损坏的定论。候选机器材料和空库只读安全检查只能作为辅助证据，不能证明 U-5 的 GUI 行为、页面稳定态、业务数据隔离或用户验收。

恢复时必须在可交互的 Windows 桌面上，继续使用同一启动包装、同一候选 app 和同一独立 userData，按 R3 §21.5/§22 验收矩阵取得 U5-01 至 U5-06 的 GUI、刷新/重进、F2/临时对照、未来行保留、只读持久化和前后哈希证据。补证后回到 `05` 使用下一未占用正式 Review 编号；若候选代码、依赖或构建输入发生变化，先回对应 Fix/候选准备。

本次不交 `15`，不调用 `14`，不创建 R3-1 或 `B3-merge`，不写用户实测通过、G1、C1 或维护期结论。
