# v0.3GPT实测 2

## 文档元信息

- **记录标题**：`v0.3GPT实测 2`
- **实测版本**：`v0.3`
- **功能名称**：项目投影模板与周统筹创建（UF1/UF2/UF3 回归）
- **实测日期**：`2026-08-21`
- **实测人员**：GPT
- **记录状态**：`🔴驳回`
- **本次结论**：`🔴驳回（可见桌面控制通道阻断，非产品缺陷判定）`
- **关联用户测试流程**：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\v0.3修复用户测试1.md`
- **关联开发单元闭环记录**：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\v0.3-用户反馈1-项目投影模板与周统筹创建-全流程闭环记录.md`
- **上一份 GPT 实测记录**：`E:\workspace\GPT实测记录\v0.3实测内容\v0.3GPT实测 1.md`

## 一、实测环境

- **操作系统**：Windows（具体版本号未通过窗口工具取得）
- **程序入口**：`E:\workbuddy-test\app-v0.3\WorkBuddy.exe`
- **启动命令**：可见启动 `E:\workbuddy-test\启动实测.bat`
- **测试专用目录**：`E:\workbuddy-test`
- **测试数据目录**：`E:\workbuddy-test\userData\`
- **真实用户数据目录核对**：测试目录与 `%APPDATA%\workbuddy\` 路径不同；两者均存在，未读取真实数据库内容
- **重置命令**：无（本轮不执行重置脚本）
- **窗口尺寸**：WorkBuddy 进程已启动，但窗口尺寸未通过可见窗口工具取得
- **网络状态**：未测
- **证据目录**：`E:\workspace\GPT实测记录\v0.3实测内容\v0.3实测内容\证据\`
- **启动输出摘要**：启动脚本已调用；WorkBuddy 进程存在，主窗口标题为 `WorkBuddy`。未把后台控制台输出当作可见窗口证据。

### 隔离核对

- [x] 被测程序路径和测试目录标记为 `v0.3`；`WorkBuddy.exe` 文件版本为 Electron `33.4.11`，文件修改时间为 `2026-08-20 17:03:47`；测试说明注明其为 2026-08-20 生成的最新打包结果。
- [x] 测试专用 `userData` 与真实用户目录不同：测试目录 `E:\workbuddy-test\userData\`，真实目录 `%APPDATA%\workbuddy\`，只读路径比较结果不同；未读取真实数据库内容。
- [x] 测试数据前缀预留为 `v0.3-GPT2-20260821-`；本轮未完成业务 GUI 操作，实际未写入业务数据。
- [x] 不记录真实 API Key、Cookie、私人数据或敏感路径。
- [x] 本轮不执行重置，因此不会删除测试数据。

## 二、实测前置

### 2.1 阅读依据

- **`当前审查状态.md`**：当前开发单元为 GPT 复审通过、达到本子阶段开发闭环；UF1/UF2/UF3 已修复并复审通过；UF4 项目日期投影语义待 A/B 裁决；U8/U9 和历史 F2/F4/F6 仍待后续实测。该状态不替代本轮 GUI 运行证据。
- **`当前代码状态.md`**：DeepSeek 声明 UF1/UF2/UF3 已修复，未放宽 IPC 必填校验、未改事件契约；自测为 364/364、build 通过，并报告隔离 Electron 复跑 5/5。该文件仅作实现事实快照。
- **用户测试流程**：本轮按 UF1 周核心目标、UF2 一次型/场次型任务、UF3 模板创建/套用/编辑/删除/错误反馈顺序执行；每项记录操作、预期、实际、截图和结论。
- **开发单元闭环记录**：第 5 节要求 UF1/UF2/UF3 修复，第 7 节复审通过；第 8 节要求本轮复测 UF1/UF2/UF3，并指出 UF4、U8、U9 另行处理。
- **上一份 GPT 实测记录**：发现 F1 周核心目标创建失败、F2 复盘统计不一致、F3 模板创建后列表为空、F4 历史未关闭月目标误显示删除线、F5 空红色反馈条、F6 清空 AI Key 后 401 文案残留。本轮将 UF1/UF3/F5 纳入主链路回归，并单独核对 F2/F4/F6；无法执行的历史项列入待用户实测。
- **治理规范**：文档写入文档根 `E:\workspace\` 下的 `GPT实测记录\`，不在代码根创建副本；GPT 不修改 `当前代码状态.md`、产品代码或真实用户数据；实测闭环仍由用户裁决。

### 2.2 前置矛盾与处理

| 编号 | 文档/事实 | 矛盾内容 | 对实测的影响 | 处理/结论 |
| --- | --- | --- | --- | --- |
| P1 | 参数中的启动命令写作“点击启动测试”；测试目录实际提供 `启动实测.bat` | 参数未给出可执行文件名，但 `说明.md` 和启动脚本共同指向 `app-v0.3\WorkBuddy.exe` | 需要用隔离脚本启动，不能直接启动 exe 以免丢失 `WORKBUDDY_USER_DATA` | 采用现有测试脚本，并在环境记录中保留实际入口 |
| P2 | 参数写明“重置命令：无”；测试目录另有重置 bat | 是否删除现有测试数据的授权不一致 | 不能为获得空态而删除或覆盖已有测试库 | 按参数不执行重置，使用唯一前缀追加数据 |
| P3 | UF4 在闭环记录中存在日期投影语义 A/B 冲突 | 本轮用户测试文件不把 UF4 纳入判定 | 不把计划日期自动投影或“加入今日”推断为本轮通过/缺陷 | UF4 记录为非本轮范围/待用户确认 |

## 三、操作方式声明

- [ ] 全程使用可见 WorkBuddy 窗口完成用户动作：未完成，桌面控制通道不可用。
- [ ] 点击、输入、导航、确认/取消和滚动均通过可见窗口完成：未执行。
- [x] 原生文件对话框、日期选择器或系统弹窗未完成的项目将列入待用户实测。
- [x] 未直接调用业务 IPC、预加载 API、HTTP 接口、数据库 SQL 或内部函数代替用户操作。
- [x] 未直接修改业务数据库、测试程序、产品代码或 `当前代码状态.md`。

## 四、逐项实测记录

### 0.1 启动与隔离核对

**操作**

1. 通过测试专用目录中的 `启动实测.bat` 启动 WorkBuddy。
2. 通过只读进程核对确认 `E:\workbuddy-test\app-v0.3\WorkBuddy.exe` 已启动，主窗口标题为 `WorkBuddy`。
3. 通过只读文件核对确认测试数据目录存在 `workbuddy.db`，且测试目录与 `%APPDATA%\workbuddy\` 路径不同。

**预期反馈**

隔离版 WorkBuddy 正常开窗，测试数据写入测试专用 `userData`，真实用户数据路径不被替代或重置。

**实际反馈**

WorkBuddy 进程存在，主窗口标题为 `WorkBuddy`；测试目录存在 `workbuddy.db`。只读路径核对确认 `E:\workbuddy-test\userData\` 与真实 `%APPDATA%\workbuddy\` 不同，未读取或修改真实数据库。由于可见窗口控制通道不可用，无法观察窗口截图、默认页面、启动页面文本、白屏/闪退状态、窗口尺寸、空态/错误态或脚本控制台乱码。

**证据**

- 截图：无。Computer Use native pipe 连接失败，不能伪造窗口截图。
- 命令/输出：只读进程核对显示 `WorkBuddy`，路径为 `E:\workbuddy-test\app-v0.3\WorkBuddy.exe`，启动时间约为 `2026-08-21 17:07:28`；测试目录存在 `workbuddy.db`。
- 只读核对：测试目录与 `%APPDATA%\workbuddy\` 不同；未执行重置命令。

**结论**：`⚠️待用户实测`

**严重度**：`ℹ️待用户确认`

**备注**：启动进程事实不是可见 GUI 通过证据；桌面控制失败条件为 `Computer Use native pipe is unavailable: failed to connect native pipe: 系统找不到指定的文件`，在初始化、等待重试、重置 Node 会话后仍重复出现。

### 4.1 UF1 周核心目标

**操作**：未执行。无法通过可见窗口进入周统筹、创建月目标、从月目标选取核心目标、手动创建、完成/取消完成、删除取消/确认或离开后返回。

**预期反馈**：两条创建路径均成功；月目标关联、手动目标、计数、完成/取消完成、删除取消/确认和重进页面保持符合用户测试流程；不出现 `weekStart/title 必填`。

**实际反馈**：无法取得窗口焦点、点击或输入结果；没有 GUI 证据。

**证据**：无截图；原因同启动项的 Computer Use native pipe 故障。

**结论**：`⚠️待用户实测`

**严重度**：`ℹ️待用户确认`

### 4.2 UF2 本周任务

**操作**：未执行。无法通过可见窗口创建一次型任务、创建目标场次为 3 的场次型任务、操作 `0/3 → 1/3 → 2/3 → 3/3` 或刷新/离开后返回。

**预期反馈**：一次型创建成功；场次型初始显示 `0/3`，进度依次正确更新至 `3/3`，刷新后保持且不超过总场次；不出现 `weekStart/title/kind 必填`。

**实际反馈**：无法取得窗口焦点、点击或输入结果；没有 GUI 证据。

**证据**：无截图；原因同启动项的 Computer Use native pipe 故障。

**结论**：`⚠️待用户实测`

**严重度**：`ℹ️待用户确认`

### 4.3 UF3 日模板

**操作**：未执行。无法通过可见窗口创建两项模板、确认列表 `2项`、成功反馈、套用取消、部分勾选套用、编辑、删除取消/确认、检查已有任务保留或构造空内容失败输入。

**预期反馈**：模板列表、数量、成功反馈、错误反馈、套用、编辑和删除符合用户测试流程；取消不产生副作用；已有任务不因模板删除而消失；不出现空红条。

**实际反馈**：无法取得窗口焦点、点击或输入结果；没有 GUI 证据。

**证据**：无截图；原因同启动项的 Computer Use native pipe 故障。

**结论**：`⚠️待用户实测`

**严重度**：`ℹ️待用户确认`

## 五、失败场景与错误降级

本轮未执行业务失败场景。UF3 空输入、套用取消、删除取消等必须在可见窗口恢复后执行；不能以未执行写成通过。

## 六、已通过项

本轮没有业务 GUI 项目可列为通过。仅完成了非业务的只读环境核对：测试程序路径、测试目录存在性和测试/真实数据目录路径差异；这些不替代 GUI 功能通过。

## 七、发现问题

本轮未复现产品功能问题，因为没有完成任何业务 GUI 操作。观察到一个测试环境阻断：可见桌面控制辅助进程无法连接，导致主链路无法验证。

| 编号 | 严重度 | 裁定 | 问题 | 复现条件 | 影响 | 证据 | 建议去向 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F-GUI-1 | 🔴阻断 | ❓待核实 | Windows Computer Use native pipe 无法连接，不能取得可见 WorkBuddy 窗口或执行用户动作 | 启动隔离 WorkBuddy 后调用 `sky.list_apps()`；初次失败，等待 2 秒重试失败，重置 Node 会话后仍失败 | 本轮无法真实验证 UF1/UF2/UF3，不能据此认定修复通过或失败 | 工具错误：`Computer Use native pipe is unavailable: failed to connect native pipe: 系统找不到指定的文件`（三次） | 用户/运行环境：恢复桌面控制辅助进程后重跑本记录；不是产品代码缺陷判定 |

## 八、待用户实测项

### U-GUI-1 可见窗口主链路

- **为什么 GPT 未完成**：Windows Computer Use native pipe 三次初始化均失败，无法取得 WorkBuddy 窗口、截图或可用元素索引。
- **前置数据**：保留当前 `E:\workbuddy-test\userData\`；本轮未执行重置，已有数据不能删除。
- **操作**：恢复可见桌面控制能力后，启动 `E:\workbuddy-test\启动实测.bat`，在 WorkBuddy 窗口按本记录 UF1、UF2、UF3 顺序执行并每项截图。
- **预期反馈**：UF1 两条目标创建与状态保护通过；UF2 一次型及 `0/3 → 3/3` 通过；UF3 模板完整链路和错误降级通过。
- **必须记录**：窗口截图、页面文本、输入值、刷新/离开后状态和错误反馈；不得记录真实 API Key。
- **失败时**：保留失败页面和错误截图，不删除测试数据；若必须重置，先取得用户授权并确认 WorkBuddy 已关闭。
- **用户结论**：留给用户填写：`✅通过 / ❌问题 / ⚠️无法完成`

### U-F4 UF4 项目日期投影语义

- **为什么 GPT 未完成**：本轮用户流程将 UF4 排除，闭环记录要求先由产品选择“计划日期自动投影”或“必须点击加入今日”；本轮不擅自裁决。
- **前置数据**：按闭环记录第 4.1 节创建测试项目任务。
- **操作**：用户按闭环记录 §4.1 先将计划日期设为今天但不点击“加入今日”，记录今日总览结果；再点击“加入今日”，再次检查今日总览并验证重复点击幂等。
- **预期反馈**：由产品选择 A/B 后判定，记录来源、任务保留及重复点击结果。
- **必须记录**：日期选择器截图、项目任务日期、今日总览任务与来源、重复点击结果和最终 A/B 选择。
- **失败时**：保留截图，不将规格冲突写成产品缺陷；不修改业务代码。
- **用户结论**：留给用户填写：`✅通过 / ❌问题 / ⚠️无法完成`

### U-HISTORY-1 历史 GPT F2/F4/F6

- **为什么 GPT 未完成**：本轮 GUI 通道不可用，且这些项目不属于本轮 UF1-UF3 用户测试流程核心范围。
- **前置数据**：需要在隔离环境准备对应任务/月目标/AI 设置状态。
- **操作**：按上一份 GPT 实测记录中的复现步骤分别检查复盘统计、历史未关闭目标样式和清空 Key 后旧 401 文案。
- **预期反馈**：统计与任务事实一致；未关闭目标不显示关闭样式；清空 Key 后旧错误状态按规格清理。
- **必须记录**：可见窗口截图、页面文字、刷新后结果；API Key 仅使用虚构值且不写入证据。
- **失败时**：记录复现条件和证据，不把历史结论当作本轮结果。
- **用户结论**：留给用户填写：`✅通过 / ❌问题 / ⚠️无法完成`

## 九、复测追加区

本轮为修复后的首次 GPT 自主复测；因可见窗口控制通道阻断，未形成业务 GUI 复测结果。

## 十、正式结论

### 10.1 本轮结论

`🔴驳回`

**结论依据**：

- 已执行测试项：启动/隔离只读核对；业务 GUI 测试 0 项
- 有证据通过：无业务 GUI 项；仅有隔离路径只读核对
- 发现问题：`F-GUI-1 🔴阻断`，可见窗口控制通道不可用，导致本轮无法可靠验证主链路
- 待用户实测：`U-GUI-1`、UF4、U8、U9 及历史 F2/F4/F6
- 未测试项：UF1、UF2、UF3 全部业务操作；原生日期/文件对话框
- 用户实测闭环：本记录不替代用户实测闭环

### 10.2 对用户的交接说明

本轮没有对产品修复作通过/失败判定。请先恢复 Windows 可见桌面控制能力，再按本记录 U-GUI-1 重跑 UF1、UF2、UF3；本结论也不替代用户验收闭环。UF4、U8/U9 和历史回归仍需独立完成。

## 十一、记录完整性检查

- [x] 所有当前结论均明确区分 GUI 未执行与只读环境核对。
- [x] 已通过项没有包含未执行或只读推断项目。
- [x] 环境阻断项有复现条件、影响、严重度和工具错误证据。
- [x] 原生桌面/业务 GUI 未完成项均在“待用户实测项”。
- [x] 本记录不替代用户验收闭环。
- [x] 用户反馈文档未因本记录自动修改。
- [x] 未修改产品代码、`当前代码状态.md` 或真实用户数据。
