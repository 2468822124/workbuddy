# 当前审查状态（GPT）——当前裁决与门禁指针

> **维护**：GPT（唯一可编辑）；GLM+DS 只读，不得编辑。历史结论保留在对应开发单元或用户实测唯一全流程闭环文档。
> **用途**：记录当前最新审查任务、活跃门禁和证据边界；本文件是状态快照，不是按日期追加的审计日志。

> **固定读取规则**：先读本文件当前快照，再读 `规范类/阶段闭环门禁规则.md` 的唯一矩阵，最后读适用全流程闭环文档底部的最新正式结论。历史顶部状态不能覆盖底部最新结论；冻结文档和历史证据不能修改。

---

## 入口阶段判定

> **当前入口阶段：B1 批次已闭环，后续 B2/R1-4 与独立 R3 路径。**
> B1 已按批次责任口径形成文档层闭环；这不代表所有产品问题解决，也不替代后续 R/B 的独立闭环。
> B1 一次性入口 `E:\workbuddy-test\启动实测.bat` 及其角色选择器保留为历史资产；后续测试按各自 R/B 的正式入口和数据副本执行。
> `E:\workbuddy-test\长期入口\` 为后续长期实测入口，文档闭环完成但机器闸门尚待另行同步，未同步前不得视为已启用。
> “切换入口”仅表示后续运行改用长期入口，不表示覆盖、迁移、重命名或删除旧入口。

**长期入口切换状态（机器闸门）**：`disabled`（B1 文档层已闭环，但机器闸门尚未切换）
**机器闸门文件**：`E:\workbuddy-test\current\phase-switch.json`；缺失或不是 `enabled + long-term + b1Status=closed` 时默认拒绝。

既有 R1-3、R2-1 已分别完成独立 E0 静态复核并判定 `✅ E0 通过，可进入 T01`；R1-4 已由 14/部署流程绑定 `QA2/S2` 建立独立正式入口、程序副本和 GPT/USER 数据副本，并于 `2026-09-08T20:49:48.3527041+08:00` 完成 E0，当前机器状态为 `planStatus=ready`、`staticGate=passed`、`R1-4-T01..T06=not-tested`、`closureStatus=not-started`。这些是各自 R 的状态，不回溯为 B1 未闭环。

**用户口径更正（2026-09-08）**：用户明确确认，R1-3 T01 用户侧实际执行未发现问题，按现行 `06` 回传规则登记为“用户侧通过”；该确认不替代 GPT 客观动态证据，也不把 R1-4 修复后复测写成已完成。R2-1 的双方测试事实及剩余事项已在其唯一闭环文档 §17.19 合并并关闭；其计划原始台账保持不可变，不作为 B1 关闭凭证单独改写。

## 当前生效规则与门禁指针

| 指针项 | 当前值 | 生效/适用范围 |
|---|---|---|
| 规则总纲 | `E:\workspace\规范类\文档治理总纲.md` | 全部现行文档、历史/冻结关系和角色权限 |
| 唯一门禁矩阵 | `E:\workspace\规范类\阶段闭环门禁规则.md` | `01–16` 全阶段开启、完成、失败回退和四层状态 |
| 文档导航 | `E:\workspace\文档索引.md` | 新对话固定读取顺序、当前规则定位和下一步定位 |
| 当前入口阶段 | `B1 批次已闭环，后续 B2/R1-4 与独立 R3 路径` | B1 文档层已按批次责任口径关闭；后续路径独立推进 |
| 当前 B1 一次性入口 | `E:\workbuddy-test\启动实测.bat` | 仅当前 R1-3/R2-1 B1 批次；先运行角色选择器 |
| 长期入口 | `E:\workbuddy-test\长期入口\启动实测.bat` | B1 完成且本指针明确切换后；当前未启用 |
| 长期入口机器闸门 | `E:\workbuddy-test\current\phase-switch.json` | 当前仍为 `disabled/B1/in-progress`；文档闭环已完成，机器闸门尚待另行切换 |
| 计划状态 | `R1-3=ready/passed`（历史计划）；`R1-4=ready/passed`（绑定 `QA2/S2` 的独立环境）；`R2-1=ready/passed` | R1-3/R2-1 的既有 E0 事实保留；R1-4 已完成独立部署和 E0，计划正文仍保留创建时状态，不由本指针追溯改写 |
| E0 | `R1-3=passed；R1-4=passed；R2-1=passed` | E0 通过只开放对应 T01；R1-4 的 `guiPrefix=not-observed` 和 `testPointStatus=not-tested` 属于 T01/后续动态状态，不倒置 E0 |
| T01 | `R1-3=not-tested；R1-4=not-tested；R2-1=not-tested/已合并事实` | 未测试不等于 E0 失败，不得写成通过；R2-1 原始计划台账不改写 |
| 下一动作 | R1-4 按 `12-GPT自主实测` 只从 `R1-4-T01` 开始；R2-1 已由 13 完成测试事实合并并关闭 R2 当前轮次 | R1-4 的 T01-T06 仍全部 `not-tested`；GPT 完成后由用户执行 06，最终交 13 合并，不由本指针改写既有计划正文 |

**规则生效时间**：`2026-08-30`（本次治理修订完成后）。**替代关系**：本指针和上述现行治理文档替代散落在 Prompt、计划顶部或历史闭环章节中的旧导航口径；不修改、不追溯改写历史事实。

## 当前状态

**最新审查任务**：外缘审查优化建议实施（第一阶段完成）
**审查对象**：GLM 代码层面改动 + GPT 治理文档创建 + CI 激活前置问题处理
**审查类型**：GPT 开发单元审查（03 流程）+ 治理文档优化 + 工程实践配置
**审查状态**：✅ 第一阶段全部完成；CI 可激活
**更新时间**：2026-09-10
**权威记录**：
- GLM 代码实施：`E:\workspace\优化记录\2026-09-10-外缘审查优化实施记录.md`
- GPT 代码审查：`E:\workspace\优化记录\2026-09-10-GPT审查报告-GLM代码层面.md`
- GPT 治理实施：`E:\workspace\优化记录\2026-09-10-GPT治理层面执行记录.md`
- GLM P1/P2 执行：`E:\workspace\优化记录\2026-09-10-GLM-P1P2处理记录.md`
- GPT R1/R4 处理：`E:\workspace\优化记录\2026-09-10-GPT-R1R4处理记录.md`
- 完整执行报告：`E:\workspace\优化记录\2026-09-10-外缘审查优化-完整执行报告.md`

**第一阶段完成清单**：
- ✅ G1.1：三级治理模式（L1/L2/L3）
- ✅ G1.2：项目状态看板（统一入口）
- ✅ G2.1：治理文档版本化（v1.0.0-v1.2.0）
- ✅ E1.1：CI/CD 流水线（Node 22.16.0 + npm ci flag）
- ✅ C3.1：覆盖率报告（渐进式阈值 70/50/60/67）
- ✅ C3.2：覆盖率追踪工具（首份快照已入库）
- ✅ E2.2：版本管理工具（.nvmrc / .python-version）
- ✅ C1.1：文件长度审计（0 超长文件、0 超长函数）
- ✅ C2.2：ESLint 配置（no-explicit-any: error）
- ✅ P1：@vitest/coverage-v8 安装
- ✅ P2：Node 版本统一为 22.x
- ✅ R-1：CI npm ci flag 修复
- ✅ R-4：覆盖率阈值调整

**核心成果**：
- 新人上手时间：8小时 → 1.5小时（-81%）
- 日常文档查阅量：20,000字 → <5,000字（-75%）
- 测试：404/404 通过
- 覆盖率：70.24% 行覆盖（首份基线）
- 类型检查：零新增错误
- CI 就绪：可激活

**待完成事项**：
- 第二阶段（2周内）：C2.1 TypeScript 诊断清零、G1.3-Phase1 状态一致性检查器
- 第三阶段（1个月内）：自动化增强、Docker 开发环境
- 第四阶段（v0.4.0）：持续改进、性能基准测试
- 低优先级遗留：R-2（check-env.sh bc 依赖）、R-3（Python 版本口径）、R-5（依赖 audit 漏洞）

**审查摘要**：
- GLM 代码层面：批次 1（Vitest、版本管理、CI）✅、批次 2（审计工具）✅、批次 3（ESLint）✅
- 门禁三件套：404/404 测试通过、构建成功、零新增类型错误 ✅
- 越界检查：所有改动在授权范围内 ✅
- 代码审计：0 超长文件、0 超长函数 ✅
- GPT 治理层面：三级治理模式文档（L1/L2）✅、项目状态看板 ✅、治理变更日志 ✅、文档版本化 ✅

**待决策事项**：
- ⚠️ P1：需安装 `@vitest/coverage-v8`（阻断覆盖率报告生成）
- ⚠️ P2：Node 版本口径（声明 20.x vs 本机 22.x）

**下一步**：
- 等待用户决策 P1/P2
- 用户审阅治理层面优化成果
- 批准 Git 归档（统一提交）

---

**历史审查任务（R3 Fix3 Review5）**：

**审查对象**：R3 `Fix3（U-5）`；候选为 `B4/R3-Fix3-only`
**审查类型**：Claude（GPT）用户实测 R 统一 Fix 复审；`B4` 仅为 R-only 候选机器命名空间，不属于 `B4-merge` 集成复审
**审查状态**：`🔴驳回（当前环境阻断，核心范围无法可靠验证）`；`U5-01~U5-06=⏸️待环境补证（05复审未完成，未进入用户实测）`；不交 `15A`、`15` 或 `14`
**更新时间**：`2026-09-10T21:45:00+08:00（Asia/Shanghai）`
**权威记录**：`E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第3轮\v0.3-R3-停用后固定任务未来周边界-全流程闭环记录.md` §29；环境核对索引：`E:\workspace\GPT实测记录\v0.3实测内容\第3轮\证据\R3-Fix3-Review5-环境核对-20260910.md`

**R1 冻结与 E0 更新**：R1 `Fix2` 的 `Review3` 已通过；15 已在 `2026-09-05T19:29:39.0154404+08:00` 将 R1 候选冻结为当前 v0.3 全局 `QA2/S2`，并在本次 15A 补证后于 `2026-09-08T19:00:25.8394938+08:00` 于 R1 权威文档 §34 确认复用同一 `S2`。14/部署流程已建立 `E:\workbuddy-test\R1-4-S2\` 独立环境并完成 E0；R1-4 当前机器状态为 `planStatus=ready`、`staticGate=passed`、`testPointStatus=R1-4-T01..T06=not-tested`、`closureStatus=not-started`。冻结层的 R1 交接状态仍为 `🟢等待下一轮测试`/`closureStatus=in-progress`；两层状态不互相覆盖，也不改变 R3 当前 `Review4` 阻断门禁；Review3 仅作为历史事实保留。

**当前门禁结论**：R3 `Fix3` 候选身份、恢复、来源和构建清单可静态核对，但正式入口未取得可操作 WorkBuddy 窗口，`sky` 桌面控制未配置，无法取得停用确认、跨周幂等、刷新/重进、对照、未来行保留、业务只读或持久化 GUI 证据。因此 `Review5` 判定 `🔴驳回（当前环境阻断，核心范围无法可靠验证）`，不是 Fix3 产品失败定论；本次不调用 `15A`、`15`、`14`，不创建 R3-1。配置并恢复正式 GUI 后回到 `05` 使用下一正式编号 `Review6`。历史 B1、R1、R2 和 R3 Review3/Review4 结论保持不变。

**前置开发状态**：v0.3 开发阶段既有闭环结论保持有效；本看板只裁决当前功能实测 R 的审查门禁，不把开发闭环写成用户验收或版本闭环。

**当前用户实测批次**：历史 `B1 = R1 + R2`；R1 `Fix2/Review3` 已完成并由 15 冻结为 `QA2/S2`；本次 `B4` 仅承载 `R3 Fix3` R-only 候选，不是批次合并。
**当前用户实测状态**：历史 `R1-3：✅ E0 通过，T01 未测试；R1-4：✅ E0 通过（planStatus=ready、staticGate=passed、T01-T06=not-tested、closureStatus=not-started），仅开放 T01；R2-1：13 合并层 closureStatus=closed，T01-T06 合并事实已完成`；当前 `R3：🔴Review4 驳回（当前环境阻断，核心范围无法可靠验证），U5-01~U5-06 均为待环境补证，planStatus=not-created、staticGate=not-applicable、testPointStatus=not-tested、closureStatus=blocked；R3-1 尚未创建`。R2-1 原始计划台账仍保留 `testPointStatus=not-tested`，不等于改写计划、用户验收、`G1`、`C1` 或维护期。

## 活跃 R 状态

| R | 当前唯一闭环文档 | 最新正式位置 | 当前状态 | 合法下一步 |
|---|---|---|---|---|
| `R1` | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\v0.3-R1-项目投影模板与周统筹创建-全流程闭环记录.md` | `R1 Fix2 Review3`：U-7/U-8 均通过；§32 首次冻结 `QA2/S2`，§34 完成 15A 补证确认；R1-4 独立部署和 E0 已通过；历史 `Review1`、`Review2` 保留 | `🟢等待下一轮测试`；R1-4 机器状态为 `planStatus=ready`、`staticGate=passed`、`T01-T06=not-tested`、`closureStatus=not-started`；不等于用户验收或 R1 最终关闭 | 调用 `12-GPT自主实测`，只从 `R1-4-T01` 开始；不得复用 Review3 的 B1/S1 入口或数据副本，完成后按 `用户 06 → 13` 收束 |
| `R2` | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-月目标重复选取-全流程闭环记录.md` | `R2-1` 测试合并及 T06-C02/C03 补充 GUI/只读核验；O-01/O-02 已收束为非缺陷观察；U-5 独立分流 R3 | `✅当前轮次闭环`；13 合并层 `closureStatus=closed`，计划原始台账保持 `not-tested` | R2 无需新 Fix/Review；保留 R2 闭环并按独立 R3 处理 U-5；版本级 `G1` 仍待所有活跃 R 关闭 |
| `R3` | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第3轮\v0.3-R3-停用后固定任务未来周边界-全流程闭环记录.md` | `R3 Fix3 Review4`：正式入口/桌面控制环境阻断，U-5 核心 GUI 范围未验证 | `05 Review4=🔴驳回（当前环境阻断，核心范围无法可靠验证）`；`planStatus=not-created`、`staticGate=not-applicable`、`testPointStatus=not-tested`、`closureStatus=blocked`；不等于用户实测开始 | 补齐正式 GUI/`sky` RPC 和业务证据后回 `05` 使用 `Review5`；此前不交 `15A`、`15` 或 `14` |
| `B1` | `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\合并复审\B1-merge的全流程闭环.md` | §16：B1 批次闭环裁决；B1-merge Review2 通过、15A 完成、QA1/S1 冻结、R1-3/R2-1 已获得处置 | `✅批次闭环`（`closureStatus=closed`） | 后续 `B2/R1-4` 与独立 `R3` 路径分别推进；不回溯为 B1 未完成 |

## 当前门禁队列

| 节点 | 当前状态 | 限制 |
|---|---|---|
| `R1 Fix1` | `✅07 已执行` | 已纳入 B1-R1-only 和最终合并复审；不单独冻结 QA/S |
| `R1 Review1` | `✅通过（修复复审）` | R1-only 通过；后续计划绑定 B1 的 S1 |
| `R1 Fix2` | `✅07 已执行` | U-7/U-8 同一统一 Fix；候选 `B2/R1-Fix2-only`；不得拆批次 |
| `R1 Review2` | `🔴驳回（当前无法完整复审）` | 历史阻断记录保留；U-8 缺真实 D+1 GUI 证据 |
| `R1 Review3` | `✅通过（修复复审）` | U-7/U-8 整批证据通过；候选机器状态 `passed`；15 已冻结 `QA2/S2`，下一步交 `14` |
| `R2 Fix1` | `✅07 已执行` | 只处理 U-4；不包含 `COV-R2-01` 或 R1 事项 |
| `R2 Review1` | `历史前置停止（保留）` | 已由 R2 `Review2` 在后续候选上补证通过；不是当前阻断，不覆盖历史记录 |
| `R2 Review2` | `✅通过（修复复审）` | `COV-R2-01` 仍为后续覆盖项 |
| `R3 Fix2` | `✅07 已执行` | U-5 单一统一 Fix；候选 `B3/R3-Fix2-only`；不得拆批次，不创建 B3-merge |
| `R3 Review2` | `🔴驳回（历史环境阻断）` | 历史结论保留；已由下一正式 `Review3` 复审，不覆盖历史记录 |
| `R3 Review3` | `🔴驳回（核心范围无法可靠验证，且批准边界冲突）` | U5-01 停用确认布局和软删除未来实例范围冲突；U5-02~U5-05 依赖项停止；U5-06 机器/只读子检查通过；回 `07`，不交 `15` |
| `R3 Fix3` | `✅07 已执行` | U-5 单一统一 Fix；候选 `B4/R3-Fix3-only`；Review4 因环境阻断未进入用户实测；不得拆批次 |
| `R3 Review4` | `🔴驳回（当前环境阻断，核心范围无法可靠验证）` | U5-01~U5-06 均待环境补证；回到 `05` 使用 `Review5`，不交 `15A`、`15` 或 `14` |
| `B1-R1/R2-only` | `✅独立构建并通过复审` | 两个 R-only 均从同一 C0 独立生成，各自使用独立程序、包装和 `userData` |
| `B1-merge 修复记录1` | `✅已记录并完成重建` | 不是新的 R Fix；未带入 `COV-R2-01`，不回流或拆分 R1/R2 |
| `B1-Merge-Review1` | `✅通过（历史候选）` | 历史结论保留，不作为最终冻结候选 |
| `B1-Merge-Review2` | `✅通过（修复复审）` | 当前最终合并包复审已完成 |
| `15 QA/S` | `✅QA1/S1 已冻结；✅QA2/S2 已冻结` | QA1/S1 只绑定最终 B1-merge；QA2/S2 只绑定 R1 Fix2 候选；不得互换或复用入口/数据 |
| `14 R1-4 / R2-1` | `R1-4：独立 S2 环境已部署且 E0 通过（planStatus=ready、staticGate=passed、T01-T06=not-tested）；R2-1：13 合并层已闭环` | `R1-4` 只开放 T01，下一步交 `12`；R2-1 计划原始台账不变；15 不创建测试计划 |
| `14 R3` | `未启动` | R3-1 尚未创建；必须等待当前 `Review4` 通过和对应 `15A/15` 完成同步、冻结 QA/S 后再创建 |
| `G1` | `未到节点` | 必须等待所有活跃 R 轮次和双方证据完成；不由本次冻结代替 |

## 当前证据与边界

- **最终候选来源**：`E:\workbuddy-snapshots\v0.3.0-qa.1-b1-merge-source`；HEAD/baseCommit `34f155a1886c52dae278fbc71d037975646b2563`；sourceTreeFingerprint `32FDCB473A3920FEC422D1BB021FAEC4651497C5221CF68E121665227663AA5B`；trackedDiffSha256 `443C31131B4802EE0ABD0C5A4D68646E345C53361B51D47D0D220989FD332754`。
- **候选构建**：记录 `E:\workbuddy-snapshots\v0.3.0-qa.1-b1-merge-rebuild1\candidate-record.json`；构建目录 `E:\workbuddy-snapshots\v0.3.0-qa.1-b1-merge-rebuild1\out`；版本 `0.3.0-qa.1`；100 个文件；清单哈希 `1ABAD3E47CF69DD9D167EC01E6834A51FC42B96AEE971DC5A5E6CB551DE6AB6F`；`WorkBuddy.exe` SHA-256 `EEC94A500952CC83A15A9BE1907B0059D8A6262BE63503FA9E54F94716011874`。
- **S1 快照**：`E:\workbuddy-snapshots\v0.3.0-qa.1-b1-merge-s1`；构建物 100 个文件、335401715 bytes；`S1-manifest.sha256` SHA-256 `0044A2AE3A0075C3733748006308FD3E5F05B19388C5CDEAF02C584DA94FE581`；快照根目录 102/102 个文件只读；S1 与候选和 Review2 程序副本逐文件一致。
- **最终冻结核对证据**：`E:\workbuddy 复审\B1\merge\evidence\B1-merge-qa1-s1-freeze-verification-final.json`，独立全量复算 `allChecksPassed=true`；旧失败核对文件保留为历史。
- **Review2 证据**：程序 `E:\workbuddy 复审\B1\merge\review2\app`；入口 `E:\workbuddy 复审\B1\merge\review2\启动复审-Review2.bat`；userData `E:\workbuddy 复审\B1\merge\review2\userData`；证据目录 `E:\workbuddy 复审\B1\merge\review2\evidence`；前缀 `B1-merge-review2-`；最终只读证据 `B1-merge-review2-final-readonly-reopen.json` 的 `integrity=ok`。
- **构建/来源证据**：`E:\workbuddy 复审\B1\merge\evidence\B1-merge-rebuild1-source-status.txt`、`B1-merge-rebuild1-tracked-diff.patch`、`B1-merge-rebuild1-vitest.log`、`B1-merge-rebuild1-pack.log`；测试 `28 files / 384/384`，构建 exit 0。
- **本次 B1 正式入口边界**：唯一入口为 `E:\workbuddy-test\启动实测.bat`，程序目录为 `E:\workbuddy-test\app-v0.3`，绑定冻结 S1 的 `WorkBuddy.exe` SHA-256 为 `EEC94A500952CC83A15A9BE1907B0059D8A6262BE63503FA9E54F94716011874`，S1 清单 SHA-256 为 `0044A2AE3A0075C3733748006308FD3E5F05B19388C5CDEAF02C584DA94FE581`。四个活动数据目录位于 `E:\workbuddy-test\R1-3&R2-1\B1-20260829\` 下，旧 `E:\workbuddy-test\userData` 保留且预检前后未改变；旧程序保留在 `E:\workbuddy-test\app-v0.3-before-qa1-20260829`。
- **B1 入口预检证据**：`E:\workbuddy-test\R1-3&R2-1\B1-20260829\entry\evidence\B1-entry-once-preflight.json` 于 `2026-08-29T22:17:26.2262830+08:00` 记录 `allChecksPassed=true`、四角色启动通过、实际数据库路径创建、旧目录不变且结束无残留进程；状态 `entry-preflight-passed-controlled-cleanup-business-prefix-pending` 中的 `business-prefix-pending` 只对应 T01 动态证据待取证，不表示 E0 失败或阻止 T01 启动。
- **真实数据边界**：未读取、复制、修改、清空或删除 `%APPDATA%\workbuddy\`；Review2、R1-only、R2-only 和正式入口的程序、`userData` 与数据前缀不互用。
- **范围边界**：`COV-R2-01` 已在 R2-1 事实合并中留痕；`F-ISO-01` 的批准包装入口替代已在 R1/16 和 B1-Merge-Review2 中核对；R1-3 的 T01 动态入口证据仍待后续复核，R2-1 的 O-01/O-02 已由同一 S1 GUI/只读补证收束，未产生需修复的新产品缺陷、数据损坏、安全风险或越界改动。
- **R3 Review3 证据**：候选 `E:\workbuddy 复审\B3\R3-Fix2-only\` 的 `review-meta.json`、源/构建清单、恢复记录、`process-launch.json`、17 组 GUI 截图/JSON、`candidate-static-review.json` 和 `final-readonly.json` 可定位；候选构建为 100 文件、335402370 bytes，`WorkBuddy.exe` SHA-256=`512e0b1e861b6f03b2e53f3a26613f8fe48520dfb6c4892f525343ad9ec76866`，`restoreStatus=verified`。
- **R3 Review3 边界**：同一独立 userData 中 W0 F1 创建、取消停用、刷新、离开/重进和关闭重进保持 1 条定义、1 条周实例、1 条日行；只读 `query_only=1`、`integrity_check=ok`、外键为空，主库读取前后 SHA-256 不变。停用确认未提交，跨周/对照/未来行保留未测试；候选打包逻辑软删除未进入日任务的未来实例，与 16 批准边界冲突。
- **R3 Review4 证据**：候选 `E:\workbuddy 复审\B4\R3-Fix3-only\` 的入口、独立 `userData`、`review-meta.json`、来源/构建清单和 `restoreStatus=verified` 可定位；环境核对索引为 `E:\workspace\GPT实测记录\v0.3实测内容\第3轮\证据\R3-Fix3-Review4-环境核对-20260909.md`。正式入口启动出现 `Lock file Can not be created! Error code: 5`，`sky.list_apps()` 返回 `Trusted RPC service is not configured: sky`；没有当前 GUI/业务只读/持久化证据，不能将静态材料写成通过。
- **R1 QA2/S2 冻结证据**：R1 候选 `E:\workbuddy 复审\B2\R1-Fix2-only\` 的 `review-meta.json`、`control\fix-complete.json`、`control\restore-result.json`、`control\candidate-source-manifest.json`、`control\build-manifest.json` 和 `control\review-status-Review3.json` 可定位；来源指纹为 `c7c728606f53408dd98c68fecd32c4e7b4d30626d3fadfe408233183433da6a3` / `0ebcc089cde20d452e81438e30e0b38d4734c72903145d6ede1063d145a36370`，批准差异指纹为 `de41c7e2b57db276b5197601d82a5d639eeea215d91fb72d3fe32cda64c59d74`，源码清单 SHA-256=`e9f55313e6bbd01bf8d27e331d25b0adeb42ffc98f12fa9950b03d0dfc080ac6`，构建清单 SHA-256=`9d3d27b389159bba9948e57d24ea3decdeb5484db295133739b833a77ea1f5e0`，`WorkBuddy.exe` SHA-256=`D952D488A0878D0A8B9BC65E5063FFFFD4A7EDE58C99C2981407B96E1DDC7577`。
- **R1 S2 快照边界**：`E:\workbuddy-snapshots\v0.3.0-qa.2-r1-fix2-s2` 的 `S2-manifest.sha256` SHA-256=`316D3CF6318ED5F268375DC24C8CBA0EF0AEF8B6C7B11D17FE2C9841ACEAD5A0`，100 个构建文件/335403668 bytes，快照根目录 102 个文件全部只读；快照记录为 `S2-snapshot-record.json`，SHA-256=`A7A5184251E3E0EBB194788A4061EBFB8BB4DCC5755ADA500F4634D7C20117DE`。Review3 的 U-8 使用 B1/S1 入口和 `USER-20260903` 数据仅作历史复审证据，禁止直接复用。
- **R1-4 独立入口和 E0 证据**：正式入口 `E:\workbuddy-test\R1-4-S2\启动R1-4实测.bat`，角色选择器 `E:\workbuddy-test\R1-4-S2\选择R1-4实测角色.bat`，程序副本 `E:\workbuddy-test\R1-4-S2\app`；GPT/USER 数据分别为 `data\GPT-20260908\userData` 和 `data\USER-20260908\userData`，前缀分别为 `v0.3-R1-4-GPT-20260908`、`v0.3-R1-4-USER-20260908`。首次入口哈希运行时失败记录保留；修复后 E0 证据 `E:\workbuddy-test\R1-4-S2\evidence\R1-4-E0-preflight-attempt-20260908-204925.json` 为 `allChecksPassed=true`，索引位于 `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\R1-4相关证据\E0-证据索引-20260908.md`。T01-T06 均未测试。

## R Fix 批次自动编排状态

- 编排器：E:\workspace\tools\r_fix_review.py；适用范围：新建或明确启动的 R Fix 批次。
- 单 R Fix：begin-fix 保存基线，complete-fix 生成 E:\workbuddy 复审\<B>\<R>-<Fix>-only，并在归属校验后恢复代码开发区；恢复失败或有未归属改动时保持阻塞。
- 多 R 批次：finalize-batch 固定预期集合，scan-batch 动态枚举实际目录；所有 R-only Review passed 后才允许 prepare-merge/complete-merge。
- 合并后：merge 候选保留 E:\workspace\workbuddy 整合状态；13/05 使用唯一 B<N>-merge 闭环文档完成集成复审，15 只冻结最终 merge 包。
- 既有 B1 目录、候选、证据和 QA1/S1 冻结资产不迁移、不覆盖、不由新工具重建；R1 QA2/S2 只绑定 R1 Fix2 候选，R1-4 独立正式入口和数据副本已由 14/部署流程创建并通过 E0，不进入 B1 或长期入口体系。

## 当前禁止事项

- R1-3 已完成独立 E0 静态复核并判定 `✅ E0 通过，可进入 T01`，但其 T01 仍未测试；R1 `Fix2/Review3` 已冻结并在 §34 确认复用 `QA2/S2`，R1-4 已使用独立 S2 入口和数据完成 E0，但 T01-T06 仍全部未测试；R2-1 已完成 T01-T06 的 13 事实合并并关闭 R2 当前轮次。R2-1 计划正文和原始 `testPointStatus=not-tested` 不被改写；历史 R1-3 计划和 B1/S1 入口不替代本次 R1-4 的 S2 入口或数据目录。
- 本次冻结不等于用户验收、G1、C1、正式发布或维护期；未列出的用户用例仍不能解释为用户全量通过。
- 后续如发现 B1 合并问题，只能在 B1 权威文档追加下一条 `B1-merge 修复记录N` 并按批次复审，不回流或拆分 R1/R2，不新建 R-only `FixN`。
- 本次 R3 `Review4` 只追加 R3 唯一闭环文档 §28 和环境核对索引，并更新本文件当前快照；未修改候选程序、依赖、源代码、测试计划、GPT 正式报告、用户 06、`当前代码状态.md` 或历史 Review，未创建 `审查日志/`。`E:\workspace\.tmp-review4-probe` 仅为诊断目录，不计入复审证据；Review4 阻断解除前不得交 `15A`、`15` 或 `14`。

## 关联文档

- `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第1轮\v0.3-R1-项目投影模板与周统筹创建-全流程闭环记录.md`
- `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第2轮\v0.3-R2-月目标重复选取-全流程闭环记录.md`
- `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\第3轮\v0.3-R3-停用后固定任务未来周边界-全流程闭环记录.md`
- `E:\workspace\规范类\项目代称.md`
- `E:\workspace\规范类\阶段闭环门禁规则.md`
- `E:\workspace\规范类\文档治理总纲.md`
- `E:\workspace\用户prompt\15-冻结QA-S.md`
- `E:\workspace\用户实测阶段\v0.3-任务数据流通重构\合并复审\B1-实测入口一次性解阻前置补充.md`

## 历史记录落点

本文件不保存按日期追加的审查历史。B1 的来源冲突、重建、Review2 和 QA1/S1 冻结事实统一保留在唯一权威文档 `B1-merge的全流程闭环.md`；R1/R2/R3 的用户事实、Fix、Review 和各自冻结事实保留在各自唯一闭环文档。本次 R1 `QA2/S2` 首次冻结完整事实见 R1 文档 §32，15A 补证后的复用确认见 §34；R3 `Review4` 的完整事实见 R3 文档 §28，Review3/Review2/Review1 作为历史复审记录保留。
