# WorkBuddy 安装说明

**版本**：0.2.0（计划与复盘体系）
**平台**：Windows 11 x64

---

## 安装步骤

### 1. 双击安装包

双击 `WorkBuddy-Setup-0.2.0.exe`。

### 2. SmartScreen 提示（预期，非异常）

本应用**未经过数字签名**（个人项目，无代码证书）。Windows SmartScreen 会弹出：

> **Windows 已保护你的电脑**
> Microsoft Defender SmartScreen 阻止了无法识别的应用启动。

**操作**：点击「**更多信息**」，然后点击「**仍要运行**」即可继续。

> 若不出现「更多信息」，左键点一下弹窗空白区域即可展开选项。

### 3. NSIS 安装向导

- 选择安装目录（默认为 `%LOCALAPPDATA%\Programs\WorkBuddy`）。
- 安装为当前用户，无需管理员权限。
- 勾选「创建桌面快捷方式」（默认勾选）。
- 点击「安装」→ 完成。

### 4. 首次启动

- 桌面快捷方式或开始菜单「WorkBuddy」双击。
- 首次进入今日总览为空——这是正常的，请先配置：
  1. 点击左侧「设置」进入设置页。
  2. **通用**：填入你的称呼。
  3. **热点源**：默认已含 36氪 / 少数派 / V2EX RSS，可增删改。点「刷新热点」立即拉取。
  4. **AI 配置（可选）**：填入 Base URL / Model / API Key（API Key 本地密文存储），点「测试连接」验证。不配 AI 亦可正常使用（计划/复盘基础功能离线可用，仅「AI 周/月复盘」「AI 引导」需配置）。
  5. 返回「今日总览」查看晨间数据。

### 5. v0.2 新增：计划与复盘（「规划」页）

- 左侧栏「规划」可展开**日 / 周 / 月**三级：
  - **日规划**：每天写当日计划，保存后 `- [ ]` 任务自动生成「今日待办」。
  - **周统筹 / 月指导**：制定下周/下月计划，作为日规划的级联源头（点「选取」把上级任务下沉到当日）。
  - **复盘**：日复盘（确定性统计，离线）/ 周复盘 + 月复盘（聚合 + AI 生成 Markdown，需配 AI）。
- 周日会出现「📌 做周统筹」、月末「📌 做月指导」周期提醒待办，带「前往」直达。

---

## 数据位置

所有数据（数据库、配置）存储在：

```
%APPDATA%\workbuddy\
```

- `workbuddy.db`：SQLite 数据库（WAL 模式）
- API Key 使用操作系统级安全存储加密（DPAPI），数据库中为密文。

---

## 备份与恢复

在设置页「数据管理」中：
- **导出**：将全部业务数据导出为 JSON 文件，可自行保管。
- **导入**：选导出文件恢复（会覆盖当前数据，导入前二次确认）。

---

## 卸载

- **Windows 设置 → 应用 → 已安装的应用** → 搜「WorkBuddy」→ 卸载，或
- 安装目录中运行 `Uninstall WorkBuddy.exe`

卸载会删除应用文件，数据文件（`%APPDATA%\workbuddy\`）需手动备份后删除。

---

## 已知限制

- 未代码签名（SmartScreen 警告如上）。
- 仅 Windows x64（Mac / Linux 未适配）。
- 应用图标为临时占位。
- 不支持自动更新。
