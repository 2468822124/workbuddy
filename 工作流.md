# WorkBuddy 当前全阶段工作流

> 更新日期：2026-09-08
>
> 本文档根据当前仓库中的项目计划、门禁规则、Prompt、阶段闭环记录、实测计划和当前状态整理。它描述的是当前应执行的流程，不改写历史记录。历史 v0.1、v0.2 文档仍保留在归档目录中，仅用于回查，不作为新流程的落点。

## 一、先看结论

WorkBuddy 当前采用三方协作：

- GPT&Claude：项目经理、规格编写者、审查者、客观 GUI 实测者、文档和门禁维护者。
- GLM+DS：产品代码、自动化测试、构建/配置、实现/修复日志和 `当前代码状态.md` 的代码执行联合团队。
- 用户：需求决策者、真实窗口实测者、主观体验裁决者、最终验收者。

`GLM+DS` 是现行代码执行团队，不是新的审查角色。每个 `02/04/07/08` 开始卡和实现/修复日志必须填写真实的 `实际执行者=GLM`、`DS` 或 `GLM+DS`；联合执行时还要写明文件/命令分工和唯一的状态文件写入者。GLM 与 DS 都不得对自己的代码作 GPT&Claude 门禁结论，审查和关闭仍由 GPT&Claude 独立执行。

当前项目主线是 `v0.3 任务数据流通重构`。阶段 1 至阶段 6 的开发部分均已达到 `✅开发闭环`；C0 已归档为提交 `34f155a`，Tag 为 `v0.3.0-c0`。当前处于功能实测阶段，R 是实测处置来源轮次；R1/R2 的事实、处置和未决事项分别推进，R1-3 与 R2-1 计划均已创建，当前均为 `🟡待补前置`，等待独立 E0 复核。

全生命周期总链路如下：

```text
需求/功能立项
  -> 10 功能项目计划
  -> 01 开发单元规格
  -> 02 GLM+DS 实现
  -> 03 GPT 审查
  -> 04 GLM+DS 修复（如有）
  -> 05 GPT 复审
  -> ✅开发单元闭环
  -> 下一开发单元
  -> 全部开发单元完成
  -> 11 C0 Git 基线归档
  -> S0 不可变实测快照
  -> R 轮次测试：12 GPT 实测 + 用户实测 + 06 回传
  -> 13 合并双方证据并写事项处置总表
  -> 16 处理环境/产品决策（如有）
  -> 07 统一 Fix（修复/替代/删除）
  -> 05 整批 Review
  -> 15 冻结 QA/S
  -> 14 创建同轮下一计划或覆盖补充计划
  -> 所有活跃 R 轮次关闭
  -> G1 版本级整合回归
  -> 用户最终验收
  -> C1 正式版本
  -> 维护期 CR：GPT 起单 -> GLM+DS 实现 -> GPT 审查 -> 关闭
```

## 二、双根、角色和单一真相源

### 2.1 双根规则

1. 文档根是 `E:\workspace\`。所有治理文档、规格、日志、Prompt、测试计划、GPT 报告和闭环记录都写在这里或其子目录中。
2. 代码根是 `E:\workspace\workbuddy\`。这里只放代码、测试、构建配置、依赖和构建产物。
3. GLM 或 DS 在代码根开发时，写文档必须回到文档根；不得在 `workbuddy\` 下创建 `当前状态.md`、项目日志或其他治理文档副本。
4. 相对文档路径都相对于 `E:\workspace\`，不是相对于 `workbuddy\`。

### 2.2 主要文档的权威关系

| 需要确认的事实 | 首要文件 | 说明 |
|---|---|---|
| 项目功能范围、阶段依赖、功能级验收 | `用户打回重构/任务数据流通重构（v0.3）/项目计划.md` | 当前 v0.3 主线计划 |
| 某开发阶段如何实现、验收什么 | 对应阶段的 `*-全流程闭环记录.md` | 规格环节是实现基准，最后复审是开发闭环裁决 |
| GLM+DS 当前实现事实 | `当前代码状态.md` | 只描述当前实现快照，不裁决是否闭环 |
| GPT 当前审查、门禁和证据边界 | `当前审查状态.md` | GPT 维护，审查前必须读取 |
| 角色、路径和文档治理 | `规范类/文档治理总纲.md` | 全部文档治理的上位规则 |
| 开发/实测/维护门禁 | `规范类/阶段闭环门禁规则.md` | C0、S、R、G1、C1 的门禁定义 |
| 维护期 CR 操作 | `规范类/变更闭环规则.md` | 阶段 6 用户 E2E 完成后使用 |
| 编号和版本节点含义 | `规范类/项目代称.md` | C0、QA、S、R、Fix、Review、G1、C1、CR |
| Git 提交和归档 | `规范类/git提交规范.md`、`用户prompt/11-在git中归档.md` | 只在对应终态执行 |

### 2.3 开发前环境条件

1. Windows x64 开发/实测环境可用。
2. 代码根存在 `workbuddy\package.json`、锁文件和 `node_modules`，或者已先安装依赖。
3. 原生依赖 `better-sqlite3` 与 Electron 版本匹配；必要时执行 `npm.cmd run rebuild`。
4. 不能把真实 API Key、Cookie、私人数据库或真实用户数据放进代码、日志、截图和测试报告。
5. 需要 GUI 实测时，必须有可见桌面窗口、测试专用数据目录和恢复步骤。
6. 测试数据必须与真实 `%APPDATA%\workbuddy\` 隔离。

## 三、功能立项工作流

功能立项使用 `用户prompt/10-功能项目计划启动.md`。它先于开发单元的 01-05 循环。

功能立项工作流：

```text
用户提出功能需求
  -> GPT 读取上位规则和当前状态
  -> 建立功能目录
  -> 编写功能级项目计划
  -> 划分子阶段和依赖
  -> 定义功能验收、禁止边界、错误降级和版本
  -> 核对新依赖
  -> 用户确认计划
  -> 用 01 逐个启动开发单元
```

前置条件：

- 1 的前置条件：用户已经提出可说明目标和范围的功能需求。
- 2 的前置条件：已明确当前项目版本、代码根、文档根和当前未完成任务。
- 3 的前置条件：GPT 已读取 `文档索引.md`、`当前代码状态.md`、`当前审查状态.md`、技术栈规范和项目上位计划。
- 4 的前置条件：功能名称、目标用户和用户可见成果已经明确。
- 5 的前置条件：功能范围已经足够明确，可以拆成互不重叠的开发单元。
- 6 的前置条件：每个子阶段的前置依赖、验收点和越界边界已经写清楚。
- 7 的前置条件：如果需要新依赖，计划中已写包名、版本、用途、体积影响和替代方案，并等待 GPT 审查、用户确认。
- 8 的前置条件：功能计划已经写完，用户同意按该计划进入开发。

逐条执行：

1. GPT 接收用户需求，先判断这是新功能、已有功能修复、用户实测问题，还是维护期 CR。已有功能代码改动不能借“新功能”绕过对应的闭环。
2. GPT 读取项目当前状态和上位规范，确认功能是否与正在开发的阶段冲突，确认是否触碰旧模块冻结区、数据模型铁律或版本范围。
3. GPT 在文档根建立功能目录。新功能通常落在 `开发阶段/vX-功能名/`；当前 v0.3 任务数据流通重构是已有的用户打回重构主线，落点为 `用户打回重构/任务数据流通重构（v0.3）/`。
4. GPT 创建功能级 `项目计划.md`，至少写入：功能目标、用户可见成果、包含范围、不包含范围、子阶段列表、依赖关系、功能级验收点、禁止边界、错误降级、版本计划和交付方式。
5. GPT 将大功能拆成开发单元。每个单元必须能单独写规格、实现、自测、审查、修复、复审和闭环；不能把多个无法独立验收的功能混成一个模糊单元。
6. GPT 规定每个单元的顺序。依赖数据模型的 UI 必须排在基建之后；依赖周统筹的日规划必须排在周统筹之后；依赖四渠道的复盘必须排在今日总览之后。
7. GPT 把上位项目的验收标准、禁止边界和错误降级具体化到本功能，不得只写“按规范执行”。
8. GPT 核对技术选型。新依赖必须走技术栈规范的评审流程，不能在实现时顺手引入。
9. GPT 将计划作为后续 01 的输入，并告诉用户“项目计划已就绪，包含 N 个开发单元”。
10. 用户确认后，GPT 才能按子阶段顺序启动 01；用户未确认逻辑模型时，不得开始功能代码实现。

## 四、开发阶段工作流

开发阶段工作流：

```text
01 写规格
  -> 02 GLM+DS 实现
  -> 03 GPT 首审
  -> 若有问题：04 GLM+DS 修复
  -> 05 GPT 复审
  -> 若仍有问题：再次 04 -> 05
  -> ✅开发单元闭环
  -> 检查下一单元前置
  -> 启动下一个单元
```

前置条件：

- 01 的前置条件：功能级 `项目计划.md` 已存在；如果不是首个单元，上一单元必须是 `✅开发闭环`，除非用户明确要求带风险跳过并完成记录。
- 02 的前置条件：GPT 已在该单元全流程闭环记录中完成规格，任务拆解、验收点、禁止边界和降级行为足够具体。
- 03 的前置条件：GLM+DS 已完成代码、自测、实现日志和 `当前代码状态.md` 更新，并报告“待 GPT 审查”。
- 04 的前置条件：GPT 03 审查已经列出明确问题 ID、严重度、根因/证据、允许改动范围和验收条件。
- 05 的前置条件：GLM+DS 已完成对应修复批次，重新自测并把修复日志写回同一份全流程记录。
- 下一开发单元的前置条件：当前单元最新正式复审结论为 `✅通过（开发单元闭环）`，没有未关闭 HIGH/CRITICAL、红线违规或越界改动。
- C0 的前置条件：当前目标版本所有开发单元均已开发闭环，并完成工作区归属、版本、入口、隔离和独立验证核对。

### 4.1 第 01 步：GPT&Claude 启动开发单元并写规格

1. GPT&Claude 读取功能级项目计划，确定本单元的名称、范围、前置单元和依赖。
2. GPT&Claude 读取上一单元全流程记录底部最新正式结论，并与 `当前审查状态.md` 当前指针核对。上一单元为 `✅开发闭环` 才能正常启动；若是 `🟡待审`、`🟡实现中`、`🔴需修复`，先提示用户完成前置闭环。顶部状态只作历史元信息，不能单独裁决当前状态。
3. 如果用户明确要求跳过未闭环前置，GPT&Claude 必须在 `当前审查状态.md` 和本单元规格顶部记录风险、未闭环项、日期和用户决定，不能静默跳过。
4. GPT&Claude 在本单元的 `*-全流程闭环记录.md` 内写”规格”环节，不再创建新的独立规格文件。
5. 规格必须写明：
   - 单元目标、用户可见成果。
   - 包含范围和不包含范围。
   - 前置依赖和不能提前做的内容。
   - 技术设计、数据模型和迁移策略。
   - IPC 通道、入参、返回的 `Result<T>` 契约。
   - 文件落位和模块边界。
   - GLM+DS 的有序任务拆解。
   - 可逐条验证的验收点。
   - 禁止边界、失败场景和错误降级。
   - 运行、测试和交付说明。
6. GPT&Claude 将本单元顶部门禁初始化为 `🟡实现中`，完成后报告”规格已就绪，待 GLM+DS 执行”。首次由 GLM 执行代码任务前，先完成一次 `02A-GLM代码熟悉.md` 只读熟悉。

### 4.2 第 02 步：GLM+DS 按规格实现

1. GLM+DS 先读取本单元全流程记录的规格，再读取功能计划、当前两个状态看板、技术栈规范、命名规则和双根规则。
2. GLM+DS 先确认任务确实属于本单元，不能借实现顺手处理未列入范围的重构或新功能。
3. 代码按数据流和分层实现。常见顺序是：共享类型/迁移 -> repository -> service/派生查询 -> IPC -> preload -> composable -> view/component -> 测试。
4. SQL 只放在 repository；service 负责业务编排；IPC 负责通道和参数校验；renderer 不能直连数据库、SQL 或 Electron 内部 API。
5. 新任务域必须使用结构化数据和 `source/sourceRef`，不能通过文本内容推断出处，不能把任务存进 Markdown。
6. 完成态、统计和消耗状态必须通过凭据和派生查询实时计算，不能新增完成缓存字段。
7. 删除按项目规则使用软删除；旧模块冻结区只读、不改、不扩，新功能禁止 import 冻结区。
8. GLM+DS 实现规格中的每项任务，逐项自测规格验收点、正常路径、边界、错误降级和技术栈红线。
9. 若实现必须偏离规格，先记录偏差、原因、影响和补充验收；不能先改后解释。
10. GLM+DS 将实现概要、完整文件清单、偏差、自测结果、已知问题、实际执行者和下一步写入同一份全流程记录的“实现日志”。
11. GLM+DS 更新 `当前代码状态.md` 的当前状态、任务概要、自测、已知问题和下一步，完成后报告“实现完成，待 GPT 审查”。共享状态文件同一时刻只允许一名实际执行者写入，并填写 `updatedBy=GLM`、`DS` 或 `GLM+DS`；联合执行时记录唯一写入者。

### 4.3 第 03 步：GPT&Claude 独立审查

1. GPT&Claude 先读取 `当前审查状态.md`，再读取本单元规格、实现日志和 `当前代码状态.md`。
2. GPT&Claude 先核对 GLM+DS 状态是否真实，不能直接把”自测通过”当作事实。
3. GPT&Claude 按规格验收点逐项检查，必须写出每一项的预期、实际、证据和结论。
4. GPT&Claude 运行项目实际入口，至少按影响范围运行测试、构建、类型检查和必要的 Electron/GUI 链路。
5. GPT&Claude 运行门禁三件套：
   - `npm.cmd test`：Vitest 全量测试。
   - `npm.cmd run build`：Electron Vite 构建。
   - `tsc -p tsconfig.node.json --noEmit`：Node 侧类型诊断，必须区分存量基线和新增错误。
6. 涉及 web/vue 的单元还要运行 `tsc -p tsconfig.web.json --noEmit`，如有存量诊断必须原样记录，不能隐藏。
7. GPT&Claude 运行 `git diff --check` 和必要的 `rg` 静态边界扫描，确认 SQL、旧模块 import、硬编码密钥、敏感日志和越层调用没有越界。
8. 涉及 UI 时，GPT&Claude 核对阶段 0 视觉基准、token 色源、窗口适配、空态、加载态、错误态、成功态和实际可点击路径。
9. GPT&Claude 构造规格要求的失败场景，例如非法输入、缺必填字段、数据库不可用、导入载荷不完整、错误 Key 或不可达地址，并确认可读反馈、不崩溃、不清空、不泄露。
10. GPT&Claude 将审查写回本单元全流程记录的”审查”环节，并同步 `当前审查状态.md`。
11. 结论只能是：
   - `✅通过（开发闭环）`：所有验收点有证据，红线和 HIGH/CRITICAL 均关闭。
   - `🟡需修复`：有明确可修复问题，进入 04。
   - `🔴驳回`：范围、数据、安全、隔离或核心实现失效，先恢复条件再继续。

### 4.4 第 04 步：GLM+DS 按审查单修复

1. GLM+DS 只读取 GPT 审查环节中的问题清单和修复要求，不自行扩大范围。
2. 先复现 GPT 的失败步骤，再改最小必要代码。
3. 每个问题都记录修改文件、原因、修复方式、对应验收点和残余风险。
4. 重新运行受影响测试、全量测试、构建、类型检查和边界扫描。
5. 按原失败操作重新运行；单元测试绿不能替代真实运行或 GUI 复跑。
6. 未能实际验证的项目必须写 `⚠️待复测`，不能写“已解决”。
7. GLM+DS 把修复日志追加到同一份全流程记录，更新 `当前代码状态.md`，报告“修复完成，待 GPT 复审”。

### 4.5 第 05 步：GPT&Claude 复审并决定是否闭环

1. GPT&Claude 读取同一份全流程记录，不接受 GPT&Claude 报告、测试计划或新建审查日志作为替代落点。
2. GPT&Claude 逐项复跑原始失败场景和修复验收点。
3. GPT&Claude 检查取消/确认、刷新/重进、数据一致性、错误降级、敏感信息、越界改动和新引入回归。
4. 如果是构建期单元，复审通过后把门禁改为 `✅开发闭环`，才能启动下一单元。
5. 如果仍有问题，追加 `ReviewN`，写清问题 ID、严重度、证据和下一轮 04 要求。
6. 如果已经通过但涉及用户实测问题，只能表示修复批次达到复测门槛，不得写成用户验收通过；后续必须冻结新的 QA/S 或复用有效 S，并创建下一份测试计划。

### 4.6 开发阶段的技术实现顺序

对当前 Electron + Vue 任务域，通常按以下代码路径落地：

```text
规格中的数据模型
  -> SQLite migration
  -> shared types / IPC 常量
  -> db repositories
  -> main services / 派生 DTO
  -> main IPC handlers
  -> preload 安全桥
  -> renderer useX composable
  -> views/components
  -> Vitest 单测与组件测试
  -> build / tsc / 静态边界扫描
  -> Electron 可见窗口验证
```

每层的前置条件和硬边界：

- migration 的前置条件：数据模型和新表责任已经由规格确定；已发布迁移脚本不能修改，只能新增迁移。
- repository 的前置条件：表结构、键、软删除和事务边界已确定；所有 SQL 必须落在 repository。
- service 的前置条件：repository API 已稳定；service 不直接拼 SQL，不绕过 repository。
- IPC 的前置条件：service 返回值、错误码和入参已经确定；通道必须校验必填参数并返回 `Result<T>`。
- preload 的前置条件：IPC 通道已命名并经过校验；只暴露最小安全 API。
- renderer 的前置条件：preload API 已存在；页面只调用 `useApi` 或 composable，不触碰数据库。
- 测试的前置条件：主逻辑已经可以被真实测试触达；纯函数、repository、service、IPC 和关键渲染消费均要覆盖。
- GUI 验证的前置条件：构建完成、启动入口明确、测试数据隔离并且能从用户可见窗口操作。

## 五、v0.3 六个实际开发阶段

本项目当前主线的阶段顺序由 `用户打回重构/任务数据流通重构（v0.3）/项目计划.md` 定义。一阶段一闭环，前一阶段未达到 `✅开发闭环`，后一阶段原则上不得启动。

### 阶段 1：数据模型与基建

阶段 1 工作流：

```text
确认无前置阶段
  -> 设计 flow 数据模型
  -> 新增迁移 0008 及新表
  -> shared 类型和 IPC 常量
  -> repositories
  -> 惰性克隆引擎
  -> 派生查询 service
  -> flow IPC/preload 骨架
  -> 全套单测、门禁和审查
```

前置条件：

- 用户已确认任务数据流重构的结构化模型。
- 新任务域与旧冻结区已经物理隔离。
- 规格已明确七张 `flow_` 表、凭据派生完成态和 `sourceRef` 语义。

逐条工作：

1. 新增 `flow_fixed_defs`、`flow_week_instances`、`flow_day_entries`、`flow_vouchers`、`flow_month_goals`、`flow_week_focus`、`flow_plan_templates`、`flow_journals` 等新任务域结构，并保持旧表到阶段 6 才处理。
2. 新增 migration，不修改已发布迁移。
3. 新增 shared flow 类型、IPC 常量和返回类型。
4. 以 repository 为唯一 SQL 入口，补齐 CRUD、事务和查询。
5. 实现按周隔离、按访问时惰性物化的克隆引擎，不使用定时任务。
6. 实现完成态、rail 可见性和统计的实时派生查询，不增加完成状态缓存字段。
7. 建立 flow IPC 和 preload 桥，验证输入、错误和 `Result<T>`。
8. 对迁移、repository、克隆、派生、IPC、非法输入和边界日期写测试。
9. 通过 03/04/05 后，阶段 1 标记 `✅开发闭环`，才能进入周统筹。

阶段 1 主要验收：迁移成功、SQL 全在 repository、克隆不重复、凭据撤销后派生状态实时回退、IPC 参数校验正确、旧冻结区无 import、门禁三件套通过。

### 阶段 2：周统筹页

阶段 2 工作流：

```text
阶段 1 开发闭环
  -> 周页面结构
  -> 周任务临时/固定 CRUD
  -> 惯常日和停用
  -> 周核心推进目标
  -> 月目标关联和手动输入
  -> rail/完成态/删除传播
  -> 测试、审查、复审
```

前置条件：

- 阶段 1 已 `✅开发闭环`。
- flow repository、克隆引擎和派生查询可被页面调用。
- 周实例的完成态和场次语义已确定。

逐条工作：

1. 搭建周统筹页面，包含核心目标、周任务清单、固定任务/临时任务和感想区。
2. 实现一次型和场次型周任务创建、编辑、完成、取消完成、删除和刷新保持。
3. 实现固定任务定义、惯常日、停用和本周惰性克隆。
4. 实现周核心推进目标的手动新增、月目标选取、标题、周归属和删除。
5. 实现月目标页，以及月目标向周目标的选取/转化链路。
6. 验证 rail 中已安排任务隐藏、移除后回 rail、跨周不重复选取。
7. 验证标题改名、删除和完成凭据向下游的传播语义。
8. 运行组件/组合式函数/IPC/repository 测试、build、tsc 和 GUI 复核。
9. 03 审查不通过时只按问题进入 04；05 通过后才进入阶段 3。

阶段 2 主要验收：月目标与周目标关联正确、一次型/场次型任务参数正确、固定任务按惯常日生成、完成进度实时正确、刷新重进不丢数据。

### 阶段 3：日规划与 rail

阶段 3 工作流：

```text
阶段 2 开发闭环
  -> 日规划页面与日期状态
  -> 锁定行/自由行/顺延/跳过/挪动
  -> rail 选取与移除
  -> 模板创建、套用、取消、编辑、删除
  -> 感想区和历史日期
  -> 测试、审查、复审
```

前置条件：

- 阶段 2 已 `✅开发闭环`。
- 周任务、固定任务、rail 和来源元数据可以被日页读取。
- 日日期参数和历史/未来日期规则已写入规格。

逐条工作：

1. 搭建 `/flow/day` 日规划页面，统一当日清单和 rail。
2. 区分锁定行与自由行；锁定行按规则只读，不能被自由拖动、删除或修改来源。
3. 实现 rail 的选取、移除、挪动、跳过和跨日去重。
4. 实现自由任务创建、编辑、删除和顺延渲染；验证顺延天数和高亮。
5. 实现日模板创建、模板项编辑、模板列表、套用确认、取消、部分勾选、改名和删除。
6. 模板套用必须复制断链，已插入的日任务不受后续模板改删影响。
7. 实现日感想，与任务域保持零外键、零同步。
8. 用 URL query 保存日期状态，验证默认今天、历史日、未来日、非法日期降级和重载。
9. 验证来源徽标覆盖七种来源，错误以页面提示条和空态呈现，不用崩溃或空白替代。
10. 完成自动化测试、build、tsc、GUI 操作链路和 03/04/05 闭环。

阶段 3 主要验收：锁定行语义、rail 不重复、模板列表和反馈可见、顺延正确、历史日可改、非法日期降级、URL 与页面状态一致。

### 阶段 4：今日总览四渠道

阶段 4 工作流：

```text
阶段 3 开发闭环
  -> 今日总览任务区重写
  -> day_entries 同实体双视图
  -> 项目任务适配层
  -> 提醒打卡型适配
  -> 手动待办接入
  -> 去重/来源/完成回写
  -> 四渠道测试、审查、复审
```

前置条件：

- 阶段 3 已 `✅开发闭环`。
- `flow_day_entries`、来源元数据和凭据派生查询稳定。
- 项目、提醒和手动待办只作为适配来源，不能反过来污染新任务域。
- UF4 的产品语义必须统一：当前 C0-01 用户裁决为方案 B，即项目任务必须显式点击“加入今日”后才生成今日投影；仅设置计划日期为今天不算投影成功。

逐条工作：

1. 将今日总览任务区切换到 `flow_day_entries` 的同实体双视图。
2. 为每个来源保存 `source` 和 `sourceRef`，展示时从源实体取实时名称，禁止内容匹配。
3. 项目任务提供“加入今日”写入口和完成回写；移出今日只移除投影，不删除项目源任务。
4. 接入提醒的当日打卡型任务，处理来源级错误，不影响其它渠道。
5. 接入手动待办，保证今日页与项目页看到同一实体语义，不重复生成。
6. 按来源动作矩阵验证可完成、可移出、可跳过、可编辑和不可编辑动作。
7. 验证重复来源去重、源任务改名传播、源任务删除传播和相关错误提示。
8. 验证跨午夜、非法日期、空态、网络/配置失败和其它渠道继续可用。
9. 通过测试、build、tsc、静态边界和 GUI 复核后进入阶段 5。

阶段 4 主要验收：四渠道统一、来源可追溯、同实体双视图、项目任务显式加入今日、完成回写、不重复、不误删源任务。

### 阶段 5：复盘与趋势

阶段 5 工作流：

```text
阶段 4 开发闭环
  -> 复盘 DTO 和唯一统计口径
  -> 日/周边界与跨周聚合
  -> 完成率/顺延/未安排/跳过/场次
  -> 封账视图
  -> 转下周清债
  -> 历史可编辑实时重算
  -> 测试、审查、复审
```

前置条件：

- 阶段 4 已 `✅开发闭环`。
- 四渠道任务和完成凭据能提供统一数据来源。
- 统计分母、跳过免罪、未完成和周边界语义已经确定。

逐条工作：

1. 建立复盘页面和派生 DTO，不新建统计缓存表。
2. 统一完成率、计划内任务、跳过、未安排、顺延和场次统计口径。
3. 实现跨日、跨周边界和趋势聚合，验证周一、周日、跨午夜和历史日期。
4. 实现封账视图，把未完成任务定格进入复盘清单。
5. 实现“转下周”显式清债：只允许合法历史周、未完成任务和合法目标周一；重复操作必须幂等。
6. 验证固定任务下周是全新实例，完成计数不能错误继承。
7. 验证历史日补勾后周完成态和趋势实时重算，不使用快照覆盖真实数据。
8. 验证复盘感想和任务域分层，错误显示空态和反馈条。
9. 运行全量测试、build、tsc、边界扫描和实际页面验证，完成 03/04/05。

阶段 5 主要验收：统计口径一致、趋势跨周正确、封账后可解释、转下周不重复、历史可改、跳过不计未完成、固定任务下周重新计数。

### 阶段 6：旧模块切换清理

阶段 6 工作流：

```text
阶段 5 开发闭环
  -> 固定旧代码/旧表/旧入口基线
  -> 路由和侧边栏切换
  -> 项目任务兼容适配
  -> 清理 LLM 旧规划路径
  -> 删除冻结区代码
  -> 旧表归档和物理清理
  -> 白名单导入导出
  -> 静态边界扫描
  -> 六轮历史场景回归
  -> 全量门禁、Electron/CDP 和审查
```

前置条件：

- 阶段 5 已 `✅开发闭环`。
- 新页面和 flow 数据域已经覆盖旧模块的实际调用入口。
- 删除前已经有数据库备份、恢复步骤和旧表归档方案。
- 阶段 6 只清理旧模块，不新增新的业务域，不修改已发布 0001-0009 迁移，不自动清理真实用户数据。

逐条工作：

1. 用 `rg` 建立冻结区引用、项目提醒调用、旧表和备份清单，固定可核对基线。
2. 先切换 router 和 AppSidebar，再让现有页面改用新 API，避免先删后找调用方。
3. 新增/调整 `projectTaskRepo`，把项目任务读写和 21 条 SQL 放到 repository。
4. 建立 projectTasks IPC 和 preload 桥，项目详情页改用新通道。
5. 把 flow 服务适配到 `projectTaskRepo`，确认没有旧 `todoRepo` 残留。
6. 清理 `llm.ts` 中旧规划路径，避免旧 AI 规划链路继续引用冻结区。
7. 删除冻结区源文件和对应旧测试，但保留新链路所需的兼容适配和历史归档数据。
8. 实现旧表归档服务和设置页入口，归档前确认数据库状态、事务和失败回滚。
9. 重写白名单导入导出，验证 16 张正常表键完整存在；空载荷或缺键载荷必须在事务前拒绝，原库计数不变。
10. 处理只读/损坏数据库探针；探针失败统一进入 `unavailable`，IPC 返回 `DB_UNAVAILABLE`，页面显示可读反馈。
11. 运行静态 grep，确认 services/ipc 不直接执行 SQL，renderer 不直连 DB，冻结区无 import。
12. 运行 `phase6Regression.spec.ts` 和全量测试，覆盖旧 daily/weekly/monthly/plan/review 入口重定向、导入导出、归档和数据安全。
13. 构建独立 Electron 包，执行可见窗口和 CDP/运行时复核，确认不白屏、无 runtime exception、无 console error。
14. 记录所有偏差、失败和风险，完成 GPT 首审、GLM+DS 修复和 GPT 复审。
15. 阶段 6 `✅开发闭环` 不等于项目维护期；阶段 6 仍要作为功能实测范围的一部分接受用户 GUI E2E。

阶段 6 主要验收：旧入口重定向、旧冻结区清理、旧表归档、导入导出安全、损坏数据库降级、全量测试、构建、运行时和六轮回归均可复核。

## 六、C0 和 S0：从开发交给功能实测

C0/S0 工作流：

```text
全部开发单元闭环
  -> 核对整个工作区变更归属
  -> 核对版本字段和入口
  -> 独立跑测试/build/tsc/diff-check
  -> 主体 Git 提交
  -> 创建 C0 Tag
  -> 从 C0 构建不可变 S0
  -> 记录构建目录/哈希/测试数据隔离
  -> 创建当前版本第一份 R 计划
```

前置条件：

- 所有当前目标版本的开发单元都是 `✅开发闭环`。
- 所有非忽略工作区变更都已明确归属；无法归属时停止，不能删除、还原或擅自提交。
- 代码版本字段已同步到 C0 预发布版本，例如 `0.3.0-c0`。
- 程序入口、构建方式、测试目录、数据目录和恢复脚本已经明确。
- `npm.cmd test`、`npm.cmd run build`、相关 tsc、`git diff --check` 和技术边界检查均已记录。

逐条执行：

1. GPT 读取各阶段全流程文档最后正式复审，确认不是只看历史章节中的旧红色问题。
2. 执行 `git status --short --untracked-files=all`、`git diff --name-only` 和 `git diff --stat`，把变更分成产品代码/测试、治理文档、历史归档和无关用户变更。
3. 无关用户变更不得被 `git add -A` 带入，不得清理用户已有文件。
4. 核对历史归档完整性，确认迁移的历史文档原文、路径和内容可回查。
5. 核对 `workbuddy/package.json` 和 `package-lock.json` 的版本字段，包括顶层和 `packages[""]`。
6. 运行独立门禁和代码边界扫描。C0 的目的只是锁定“开发进入实测”的代码，不是宣布用户验收。
7. 使用 Conventional Commit 提交 C0 主体，例如 `feat(v0.3): establish C0 functional test baseline`。
8. 创建 `v0.3.0-c0` Tag。C0 Tag 和 C1 Tag 由 GPT 的归档流程执行；GLM+DS 不自行创建。
9. 从 C0 构建 S0，记录来源提交、版本字段、构建目录、构建物哈希、清单哈希、冻结时间和独立测试数据目录。
10. GPT 通过 `用户prompt/14-生成用户实测计划.md` 创建当前版本第一份 `R<N>-1` 测试计划。后续 Fix/Review 不需要每次重新提交 Git，但每次代码、依赖或构建输入变化都必须形成新的 QA 和 S；后续计划仍由 `14` 在 QA/S 形成后创建。

当前 v0.3 事实：C0 主体提交为 `34f155a1886c52dae278fbc71d037975646b2563`，Tag 为 `v0.3.0-c0`；当前被测基准为 15 冻结的 `QA1/S1`。R1/R2 处于 B1 当前批次业务实测入口阶段，R1-3 与 R2-1 计划文件均已存在但仍为 `🟡待补前置`，不得把未执行结果写成通过。

## 七、用户实测阶段工作流

用户实测阶段工作流：

```text
C0
  -> S0
  -> 13 R 初始化创建唯一 R 骨架
  -> 14 创建首次 R<N>-1 计划
  -> GPT 用 12 做可见窗口自主实测
  -> 用户使用同一计划和同一 S 快照实测
  -> 用户用 06 三态回传
  -> GPT 用 13 校验证据、合并事实并写事项处置总表
  -> 环境/产品事项：16 记录证据和用户选择
  -> 所有事项处置完成且无阻塞：07 统一 FixN（修复/替代/删除）
  -> 05 对整个 FixN Review
  -> 15 冻结 QA/S
  -> 14 创建同一 R 的下一份计划，或覆盖补充计划
  -> 独立新增/独立覆盖/独立处置：13 建立下一 R 骨架，不创建计划
  -> 无未决事项且全部问题重测通过时关闭 R
  -> 全部 R 关闭后创建 G1
  -> GPT + 用户完成 G1
  -> 用户最终验收
  -> C1
```

用户实测阶段前置条件：

- 创建第一份 R 计划的前置条件：目标版本所有开发单元已闭环，C0 已提交、Tag、版本字段和工作区归属可核对。
- 绑定 S0 的前置条件：S0 必须来自 C0，构建目录独立，构建物哈希和测试数据隔离可复核。
- 执行 12 的前置条件：测试计划路径、测试标识、程序入口、窗口环境、测试目录、数据目录和 S 快照全部明确。
- 执行用户实测的前置条件：用户和 GPT 使用同一个 S 构建物；用户能看到并操作 WorkBuddy 窗口；测试数据为空或已按计划重置。
- 执行 06 的前置条件：用户确实执行了需要回传的用例；`发现问题` 可以只列发现问题的用例，并可使用 `用户补充问题 1/2/3…` 报告额外发现或计划覆盖缺口。
- 执行 13 的前置条件：GPT 正式报告、用户 06 回传、双方共同核心链路和快照关系可核对；13 必须把所有事项写入处置总表，并按回归/新增/覆盖/环境/待核实/用户决策分流。
- 执行 16 的前置条件：环境/产品事项有同一 S/副本的最小核对；每项有责任人、独立期限、复评点和继续排障/替代/删除选项，用户明确选择后才可转下一步。
- 执行 07 的前置条件：13 已为当前 R 的全部事项形成终态处置和统一 FixN；没有待核实、环境阻塞、待用户确认，覆盖缺口已转下一计划，修复/替代/删除范围和验收条件完整。
- 执行 05 的前置条件：07 已完成整个统一 FixN 并回写同一 R，原始失败场景、替代/删除结果和构建候选均有事实。
- 执行 15 的前置条件：05 已对整个 FixN `✅通过（修复复审）`，且候选来源指纹、构建目录、构建物哈希、版本字段和数据隔离可核对。
- 创建 G1 的前置条件：所有活跃 R 轮次都已关闭，双方报告、回传、共同链路和证据完整。
- 形成 C1 的前置条件：G1 通过、用户完成最终验收、正式版本字段和 CHANGELOG 准备完成。

### 7.1 创建 R 轮次测试计划

1. GPT 使用 `模板/05_实测部分/项目测试计划模板.md`。
2. R 计划落在 `用户实测阶段/vX-功能名/第N轮/`，例如 `用户实测阶段/v0.3-任务数据流通重构/第1轮/`。
3. 计划必须有唯一标识 `R<N>-<n>`；首次功能实测、修复后复测、覆盖补充计划均由 `14` 创建，不由 `13` 在测试完成后创建。首次和修复后计划必须引用已存在的 R 骨架。
4. 记录 C0/QA、S 快照、来源提交或完整工作树指纹、版本字段、构建目录、构建物哈希、测试专用程序、数据目录和重置脚本。
5. 定义双方范围：GPT 负责客观状态、数据一致性、错误降级、隔离、安全和回归；用户负责真实窗口、自然性、文案、信任和确认/取消体验。
6. 为每个测试点分配稳定 ID：R 计划使用 `R<N>-<n>-T<NN>`，G1 使用 `G1-T<NN>`。
7. 写清成功、失败、阻断、证据命名、数据保护、刷新/重进和取消/确认标准。
8. 覆盖补充计划可复用有效 S，但不得伪造 QA/S、缺陷或用户结果；计划状态只有在所有前置核对通过后才能标记 `✅可执行`，资料冲突时必须标记待补，不得凭猜测开测。

### 7.2 12：GPT&Claude 可见窗口自主实测

1. GPT&Claude 读取测试计划和所有关联状态，确认测试标识匹配、S 快照有效、独立构建物存在、测试数据隔离。
2. GPT&Claude 只能从计划指定的独立构建目录启动程序，不能从可变工作树临时替代。
3. GPT&Claude 保持 WorkBuddy 窗口可见，用鼠标、键盘、菜单、下拉、确认/取消、拖动和滚动执行测试。
4. 禁止直接调用业务 IPC、preload API、数据库 SQL、SQLite 文件、内部函数、隐藏 DOM 事件或脚本注入来替代 UI 操作。
5. 每个测试点记录操作、预期、实际、等待条件、截图/日志证据和结论。
6. 每个会改变数据的场景至少验证刷新或离开后重进；删除、导入、封账、转下周等破坏性动作必须分别验证取消和确认。
7. 遇到 `🔴阻断` 或 `🟡需修复` 时停止依赖它的后续测试；不相关的独立安全项可继续，但要标注依赖关系。
8. 报告必须分开列出：已通过、发现问题、待用户实测、未测试。
9. 报告落在 `GPT实测记录/vX实测内容/第N轮/`；截图落在计划指定证据目录，且必须脱敏。
10. 结论只能是 GPT&Claude 自主实测通过、需修复、驳回或停止补充信息；不能写用户验收通过、功能实测闭环或维护期。

### 7.3 用户按同一计划完成真实实测

1. 用户使用与 GPT 相同的 S 构建物和测试专用数据目录。
2. 用户按测试计划顺序执行共同核心链路和用户范围，不能用代码测试、历史截图或开发者自测替代。
3. 用户实际操作要从启动开始记录到最后一步，包含点击、输入、选择、确认/取消、刷新、离开和重进。
4. 用户同时记录客观结果和主观体验。理解成本、文案是否清楚、是否敢于确认/取消、是否担心误删，必须与页面事实分栏。
5. 用户遇到问题不需要先判断是回归还是新增，只需准确记录现象、复现步骤、数据影响和证据。
6. 用户未能执行时，必须记录已完成到哪一步、环境阻塞是什么、哪些测试点未完成，不能写成产品通过。

### 7.4 06：用户三态回传

用户使用 `用户prompt/06-用户反馈测试问题.md`，提交状态只有三种：

1. `发现问题`：逐条写已发现问题用例的测试点/子用例 ID、操作、预期、实际、主观体验、数据影响、证据和复现次数；未列用例只记为“本次暂未发现问题”，不补写通过事实。可追加固定来源标签 `用户补充问题 1/2/3…`，不创建新测试点或子用例。
2. `全部完成且无问题`：列出实际执行的全部测试点、共同核心链路、取消/确认、刷新/重进、错误场景、页面结果和证据，不能只写“通过”。
3. `无法完成-环境阻塞`：写最后可确认步骤、可见阻塞、影响范围、环境证据和需要补充的环境动作。

用户回传的硬边界：

- 不提供真实 API Key、Cookie、私人数据或不必要的完整敏感路径。
- 不直接改测试数据库或产品文件来恢复结果。
- 不以“感觉没问题”覆盖客观数据问题，也不把环境阻塞猜成产品根因。
- 不建立独立用户报告；用户事实通过 06 回传，由 13 合并到规定的唯一文档。

### 7.5 13：合并 GPT&Claude 与用户证据

1. GPT&Claude 读取计划、GPT&Claude 报告、用户 06 原文、用户证据、当前 R 目录和相关开发记录；R 初始化不需要计划、报告或 06，只建立骨架。
2. GPT&Claude 校验计划标识、测试点 ID、双方证据路径、共同核心链路、S、构建物哈希和数据隔离；计划完成后的合并不得创建或修改计划。
3. R 测试合并必须在同一 `R<N>` 目录保持一份唯一全流程闭环文档；原 R 保留所有用户/GPT&Claude来源事实，包括步骤 1-6 和全部实际判断。
4. GPT&Claude 分栏记录客观事实、用户主观体验、证据等级、未测试项、环境阻塞、待核实和待用户确认，不把未列用户用例补写成通过。
5. GPT&Claude 写入事项处置总表：代码 Fix、覆盖补充计划、环境处置决策、替代、删除、补证/待核实或待用户确认。
6. 只有本 R 每项都有终态处置，覆盖缺口已转计划，批准替代/删除有 16 记录，且无待核实、环境或用户决策阻塞时，才能生成统一 `FixN`；不能按单问题拆 Fix。
7. 用户补充问题保留原始来源标签，由 13 建立稳定问题 ID；可客观复核的事项在同一 S/副本中做独立 GUI/只读核验并单独归档，不改写 GPT&Claude 正式报告或测试计划。
8. 发现独立新增问题、独立覆盖缺口或独立处置事项时，原 R 保留来源，13 新建下一 R 唯一闭环文档；13 不创建下一测试计划。同一问题补证仍回写原 R。

G1 模式不创建 R 闭环文档，只把双方结果写回 G1 测试计划的“合并与裁决”区。

### 7.6 问题分流规则

| 本轮结果 | 文档处理 | 下一步 |
|---|---|---|
| 无回归、无新增且无其他事项 | 当前 R 记 `✅当前轮次闭环` | 等其它 R 关闭后做 G1 |
| 只有回归 | 回写当前 R 唯一文档并完成事项处置 | `07 FixN -> 05 ReviewN -> 15 QA/S -> 14 创建同 R 下一计划` |
| 只有全新问题 | 原 R 保留无回归事实；13 新建 `R<N+1>` 唯一文档 | 新 R 先统一处置，13 不创建 `R<N+1>-1` |
| 回归和全新问题都有 | 回归留当前 R，全新问题由 13 新建下一 R 唯一文档 | 两个活跃 R 可并行，代码修改串行；均关闭后才能 G1 |
| 覆盖缺口 | 登记为下一测试计划纳入项，必要时建立覆盖补充 R | 14 创建覆盖补充计划，不进代码 Fix |
| 证据不足/待核实/环境阻塞/用户未决策 | 记 `📝待补证据/待核实` 或 `⏸️待环境/产品决策` | 补证或 16 决策，未完成不进入 Fix |

单项问题证据门通过不等于统一 Fix 可执行；本 R 仍须完成全部事项处置和阻塞清理。R 也不能因此写成用户验收、G1 或 C1 完成。

### 7.7 07：GLM+DS 修复用户反馈

1. GLM+DS 只接受当前 R 唯一全流程文档中已通过统一 Fix 门的 `FixN` 计划。
2. 先读取原始测试计划、GPT 报告、用户回传、证据、相关开发阶段记录和技术栈规范。
3. 如果事项处置、证据、分类、用户决定、修复/替代/删除范围或验收条件不清，停止整个批次；不要把其他待核实或已分流事项带入。
4. 一次处理本 R 中全部获准的代码修复、批准替代和批准删除；覆盖缺口不进 Fix。
5. 逐项复现原始失败步骤，执行最小必要方案，补自动化测试并复跑原始场景，验证数据未丢失、未重复、未误删和错误降级可读。
6. 代码、依赖或构建输入有变化时记录候选 QA、版本字段、来源指纹、构建物和待冻结 S；不由 07 冻结 QA/S。
7. 不要求每个 Fix 创建 Git 提交；Fix 日志写回同一 R 文档并更新真实 `当前代码状态.md`，不得修改测试计划结论或 GPT 报告。

### 7.8 05：GPT&Claude 复审用户反馈修复

1. GPT&Claude 重新使用原始失败步骤，不只看 diff、单测或 GLM+DS 自测。
2. 对整个 FixN 逐项核对代码修复、替代、删除、覆盖登记、确认/取消、刷新/重进、错误降级、数据一致性、隔离和安全。
3. 检查越界改动、技术栈红线、白屏、runtime exception、console error、重复写入和敏感信息。
4. 复审通过后只交给 `15-冻结QA-S.md`；05 不冻结 QA/S、不创建测试计划。
5. 复审不通过则追加 ReviewN 结论，回到 07 的下一个完整 FixN；15 冻结后才由 14 创建后续计划。

### 7.9 15：冻结 QA/S

1. 只有 05 对整个 FixN `✅通过（修复复审）` 后才能调用 15。
2. 15 核对当前版本全局 QA 编号、来源提交/完整工作树指纹、版本字段、构建目录、构建物哈希、快照目录、入口和数据隔离。
3. 15 只冻结候选 S 并把 QA/S 记录追加到原 R；不改代码、数据库、测试计划或状态文件。

### 7.10 16：环境与产品处置

1. 环境事项按同一 S/副本正式入口重复启动，核对进程参数、环境变量、实际数据目录、GUI 结果和至少一个候选替代入口。
2. 每项登记排障责任人、独立期限、用户/PM 复评点及”继续排障/替代/删除”选项。
3. 用户明确决定后，16 记录原文、证据、影响和后续计划；GPT&Claude 不替用户决定，16 不改代码。替代/删除后续由 07 执行、05 复审。

### 7.11 文档级场景演练

| 场景 | 文档动作 | 必须得到的结果 |
|---|---|---|
| 同一 R 同时出现回归、独立新增、覆盖缺口、待核实和环境阻塞 | `13` 续写原 R 的全部来源事实；独立新增/独立覆盖建立下一 R；同一问题补证仍回原 R | 原 R 不丢事实；回归留原 R；新 R 只承接新增事项；覆盖项不变成产品缺陷 |
| R1/R2 并行 | R1、R2 分别维护各自唯一闭环；代码任务排队执行 | R1 阻塞不阻断 R2 的合法 Fix；共享工作区代码修改仍串行；最终统一交 G1 |
| R1 当前结果 | R1 `Fix1` 已完成，R1-only `Review1` 和最终 B1-merge 复审均已通过，15 已冻结全局 `QA1/S1`，R1-3 计划已创建 | R1-3 仍须独立 E0/T01；未执行的测试点不能预填通过 |
| R2 当前结果 | U-4 进入 R2 统一 `Fix1`；“每周固定”进入覆盖补充项；`R2-1` 已由 14 创建 | `07` 只能处理 U-4；`R2-1` 仍须经独立 E0/T01 复核，不得预填测试结果 |
| 没有 `FixN` 或事项处置不完整 | `07` 读取 R 文档的总表和统一 Fix 门 | 必须停止，不拆成 `Fix1a/Fix1b`，不自行缩小范围 |
| 环境事项三分支 | `16` 记录用户选择“继续排障/替代/删除” | 继续排障仍阻断；批准替代/删除才可和代码事项并入同一 Fix；GPT 不替用户决定 |
| 复审和后续计划 | `07` 完成整批 → `05` 整批 Review → `15` 冻结 QA/S → `14` 创建下一计划 | 顺序可追溯；05 不冻结 QA/S，14 不创建 R 文档，13 不创建测试计划 |
| 版本级整合 | 所有活跃 R 关闭后由 `14` 创建 G1，`13` 只回写 G1 计划 | G1 不创建 R；`✅G1通过` 不直接等于 C1 或用户验收 |

## 八、当前 R1-2 用户实测的具体执行流

当前 R1-2 计划路径为：

`用户实测阶段/v0.3-任务数据流通重构/第1轮/v0.3-R1-2-实测计划.md`

当前已绑定：

- 版本字段：`0.3.0-c0`。
- 来源节点：`C0`，Tag `v0.3.0-c0`。
- 快照：`S0`。
- 测试程序：`E:\workbuddy-test\app-v0.3\WorkBuddy.exe`。
- 测试数据：`E:\workbuddy-test\userData\`。
- 真实数据边界：真实 `%APPDATA%\workbuddy\` 禁止读取、复制、修改或删除。
- 测试前缀：`[v0.3-R1-2-20260822]`。

R1-2 的六个测试点：

### R1-2-T01：启动、隔离、首次使用、窗口自适应

1. 关闭 WorkBuddy，确认测试数据目录按计划重置。
2. 使用 `E:\workbuddy-test\启动实测.bat` 或计划指定的 S0 包启动。
3. 确认窗口可见、页面可进入、没有白屏、启动异常或数据串档。
4. 首次创建带测试前缀的数据。
5. 调整窗口尺寸，确认主要内容仍可用。
6. 关闭并重新启动同一个 S0，确认测试数据保持。
7. 核对真实 `%APPDATA%\workbuddy\` 未被触碰。

### R1-2-T02：月目标、周统筹、固定/临时任务

1. 创建测试月目标。
2. 从月目标选取周核心目标，并另外验证手动新增路径。
3. 编辑、完成、取消完成和删除周核心目标，验证确认和取消。
4. 创建一次型周任务和场次型周任务，验证场次从 `0/3` 到完成的过程。
5. 创建固定任务，设置惯常日，验证本周物化和停用。
6. 刷新或离开后重进，确认标题、关联、进度和删除结果保持。
7. 重点回归 UF1/UF2，不能出现 `weekStart/title/kind 必填`。

### R1-2-T03：周任务、日规划 rail、模板

1. 从周任务进入日规划，确认日期、来源和 rail 展示。
2. 选取、移除、挪动、跳过任务，确认锁定行和自由行动作不同。
3. 创建包含两项的日模板，确认列表、数量和成功反馈显示。
4. 套用模板，验证取消、部分勾选、编辑、删除和已插入任务断链。
5. 离开日页、重进相同日期，确认任务、模板和感想保持。
6. 重点回归 UF3：不能出现模板列表为空、空红条或 Proxy 结构化克隆错误。

### R1-2-T04：四渠道汇聚到今日总览

1. 先在项目任务中设置计划日期，观察是否仅改变计划信息。
2. 按当前用户裁决的方案 B，显式点击“加入今日”。
3. 返回今日总览，确认项目任务产生今日投影。
4. 检查周/日任务、项目任务、提醒、手动待办四渠道来源标识。
5. 完成、移出、刷新和重进，确认完成回写正确，移出不删除项目源任务。
6. 验证不重复、不产生幽灵条目、不误删源任务。
7. 只有在测试计划及关联闭环文档都统一 UF4 语义后，T04 才能正式执行；目前 GPT 报告中的 P-01 表明该前置仍需统一。

### R1-2-T05：完成凭据、复盘、趋势、封账、转下周

1. 先生成完成和未完成的测试数据。
2. 添加或查看完成凭据，检查周任务完成态实时派生。
3. 打开复盘，核对完成率、顺延、未安排、跳过和场次统计。
4. 查看跨周趋势，验证周边界、历史日补勾和刷新后的重算。
5. 执行封账，先走取消，再走确认，检查未完成清单。
6. 执行“转下周”，验证目标周合法、动作幂等、固定任务下周是新实例。
7. 离开/重进，确认没有丢失、重复或跨周串档。

### R1-2-T06：导入导出、旧入口、错误降级、数据安全

1. 在测试副本导出合法数据，保留恢复文件和哈希。
2. 先取消一次导入/覆盖，再对测试副本执行合法导入。
3. 使用空载荷、缺键载荷、非法字段和重复操作，确认事务前拒绝且原数据计数不变。
4. 访问旧入口，确认路由重定向到新页面，不能白屏。
5. 使用无效 Key、不可达地址或空输入，确认有可读反馈，不泄露内部堆栈。
6. 在明确的隔离副本上验证只读/损坏数据库降级；没有恢复步骤时不得操作。
7. 全程确认真实用户目录不变，截图和日志不包含敏感信息。

## 九、G1、C1 和正式版本

### 9.1 G1 版本级整合回归

G1 工作流：

```text
所有活跃 R 轮次关闭
  -> 创建独立 G1 测试计划
  -> 绑定统一 S 快照
  -> GPT 12 执行 G1
  -> 用户按 G1 计划实测并 06 回传
  -> 13 回写 G1 合并与裁决区
  -> G1 通过或分流问题
```

前置条件：

- 所有活跃 R 的唯一闭环文档都是 `✅当前轮次闭环`。
- 每个 R 都有 GPT 正式报告、用户正式回传、共同核心链路和关键证据。
- G1 绑定统一且不可变的 S 快照。
- G1 使用 `G1-T<NN>`，不创建新的 R 编号。

逐条执行：

1. 覆盖所有已关闭 R 的核心链路和修复问题。
2. 覆盖跨功能数据流、刷新/重进、错误降级、取消保护、数据一致性、隔离和安全。
3. GPT 和用户分别完成同一份 G1 计划，不能互相替代。
4. 13 将双方报告、证据、共同链路对照、待确认项和问题分类写回 G1 计划。
5. G1 无问题时写 `✅G1通过`；这不等于 C1 或用户最终验收。
6. G1 发现已有 R 回归时回写对应 R；发现全新问题时新建下一 R；证据不足时停在待补状态。

### 9.2 C1 正式版本

1. 用户完成当前目标版本的最终验收，确认产品范围、核心链路和主观体验。
2. GPT 核对所有活跃 R、G1、阶段 6 用户 E2E、版本字段和证据完整性。
3. GLM+DS 将版本字段从预发布版更新为正式版，例如 `0.3.0`，并同步 lockfile。
4. GPT 在 `变更记录/CHANGELOG.md` 追加版本、范围、日期和闭环依据。
5. 使用 11 归档 C1 主体提交并创建正式 Tag，例如 `v0.3.0`。
6. C1 形成后才可以在全部功能和阶段 6 用户 E2E 均满足的前提下进入项目维护期。

## 十、维护期 CR 工作流

阶段 6 用户 E2E 和项目级版本闭环完成后，任何代码改动都必须走 CR。纯文档治理改动可由 GPT 直接维护并在对应决策/变更记录中留痕；只要涉及代码，必须有 CR 或用户反馈编号。

维护期 CR 工作流：

```text
用户反馈/滚动改进项/GPT发现缺陷
  -> GPT 创建 CR-NNN 全流程记录
  -> GLM+DS 用 08 复现并实现
  -> GPT 用 09 独立审查
  -> 仍有问题：08 -> 09 循环
  -> 通过：版本 bump + CHANGELOG + 用户复测（如需）
  -> CR 关闭
  -> 11 Git 归档
```

前置条件：

- 阶段 6 已达到“全部关闭，含用户实跑 E2E”。
- 项目已进入维护期，不能用 CR 代替尚未完成的构建期阶段。
- 问题有用户反馈、滚动改进项或 GPT/GLM+DS 可复现的内部缺陷来源。
- CR 编号从 `CR-001` 起全局递增，不复用、不跳号。

逐条执行：

1. GPT 诊断问题并创建 `变更开发/vX-功能名/CR-NNN-简述-全流程闭环记录.md`。
2. 变更请求必须写现象/复现、证据、根因诊断、范围、排除范围、具体 file:line 修复计划、验收点、严重度、禁止边界和版本影响。
3. GLM+DS 使用 08，先读取 CR，再实跑原始失败场景，不能只查代码。
4. GLM+DS 实施最小修复，更新测试/构建/自测，写变更日志和闭环记录，更新 `当前代码状态.md`。
5. 如果代码、依赖或构建输入改变，形成新的 QA/S；如果需要正式版本，按 CR 严重度和版本规则 bump。
6. GPT 使用 09，按 CR 验收点实地验证，复跑失败场景，核对技术栈、错误降级、回归和越界。
7. GPT 将审查结果写回同一 CR 全流程记录和 `当前审查状态.md`，不新建 `审查日志`。
8. 复审通过时：更新 CR 终态、版本字段、CHANGELOG；若来源是用户反馈，回链用户实测轮次并要求相应复测。
9. 复审不通过时：保留历史审查事实，回到 08 的下一个修复批次，再由 09 复审。
10. CR 关闭后用 11 归档；没有 CR 编号、GPT 审查或复跑失败场景的代码改动不视为完成。

## 十一、Git 归档工作流

Git 归档工作流：

```text
确认对应终态
  -> 读取闭环文档和两个状态看板
  -> status/diff/diff-check
  -> 归属变更并选择性暂存
  -> 提交主体快照
  -> 回写 Git 归档记录
  -> 提交归档记录
  -> C0/C1 创建必要 Tag
  -> 检查最终工作区
```

前置条件：

- 开发单元归档：最新 GPT 复审是 `✅开发闭环`，没有当前有效 HIGH/CRITICAL。
- C0 归档：所有开发单元闭环、版本字段为 C0、工作区变更已归属、入口/隔离/S0 已核对。
- QA 归档：只有确实需要可定位提交时使用，不能借 QA Tag 宣称发布。
- C1 归档：所有 R 关闭、G1 通过、用户最终验收完成、正式版本和 CHANGELOG 已准备。
- CR 归档：CR 已由 GPT 09 审查关闭。

逐条执行：

1. 读取闭环文档底部最新正式结论，并与当前指针核对；顶部当前门禁和历史章节中的旧 `🔴/🟡` 不能单独裁决当前状态。
2. 读取 `当前审查状态.md`，确认 GPT 终态；读取 `当前代码状态.md` 只做真实性核对，不由 GPT 改写。
3. 执行：

   ```text
   git status --short
   git diff --name-only
   git diff --stat
   git diff --check
   ```

4. 逐个分类变更：本次代码/测试、本次闭环文档/治理同步、无关用户变更。无关变更不得加入提交，也不得删除或还原。
5. 不能使用 `git add -A` 代替归属判断；只暂存本次明确归属的路径。
6. 如果最后一次审查后又发生代码或测试改动，停止归档，回到 03 或 05 重新验证。
7. 主体提交使用 Conventional Commit，例如 `feat(stage6): close legacy module cutover` 或 `feat(v0.3): establish C0 functional test baseline`。
8. 在同一全流程文档追加 Git 归档记录，包含对象、层级、日期、裁决来源、主体提交 hash 和工作区结果。
9. 文档归档记录再用独立的 `docs(archive): ...` 提交保存。
10. C0/C1 创建必须 Tag；QA 只有在存在可定位提交时才可选 Tag；没有提交就记录完整工作树指纹，不伪造 Tag。
11. 最后再次运行 `git status --short`，把是否干净和仍存在的用户变更写入归档报告。

## 十二、常用开发、测试和构建命令

命令均从 `E:\workspace\workbuddy\` 执行。Windows PowerShell 下优先使用 `npm.cmd`。

```text
npm.cmd run dev
npm.cmd test
npm.cmd run build
npm.cmd run rebuild
npm.cmd run pack:win
npm.cmd run dist:win
npx tsc -p tsconfig.node.json --noEmit
npx tsc -p tsconfig.web.json --noEmit
git diff --check
```

命令用途：

1. `npm.cmd run dev`：开发期启动 Electron/Vite。
2. `npm.cmd test`：运行 Vitest 全量测试。
3. `npm.cmd run build`：构建 main、preload 和 renderer。
4. `npm.cmd run rebuild`：better-sqlite3 等原生模块与 Electron 版本不匹配时重建。
5. `npm.cmd run pack:win`：构建 Windows 未安装目录包。
6. `npm.cmd run dist:win`：构建 Windows 安装包。
7. 两套 `tsc`：分别检查 Node 和 web/renderer 侧类型诊断；存量诊断必须区分记录，不能通过隐藏错误来“变绿”。
8. `git diff --check`：提交前检查空白和格式问题。

当前 package 脚本和主要技术栈：Electron 33、Vue 3、TypeScript 5、Vite/Electron Vite、Vitest、better-sqlite3、Vue Router、DOMPurify、lucide-vue-next。依赖版本以 `workbuddy/package.json` 和 lockfile 实际内容为准。

## 十三、不可绕过的通用门禁

1. 规格未确认，不能写功能代码。
2. 上一开发单元未闭环，不能默认启动下一单元。
3. GPT 审查未通过，不能把开发单元标为闭环。
4. 单元测试通过不等于 GUI 可用；需要真实运行时和可见窗口证据的场景必须实跑。
5. GPT 自主实测通过不等于用户验收通过。
6. 用户“本轮无问题”不等于版本 C1；还要完成所有活跃 R、G1 和用户最终验收。
7. `C0` 只代表开发进入实测，不是正式发布。
8. `QA/S` 只代表待测候选和不可变构建，不是通过或发布。
9. 任何问题必须进入当前 R 唯一闭环文档或 CR，不能只留在聊天里。
10. 证据不足、环境阻塞、语义冲突时停止并列出补充项，不能猜测通过。
11. renderer 禁止直连数据库、SQL 和 Electron 内部 API；SQL 只能位于 repository。
12. IPC 必须校验输入并返回 `Result<T>`；错误必须有日志和用户可见反馈，不能静默吞错。
13. 新任务域不使用 Markdown 存任务，不用文本匹配推断来源，不缓存完成态。
14. 旧冻结区只读不改不扩；新功能不得 import，阶段 6 才统一清理。
15. 删除默认软删除；导入、数据库异常和破坏性动作必须保护原数据。
16. 新依赖必须先评审；token.css 是唯一色源；禁止硬编码密钥、业务明文和敏感路径。

## 十四、当前状态和下一步

### 已完成

1. v0.3 阶段 1：数据模型与基建，开发闭环。
2. v0.3 阶段 2：周统筹页，开发闭环。
3. v0.3 阶段 3：日规划与 rail，开发闭环。
4. v0.3 阶段 4：今日总览四渠道，开发闭环。
5. v0.3 阶段 5：复盘与趋势，开发闭环。
6. v0.3 阶段 6：旧模块切换清理，开发闭环。
7. v0.3 用户反馈 1 的 UF1、UF2、UF3 修复和 GPT 复审，开发修复闭环。
8. UF4 已由用户选择方案 B：显式点击“加入今日”才产生项目任务今日投影；该决策本身不要求追加代码修复。
9. C0 Git 基线：`34f155a` / `v0.3.0-c0`。

### 当前未完成

1. `R1-3` 与 `R2-1` 计划均已创建并绑定 15 冻结的 `QA1/S1`，但当前仍为 `🟡待补前置`，尚无本轮 GPT 报告、用户 06 回传或业务测试结果。
2. 两份计划的 `staticGate/E0` 仍待独立复核；B1 当前批次继续使用一次性入口，长期入口机器闸门保持关闭。
3. 两份计划的 `T01` 为 `not-tested`；E0 通过后才允许开始各自 T01，T01 未测试不能写成通过，也不能倒置成启动阻塞。
4. R1/R2 的本轮实测结果尚未由 13 合并；未发现的事项、覆盖项、环境事项和用户决策仍按各自唯一闭环文档分流。
5. 所有活跃 R 轮次尚未关闭，G1、C1 和维护期尚未形成。

### 当前恢复顺序

1. 由 GPT 独立复核 R1-3、R2-1 的计划门禁和 B1 当前批次 E0，分别记录四层状态、证据、责任人和下一动作。
2. 只有对应计划 E0 通过，才从 `E:\workbuddy-test\启动实测.bat` 进入该计划的 T01；本次治理不代替这次独立复核，也不启动 T01。
3. T01 失败只停止依赖它的后续测试；独立测试点按计划继续，结果交 `13` 合并，产品问题再按事项类型回 `07` 或 `16`。
4. 双方实测完成后，由 `13` 续写原 R 的最新事实和分流；若有代码 Fix，继续 `07 → 05 → 15 → 14`。
5. 全部活跃 R 关闭后，14 创建 G1 计划；G1 通过且用户完成最终验收后，才考虑 C1，之后才可能进入维护期。

## 十五、B1 双 R 并行复审与合并冻结

### 15.0 通用自动编排入口

B1 的目录形态是历史经验；从本规则生效后，任何多 R Fix 批次都必须由 E:\workspace\tools\r_fix_review.py 产生机器状态。07 在每个 R 的统一 Fix 前调用 begin-fix，Fix 完成并构建后调用 complete-fix。complete-fix 负责建立 E:\workbuddy 复审\<B>\<R>-<Fix>-only、独立 app/启动包装/userData/evidence 和完整哈希，并在验证归属后恢复 E:\workspace\workbuddy 到 Fix 前指纹。

批次由 finalize-batch 冻结预期 R/Fix 集合，但不能用它替代实际扫描。05、13 和 15 在每个门节点调用 scan-batch；实际目录中出现未登记 R、重复 R、孤立候选、缺失 Review、非 passed Review、恢复未验证或构建哈希异常时，merge 和 QA/S 均停止。全部 R-only 通过后，GLM+DS 调用 prepare-merge，再在保留整合工作区的前提下调用 complete-merge。13 用 create-merge-document 创建或验证同一批次唯一的合并闭环文档，05 在该文档完成集成 Review，15 只冻结 merge 最终包。

这一顺序的机器状态和可读记录必须互相引用：batch-state.json、batch-scan.json、merge-ready.json、各 R-only review-meta.json、merge/review-meta.json、Review 证据以及批次闭环文档。所有命令可重复执行但不得覆盖终态；R-only 失败不生成 merge，merge 失败只追加批次级修复记录，不回流或拆分 R。真实用户数据目录不进入快照、候选、userData 或 evidence。

当 R1、R2 的 Fix 已在各自唯一闭环文档中完成时，可以将它们编入同一 `B1` 修复批次。B1 不把两个 R 合并成一个问题来源，也不把合并包称作 G1：

1. 保留共同 `C0/S0`，从 C0 独立重建 `B1-R1`（C0 + R1 Fix1）和 `B1-R2`（C0 + R2 Fix1）。当前混合工作树和已有可能混入 R1 的 R2 候选只能作为参考，不能直接作为 R2-only。
2. R1-only 和 R2-only 使用不同程序目录、启动包装和 `userData`，在满足各自 Review 编号和完整证据门后并行进行 05。R1 使用 `Review1`；R2 已有前置停止 `Review1`，因此使用 `Review2`，不得覆盖历史记录。
3. 两个 R-only 复审都通过且 scan-batch 返回 readyToMerge=true 后，GLM+DS 调用 prepare-merge，在 E:\workspace\workbuddy 按批准差异并集完成全面整合和构建，再调用 complete-merge 生成同批次 merge 候选；随后由 13 创建或验证唯一 `B1-merge` 批次全流程文档。B1-merge 的版本字段由 GLM+DS 设置并重建，GPT 只读核对，合并候选生成后不恢复开发区。
4. B1-merge 复审失败时，失败事实、`B1-merge 修复记录N`、重建和后续复审全部写在批次文档中，不回流 R1/R2、不创建新的 R `FixN`。未最终通过不得调用 15。
5. 三次初始复审（R1-only、R2-only、B1-merge）以及必要的合并追加复审已全部通过，15 已将最终合并包冻结为全局 `QA1/S1`，14 已创建绑定同一 S 的 `R1-3` 与 `R2-1` 计划；当前实测仍从 `E:\workbuddy-test\启动实测.bat` 开始，长期入口由机器闸门保持关闭。

`B1-merge` 通过只是具备冻结资格；`QA1/S1` 冻结不等于用户验收、R 关闭、G1、C1 或维护期。
